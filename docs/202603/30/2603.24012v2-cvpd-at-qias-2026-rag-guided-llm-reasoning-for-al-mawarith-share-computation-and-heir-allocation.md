---
title: "CVPD at QIAS 2026: RAG-Guided LLM Reasoning for Al-Mawarith Share Computation and Heir Allocation"
title_zh: CVPD 在 QIAS 2026：用于 Al-Mawarith 份额计算与继承人分配的 RAG 引导大语言模型推理
authors: "Wassim Swaileh, Mohammed-En-Nadhir Zighem, Hichem Telli, Salah Eddine Bekhouche, Abdellah Zakaria Sellam, Fadi Dornaika, Dimitrios Kotzinos"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24012v2"
tags: ["query:q6"]
score: 9.0
evidence: 遗产分配的多阶段法律推理
tldr: 本研究针对复杂的伊斯兰继承法（Al-Mawarith）计算任务，提出了一种基于检索增强生成（RAG）的LLM推理框架。该方法结合了符号计算器生成的合成数据、混合检索与重排序技术，以及模式约束的输出验证，有效解决了法律规则复杂和数值计算一致性问题。在QIAS 2026竞赛中，该系统以0.935的MIR-E得分荣获冠军，证明了RAG在阿拉伯语高精度法律推理中的卓越性能。
selection_source: fresh_fetch
motivation: 伊斯兰继承法涉及多阶段法律推理和复杂的规则变体，传统模型难以在确保法律一致性的同时处理复杂的份额分配。
method: 提出一种结合符号计算器生成合成数据、混合检索重排序及模式约束验证的RAG流水线。
result: 该系统在QIAS 2026盲测中获得0.935的MIR-E得分，位列排行榜第一。
conclusion: 检索增强与模式感知生成技术显著提升了大型语言模型在处理高精度阿拉伯法律推理任务中的可靠性。
---

## 摘要
伊斯兰遗产继承（Ilm al-Mawarith）是一项多阶段的法律推理任务，需要识别合格继承人、解决排除规则（hajb）、分配固定份额和剩余份额、处理诸如 awl 和 radd 等调整，并生成一致的最终分配方案。由于不同法学派别和民法典的差异，该任务变得更加复杂，要求模型在明确的法律配置下运行。我们针对这一场景提出了一种检索增强生成（RAG）流水线，结合了基于规则的合成数据生成、混合检索（稠密检索与 BM25）及交叉编码器重排序，以及基于模式约束的输出验证。我们使用符号化继承计算器生成了包含完整中间推理轨迹的大规模高质量合成语料库，确保了法律和数值的一致性。所提出的系统实现了 0.935 的 MIR-E 评分，并在 QIAS 2026 官方盲测排行榜上排名第一。结果表明，基于检索且感知模式的生成显著提高了高精度阿拉伯语法律推理任务的可靠性。

## Abstract
Islamic inheritance (Ilm al-Mawarith) is a multi-stage legal reasoning task requiring the identification of eligible heirs, resolution of blocking rules (hajb), assignment of fixed and residual shares, handling of adjustments such as awl and radd, and generation of a consistent final distribution. The task is further complicated by variations across legal schools and civil-law codifications, requiring models to operate under explicit legal configurations.   We present a retrieval-augmented generation (RAG) pipeline for this setting, combining rule-grounded synthetic data generation, hybrid retrieval (dense and BM25) with cross-encoder reranking, and schema-constrained output validation. A symbolic inheritance calculator is used to generate a large high-quality synthetic corpus with full intermediate reasoning traces, ensuring legal and numerical consistency.   The proposed system achieves a MIR-E score of 0.935 and ranks first on the official QIAS 2026 blind-test leaderboard. Results demonstrate that retrieval-grounded, schema-aware generation significantly improves reliability in high-precision Arabic legal reasoning tasks.

---

## 论文详细总结（自动生成）

### 论文总结：CVPD at QIAS 2026

#### 1. 核心问题与整体含义
论文针对**伊斯兰继承法（Al-Mawarith）**自动推理中的复杂性问题展开研究。该任务涉及多阶段法律逻辑，包括识别合法继承人、处理继承排除（hajb）、分配固定与剩余份额，以及进行复杂的数学调整（如 *'awl* 和 *radd*）。
**研究动机**在于：
*   **法律复杂性**：规则随教法学派（Madhahib）和各国法律而异，通用模型难以掌握。
*   **LLM 局限性**：大语言模型在多步逻辑推理、严格格式输出和数值计算一致性方面表现较差。
*   **数据不平衡**：现有训练数据多为简单案例，缺乏复杂场景。

#### 2. 方法论
作者提出了一种**检索增强生成（RAG）引导的结构化预测流水线**，核心包含四个阶段：
*   **符号计算器与合成数据生成**：利用基于规则的确定性计算器生成了约 10 万个合成案例。每个案例包含阿拉伯语描述、自然语言问答、显式推理轨迹和结构化 JSON 输出，解决了数据稀疏问题。
*   **混合检索与重排序**：
    *   **检索**：结合稠密语义搜索（Dense Retrieval）和稀疏词法搜索（BM25）。
    *   **融合**：使用加权倒数排名融合（RRF）整合结果。
    *   **重排序**：通过交叉编码器（Cross-Encoder）对 Top-K 结果进行精细化评分，筛选最相关的法律先例。
*   **模式约束生成**：使用 **Qwen 3.5-9B** 模型，通过 Prompt 引导其生成包含 `<think>`（推理过程）和 `<answer>`（结构化 JSON）的内容。
*   **输出解析与验证**：建立验证层，强制执行四类约束：键名存在性（$C_{keys}$）、数据类型（$C_{types}$）、法律标签合法性（$C_{labels}$）以及份额总和一致性（$C_{mass}$）。

#### 3. 实验设计
*   **数据集**：使用 QIAS 2026 官方数据集（5,900 个训练样本，200 个开发样本，500 个盲测样本）。
*   **Benchmark**：官方评估指标为 **MIR-E**，该指标对继承人识别、排除状态、份额分配、调整状态和最终分布等多个阶段进行加权评分。
*   **对比方法**：
    *   **基准测试**：对比了 Qwen 3.5-9B、Qwen 2.5-7B、Command-R、Llama 3.2 和 Gemma 3 等开源模型在零样本（Off-the-shelf）下的表现。
    *   **竞赛对比**：与 QIAS 2026 排行榜上的其他 7 支团队（如 Simplicity, KMS 等）进行对比。

#### 4. 资源与算力
论文提到了使用的核心模型是 **Qwen 3.5-9B**。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量或训练/推理的总时长。由于该系统主要基于 RAG 和推理，而非大规模全参数微调，其算力需求主要集中在合成数据生成和推理阶段。

#### 5. 实验数量与充分性
*   **实验规模**：进行了基准模型对比、官方盲测排名、数据源消融实验（PDF vs. Web）以及详细的错误分析。
*   **充分性与客观性**：
    *   消融实验展示了高质量 PDF 法律文献作为检索源的优越性（成功率接近 100%），对比了 Web 数据的噪声影响，实验设计较为客观。
    *   通过 200 例开发集和 500 例盲测集的验证，结果具有统计学意义。
    *   错误分析深入到了逻辑层面（如 42% 的错误源于排除逻辑），体现了评估的深度。

#### 6. 主要结论与发现
*   **系统性能**：该流水线以 **0.935** 的 MIR-E 得分在 QIAS 2026 盲测中**排名第一**。
*   **RAG 的必要性**：原生 LLM 即使推理能力强，也无法在没有外部法律知识引导的情况下保证结构化输出的严谨性。
*   **数据质量胜过数量**：精心策划的 PDF 法律文本在语义精度上远超开放的 Web 数据。
*   **瓶颈所在**：目前的错误主要集中在复杂的组合逻辑（如多代排除链）和罕见的分数运算，而非语言理解。

#### 7. 优点
*   **高精度与鲁棒性**：通过符号计算器生成合成数据，确保了模型在罕见复杂案例上的泛化能力。
*   **确定性验证**：引入后处理验证层，有效解决了 LLM 常见的格式崩溃和数值不一致问题。
*   **混合检索策略**：结合语义和词法检索，并辅以重排序，极大提升了法律上下文的检索质量。

#### 8. 不足与局限
*   **法律覆盖范围**：目前的规则配置尚未覆盖所有主要的逊尼派教法学派及各国特有的民法变体。
*   **复杂逻辑挑战**：在处理多代继承人的复杂排除链（hajb）和极端分数分母时，仍存在约 30%-40% 的错误率。
*   **评估局限**：MIR-E 指标虽然全面，但无法完全衡量法律解释的忠实度及实际应用中的易用性。

（完）
