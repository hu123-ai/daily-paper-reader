---
title: "CVPD at QIAS 2026: RAG-Guided LLM Reasoning for Al-Mawarith Share Computation and Heir Allocation"
title_zh: CVPD 在 QIAS 2026：用于 Al-Mawarith 份额计算与继承人分配的 RAG 引导大语言模型推理
authors: "Wassim Swaileh, Mohammed-En-Nadhir Zighem, Hichem Telli, Salah Eddine Bekhouche, Abdellah Zakaria Sellam, Fadi Dornaika, Dimitrios Kotzinos"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24012v1"
tags: ["query:profile-1"]
score: 10.0
evidence: RAG引导的大模型推理用于伊斯兰继承法计算
tldr: 本研究针对复杂的伊斯兰继承法（Al-Mawarith）计算任务，提出了一种基于检索增强生成（RAG）的LLM推理框架。该方法结合了符号计算器生成的合成数据、混合检索重排机制以及模式约束验证，有效解决了法律规则复杂、数值计算精确度要求高等难题。在QIAS 2026竞赛中，该系统以0.935的MIR-E得分荣获第一名，证明了RAG在阿拉伯语高精度法律推理中的卓越性能。
selection_source: fresh_fetch
motivation: 伊斯兰继承法涉及多阶段法律推理和复杂的规则变体，传统模型难以在确保法律一致性的同时处理精确的份额分配。
method: 采用RAG流水线，结合符号计算器生成的带推理轨迹的合成语料、混合检索重排技术以及模式约束的输出验证。
result: 该系统在QIAS 2026盲测中取得了0.935的MIR-E得分，位列官方排行榜首位。
conclusion: 检索增强与模式感知的生成方法显著提升了LLM在处理高精度阿拉伯法律推理任务中的可靠性。
---

## 摘要
伊斯兰遗产继承（Ilm al-Mawarith）是一项多阶段的法律推理任务，需要识别合格继承人、解决排除规则（hajb）、分配固定份额和剩余份额、处理诸如 awl 和 radd 等调整，并生成一致的最终分配方案。由于不同法学派别和民法典的差异，该任务变得更加复杂，要求模型在明确的法律配置下运行。我们针对这一场景提出了一种检索增强生成（RAG）流水线，结合了基于规则的合成数据生成、混合检索（稠密检索与 BM25）及交叉编码器重排序，以及基于模式约束的输出验证。我们使用符号化继承计算器生成了包含完整中间推理轨迹的大规模高质量合成语料库，确保了法律和数值的一致性。所提出的系统实现了 0.935 的 MIR-E 评分，并在 QIAS 2026 官方盲测排行榜上排名第一。结果表明，基于检索且感知模式的生成显著提高了高精度阿拉伯语法律推理任务的可靠性。

## Abstract
Islamic inheritance (Ilm al-Mawarith) is a multi-stage legal reasoning task requiring the identification of eligible heirs, resolution of blocking rules (hajb), assignment of fixed and residual shares, handling of adjustments such as awl and radd, and generation of a consistent final distribution. The task is further complicated by variations across legal schools and civil-law codifications, requiring models to operate under explicit legal configurations.   We present a retrieval-augmented generation (RAG) pipeline for this setting, combining rule-grounded synthetic data generation, hybrid retrieval (dense and BM25) with cross-encoder reranking, and schema-constrained output validation. A symbolic inheritance calculator is used to generate a large high-quality synthetic corpus with full intermediate reasoning traces, ensuring legal and numerical consistency.   The proposed system achieves a MIR-E score of 0.935 and ranks first on the official QIAS 2026 blind-test leaderboard. Results demonstrate that retrieval-grounded, schema-aware generation significantly improves reliability in high-precision Arabic legal reasoning tasks.

---

## 论文详细总结（自动生成）

这篇论文介绍了 CVPD 团队在 QIAS 2026 竞赛中的冠军方案，旨在解决复杂的伊斯兰继承法（Al-Mawarith）计算与份额分配问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：伊斯兰继承法是一项极具挑战性的法律推理任务，涉及识别合格继承人、处理排除规则（Hajb）、分配固定与剩余份额、以及进行复杂的数值调整（如 'Awl 和 Radd）。
*   **核心问题**：现有的通用大语言模型（LLM）在处理此类任务时面临三大困难：
    1.  **法律一致性**：不同法学派别（Madhahib）和民法典之间规则存在差异。
    2.  **数值精确度**：LLM 难以处理多阶段的分数运算和最终份额的平衡。
    3.  **结构化输出**：任务要求严格的 JSON 格式输出，而模型常出现格式错误或字段缺失。
*   **研究意义**：提出了一种结合符号推理与神经生成的 RAG 流水线，显著提升了阿拉伯语法律推理的可靠性。

### 2. 方法论
该系统由四个核心阶段组成的 RAG 流水线：
*   **知识库构建（符号计算器）**：
    *   开发了一个确定性的规则引擎，生成了约 **10 万个合成案例**。
    *   每个案例包含完整的推理轨迹（Reasoning Traces）和结构化输出，解决了训练数据中罕见案例（如 Radd 场景）不足的问题。
*   **混合检索与重排（Hybrid Retrieval）**：
    *   **检索层**：结合了稠密语义检索（Dense）和基于 BM25 的稀疏检索。
    *   **融合层**：使用加权倒数排名融合（RRF）整合结果。
    *   **重排层**：利用交叉编码器（Cross-Encoder）对 Top-K 候选文档进行精细化评分，确保检索到的法律先例高度相关。
*   **模式约束生成**：
    *   使用 **Qwen 3.5 9B** 作为基础模型。
    *   采用思维链（CoT）提示词，要求模型在 `<think>` 标签内进行推理，在 `<answer>` 标签内输出结构化 JSON。
*   **输出验证与修正**：
    *   实施了四种约束检查：键名完整性（$C_{keys}$）、类型正确性（$C_{types}$）、标签合法性（$C_{labels}$）和质量平衡（$C_{mass}$，即份额总和必须接近 100%）。
    *   对于非关键字段缺失，采用规则驱动的默认值进行填充。

### 3. 实验设计
*   **数据集**：QIAS 2026 官方数据集（5,900 条训练集，200 条开发集，500 条盲测集）。
*   **评估指标**：**MIR-E**（Mawarith Inheritance Reasoning Evaluation），这是一个加权得分，涵盖了继承人识别、排除逻辑、份额分配和最终分布等多个阶段。
*   **对比方法**：
    *   **基准测试**：对比了 Qwen 3.5 9B、Qwen 2.5 7B、Command-R 7B、Llama 3.2 1B 和 Gemma 3 12B 的原生表现。
    *   **排行榜对比**：与 Simplicity、KMS、QU-NLP 等其他参赛团队的方法进行对比。

### 4. 资源与算力
*   **模型**：主要使用了 Qwen 3.5 9B 开源模型。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。由于该方案侧重于 RAG 和推理阶段的优化，而非大规模预训练，其算力需求主要集中在合成数据生成和推理验证上。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了 10 万量级的合成数据生成。
    *   在 200 例开发集上进行了消融实验。
    *   在 500 例盲测集上验证了最终性能。
*   **充分性评价**：实验设计较为充分。作者不仅提供了排行榜结果，还专门针对**检索来源**（PDF 法律文本 vs. 网页数据）进行了消融分析，并对错误类型（排除逻辑、分数算术等）进行了详细的占比分析，体现了客观性和科学性。

### 6. 主要结论与发现
*   **RAG 的优越性**：单纯依靠 LLM 的参数化知识无法处理高精度的法律计算，检索增强和模式约束是必须的。
*   **数据质量至关重要**：消融实验显示，基于经过整理的 **PDF 法律文献**的检索效果远好于网页数据（PDF-only 组合得分 0.718 vs. Web-only 0.463）。
*   **性能表现**：该系统以 **0.935** 的 MIR-E 得分在 QIAS 2026 盲测中排名**第一**。

### 7. 优点
*   **神经符号结合**：利用符号计算器生成高质量合成数据，弥补了真实数据分布不均（如开发集中 Radd 案例占比远高于训练集）的问题。
*   **鲁棒性设计**：引入了确定性的后处理验证层，有效解决了 LLM 常见的格式崩溃和数值漂移问题。
*   **检索优化**：混合检索与交叉编码器重排的组合确保了法律依据的精准提取。

### 8. 不足与局限
*   **复杂逻辑瓶颈**：错误分析显示，42% 的错误源于复杂的**多代排除逻辑（Hajb）**，31% 源于罕见的分母分数运算。
*   **法学覆盖范围**：目前的规则配置尚未完全覆盖所有主要的逊尼派教法学派（Madhahib）及各国具体的民法变体。
*   **评估局限**：MIR-E 指标虽然全面，但仍无法完全衡量法律解释的忠实度及实际应用中的易用性。

（完）
