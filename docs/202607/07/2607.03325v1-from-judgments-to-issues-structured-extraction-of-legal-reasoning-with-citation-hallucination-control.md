---
title: "From Judgments to Issues: Structured Extraction of Legal Reasoning with Citation-Hallucination Control"
title_zh: 从判决书到争点：具有引用幻觉控制的法律推理结构化提取
authors: "Giovanni Piccioli, Alessia Fidelangeli, Piera Santin, Pierpaolo Vivo"
date: 2026-07-03
pdf: "https://arxiv.org/pdf/2607.03325v1"
tags: ["query:profile-1"]
score: 10.0
evidence: 利用大语言模型从法院判决中进行法律推理的结构化提取
tldr: 本研究针对意大利税务法院判决书，开发了一套自动化流水线，旨在将判决分解为独立的法律争议点，并提取基于IRAC框架的结构化XML表示。该系统采用DeepSeek V3模型以平衡性能与成本，并引入Linkoln解析器进行引用幻觉检测。通过专家验证，该流水线在争议点提取和引用准确性上表现优异，为法律检索和推理分析提供了大规模结构化数据支持。
selection_source: fresh_fetch
motivation: 旨在解决大规模法律判决书中法律推理提取困难以及大语言模型在法律引用中存在的幻觉问题。
method: 利用DeepSeek V3模型结合IRAC框架进行争议点提取，并配合Linkoln解析器建立引用幻觉检测过滤机制。
result: 在50份由法学博士标注的判决书上验证了流水线的有效性，实现了高一致性的争议点提取与引用识别。
conclusion: 该研究首次实现了针对意大利税务判决的专家验证级、具备幻觉控制的争议点结构化提取，为下游法律应用奠定了基础。
---

## 摘要
我们提出了一种自动化流水线，旨在将意大利税务法院的判决书分解为独立的法律争点，并为每个争点提取基于 IRAC 框架和法律三段论的结构化 XML 表示。该流水线针对约 330,000 份意大利税务法院的一审和二审判决书语料库，并围绕功能强大且具有成本效益的通用模型（DeepSeek V3）构建，这一选择源于以可持续成本处理数十万份文档的需求。为了解决大语言模型在法律引用方面公认的不可靠性，我们将提取步骤与自动幻觉检测过滤器相结合，该过滤器将模型生成的引用与专用解析器（Linkoln）在判决书文本中识别出的引用进行对比，并将其归一化为标准标识符（URN-NIR、ECLI、CELEX）。我们在由两名税务法博士标注的 50 份判决书上验证了该流水线，计算了争点提取和法律引用方面的标注者间一致性以及 LLM 与专家的一致性，并对幻觉过滤器进行了独立评估。据我们所知，这是首个针对意大利税务法院判决书的、经过专家验证且具有幻觉控制的争点级结构化提取流水线，它为争点级检索、引用网络分析以及大规模法律推理数据集构建等下游应用提供了具体的起点。

## Abstract
We present an automated pipeline that decomposes Italian tax-court judgments into individual legal issues and extracts, for each issue, a structured XML representation grounded in the IRAC framework and the legal syllogism. The pipeline targets a corpus of approximately $330{,}000$ first- and second-instance decisions of the Italian tax courts and is built around a capable yet cost-efficient general-purpose model (DeepSeek V3), a choice driven by the need to process several hundred thousand documents at a sustainable cost. To address the well-documented unreliability of large language models on legal citations, we couple the extraction step with an automatic hallucination-detection filter that compares the references produced by the model with those identified in the judgment text by a dedicated parser (Linkoln), normalised to standard identifiers (URN-NIR, ECLI, CELEX). We validate the pipeline on $50$ judgments annotated by two PhDs in tax law, computing inter-annotator agreement and LLM-vs-expert agreement on both issue extraction and legal citations, together with a stand-alone evaluation of the hallucination filter. To the best of our knowledge, this is the first issue-level, expert-validated structured extraction pipeline with hallucination control for Italian tax-court decisions, and it provides a concrete starting point for downstream applications such as issue-level retrieval, citation-network analysis, and the construction of large-scale datasets of legal reasoning.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为“从判决书到争点”的自动化流水线，旨在将海量的法律判决书转化为结构化、可机器读取且经过专家验证的法律推理数据集。

### 1. 核心问题与整体含义
*   **研究动机**：现代司法系统产生的判决书数量巨大（仅意大利税务法院每年就增加数十万份），法律专业人士难以高效检索和分析。
*   **核心挑战**：
    1.  **粒度问题**：判决书通常包含多个互不相关的法律争点，以整篇文档为单位进行检索或分析效率低下。
    2.  **幻觉风险**：大语言模型（LLM）在处理法律引用（法律条文、判例）时极易产生虚假引用（Hallucination）。
    3.  **成本平衡**：在大规模语料（33万份以上）上运行高性能模型需要极高的成本。
*   **整体含义**：论文提出了一种以“法律争点（Legal Issue）”为基本单位的提取框架，结合成本低廉的 DeepSeek V3 模型和严谨的引用过滤机制，实现了法律推理的大规模结构化。

### 2. 方法论
*   **核心思想**：将判决书分解为独立的法律争点，并基于 **IRAC（争点、规则、应用、结论）** 框架和**法律三段论**构建 XML 结构化表示。
*   **关键技术流程**：
    1.  **预处理**：清洗 PDF 文本，利用正则匹配识别“裁决理由”部分，过滤掉过短或无实质内容的判决。
    2.  **LLM 提取**：使用 DeepSeek V3 模型。针对长文档采用“两步走”提示策略：先识别争点、摘要和结果；再针对每个争点提取事实前提、法律引用、引用理由和法官推理。
    3.  **XML 模式验证**：通过 XSD 文件确保输出格式的严谨性。
    4.  **引用幻觉控制（核心创新）**：
        *   使用 **Linkoln** 解析器提取判决书原文中的所有合法引用，并归一化为标准标识符（如 ECLI, CELEX）。
        *   将 LLM 提取的引用与原文引用进行比对（文本匹配、URI 匹配、年份+编号匹配）。
        *   自动剔除无法在原文中找到对应项的引用，特别是针对“法律原则”类引用的严格过滤。

### 3. 实验设计
*   **数据集**：
    *   **全量语料**：约 330,000 份意大利一审和二审税务法院判决书（2021-2023年）。
    *   **验证集**：随机抽取 50 份判决书，由两名税务法学博士进行人工标注。
*   **Benchmark 与对比**：
    *   **标注者间一致性（IAA）**：衡量专家之间在争点识别和引用判断上的共识。
    *   **LLM vs. 专家**：评估模型在争点提取、引用识别及文本质量（Likert 5分制）上的表现。
    *   **基准对比**：将 LLM 提取效果与基于规则的全文解析（Linkoln 直接解析全文）进行对比。

### 4. 资源与算力
*   **模型选择**：主要使用 **DeepSeek V3**（API 调用），辅助使用 DeepSeek V3.1 进行标题修正。
*   **成本开销**：单份判决书处理成本约为 **0.0035 美元**。处理 32.8 万份文档的总成本约为 **1,150 美元**。
*   **算力说明**：文中未提及具体的 GPU 训练算力，因为该研究主要基于现有的 LLM API 进行推理和流水线构建，而非从头训练模型。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对 33 万份文档进行了全量处理。
    *   人工验证集为 50 份（其中 20 份为双盲交叉标注，30 份为独立标注）。
    *   进行了**敏感性分析**：运行两次相同的流水线以评估 LLM 的非确定性（Run-to-run consistency）。
*   **充分性评价**：虽然人工验证的样本量（50份）在统计学上不算巨大，但考虑到法律专家标注的高昂成本和深度（涉及 1-5 分的详细定性评分），实验设计较为客观且严谨。通过引入 IAA（标注者间一致性）作为性能上限，使得评估更具说服力。

### 6. 主要结论与发现
*   **争点提取**：LLM 表现出**高精确率（>93%）但中等召回率**。模型倾向于漏掉一些次要争点，但提取出的争点几乎都是真实的。
*   **引用准确性**：幻觉过滤器效果显著，将引用幻觉率从 **11.7% 降低至 0.9%**，且仅误删了 3% 的有效引用。
*   **幻觉类型**：LLM 在“法律原则（Principles）”类引用上表现最差（幻觉率达 69%），而在具体法条和判例上表现较好。
*   **文本质量**：专家对 LLM 生成的摘要、推理过程和结果的满意度极高（平均分 > 4.6/5）。

### 7. 优点
*   **实用性强**：直接解决了法律 AI 应用中“引用不可信”的痛点。
*   **成本效益高**：证明了使用中端模型（DeepSeek）配合结构化验证，可以达到甚至超过昂贵模型（如 Claude 4.5）的大规模处理效果。
*   **理论支撑**：提取模式并非随机设计，而是植根于 IRAC 和三段论等经典法律论证理论。
*   **模块化**：幻觉检测模块可以独立于 LLM 使用，具有良好的迁移性。

### 8. 不足与局限
*   **领域局限**：实验仅限于意大利税务法，不同法系（如英美法系）或不同法律领域的适用性有待验证。
*   **召回率瓶颈**：模型存在“欠提取”现象，可能会遗漏复杂的法律细节。
*   **非确定性**：实验发现 LLM 即使在 Temperature=0 时仍存在输出波动，这影响了结果的绝对稳定性。
*   **解析器依赖**：幻觉过滤器的效果高度依赖于底层解析器（Linkoln）的性能，若解析器无法识别某些特殊格式的引用，则会被误判为幻觉。

（完）
