---
title: "ClawArena-Team: Benchmarking Subagent Orchestration and Dynamic Workflows in Language-Model Agents"
title_zh: ClawArena-Team：语言模型智能体中子智能体编排与动态工作流的基准测试
authors: "Kaiwen Xiong, Haonian Ji, Shi Qiu, Zeyu Zheng, Cihang Xie, Xinyu Ye, Huaxiu Yao"
date: 2026-06-30
pdf: "https://arxiv.org/pdf/2606.31174v1"
tags: ["query:malm"]
score: 10.0
evidence: 子智能体编排与动态工作流基准测试
tldr: 本研究针对大语言模型（LLM）作为管理者的角色，提出了ClawArena-Team基准测试。该基准包含41个多轮、多模态场景，旨在隔离并衡量单个LLM领导者编排子智能体、管理动态工作流及分配权限的能力。通过限制主智能体的直接操作权限并使用固定子智能体池，研究发现权限管理是当前模型的主要瓶颈，且管理效能与API成本并不直接挂钩，为未来多智能体系统的优化提供了重要见解。
selection_source: fresh_fetch
motivation: 现有的基准测试侧重于单模型任务解决或固定多智能体系统的涌现行为，缺乏对单个LLM作为领导者进行子智能体编排和管理能力的独立评估。
method: 引入ClawArena-Team基准，通过限制主智能体权限并要求其指挥固定子智能体池，利用基于执行的SMS指标评估其管理水平。
result: "实验发现权限授予是主要瓶颈（准确率均低于50%），且管理质量与成本高度解耦，部分廉价开源模型表现出极高的性价比。"
conclusion: 该基准填补了智能体管理能力评估的空白，揭示了当前模型在权限控制和编排策略上的显著差异，为开发高效智能体管理架构提供了参考。
---

## 摘要
生产环境中的大语言模型（LLM）智能体越来越多地不再作为独立的解题者，而是作为管理者进行部署：主模型创建专门的子智能体，委派工作，并通过动态工作流编排它们的并行、异步返回结果。单个模型是否真的能运行这样一个团队在很大程度上尚未得到衡量：现有的基准测试评估的是策略自身的任务解决能力或固定多智能体系统的涌现行为，但没有一个基准测试能孤立出作为领导者的单个 LLM 的管理能力。我们推出了 ClawArena-Team，这是一个包含 41 个多轮、多模态、多目录场景的基准测试，涵盖 258 轮评估和 72 次阶段性更新，旨在衡量这种管理能力。主智能体被刻意限制：它原生仅能感知文本，且只能直接访问工作区的一部分。它指挥一个固定的、本地服务的子智能体池，因此得分差异反映的是管理技巧，而非原始能力。所有评分均基于执行，无需 LLM 评判：总分——子智能体管理得分（SMS）——由任务正确性乘以最小权限和模态路由因子得出。在 12 个闭源、社区托管和自托管模型上的实验表明，管理瓶颈在于权限授予而非感知（没有模型的工作区权限精确度超过 50%）；成本与管理质量是解耦的（API 成本跨度超过 100 倍，而总分跨度不到 4 倍，最便宜的开源模型处于帕累托前沿）；大多数排行榜得分聚集在 9.9 分的范围内，而编排行为的差异则超过一个数量级。代码和数据将会发布。

## Abstract
Production large language-model (LLM) agents are increasingly deployed not as lone problem-solvers but as managers: a main model creates specialized subagents, delegates work, and orchestrates their parallel, asynchronous returns through dynamic workflows. Whether one model can actually run such a team is largely unmeasured: existing benchmarks score a policy's own task-solving or a fixed multi-agent system's emergent behavior, but none isolate the management ability of the single LLM acting as leader. We introduce ClawArena-Team, a benchmark of 41 multi-turn, multimodal, multi-directory scenarios spanning 258 evaluation rounds and 72 staged updates that measures this management ability. The main agent is deliberately constrained: it natively perceives only text and directly accesses only part of the workspace. It commands a fixed, locally served subagent pool, so score differences reflect management skill, not raw capability. All scoring is execution-based with no LLM judge: an overall score -- the Subagent-Management Score (SMS) -- multiplies task correctness by a least-privilege and modality-routing factor. Across twelve proprietary, community-hosted, and self-hosted models, experiments show that the management bottleneck is privilege granting rather than perception (no model exceeds 50% workspace-permission precision); that cost and management quality are decoupled (API cost spans over 100 times while the overall score spans under 4 times, with the cheapest open models on the Pareto frontier); and that most leaderboard scores cluster within a 9.9-point band while orchestration behaviors diverge by more than an order of magnitude. Code and data will be released.

---

## 论文详细总结（自动生成）

以下是对论文《ClawArena-Team: Benchmarking Subagent Orchestration and Dynamic Workflows in Language-Model Agents》的结构化深度总结：

### 1. 论文的核心问题与整体含义
随着大语言模型（LLM）在生产环境中的应用，它们正从单纯的“问题解决者”转向“管理者”角色，即负责创建专门的子智能体、委派任务并编排动态工作流。然而，现有的基准测试（如衡量模型自身的解题能力或固定多智能体系统的涌现行为）无法独立评估单个 LLM 作为“领导者”的编排和管理能力。
**核心问题：** 如何量化评估单个 LLM 在指挥子智能体团队、管理权限和处理多模态动态流时的管理效能？

### 2. 论文提出的方法论
论文引入了 **ClawArena-Team** 基准测试，其核心思想是**“受限管理”**：
*   **角色设定：** 主智能体（Leader）被刻意限制，仅能感知文本且只能访问工作区的一小部分。它必须通过指挥一个固定的、本地服务的子智能体池（如视觉专家、文件专家等）来完成任务。
*   **动态工作流：** 任务涉及多轮对话、多目录操作和阶段性更新，要求主智能体进行并行的、异步的任务分配。
*   **核心指标：子智能体管理得分（SMS）**。该指标不依赖 LLM 评判，而是基于执行结果的客观计算：
    *   **公式逻辑：** $SMS = 任务正确性 \times 最小权限因子 \times 模态路由因子$。
    *   **最小权限原则：** 评估主智能体是否仅授予子智能体完成任务所需的最小权限，而非过度授权。
    *   **模态路由：** 评估主智能体是否正确地将多模态任务（如图像识别）分配给具备相应能力的子智能体。

### 3. 实验设计
*   **场景设置：** 包含 41 个复杂场景，涵盖 258 轮评估和 72 次阶段性更新。场景具有多模态（图像/文本混合）和多目录（复杂文件系统）的特点。
*   **对比方法：** 测试了 12 个主流模型，涵盖：
    *   **闭源模型：** GPT-4o, Claude 3.5 Sonnet, Gemini 1.5 Pro 等。
    *   **社区/开源模型：** Llama-3-70B, Qwen-2.5-72B, DeepSeek-V2.5 等。
*   **基准测试特性：** 采用基于执行（Execution-based）的评分，确保了结果的客观性，避免了“LLM 评判 LLM”带来的偏差。

### 4. 资源与算力
论文中提到子智能体池是“本地服务”的，但**未明确说明**具体的硬件配置（如 GPU 型号、数量）或总训练/推理时长。实验主要侧重于对现有 API 模型和开源模型推理能力的评估，而非从头训练模型。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了从顶级闭源模型到高性能开源模型的 12 个代表性对象，实验场景丰富（41 个场景，数百轮交互）。
*   **充分性：** 实验不仅关注最终成功率，还深入分析了权限管理精度、成本效益比和编排行为差异。
*   **客观性：** 通过基于执行的自动化评分系统，消除了主观因素，实验设计较为公平，因为所有模型面对的是完全相同的子智能体工具池。

### 6. 论文的主要结论与发现
*   **权限管理是瓶颈：** 所有测试模型在权限授予的精确度上均表现不佳，没有模型能超过 50%。模型倾向于过度授权，存在安全风险。
*   **成本与质量脱钩：** API 成本差异高达 100 倍，但管理得分（SMS）的差异不到 4 倍。
*   **开源模型的崛起：** 部分廉价的开源模型（如 Qwen-2.5, Llama-3）处于帕累托前沿（Pareto frontier），在性价比上优于昂贵的闭源模型。
*   **行为差异显著：** 虽然大多数模型在排行榜上的总分差距在 10 分以内，但它们在编排策略和调用子智能体的逻辑上存在数量级的差异。

### 7. 优点
*   **视角独特：** 首次将 LLM 的“管理/编排能力”与“原生解题能力”隔离开来进行评估。
*   **评估客观：** 摒弃了不可靠的 LLM 裁判，采用基于执行结果的硬性指标。
*   **关注安全：** 将“最小权限原则”引入智能体评估，具有很强的工程实践意义。
*   **多模态集成：** 真实模拟了现实中主模型处理无法直接感知的多模态数据的场景。

### 8. 不足与局限
*   **子智能体池固定：** 实验中子智能体是预设且固定的，现实中管理者可能需要动态创建或寻找新的子智能体。
*   **文本限制：** 强制主智能体仅感知文本可能在某种程度上限制了原生多模态模型（如 GPT-4o）的潜力发挥，虽然这是为了隔离管理能力。
*   **场景覆盖：** 41 个场景虽然深入，但在覆盖软件工程、法律、医疗等特定垂直领域的广度上仍有提升空间。

（完）
