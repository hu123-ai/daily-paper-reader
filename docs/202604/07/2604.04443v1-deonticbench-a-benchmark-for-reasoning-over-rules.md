---
title: "DeonticBench: A Benchmark for Reasoning over Rules"
title_zh: DeonticBench：一个规则推理基准测试
authors: "Guangyao Dou, Luis Brena, Akhil Deo, William Jurayj, Jingyu Zhang, Nils Holzenberger, Benjamin Van Durme"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04443v1"
tags: ["query:q6"]
score: 8.0
evidence: 针对法律规则和州住房法的推理基准测试
tldr: "针对大语言模型在处理复杂法律和政策规则（义务、许可、禁止）时推理能力不足的问题，本文推出了DeonticBench基准测试。该基准包含6,232个涵盖美国税务、航空、移民和住房法律的任务，支持自然语言推理和符号计算（Prolog）两种模式。研究发现，即使是顶尖模型在处理这些高风险、长上下文的规则推理时表现依然欠佳，为研究受限规则推理提供了重要资源。"
selection_source: fresh_fetch
motivation: 现有基准多侧重数学推理，缺乏针对法律和政策等高风险领域中复杂、长上下文规则推理（道义推理）的评估。
method: "构建了包含6,232个任务的DeonticBench，涵盖四个真实领域，并提供参考Prolog程序以支持模型通过符号计算进行推理。"
result: "顶尖模型在困难子集上的表现仅为44.4%至46.6%，且通过强化学习提升符号生成质量的效果仍不足以可靠解决任务。"
conclusion: DeonticBench为研究真实世界领域中基于上下文的规则推理提供了挑战性平台，揭示了当前模型在符号与非符号推理结合方面的局限性。
---

## 摘要
处理复杂的、特定上下文的规则对于大语言模型（LLMs）而言仍然具有挑战性。在法律和政策场景中，这表现为道义推理（deontic reasoning）：即在明确规则下对义务、许可和禁止事项进行推理。虽然近期许多基准测试强调短上下文的数学推理，但很少有关注长上下文、高风险的道义推理。为了填补这一空白，我们推出了 DEONTICBENCH，这是一个包含 6,232 个任务的基准测试，涵盖了美国联邦税收、航空公司行李政策、美国移民管理以及美国州住房法。这些任务可以通过多种方式解决，包括直接使用语言进行推理，或借助符号计算。除了自由形式的思维链（chain-of-thought）推理外，DEONTICBENCH 还支持一种可选的基于求解器的工作流，模型可以将法规和案例事实翻译成可执行的 Prolog 代码，从而生成形式化的题目解释和显式的程序追踪。我们为所有实例发布了参考 Prolog 程序。在最先进的 LLMs 和编程模型中，困难子集的最佳性能在 SARA Numeric 上仅为 44.4%，在 Housing 上仅为 46.6 macro-F1。我们进一步研究了通过监督微调和强化学习进行符号程序生成的训练。尽管训练提高了 Prolog 的生成质量，但目前的强化学习方法仍无法可靠地解决这些任务。总之，DEONTICBENCH 为研究真实世界领域中基于上下文的规则推理提供了一个基准，涵盖了符号和非符号两种设置。

## Abstract
Reasoning with complex, context-specific rules remains challenging for large language models (LLMs). In legal and policy settings, this manifests as deontic reasoning: reasoning about obligations, permissions, and prohibitions under explicit rules. While many recent benchmarks emphasize short-context mathematical reasoning, fewer focus on long-context, high-stakes deontic reasoning. To address this gap, we introduce DEONTICBENCH, a benchmark of 6,232 tasks across U.S. federal taxes, airline baggage policies, U.S. immigration administration, and U.S. state housing law. These tasks can be approached in multiple ways, including direct reasoning in language or with the aid of symbolic computation. Besides free-form chain-of-thought reasoning, DEONTICBENCH enables an optional solver-based workflow in which models translate statutes and case facts into executable Prolog, leading to formal problem interpretations and an explicit program trace. We release reference Prolog programs for all instances. Across frontier LLMs and coding models, best hard-subset performance reaches only 44.4% on SARA Numeric and 46.6 macro-F1 on Housing. We further study training with supervised fine-tuning and reinforcement learning for symbolic program generation. Although training improves Prolog generation quality, current RL methods still fail to solve these tasks reliably. Overall, DEONTICBENCH provides a benchmark for studying context-grounded rule reasoning in real-world domains under both symbolic and non-symbolic settings.

---

## 论文详细总结（自动生成）

以下是对论文《DeonticBench: A Benchmark for Reasoning over Rules》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：评估大语言模型（LLMs）在处理复杂、特定上下文规则下的“道义推理”（Deontic Reasoning）能力，即对法律、政策中的义务、许可和禁止事项的推理。
*   **研究动机**：
    *   **高风险场景需求**：LLMs 正被部署于医疗、法律等严谨领域，推理错误可能导致严重的法律和财务风险。
    *   **现有基准局限**：目前的推理基准多侧重于短上下文的数学或 STEM 问题，缺乏针对长上下文、真实世界法规（如税法、住房法）的深度评估。
    *   **幻觉问题**：即使是具备思维链（CoT）能力的模型，在处理形式化规则时仍容易产生难以察觉的幻觉。

### 2. 方法论：DeonticBench 框架
*   **核心思想**：构建一个多领域、可执行的规则推理基准，支持“直接语言推理”和“符号求解器辅助推理”两种模式。
*   **关键技术细节**：
    *   **统一格式**：将不同领域的法律条文、案例事实和问题转化为统一的输入输出格式。
    *   **符号化工作流（LLM-to-Prolog）**：模型将自然语言描述的法规和事实翻译成 Prolog 代码，利用外部求解器（SWI-Prolog）执行并得出确定性结论。
    *   **自动形式化流水线**：采用动态生成模式，针对每个实例实时生成 Prolog 模块，避免了维护全局库的复杂性，并提供参考程序供训练使用。
    *   **困难子集（Hard Set）构建**：通过自动化筛选（多个顶尖模型均失败的案例）结合人工校验，筛选出最具挑战性的任务。

### 3. 实验设计
*   **数据集/场景**：
    1.  **SARA (美国联邦税收)**：包含数值计算（税额）和二元逻辑判定。
    2.  **Airline (航空行李政策)**：基于航空公司政策计算行李费用。
    3.  **Housing (美国州住房法)**：基于各州法规回答二元（是/否）问题。
    4.  **USCIS-AAO (美国移民管理 - 新构建)**：基于移民上诉案例判定申请是否被接受。
*   **Benchmark 规模**：共 6,232 个任务。
*   **对比方法**：
    *   **前沿模型**：GPT-4.1, GPT-5.1, GPT-5.2, O3, Claude 4.5 Sonnet, Gemini 2.5 Flash。
    *   **开源/推理模型**：Kimi K2, Qwen3-235B。
    *   **编程专用模型**：GPT-5.2-Codex, Qwen3-Coder 系列。
*   **评估指标**：数值任务采用带 1 美元容差的准确率，逻辑任务采用 Macro-F1。

### 4. 资源与算力
*   **算力投入**：
    *   **监督微调（SFT）与 DPO**：使用 4 张 NVIDIA H100 GPU。
    *   **强化学习（Dr. GRPO）**：使用 4 张 NVIDIA H200 GPU，基于 VeRL 框架。
*   **训练细节**：针对 Qwen2.5-32B-Instruct 进行了 SFT、DPO 和 Dr. GRPO（一种带有谓词感知奖励函数的 GRPO 变体）的后训练实验。

### 5. 实验数量与充分性
*   **实验规模**：
    *   评估了 8 种通用前沿模型和 3 种编程模型。
    *   涵盖了三种提示策略（Direct, Zero-shot, Few-shot）。
    *   进行了推理努力程度（Reasoning Effort）的消融实验。
    *   对失败模式进行了详细的分类统计（规则错误、事实错误、数值错误、实现 Bug）。
*   **充分性评价**：实验设计非常充分且客观。通过 1000 次 Bootstrap 重采样计算置信区间，确保了结果的统计显著性；同时区分了“错误预测”和“弃权（代码崩溃）”，评估视角全面。

### 6. 主要结论与发现
*   **性能瓶颈**：即使是最强模型（如 O3, GPT-5.1），在困难子集上的表现也仅在 40%-50% 左右，远未达到饱和。
*   **推理努力的边际效应**：增加推理步数（Reasoning Effort）并不总能提升性能，有时甚至会产生负面影响。
*   **符号推理的权衡**：使用 Prolog 求解器能提供可审计的路径，但会增加“弃权率”（代码无法运行）；直接推理虽然覆盖率高，但错误率也更高。
*   **训练局限性**：虽然 SFT 和 RL 能显著提高 Prolog 代码的语法正确性，但在端到端任务准确率上的提升依然有限，尤其是在复杂的数值计算上。

### 7. 优点与亮点
*   **真实性与高风险**：选取了税收、移民等真实且严肃的领域，具有极高的实际应用参考价值。
*   **符号化贡献**：为 6,232 个任务提供了高质量的参考 Prolog 程序，这为神经符号（Neuro-symbolic）研究提供了宝贵的资产。
*   **新数据集**：贡献了 USCIS-AAO 移民上诉数据集，并精细处理了事实提取，避免了标签泄露。
*   **深度分析**：不仅给出了分数，还深入分析了模型在不同领域（法律 vs 数值）的失败根源。

### 8. 不足与局限
*   **领域局限**：虽然涵盖了四个领域，但主要集中在美国法律体系，对全球其他法律体系的泛化性未知。
*   **RL 奖励函数挑战**：目前的强化学习奖励函数在处理非执行代码时依赖谓词重叠度，这种信号可能不足以引导模型学习复杂的逻辑结构。
*   **提示词敏感性**：模型性能对提示词格式高度敏感，尤其是在开源模型中，这限制了系统的鲁棒性。
*   **应用限制**：论文明确指出该基准仅用于研究，不能作为专业的法律或税务建议。

（完）
