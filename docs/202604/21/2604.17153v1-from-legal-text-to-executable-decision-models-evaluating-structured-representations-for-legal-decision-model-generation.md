---
title: "From Legal Text to Executable Decision Models: Evaluating Structured Representations for Legal Decision Model Generation"
title_zh: 从法律文本到可执行决策模型：评估法律决策模型生成的结构化表示
authors: David Graus
date: 2026-04-18
pdf: "https://arxiv.org/pdf/2604.17153v1"
tags: ["query:q4"]
score: 9.0
evidence: 从法律文本生成可执行的决策模型
tldr: 本研究探讨了利用大语言模型（LLM）将法律文本转化为可执行决策模型的方法。研究基于荷兰《环境与规划法》的真实数据集，对比了原始文本、语义角色标注（SRL）和输入输出（I/O）约束等不同结构化表示对生成效果的影响。通过结构相似度和功能等效性双重评估，发现I/O约束能显著提升生成质量，且LLM能有效简化冗余逻辑。该研究为法律信息学自动化提供了新见解并开源了数据集。
selection_source: fresh_fetch
motivation: 法律文本转化为可执行逻辑长期依赖人工，效率低且评估难，亟需探索利用LLM实现自动化的有效路径。
method: 对比了原始法律文本、语义角色标注、I/O约束及其组合四种输入条件，并从结构相似度和执行结果一致性两个维度进行评估。
result: "I/O约束使模型相似度提升了37-54%，生成的模型在51-53%的测试场景中与标准模型功能等效，且LLM能消除约一半的冗余逻辑。"
conclusion: 结构化表示（尤其是I/O约束）能显著优化法律决策模型的生成，且结构相似性与结果等效性在评估中具有互补性。
---

## 摘要
将法律文本转化为可执行的决策逻辑是法律信息学中一个长期存在的挑战。随着大语言模型（LLM）的兴起，这一任务重新引起了人们的关注，但由于需要大量的手动编码和评估，它仍然具有挑战性。我们使用了一个独特的真实世界数据集，该数据集将生产级决策模型与荷兰《环境与规划法》中的法律文本配对。这些模型为 Omgevingsloket 政府平台提供支持，公民可以在该平台上查询环境活动的许可要求。我们研究了中间结构化表示是否能改进基于 LLM 的法律文本可执行决策模型生成。我们比较了四种输入条件：原始法律文本、富含语义角色标注的文本、富含输入和输出（I/O）约束的文本，以及同时包含两者的文本。我们从两个维度进行评估：结构评估，通过图核和图的描述性统计量来衡量与金标准决策模型的相似性；结果评估，通过在预配置的测试场景中执行模型来衡量功能等效性。我们的研究结果表明，I/O 约束提供了主要的改进（与基准相比相似度提高了 37-54%），而语义角色标注的改进较小。结果评估显示，尽管生成的模型通常更小、更简单，但在 51-53% 的测试场景中与金标准相匹配。我们发现 LLM 消除了占节点总数 45-55% 的冗余直通逻辑。重要的是，结构相似性和结果等效性是互补的：结构相似性并不保证结果等效，反之亦然。为了促进可重复性，我们公开了包含 95 个生产决策模型及其相关法律文本的数据集以及所有实验代码。

## Abstract
Transforming legal text into executable decision logic is a longstanding challenge in legal informatics. With the rise of LLMs, this task has gained renewed interest, but remains challenging due to requiring extensive manual coding and evaluation. We use a unique real-world dataset that pairs production-grade decision models with legal text from the Dutch Environment and Planning Act. These models power the Omgevingsloket government platform, where citizens check permit requirements for environmental activities. We study whether intermediate structured representations can improve LLM-based generation of executable decision models from legal text. We compare four input conditions: raw legal text, text enriched with semantic role labels, text enriched with input and output constraints, and text enriched with both. We evaluate along two dimensions: structural evaluation, through similarity to gold decision models with graph kernels and graphs' descriptive statistics, and outcome evaluation, through functional equivalence by executing models on pre-configured test scenarios. Our findings show that I/O constraints provide the dominant improvement (+37-54% similarity over baseline), while semantic role labels show modest improvements. Outcome evaluation shows that generated models match the gold standard on 51-53% of test scenarios, even though generated models are typically smaller and simpler. We find LLMs eliminate redundant pass-through logic that comprises up to 45-55% of nodes. Importantly, structural similarity and outcome equivalence are complementary: structural similarity does not guarantee outcome equivalence, and vice versa. To facilitate reproducibility, we publicly release our dataset of 95 production decision models with associated legal text and all experimental code.

---

## 论文详细总结（自动生成）

### 论文总结：从法律文本到可执行决策模型

#### 1. 核心问题与整体含义（研究动机和背景）
将法律文本转化为机器可读且可执行的决策逻辑（Law-to-Code）是法律信息学领域的长期挑战。传统的转化方法依赖于法律专家和程序员的大量手动编码，效率低下且难以维护。随着大语言模型（LLM）的兴起，自动化生成决策模型成为可能。本研究的核心问题是：**如何通过结构化的中间表示（如语义标注或接口约束）来辅助 LLM 更准确地从法律文本生成可执行的决策模型？** 研究背景基于荷兰《环境与规划法》（Omgevingswet）的真实政务场景，具有极高的实用价值。

#### 2. 方法论：核心思想与技术细节
研究对比了四种不同的输入表示方法，旨在探讨“增强源文本理解”与“约束目标输出”哪种策略更有效：
*   **核心思想**：利用 1-shot 提示词工程，引导 LLM 将法律条文转化为简化的 JSON 格式决策图（基于 DMN 标准）。
*   **四种实验条件**：
    1.  **Text (基准)**：仅提供原始法律条文。
    2.  **Text+srl (源增强)**：在文本中加入语义角色标注（演员、动作、对象、接收者），使用专门在荷兰法律语料上微调的 BERT 模型（FlintFiller-srl）提取。
    3.  **Text+io (目标约束)**：提供决策模型的输入变量名和最终输出定义（接口规范）。
    4.  **Text+srl+io (组合)**：同时提供语义标注和 I/O 约束。
*   **技术细节**：将复杂的 XML 格式 DMN 模型简化为轻量级 JSON 结构，保留决策表逻辑、命中策略（Hit Policies）和图拓扑结构。

#### 3. 实验设计
*   **数据集**：使用来自荷兰政府 Omgevingsloket 平台的 **95 个真实生产级决策模型** 及其对应的法律条文。
    *   **结果模型 (Outcome models, N=50)**：判定活动是否需要许可或申报。
    *   **需求模型 (Requirements models, N=45)**：判定申报或许可的具体材料要求是否满足。
*   **评估维度**：
    *   **结构评估**：使用最短路径图核（Shortest-Path Kernel）衡量生成模型与金标准（Gold Standard）在拓扑上的相似度。
    *   **结果评估**：通过执行引擎运行 13,080 个测试用例，对比生成模型与金标准在功能上的一致性（等效性）。

#### 4. 资源与算力
*   **模型**：使用了 **GPT-5.1**（注：根据论文发表时间 2026 年设定）。
*   **参数设置**：Temperature 设为 0.1 以保证确定性，开启 JSON 模式，禁用思维链（CoT）以防止输出冗余。
*   **算力说明**：论文未明确提及具体的 GPU 型号或训练时长，因为该研究主要基于 API 进行推理实验，而非模型训练。

#### 5. 实验数量与充分性
*   **实验规模**：针对 95 个目标模型，每种条件进行 5 次独立运行（更换不同的 1-shot 示例），总计生成 **1,900 个决策模型**。
*   **测试充分性**：结果评估涵盖了 58 个可测试模型和 **13,080 个输入组合**。
*   **评价**：实验设计非常充分且客观。研究不仅关注了宏观的相似度指标，还深入分析了法律文本复杂度（句子长度、交叉引用数等）对生成质量的影响，并探讨了示例选择的敏感性。

#### 6. 主要结论与发现
*   **I/O 约束是关键**：提供输入/输出规范对提升模型质量贡献最大（相似度提升 37-54%），而语义角色标注（SRL）的提升效果有限。
*   **功能等效性**：生成的模型在约 51-53% 的测试场景中实现了功能等效。在最佳运行状态下，33% 的模型达到了 100% 的准确率。
*   **LLM 的简化能力**：LLM 能够自动识别并消除金标准模型中冗余的“直通节点”（占原始节点 45-55%），生成更简洁但功能等效的结构。
*   **评估指标的互补性**：结构相似度高并不代表执行结果一致，反之亦然。这表明法律 AI 的评估必须结合静态结构和动态执行。

#### 7. 优点
*   **真实性高**：使用真实政府生产环境的数据，而非人工合成的简单案例。
*   **双重评估体系**：结合了图论相似度和软件工程中的功能测试，评估维度全面。
*   **开源贡献**：公开了包含 95 个生产级模型的数据集和实验代码，填补了法律-代码转化领域高质量开源数据的空白。

#### 8. 不足与局限
*   **地域局限性**：仅针对荷兰法律和荷兰语环境，跨司法管辖区的通用性有待验证。
*   **模型依赖**：实验仅基于单一的先进 LLM，未对比不同规模或厂商的模型表现。
*   **人机协作缺位**：虽然结论建议“人类定义接口+LLM生成逻辑”，但实验本身未包含人类在环（Human-in-the-loop）的交互式修正测试。
*   **复杂度瓶颈**：对于句子极长或交叉引用极多的复杂法律条文，生成质量显著下降。

（完）
