---
title: "From Large Language Model Predicates to Logic Tensor Networks: Neurosymbolic Offer Validation in Regulated Procurement"
title_zh: 从大语言模型谓词到逻辑张量网络：受监管采购中的神经符号报价验证
authors: "Cedric Haufe, Frieder Stolzenburg"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05539v1"
tags: ["query:q5"]
score: 8.0
evidence: 受监管采购中的法律可验证决策
tldr: 本研究针对受监管公共采购中的报价文件验证，提出了一种结合大语言模型（LLM）与逻辑张量网络（LTN）的神经符号方法。该方法利用LLM提取信息作为谓词，并通过LTN进行逻辑聚合以做出可审计的决策。其核心贡献在于将领域知识与语义理解相结合，实现了决策过程的可解释性与法律可验证性，为受监管环境下的AI应用提供了新思路。
selection_source: fresh_fetch
motivation: 受监管的公共机构需要报价验证决策既准确又符合法律可追溯性，而纯子符号AI往往缺乏必要的透明度。
method: 采用LLM从文本中提取谓词信息，并结合逻辑张量网络（LTN）将领域规则转化为可计算的逻辑约束进行决策。
result: 在真实报价文档语料库上的实验表明，该方法在保持与现有模型相当性能的同时，显著提升了决策的可解释性和模块化程度。
conclusion: 神经符号架构成功实现了语义理解与形式逻辑的融合，为受监管领域提供了具备可解释AI（XAI）特性的稳健解决方案。
---

## 摘要
我们提出了一种神经符号方法，即结合符号与亚符号人工智能，用于验证受监管公共机构中的报价文件。我们利用语言模型提取信息，然后结合逻辑张量网络（LTN）进行聚合，以做出可审计的决策。在受监管的公共机构中，决策必须以事实正确且法律可验证的方式做出。我们的神经符号方法允许将现有的特定领域知识与语言模型的语义文本理解相结合。由我们的流水线产生的决策可以通过谓词值、规则真值和相应的文本段落来证明其合理性，从而实现基于真实报价文件语料库的规则检查。我们在真实语料库上的实验表明，所提出的流水线实现了与现有模型相当的性能，而其核心优势在于其可解释性、模块化谓词提取以及对可解释人工智能（XAI）的显式支持。

## Abstract
We present a neurosymbolic approach, i.e., combining symbolic and subsymbolic artificial intelligence, to validating offer documents in regulated public institutions. We employ a language model to extract information and then aggregate with an LTN (Logic Tensor Network) to make an auditable decision. In regulated public institutions, decisions must be made in a manner that is both factually correct and legally verifiable. Our neurosymbolic approach allows existing domain-specific knowledge to be linked to the semantic text understanding of language models. The decisions resulting from our pipeline can be justified by predicate values, rule truth values, and corresponding text passages, which enables rule checking based on a real corpus of offer documents. Our experiments on a real corpus show that the proposed pipeline achieves performance comparable to existing models, while its key advantage lies in its interpretability, modular predicate extraction, and explicit support for XAI (Explainable AI).

---

## 论文详细总结（自动生成）

这篇论文介绍了一种结合大语言模型（LLM）与逻辑张量网络（LTN）的神经符号（Neurosymbolic）方法，旨在解决受监管公共采购环境下的报价文件自动验证问题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在公共机构（如大学）的采购过程中，验证报价单是否合法有效不仅需要高准确率，更需要**法律可追溯性**和**可审计性**。
*   **研究动机**：
    *   **纯神经模型（黑盒）**：虽然文本处理能力强，但无法提供决策背后的逻辑依据，难以应对法律审计。
    *   **纯规则系统**：虽然逻辑清晰，但难以直接处理非结构化的PDF文档，需要预先提取结构化特征。
    *   **解决方案**：通过神经符号架构，利用LLM的语义理解能力提取特征（谓词），再利用LTN的模糊逻辑进行推理，从而兼顾强大的文本处理能力与透明的决策逻辑。

### 2. 方法论
该研究提出了一套模块化的神经符号流水线，主要包含以下关键环节：
*   **核心思想**：将文档验证分解为“感知（LLM提取谓词）”和“推理（LTN逻辑聚合）”两个阶段。
*   **谓词提取层（LLM）**：
    *   定义了8个领域特定谓词（如报价标题、报价编号、有效期、支付条款等），并扩展为11个输入通道。
    *   采用了两种提取策略：**MCSR（多类自反思）**和**CISC（置信度知情自一致性）**，通过LLM对文档块（Chunks）进行评估并输出[0, 1]之间的模糊真值。
*   **逻辑决策层（LTN）**：
    *   **逻辑框架**：使用“实逻辑（Real Logic）”，将逻辑公式映射到张量操作上。
    *   **模糊算子**：对比了Gödel、Product和Łukasiewicz三种模糊逻辑后端。
    *   **规则建模**：手动编码领域专家知识（如：`标题清晰 ∧ 支付条款存在 → 有效报价`），通过可学习的门控参数（Gating Parameters）调整不同特征的影响力。
*   **算法流程**：PDF文本提取 -> 文本分块与检索（BM25+语义重排） -> LLM谓词评估 -> LTN逻辑推理 -> 输出决策及解释（谓词真值与规则触发情况）。

### 3. 实验设计
*   **数据集**：使用来自德国梅泽堡应用科学大学真实采购流程的200份PDF文档（35%为有效报价，65%为发票、送货单等无效文件）。
*   **实验协议**：采用分层5折交叉验证，并重复5次（共25个Folds），以确保在小样本集上的评估稳健性。
*   **对比方法（Benchmarks）**：
    1.  **LTN Baseline**：直接从文本特征（如TF-IDF）学习谓词的传统LTN。
    2.  **BERT**：纯神经模型（bert-base-german-cased）直接进行分类。
    3.  **LLM-only**：直接利用Qwen2.5（14B/32B）通过Prompt进行端到端判断。
    4.  **IE + 确定性规则**：基于模式识别的传统信息提取方法。

### 4. 资源与算力
*   **模型选择**：主要使用 **Qwen2.5-14B-Instruct**，在JSON生成失败时回退至 **Qwen2.5-32B-Instruct**。
*   **算力说明**：论文强调所有计算均在**本地基础设施**上运行，以符合欧洲通用数据保护条例（GDPR）对敏感采购数据的隐私要求。
*   **具体细节**：文中未明确提及具体的GPU型号、数量或训练总时长，但提到LLM仅通过Prompting使用，未进行微调，主要的训练开销在于LTN层极少量的门控参数优化。

### 5. 实验数量与充分性
*   **实验规模**：进行了25组交叉验证实验，涵盖了多种逻辑后端（Gödel, Product, Łukasiewicz）与谓词提取策略（MCSR, CISC）的组合。
*   **充分性评价**：
    *   **客观性**：通过与纯神经模型（BERT）和纯LLM方法的对比，客观展示了神经符号方法的性能区间。
    *   **局限性**：样本量（200份）相对较小，且标注由单人完成，可能存在一定的标注偏差。
    *   **公平性**：所有模型均在相同的交叉验证框架下评估，并针对不平衡数据使用了F1-score作为主要指标。

### 6. 主要结论与发现
*   **性能表现**：MCSR+LTN组合达到了 **0.874** 的F1值，虽略低于纯LTN基准（0.899），但优于纯BERT（0.859）和14B LLM直接分类（0.843）。
*   **提取策略影响**：MCSR（自反思）显著优于CISC（自一致性），表明在谓词提取阶段进行结构化的自反思对最终决策至关重要。
*   **逻辑后端**：不同的提取方法对逻辑后端的敏感度不同，Product范数在MCSR下表现最好，而Łukasiewicz在CISC下更优。
*   **核心价值**：该方法最大的成功在于实现了**可解释AI（XAI）**，决策可以追溯到具体的文本段落和逻辑规则，满足了监管审计需求。

### 7. 优点
*   **高透明度**：通过LTN将黑盒LLM的输出转化为可理解的逻辑推理，提供了法律所需的证据链。
*   **模块化设计**：谓词提取与逻辑推理分离，方便领域专家后期调整业务规则而无需重新训练模型。
*   **隐私合规**：通过本地化部署开源LLM，解决了敏感政务/财务数据的出域安全问题。

### 8. 不足与局限
*   **数据局限性**：数据集规模较小且仅限于德国法律/机构背景，跨语言和跨领域的泛化能力有待验证。
*   **人工依赖**：逻辑规则仍需手动编码，且谓词定义依赖领域专家经验。
*   **性能瓶颈**：LLM提取谓词的准确性直接决定了后续逻辑推理的上限，对于布局极其复杂的“边缘案例”文档仍存在误判风险。

（完）
