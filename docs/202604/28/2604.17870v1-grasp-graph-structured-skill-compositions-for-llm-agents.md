---
title: "GraSP: Graph-Structured Skill Compositions for LLM Agents"
authors: "Tianle Xia, Lingxiang Hu, Yiding Sun, Ming Xu, Lan Xu, Siying Wang, Wei Xu, Jie Jiang"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17870v1"
tags: ["query:malm"]
score: 9.0
evidence: 大语言模型智能体架构与技能编排
tldr: 针对大语言模型智能体在面对大量技能时性能下降的问题，本文提出GraSP架构。该架构通过在技能检索与执行间引入编译层，将扁平的技能集转化为具有显式因果依赖的有向无环图（DAG）。GraSP支持节点级验证和局部修复，显著提升了复杂任务下的执行效率与成功率，证明了结构化编排是提升智能体可靠性的关键。
selection_source: fresh_fetch
motivation: 智能体面临的瓶颈已从技能可用性转向技能编排，过多的技能反而会因缺乏结构化组织而损害任务性能。
method: 提出首个可执行技能图架构GraSP，将技能集编译为带类型的有向无环图，并利用五种算子进行局部修复和节点验证。
result: "在多个基准测试中，GraSP在奖励提升（最高+19分）和环境步数减少（最高41%）方面均优于ReAct和Reflexion等基准方法。"
conclusion: 结构化的技能编排而非单纯扩大技能库，是实现大语言模型智能体可靠执行的核心要素。
---

## Abstract
Skill ecosystems for LLM agents have matured rapidly, yet recent benchmarks show that providing agents with more skills does not monotonically improve performance -- focused sets of 2-3 skills outperform comprehensive documentation, and excessive skills actually hurt. The bottleneck has shifted from skill availability to skill orchestration: agents need not more skills, but a structural mechanism to select, compose, and execute them with explicit causal dependencies. We propose GraSP, the first executable skill graph architecture that introduces a compilation layer between skill retrieval and execution. GraSP transforms flat skill sets into typed directed acyclic graphs (DAGs) with precondition-effect edges, executes them with node-level verification, and performs locality-bounded repair through five typed operators -- reducing replanning from O(N) to O(d^h). Across ALFWorld, ScienceWorld, WebShop, and InterCode with eight LLM backbones, GraSP outperforms ReAct, Reflexion, ExpeL, and flat skill baselines in every configuration, improving reward by up to +19 points over the strongest baseline while cutting environment steps by up to 41%. GraSP's advantage grows with task complexity and is robust to both skill over-retrieval and quality degradation, confirming that structured orchestration -- not larger skill libraries -- is the key to reliable agent execution.

---

## 论文详细总结（自动生成）

这篇论文由腾讯团队提出，针对大语言模型（LLM）智能体在技能使用上的瓶颈进行了深入研究。以下是对该论文的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：随着智能体技能库的不断扩大，研究发现提供更多技能并不一定能提升性能。当技能超过3-4个时，性能反而会因“认知负荷”和“幻觉”而下降。
*   **核心问题**：瓶颈已从“技能可用性”转向“技能编排（Orchestration）”。现有的智能体通常将技能视为扁平的列表或简单的序列，缺乏对技能间因果依赖、前置条件和执行顺序的结构化表达，导致在复杂长程任务中容错率低、重规划成本高。
*   **整体含义**：提出 GraSP 架构，通过在检索与执行之间引入“编译层”，将技能组织为有向无环图（DAG），实现结构化的技能编排。

### 2. 论文提出的方法论
GraSP 架构包含四个关键阶段：
*   **记忆增强检索（Memory-conditioned Retrieval）**：结合语义匹配和历史成功经验，从技能库中筛选出最相关的技能子集，并计算检索置信度。
*   **DAG 编译（DAG Compilation）**：将扁平的技能集转化为带类型的 DAG。节点是实例化的技能调用，边分为三类：
    *   **状态边（State）**：前序技能的效果满足后续技能的前置条件。
    *   **数据边（Data）**：前序技能的输出作为后续技能的输入。
    *   **顺序边（Order）**：基于经验或资源冲突的软约束。
*   **验证执行与局部修复（Verified Execution & Local Repair）**：按拓扑顺序执行，每步进行前/后置条件验证。若失败，利用**局部修复代数**（包含 REBIND, INSERT_PREREQ, SUBSTITUTE, REWIRE, BYPASS 五种算子）仅修复受影响的子图，将重规划复杂度从 $O(N)$ 降低到 $O(d^h)$。
*   **置信度路由（Confidence-based Routing）**：根据检索置信度决定是采用结构化执行还是退回到反应式控制（如 ReAct）。

### 3. 实验设计
*   **数据集/场景**：
    *   **ALFWorld**：家庭环境模拟任务。
    *   **ScienceWorld**：科学实验任务（长程、复杂）。
    *   **WebShop**：在线购物交互。
    *   **InterCode**：Bash 代码执行任务。
*   **对比基准（Baselines）**：
    *   **ReAct**：基础的思考-行动循环。
    *   **Reflexion**：带自我反思的重试机制。
    *   **ExpeL**：基于经验学习的智能体。
    *   **ReAct + Skills**：将技能作为扁平工具调用的基准。
*   **模型底座**：横跨 8 个主流 LLM，包括 DeepSeek V3.2、GPT-4.1、Claude-4-Sonnet、GLM-5、Gemini 2.5 Pro 等。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。由于该研究主要基于 LLM API 进行推理和编排架构的验证，不涉及大规模的模型预训练，因此算力消耗主要集中在 API 调用成本上。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个基准测试和 8 个模型底座上进行了 48 组核心配置实验，每组实验重复 3 次取平均值。
*   **消融实验**：详细测试了记忆模块、DAG 编译、局部修复和路由机制的贡献。
*   **鲁棒性测试**：针对“技能过度检索（数量过多）”和“技能质量下降”进行了专门的压力测试。
*   **充分性评价**：实验设计非常全面，涵盖了不同参数规模和厂商的模型，验证了方法的普适性；通过任务复杂度分级分析，证明了 GraSP 在长程任务中的显著优势，实验结果客观且具有说服力。

### 6. 主要结论与发现
*   **性能领先**：GraSP 在所有配置下均优于所有基准方法，奖励值最高提升 19 分。
*   **效率提升**：相比 ReAct，环境交互步数最多减少了 41%，显著降低了推理成本。
*   **结构化优势**：任务越复杂，GraSP 的优势越明显。局部修复机制比全局重规划更有效。
*   **“少即是多”的验证**：证明了通过编译层过滤无关技能并建立显式依赖，可以克服 LLM 在面对大量技能时的性能退化问题。

### 7. 优点
*   **架构创新**：首次提出“技能编译”概念，填补了检索与执行之间的结构化空白。
*   **容错性强**：局部修复代数提供了一种优雅的错误恢复机制，避免了“牵一发而动全身”的重规划。
*   **通用性好**：不依赖特定模型，在开源和闭源模型上均表现出一致的增益。
*   **理论支撑**：将重规划复杂度从线性降低到局部指数级（$O(d^h)$），具有明确的计算效率提升。

### 8. 不足与局限
*   **DAG 限制**：目前的 DAG 假设无法处理循环执行模式（如需要反复迭代微调的任务），可能需要引入循环结构扩展。
*   **编译开销**：虽然减少了环境交互步数，但 DAG 编译和验证过程本身增加了额外的 LLM 调用次数（尽管总步数下降了）。
*   **环境依赖**：局部修复依赖于环境状态的可观测性和前/后置条件的准确定义，在部分观测或模糊反馈的环境中效果可能受限。

（完）
