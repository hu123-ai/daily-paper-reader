---
title: "CHICO-Agent: An LLM Agent for the Cross-layer Optimization of 2.5D and 3D Chiplet-based Systems"
authors: "Qihang Wu, Aman Arora, Vidya A. Chhabria"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18764v1"
tags: ["query:malm"]
score: 9.0
evidence: 用于系统优化的管理-现场多智能体协作
tldr: 随着AI负载增长，单体芯片面临物理和经济极限，2.5D/3D芯粒（Chiplet）系统成为主流，但其跨层协同设计极其复杂。本文提出CHICO-Agent，一种基于大语言模型的优化框架，通过持久化知识库捕捉参数趋势，并利用多智能体工作流协调设计空间探索。该框架在降低系统成本方面优于传统模拟退火算法，并为设计者提供了可解释的审计追踪。
selection_source: fresh_fetch
motivation: 2.5D/3D芯粒系统涉及应用、架构、芯片和封装的跨层协同，导致设计空间巨大且权衡复杂，传统方法难以高效优化。
method: 提出CHICO-Agent框架，利用大语言模型构建持久化知识库，并通过“管理-执行”多智能体协作模式进行设计空间探索。
result: 实验表明，CHICO-Agent相比模拟退火基准能找到成本更低的配置，并提供可解释的设计决策路径。
conclusion: CHICO-Agent证明了大语言模型在处理复杂的跨层硬件设计优化任务中具有显著的效率和可解释性优势。
---

## Abstract
The rapid growth of large language models (LLMs) and AI workloads has pushed monolithic silicon to its reticle and economic limits, accelerating the adoption of 2.5D/3D chiplet systems. However, these systems increase design complexity by requiring co-design across multiple levels of the computing stack, including application, architecture, chip, and package. The resulting design space is highly combinatorial, with trade-offs among latency, energy, area, and cost. To address this challenge, we propose CHICO-Agent, an LLM-driven optimization framework for 2.5D/3D chiplet-based systems. CHICO-Agent maintains a persistent knowledge base to capture parameter-outcome trends and coordinates exploration through an admin-field multi-agent workflow. Compared with a simulated-annealing baseline, CHICO-Agent finds lower-cost configurations and provides an interpretable audit trail for designers.

---

## 论文详细总结（自动生成）

以下是对论文《CHICO-Agent: An LLM Agent for the Cross-layer Optimization of 2.5D and 3D Chiplet-based Systems》的结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
随着AI负载的爆发式增长，单体硅芯片（Monolithic Silicon）已接近物理和经济极限。**芯粒（Chiplet）技术**通过2.5D/3D异构集成成为主流趋势。然而，芯粒系统的设计空间极其复杂，涉及应用层、架构层、芯片层和封装层的跨层协同优化（Co-design）。
*   **核心问题**：传统优化方法（如模拟退火 SA、强化学习 RL）属于“黑盒”搜索，依赖随机扰动，容易陷入局部最优，且超参数调节困难，缺乏可解释性。
*   **整体含义**：本文提出 **CHICO-Agent**，利用大语言模型（LLM）的推理能力，通过多智能体协作框架，在巨大的组合设计空间中进行有逻辑的探索，旨在降低系统成本并提供设计洞察。

### 2. 论文提出的方法论
CHICO-Agent 采用了一种**分层多智能体迭代框架**，核心思想是将优化任务转化为“推理-执行-学习”的循环。
*   **核心架构**：
    *   **管理智能体（Admin Agent）**：负责全局编排。它分析当前上下文，进行结构化推理，并生成 $N$ 个探索计划。
    *   **现场智能体（Field Agents）**：并行执行计划。它们调用分析模型评估候选设计的 PPAC（性能、功耗、面积、成本）指标，并反馈结果。
*   **关键技术细节**：
    *   **持久化上下文（Persistent Context）**：包含领域知识、PPAC 分析模型公式、设计约束黑名单（防止生成无效架构）。
    *   **演进上下文（Evolving Context）**：记录探索历史（KNOWHOW.md）、最优设计（BEST.csv）和所有评估日志。
    *   **优化目标**：最小化加权成本函数 $Cost = \alpha E + \beta A + \gamma L + \theta C$（能源、面积、延迟、制造成本）。
*   **流程**：编排阶段（推理生成计划） $\rightarrow$ 探索阶段（并行评估） $\rightarrow$ 评估阶段（更新知识库并决定下一步）。

### 3. 实验设计
*   **场景与负载**：选择了 6 个代表性的 GEMM（通用矩阵乘法）工作负载，涵盖 GPT-2、ViT（视觉 Transformer）、ResNet-50、VGG-16 和 MobileNetV2。
*   **应用配置文件**：定义了四种优化偏好：平衡（Balance）、移动端（Mobile）、汽车（Automotive）和可穿戴设备（Wearables），每种偏好的 PPAC 权重不同。
*   **基准方法（Benchmark）**：主要对比对象是**模拟退火（Simulated Annealing, SA）**算法。
*   **对比维度**：系统总成本（Cost）与收敛到该成本所需的墙钟时间（Wall-clock time）的帕累托前沿（Pareto Frontier）。

### 4. 资源与算力
*   **模型使用**：实验使用了 **OpenAI 的 GPT-5.3-codex**（注：论文设定时间为 2026 年）。
*   **并行度**：设置 $N=100$，即每轮迭代同时启动 100 个现场智能体进行并行探索。
*   **算力说明**：文中未明确提及运行该框架的具体 GPU 型号或具体的训练/推理总时长，但指出其单次迭代的推理开销高于 SA 的随机扰动。

### 5. 实验数量与充分性
*   **实验规模**：针对 6 种负载 $\times$ 4 种配置文件，共进行了 **24 组核心对比实验**。
*   **充分性**：
    *   为了公平对比，对 SA 进行了大规模的超参数网格搜索（390 种配置），确保 SA 处于最佳表现。
    *   对 CHICO-Agent 进行了推理努力程度（Reasoning Effort）和迭代次数的消融/扫参。
    *   **客观性**：通过帕累托前沿展示了成本与时间的权衡，不仅关注最终结果，也关注搜索效率。实验覆盖了从单体到复杂 3D 堆叠的多种硬件组合，具有较好的代表性。

### 6. 论文的主要结论与发现
*   **性能优越**：在 24 个实验案例中，CHICO-Agent 在 **20 个案例**中找到了比 SA 成本更低的系统配置。
*   **跨层洞察**：LLM 能够发现复杂的参数关联。例如，它学会了将“输入驻留（IS）数据流”与“Split-K 划分策略”结合，以提高异构芯粒的利用率。
*   **硬件偏好**：相比 SA 倾向于选择大型或异构芯粒，CHICO-Agent 往往能收敛到更小、同质化的芯粒组合，从而通过减少封装白空间来降低成本。
*   **易用性**：CHICO-Agent 显著减少了超参数调节的负担，仅需设置推理努力程度和迭代次数。

### 7. 优点
*   **可解释性（Interpretability）**：这是最大亮点。框架生成的推理轨迹（Reasoning Traces）为设计者提供了“审计追踪”，解释了为何选择特定架构，而不仅仅是给出一个数值。
*   **知识累积**：通过 `KNOWHOW.md` 维护持久知识，使优化过程具有“经验”，而非盲目搜索。
*   **处理复杂约束**：通过黑名单机制有效过滤了物理上不可行的设计（如不稳定的 3D 堆叠），提高了搜索效率。

### 8. 不足与局限
*   **运行时间开销**：由于 LLM 推理的延迟，CHICO-Agent 的单次迭代时间远高于 SA。在对实时性要求极高的场景下可能受限。
*   **信号缺失风险**：在“平衡（Balance）”配置文件下（权重均等），LLM 的推理引导信号会变弱，导致其表现有时不如纯随机搜索的 SA。
*   **模型依赖**：优化效果高度依赖于底层 LLM 的推理能力，且分析模型的准确性直接决定了优化结果的质量。

（完）
