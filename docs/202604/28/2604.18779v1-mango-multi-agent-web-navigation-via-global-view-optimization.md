---
title: "Mango: Multi-Agent Web Navigation via Global-View Optimization"
authors: "Weixi Tong, Yifeng Di, Tianyi Zhang"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18779v1"
tags: ["query:malm"]
score: 9.0
evidence: 多智能体网页导航方法
tldr: Mango利用多智能体优化和汤普森采样实现高效的网页导航。
selection_source: fresh_fetch
motivation: 多智能体网页导航方法。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## Abstract
Existing web agents typically initiate exploration from the root URL, which is inefficient for complex websites with deep hierarchical structures. Without a global view of the website's structure, agents frequently fall into navigation traps, explore irrelevant branches, or fail to reach target information within a limited budget. We propose Mango, a multi-agent web navigation method that leverages the website structure to dynamically determine optimal starting points. We formulate URL selection as a multi-armed bandit problem and employ Thompson Sampling to adaptively allocate the navigation budget across candidate URLs. Furthermore, we introduce an episodic memory component to store navigation history, enabling the agent to learn from previous attempts. Experiments on WebVoyager demonstrate that Mango achieves a success rate of 63.6% when using GPT-5-mini, outperforming the best baseline by 7.3%. Furthermore, on WebWalkerQA, Mango attains a 52.5% success rate, surpassing the best baseline by 26.8%. We also demonstrate the generalizability of Mango using both open-source and closed-source models as backbones. Our data and code are open-source and available at https://github.com/VichyTong/Mango.

---

## 论文详细总结（自动生成）

这是一份关于论文《Mango: Multi-Agent Web Navigation via Global-View Optimization》的结构化深入分析总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
现有的网页导航智能体（Web Agents）通常从网站的根 URL（主页）开始逐层探索。这种“自顶向下”的模式在面对结构复杂、层级深邃的现代网站时效率极低，容易导致智能体陷入无关的分支、遭遇导航陷阱，或在有限的动作预算内无法触达目标信息。
**核心问题**：如何在缺乏全局结构认知的情况下，避免盲目搜索，实现高效的网页导航？
**研究动机**：通过构建网站的全局视图并动态优化起始路径，提升智能体在复杂任务中的成功率和效率。

### 2. 论文提出的方法论：MANGO
MANGO 采用了一种多智能体协作框架，核心流程分为三个阶段：

*   **全局结构分析（Global Structure Analysis）**：
    *   利用轻量级爬虫（BFS 策略）获取网站内部链接。
    *   结合 LLM 生成关键词，通过 Google Search 的 `site:` 操作符检索相关页面。
    *   使用 BM25 算法对所有获取的 URL 进行语义相关性评分，筛选出前 10-20 个候选 URL 作为潜在的导航起点。
*   **基于多臂老虎机（MAB）的 URL 选择**：
    *   将 URL 选择建模为有限寿命的多臂老虎机问题。
    *   **关键技术**：采用**汤普森采样（Thompson Sampling）**。每个 URL 对应一个 Beta 分布 $(\alpha, \beta)$，初始值由 BM25 得分决定。
    *   动态更新：根据导航结果（成功、失败、死路）实时更新 Beta 分布参数，从而在“探索新起点”和“利用已知高潜力起点”之间取得平衡。
*   **多智能体协作与情境记忆**：
    *   **导航智能体**：负责在浏览器环境中执行具体动作（点击、输入等）。
    *   **反思智能体**：评估导航轨迹。若任务未完成但路径有希望，给予正向奖励；若路径无效，则标记为“枯竭”并给予负向奖励。
    *   **情境记忆（Episodic Memory）**：存储历史轨迹和反思结果，当智能体再次访问同一 URL 时，提取记忆以避免重复错误动作。

### 3. 实验设计
*   **数据集 / Benchmark**：
    1.  **WebVoyager**：包含 129 个真实世界的复杂网页任务（如 Amazon, Coursera 等）。
    2.  **WebWalkerQA**：包含 680 个任务，涵盖单源（Single-source）和多源（Multi-source）问答，难度分为易、中、难三个等级。
*   **对比方法**：
    *   **AgentOccam**：目前 SOTA 的网页智能体，侧重于优化动作和观察空间。
    *   **WebWalker**：采用探索-批判（Explore-Critic）范式的基准方法。
*   **Backbone 模型**：测试了 GPT-5-mini（闭源）以及 Qwen3 系列（4B, 8B, 14B, 32B，开源）。

### 4. 资源与算力
论文**未明确说明**具体的硬件算力（如 GPU 型号、数量）或训练/推理的总时长。由于该方法主要基于 LLM API 调用和轻量级爬虫，其核心开销在于 API 的 Token 消耗和网页解析的延迟，而非大规模模型训练。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流 Benchmark 上进行了全面测试，涵盖了从 4B 到 32B 的多种模型规模，验证了方法的泛化性。
*   **消融实验**：非常充分。对比了随机 URL 选择、仅靠搜索选择、以及基于 MCTS（蒙特卡洛树搜索）的变体，证明了汤普森采样和全局视图的优越性。
*   **敏感性分析**：对导航预算 $b$、采样迭代次数、相关性权重 $\kappa$、爬虫限制 $\tau$ 等关键超参数进行了详尽的压力测试。
*   **客观性**：通过失败案例分析（Failure Case Analysis）诚实地指出了预算耗尽和推理错误等问题，实验设计较为客观公平。

### 6. 论文的主要结论与发现
*   **性能突破**：在 GPT-5-mini 下，MANGO 在 WebVoyager 上的成功率达到 63.6%（提升 7.3%），在 WebWalkerQA 上达到 52.5%（大幅提升 26.8%）。
*   **多源任务优势**：在需要整合多个页面信息的复杂任务中，MANGO 的表现甚至达到了基准方法的两倍。
*   **效率与规模**：性能随模型规模（Qwen3 系列）单调增长。虽然在强模型下动作计数（AC）略高，但这是因为 MANGO 能够坚持完成长程任务，而基准方法往往早早放弃。
*   **搜索策略**：汤普森采样在预算受限的情况下比 MCTS 更高效，因为它不需要大量的模拟步骤即可快速收敛到潜力路径。

### 7. 优点（亮点）
*   **全局视野**：打破了传统智能体“从零开始”的局限，通过预分析网站结构极大地缩小了搜索空间。
*   **动态自适应**：引入 MAB 框架，使智能体具备了在不同候选起点之间智能分配“体力”（预算）的能力。
*   **记忆增强**：反思机制与情境记忆的结合，有效解决了 LLM 容易在复杂交互中“迷路”或“原地打转”的顽疾。

### 8. 不足与局限
*   **覆盖率瓶颈**：对于拥有数百万页面的超大型动态网站（如 Amazon 全站），轻量级爬虫只能获取局部视图，可能遗漏深层信息。
*   **初始依赖**：如果初始的关键词生成或 BM25 评分出现严重偏差，可能会导致预算在错误的候选 URL 上浪费。
*   **推理成本**：在追求高成功率的同时，多次迭代和反思增加了 API 的调用成本和响应延迟。
*   **模型固有错误**：约 15% 的失败源于 LLM 本身的阅读理解或推理错误，这属于底层模型能力限制，导航策略无法完全弥补。

（完）
