---
title: "AgenticCache: Cache-Driven Asynchronous Planning for Embodied AI Agents"
authors: "Hojoon Kim, Yuheng Wu, Thierry Tambe"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24039v1"
tags: ["query:malm"]
score: 10.0
evidence: 具身智能的多智能体规划框架
tldr: AgenticCache通过重用缓存规划来减少大模型调用延迟，优化多智能体协作。
selection_source: fresh_fetch
motivation: 具身智能的多智能体规划框架。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## Abstract
Embodied AI agents increasingly rely on large language models (LLMs) for planning, yet per-step LLM calls impose severe latency and cost. In this paper, we show that embodied tasks exhibit strong plan locality, where the next plan is largely predictable from the current one. Building on this, we introduce AgenticCache, a planning framework that reuses cached plans to avoid per-step LLM calls. In AgenticCache, each agent queries a runtime cache of frequent plan transitions, while a background Cache Updater asynchronously calls the LLM to validate and refine cached entries. Across four multi-agent embodied benchmarks, AgenticCache improves task success rate by 22% on average across 12 configurations (4 benchmarks x 3 models), reduces simulation latency by 65%, and lowers token usage by 50%. Cache-based plan reuse thus offers a practical path to low-latency, low-cost embodied agents. Code is available at https://github.com/hojoonleokim/MLSys26_AgenticCache.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **AgenticCache**，这是一个专为具身智能（Embodied AI）设计的、由缓存驱动的异步规划框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：具身智能代理（如机器人）目前高度依赖大语言模型（LLM）进行逐步骤规划。然而，每一步都调用 LLM 会带来巨大的**推理延迟**和**经济成本**，导致代理在执行任务时频繁停顿，无法实现实时响应。
*   **核心发现**：研究者发现具身任务具有强烈的“**规划局部性（Plan Locality）**”，即下一个规划步骤在很大程度上可以根据当前状态和上一个规划进行预测（例如：抓取物体后，下一步通常是移动到目标地点）。
*   **研究目标**：利用这种局部性，通过缓存重用规划，减少对 LLM 的频繁调用，从而降低延迟和成本。

### 2. 方法论
*   **核心思想**：引入一个运行时缓存来存储频繁的规划转换（Plan Transitions），并配合一个后台异步更新器（Cache Updater）来验证和修正缓存内容。
*   **关键技术细节**：
    *   **缓存结构**：采用 2-gram 模式 $\langle P_i \to P_j \rangle$，存储从规划 $P_i$ 到 $P_j$ 的转换，并附带任务状态元数据（如步数、持有物品数、完成进度等）。
    *   **评分机制**：通过公式 $S(P_i \to P_j) = C(P_i \to P_j) \cdot I(P_j)$ 选择最佳规划。其中 $C$ 是转换计数（局部频率），$I$ 是重要性因子（全局确认率，即 LLM 确认该规划正确的比例）。
    *   **异步更新器（Cache Updater）**：在后台调用 LLM。如果 LLM 返回的规划与缓存一致，则增加计数（确认）；如果不一致，则立即替换当前执行的规划并更新缓存（修正）。
    *   **查询抑制**：为了节省成本，当缓存规划被确认或刚被修正时，会暂时抑制冗余的 LLM 查询。
    *   **冷启动优化**：支持通过少量离线成功轨迹预填充缓存（Warm-start），但也支持从零开始在线学习。

### 3. 实验设计
*   **数据集/场景**：涵盖了四个复杂的多智能体具身基准测试：
    1.  **TDW-MAT**：多智能体运输任务。
    2.  **TDW-COOK**：协作烹饪任务（具有强时间依赖性）。
    3.  **TDW-GAME**：拼图组装任务（需要视觉推理）。
    4.  **BEHAVIOR-1K**：家庭运输任务（涉及异构机器人，如机械臂、机器狗、无人机）。
*   **对比方法**：
    *   **Synchronous Baseline**：标准的逐步同步规划方法。
    *   **Parallelized Planning-Acting**：边执行边规划的并行方法。
    *   **Speculative Planning**：投机采样规划（用小模型提议，大模型验证）。
*   **使用模型**：GPT-5、GPT-5-mini、GPT-5-nano（注：论文设定在 2026 年，使用了当时假设的 GPT-5 系列模型）。

### 4. 资源与算力
*   **硬件环境**：
    *   **评估**：NVIDIA GeForce RTX 4090 GPU, AMD Ryzen 9 7950X CPU。
    *   **训练（针对 COMBO 视觉模型）**：2 × NVIDIA H100 PCIe (80GB)。
*   **时长**：所有基准测试的完整评估约需 10 天；COMBO 模型训练约需 2 天。

### 5. 实验数量与充分性
*   **实验规模**：在 12 种配置（4 个基准 × 3 种模型规模）下进行了测试。
*   **充分性**：
    *   包含了**消融实验**，分别验证了“异步更新”和“规划替换”机制的有效性。
    *   进行了**冷启动分析**，证明即使没有预填充缓存，系统也能快速收敛。
    *   进行了**缓存有效性分析**，量化了随时间推移规划准确率的提升。
    *   实验设计考虑了多智能体协作、长程规划等复杂维度，结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能提升**：AgenticCache 在任务成功率上平均提高了 **22%**。
*   **效率优化**：模拟延迟平均降低了 **65%**（最高达 86%），Token 使用量减少了 **50%**，成本显著下降。
*   **鲁棒性**：相比于投机规划或简单的并行规划，AgenticCache 的异步修正机制能更好地处理动态环境中的突发变化，避免了无效动作的堆积。

### 7. 优点
*   **创新性地利用局部性**：将计算机体系结构中的“缓存”概念成功引入具身智能规划。
*   **异步修正机制**：不仅是简单的缓存，还具备实时纠错能力，平衡了速度与准确性。
*   **极低的开销**：每个代理的缓存占用仅为 0.1-1.0 KB，内存开销几乎可以忽略不计。
*   **通用性强**：兼容现有的 LLM 服务技术和多智能体框架。

### 8. 不足与局限
*   **多智能体冲突**：在极少数情况下，缓存重用可能导致智能体之间的资源竞争或协作步调不一致（虽然更新器最终会修正，但存在短暂延迟）。
*   **规划局部性的限制**：在完全开放、无规律的探索任务中，规划局部性可能变弱，导致缓存命中率下降。
*   **2-gram 的局限性**：简单的 2-gram 可能无法捕捉极长距离的逻辑依赖，未来可能需要更高阶的转换表示。

（完）
