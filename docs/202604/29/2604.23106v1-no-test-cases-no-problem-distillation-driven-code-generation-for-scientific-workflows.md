---
title: "No Test Cases, No Problem: Distillation-Driven Code Generation for Scientific Workflows"
title_zh: 无需测试用例亦无妨：蒸馏驱动的科学工作流代码生成
authors: "Siddeshwar Raghavan, Tanwi Mallick"
date: 2026-04-25
pdf: "https://arxiv.org/pdf/2604.23106v1"
tags: ["query:malm"]
score: 9.0
evidence: 无需训练的多智能体代码生成框架
tldr: 针对科学工作流中缺乏输入/输出测试用例的难题，本文提出 MOSAIC 框架。该框架无需训练，采用多智能体协作模式，通过学生-教师知识蒸馏机制，利用领域示例和结构化问题分解来引导代码生成。此外，引入统一上下文窗口（CCW）以减少子问题间的幻觉。在 SciCode 基准测试中，MOSAIC 在准确性、可执行性和数值精度方面均优于现有方法，且对轻量级模型友好。
selection_source: fresh_fetch
motivation: 传统的基于执行反馈的代码生成方法在科学领域失效，因为科学工作流往往缺乏现成的 I/O 测试用例且生成成本极高。
method: 提出 MOSAIC 框架，利用知识蒸馏、结构化分解和统一上下文窗口（CCW）在无监督情况下引导多智能体生成代码。
result: 在 SciCode 基准测试中，MOSAIC 显著提升了代码的准确性、可执行性和数值精度，且在使用轻量级模型时表现优异。
conclusion: MOSAIC 证明了在没有测试用例的情况下，通过结构化蒸馏和上下文管理也能实现高质量的科学代码生成。
---

## 摘要
现有的用于代码生成的多智能体大语言模型（LLM）框架通常利用执行反馈，并通过输入/输出（I/O）测试用例进行迭代改进。然而，这并不适用于科学工作流，因为在这些场景中不存在 I/O 测试用例，且生成这些用例本身就需要解决当前的问题。为此，我们提出了 MOSAIC，这是一个无需训练的多智能体框架，用于在没有 I/O 监督的情况下进行科学代码生成。MOSAIC 不依赖执行反馈，而是采用了一种学生-教师知识蒸馏框架，通过特定领域的示例和结构化问题分解来引导生成。为了进一步减轻链式子问题中的幻觉，我们引入了整合上下文窗口（CCW），以维持智能体之间的一致推理。在 SciCode 基准测试上的实验表明，MOSAIC 在依赖轻量级模型的同时，在准确性、可执行性和数值精度方面均优于现有方法。

## Abstract
Existing multi-agent Large Language Model (LLM) frameworks for code generation typically use execution feedback and improve iteratively using Input/Output (I/O) test cases. However, this does not work for scientific workflows, where I/O test cases do not exist, and generating them requires solving the very problem at hand. To address this, we introduce MOSAIC, a training-free multi-agent framework for scientific code generation without I/O supervision. Instead of execution feedback, MOSAIC employs a student-teacher knowledge distillation framework that grounds generation through domain-specific examples and structured problem decomposition. To further mitigate hallucinations across chained subproblems, we introduce a Consolidated Context Window (CCW) for maintaining consistent reasoning across agents. Experiments on the SciCode benchmark show that MOSAIC improves accuracy, executability, and numerical precision over existing approaches while relying on lightweight models.

---

## 论文详细总结（自动生成）

这篇论文针对科学计算领域的代码生成问题提出了创新的解决方案。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的多智能体代码生成框架（如 MapCoder, AgentCoder）高度依赖**输入/输出（I/O）测试用例**来进行迭代调试。然而，在科学研究（如物理、生物、材料科学）中，生成测试用例本身往往就需要先解决该科学问题，导致了“没有代码就没法验证，没有验证就没法生成代码”的死循环。
*   **研究背景**：科学代码要求极高的数值精度和领域推理能力，且通常由多个相互依赖的子问题组成。随着上下文增加，大模型（LLM）容易产生幻觉和逻辑不一致。

### 2. 方法论：MOSAIC 框架
MOSAIC 是一个**无需训练、跨模型通用**的多智能体框架，其核心思想是**“架构化接地”（Architectural Grounding）**，即通过结构化设计而非执行反馈来确保代码正确性。
*   **学生-教师知识蒸馏**：
    *   **教师模块**：利用极少量（≤5%）带有标准答案的验证集，由“自我反思智能体”生成领域特定的推理模板和伪代码。
    *   **学生模块**：在测试阶段，利用教师生成的模板进行 Few-shot 提示，引导生成。
*   **多智能体协作流程**：
    1.  **自我反思智能体 (Self-Reflection)**：在教师端构建领域知识库。
    2.  **推理智能体 (Rationale)**：将复杂科学问题分解为逻辑步骤。
    3.  **代码智能体 (Coding)**：根据推理步骤编写 Python 代码。
    4.  **调试智能体 (Debugger)**：仅负责**语法接地**（修复语法错误和导入错误），不涉及逻辑验证。
*   **整合上下文窗口 (CCW)**：为了防止长链子问题导致的幻觉，CCW 仅保留之前子问题的函数签名和单句摘要，而非全部代码，从而在保持逻辑连贯的同时精简上下文。

### 3. 实验设计
*   **核心数据集**：**SciCode** 基准测试，涵盖物理、化学、生物、数学、材料科学 5 个领域，包含 65 个主问题和 283 个子问题。
*   **通用数据集**：为了验证通用性，还在 HumanEval、MBPP 和 APPS 上进行了测试。
*   **对比方法**：
    *   基础方法：Direct, CoT, Self-Planning, Analogical。
    *   先进框架：MapCoder, CodeSIM, LATS。
*   **模型后端**：GPT-4o, Claude 3.5 Sonnet, Gemini 2.5 Flash 以及多种开源模型（DeepSeek R1, Llama 4 等）。

### 4. 资源与算力
*   **算力说明**：论文未详细列出具体的 GPU 型号、数量或训练时长。
*   **原因**：MOSAIC 是一个**无需训练（Training-free）**的框架，主要通过 API 调用闭源模型或部署开源模型进行推理。实验主要依赖于 LangGraph 智能体编排框架进行逻辑调度。

### 5. 实验数量与充分性
*   **实验规模**：在 SciCode 的全部 283 个子问题上进行了详尽测试，并跨越了 3 个主流闭源模型和 4 个开源模型。
*   **消融实验**：针对 Rationale 智能体、CCW 机制、Few-shot 提示和自我反思模块分别做了增量实验，验证了各组件的贡献。
*   **客观性**：实验对比了多种不同架构的基准方法，并分析了数值偏差（Precision），实验设计较为全面、客观。

### 6. 主要结论与发现
*   **性能提升**：MOSAIC 在 SciCode 上的准确率比基线提升了高达 **24%**。
*   **语法 vs 语义**：在缺乏测试用例时，优先保证“语法正确（可执行）”能显著降低整体错误率，剩下的语义错误通过蒸馏驱动的推理来弥补。
*   **上下文管理**：CCW 机制被证明能有效减少幻觉，全量保留历史代码反而会因干扰信息过多导致性能下降。
*   **模型差距**：闭源模型在科学推理上仍显著优于开源模型（主问题解决率高出约 3 倍）。

### 7. 优点与亮点
*   **突破死循环**：首次系统性地解决了科学工作流中“无测试用例”的代码生成难题。
*   **轻量且通用**：无需对 LLM 进行微调，通过智能体编排即可实现显著的性能增益。
*   **数值精度高**：实验证明 MOSAIC 生成的代码在数值计算上比基线更接近目标值。

### 8. 不足与局限
*   **领域标签依赖**：目前仍需手动提供问题的领域标签（如“物理”），自动分类会导致约 10-12% 的性能下降。
*   **生物领域表现较弱**：在生物学问题上的表现普遍低于其他学科，可能是由于该领域在验证集中的样本较少，导致蒸馏出的模板质量不高。
*   **模型能力边界**：框架效果高度依赖底层模型的推理能力，对于较弱的开源模型，提升幅度有限。

（完）
