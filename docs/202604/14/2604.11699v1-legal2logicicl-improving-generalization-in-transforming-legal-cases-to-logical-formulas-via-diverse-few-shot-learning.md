---
title: "Legal2LogicICL: Improving Generalization in Transforming Legal Cases to Logical Formulas via Diverse Few-Shot Learning"
title_zh: Legal2LogicICL：通过多样化少样本学习提升法律案例到逻辑公式转换的泛化能力
authors: "Jieying Xue, Phuong Minh Nguyen, Ha Thanh Nguyen, May Myo Zin, Ken Satoh"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11699v1"
tags: ["query:q6"]
score: 8.0
evidence: 利用大语言模型将法律案例转化为逻辑公式进行推理
tldr: 本研究针对法律逻辑推理中高质量标注数据稀缺的问题，提出了Legal2LogicICL框架。该框架利用大语言模型的上下文学习能力，通过检索增强生成技术，在语义和法律结构层面平衡示例的多样性与相似性，并有效缓解了实体信息带来的检索偏差。此外，研究还构建了Legal2Proleg数据集。实验证明，该方法在无需额外训练的情况下，显著提升了法律案例向逻辑公式转换的准确性、稳定性和泛化能力。
selection_source: fresh_fetch
motivation: 现有的法律逻辑推理系统依赖微调模型，受限于高质量标注数据的匮乏，难以实现良好的泛化。
method: 提出Legal2LogicICL框架，通过在语义和结构层面平衡示例多样性并消除实体偏差，实现高效的少样本检索与上下文学习。
result: 在多个大模型上的实验表明，该方法显著提高了法律文本转逻辑公式的准确率和稳定性，并发布了新数据集Legal2Proleg。
conclusion: 该研究证明了通过优化的上下文学习策略，可以在不增加训练成本的情况下实现可靠且可解释的法律逻辑推理。
---

## 摘要
本研究旨在通过将自然语言处理（NLP）的最新进展与使用大语言模型（LLMs）的法律领域自适应少样本学习技术相结合，提升基于逻辑的法律推理系统的泛化能力。现有的基于逻辑的法律推理流水线通常依赖微调模型将自然语言法律案例映射为逻辑公式，然后再将其转发给符号推理器。然而，此类方法受到高质量标注训练数据稀缺的严重制约。为解决这一局限性，我们提出了一种新型的基于 LLM 的法律推理框架，通过检索增强生成实现有效的上下文学习。具体而言，我们引入了 Legal2LogicICL，这是一个少样本检索框架，在潜在语义表示层面和法律文本结构层面平衡了示例的多样性与相似性。此外，我们的方法通过减轻法律文本中由实体引起的检索偏差，明确地考虑了法律结构；在法律文本中，冗长且高度具体的实体提及往往主导了语义表示，并掩盖了具有法律意义的推理模式。我们的 Legal2LogicICL 构建了具有信息量且鲁棒的少样本演示，在无需额外训练的情况下实现了准确且稳定的逻辑规则生成。此外，我们构建了一个名为 Legal2Proleg 的新数据集，该数据集标注了法律案例与 PROLEG 逻辑公式之间的对齐关系，以支持法律语义解析的评估。在开源和闭源 LLM 上的实验结果表明，我们的方法在将自然语言法律案例描述转换为逻辑表示方面显著提高了准确性、稳定性和泛化能力，突显了其在可解释且可靠的法律推理中的有效性。我们的代码可在 https://github.com/yingjie7/Legal2LogicICL 获取。

## Abstract
This work aims to improve the generalization of logic-based legal reasoning systems by integrating recent advances in NLP with legal-domain adaptive few-shot learning techniques using LLMs. Existing logic-based legal reasoning pipelines typically rely on fine-tuned models to map natural-language legal cases into logical formulas before forwarding them to a symbolic reasoner. However, such approaches are heavily constrained by the scarcity of high-quality annotated training data. To address this limitation, we propose a novel LLM-based legal reasoning framework that enables effective in-context learning through retrieval-augmented generation. Specifically, we introduce Legal2LogicICL, a few-shot retrieval framework that balances diversity and similarity of exemplars at both the latent semantic representation level and the legal text structure level. In addition, our method explicitly accounts for legal structure by mitigating entity-induced retrieval bias in legal texts, where lengthy and highly specific entity mentions often dominate semantic representations and obscure legally meaningful reasoning patterns. Our Legal2LogicICL constructs informative and robust few-shot demonstrations, leading to accurate and stable logical rule generation without requiring additional training. In addition, we construct a new dataset, named Legal2Proleg, which is annotated with alignments between legal cases and PROLEG logical formulas to support the evaluation of legal semantic parsing. Experimental results on both open-source and proprietary LLMs demonstrate that our approach significantly improves accuracy, stability, and generalization in transforming natural-language legal case descriptions into logical representations, highlighting its effectiveness for interpretable and reliable legal reasoning. Our code is available at https://github.com/yingjie7/Legal2LogicICL.

---

## 论文详细总结（自动生成）

以下是对论文《Legal2LogicICL: Improving Generalization in Transforming Legal Cases to Logical Formulas via Diverse Few-Shot Learning》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
法律推理系统（如 PROLEG 框架）通常需要将非结构化的自然语言法律案例转化为结构化的逻辑公式。现有的方法主要依赖于在特定任务上微调（SFT）的模型，但面临以下挑战：
*   **数据稀缺性**：高质量、带有逻辑公式标注的法律数据极度匮乏，导致微调模型难以泛化到未见过的法律场景。
*   **实体诱导偏差**：法律文本中冗长且具体的实体（如当事人姓名、合同编号）往往主导了语义表示，使得检索系统倾向于匹配表面实体相似的案例，而非法律逻辑结构相似的案例。
*   **脆弱性**：现有系统对表层文字的变化（如改写）非常敏感，缺乏鲁棒性。

### 2. 方法论：核心思想与关键技术
论文提出了 **Legal2LogicICL** 框架，旨在通过多样化的少样本上下文学习（In-Context Learning, ICL）提升 LLM 的法律语义解析能力。
*   **核心思想**：利用检索增强生成（RAG）技术，从候选池中挑选既具有语义相关性又具备结构多样性的示例，引导 LLM 生成逻辑公式。
*   **关键技术细节**：
    *   **DiverseSim 算法**：引入多样性控制参数 $\lambda$，在检索时平衡示例与查询的“相似度”以及示例之间的“多样性”，避免模型过拟合于单一的表面模式。
    *   **实体无关模板（Entity-agnostic Templates）**：将法律案例中的具体实体替换为类型占位符（如 `{Borrower}`, `{Lender}`），从而提取出纯粹的法律逻辑结构。
    *   **混合检索策略**：结合“案例级检索”（基于原始文本语义）和“模板级检索”（基于去实体化的结构），构建一个既包含真实语境又包含丰富逻辑结构的少样本演示集（Prompt）。

### 3. 实验设计
*   **数据集**：
    *   **Legal2Proleg（新构建）**：包含贷款、租赁、购买和版权合同等多种类型，标注了法律规则和 PROLEG 逻辑公式。
    *   **LegalCaseNER**：作为对比基准，主要用于评估实体识别和简单模式下的表现。
*   **Benchmark 与对比方法**：
    *   对比了基于 NER 的监督学习方法（如 LegalCaseNER 提出的 Roberta-large 模型）。
    *   对比了不同规模的 LLM（Qwen3-8B/14B, Llama-3.1-8B, Phi-4, GPT-5.2）。
    *   评估指标包括“精确匹配准确率（Exact-Match Acc）”和“语义感知软匹配准确率（Soft-Match Acc）”。

### 4. 资源与算力
*   **模型使用**：使用了 Qwen/Qwen3-Embedding-8B 作为向量编码模型。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练/推理时长。由于该方法基于 ICL（上下文学习），不需要对大模型进行参数微调，主要的算力消耗集中在向量检索和 LLM 的推理生成阶段。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对不同数据比例（从 20% 到 80% 的可见数据）进行了对比实验。
    *   对多样性参数 $\lambda$ 进行了消融实验（0.2 到 0.8）。
    *   对少样本策略（3c, 5c, 3c+3t 等）进行了对比。
    *   所有实验均在 5 个不同的随机种子下运行并取平均值。
*   **充分性与公平性**：实验覆盖了开源和闭源模型，并对比了传统的 SFT 方法，设计较为全面。通过严格控制测试集模板不出现在训练集中，验证了模型的真实泛化能力，实验设计客观且公平。

### 6. 主要结论与发现
*   **低资源优势**：在仅有 20% 数据可见的情况下，Legal2LogicICL 的准确率（约 83%）远高于 SFT 方法（约 16%），证明了 ICL 在数据稀缺场景下的强大泛化力。
*   **多样性的重要性**：DiverseSim 算法通过引入多样性示例，显著提升了逻辑生成的稳定性，$\lambda=0.6$ 时表现最佳。
*   **混合策略有效性**：结合案例和模板的混合检索（3c+3t）优于纯语义检索，证明了缓解实体偏差对法律推理至关重要。
*   **模型表现**：Phi-4 和 Qwen3-14B 在该任务上表现出色，甚至在某些指标上逼近或超过了闭源的 GPT-5.2。

### 7. 优点（亮点）
*   **创新性检索**：提出了实体无关的模板检索，有效解决了法律文本中“实体噪声”干扰逻辑提取的问题。
*   **无需微调**：通过纯 Prompt 工程实现高性能，降低了法律 AI 应用的门槛和成本。
*   **新基准发布**：构建并开源了 Legal2Proleg 数据集，填补了法律案例到 PROLEG 逻辑转换标注数据的空白。

### 8. 不足与局限
*   **实体匹配敏感性**：错误分析显示，模型在处理冠词（a, the）或微小标点差异时容易导致“精确匹配”失败，虽然不影响逻辑，但反映了模型对格式的过度敏感。
*   **检索池依赖**：该方法的效果高度依赖于候选示例池的质量和覆盖范围。
*   **应用限制**：目前主要针对 PROLEG 逻辑框架，对于其他逻辑表示形式或其他语言（如非英语/日语环境）的适用性尚待验证。

（完）
