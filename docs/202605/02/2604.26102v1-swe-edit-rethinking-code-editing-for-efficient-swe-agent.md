---
title: "SWE-Edit: Rethinking Code Editing for Efficient SWE-Agent"
title_zh: SWE-Edit：重新思考高效 SWE-Agent 的代码编辑
authors: "Yikai Zhang, Jiaxin Pei, Kenan Li, Maoquan Wang, Jin Pan, Yu Kang, Shengyu Fu, Elsie Nallipogu, Junjie Hu, Yufan Huang, Zijian Jin"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.26102v1"
tags: ["query:malm"]
score: 9.0
evidence: 将任务分解为专门的子智能体
tldr: 针对现有软件工程智能体在单一上下文中混合代码查看与编辑导致的效率低下问题，本文提出 SWE-Edit 框架。该框架通过解耦上下文，将任务分解为负责提取代码的 Viewer 和负责执行修改的 Editor 两个子智能体。此外，研究者利用 GRPO 训练模型以自适应选择编辑模式，并推出了一个新的代码编辑基准测试。实验表明，该方法在提升 SWE-bench 解决率的同时显著降低了推理成本。
selection_source: fresh_fetch
motivation: 现有智能体将代码查看、规划与编辑耦合在单一窗口中，导致无关信息堆积并干扰推理性能。
method: 提出 SWE-Edit 框架，将编辑任务分解为 Viewer 和 Editor 子智能体，并训练模型自适应选择最优编辑模式。
result: "在 SWE-bench Verified 基准上，该方法将任务解决率提升了 2.1%，并使推理成本降低了 17.9%。"
conclusion: 通过解耦上下文密集型操作并优化编辑策略，SWE-Edit 实现了更高效且精准的自动化软件工程处理。
---

## 摘要
大语言模型智能体在软件工程任务上取得了显著进展，但当前的方法面临一个根本性的上下文耦合问题：标准的代码编辑界面将代码检查、修改规划和编辑执行混杂在单个上下文窗口中，迫使智能体在探索性查看与严格格式化的编辑生成之间交替进行。这导致无关信息累积并降低了智能体的性能。为解决这一问题，我们提出了 SWE-Edit，它将代码编辑分解为两个专门的子智能体：一个根据需求提取任务相关代码的 Viewer（查看器），以及一个根据高层计划执行修改的 Editor（编辑器）——这使得主智能体能够专注于推理，同时将上下文密集型操作委托给干净的上下文窗口。我们进一步研究了高效编辑模型的构成要素：观察到流行的“查找并替换”格式容易出错，我们使用 GRPO 训练 Qwen3-8B 以自适应地选择编辑模式，从而在编辑效率上优于单格式基线。在 SWE-bench Verified 上，SWE-Edit 将解决率提高了 2.1%，同时将推理成本降低了 17.9%。我们还提出了一个代码编辑基准测试，能够可靠地预测下游智能体的性能，为编辑模型的选择提供实际指导。我们的代码已在 https://github.com/microsoft/SWE-Edit 公开。

## Abstract
Large language model agents have achieved remarkable progress on software engineering tasks, yet current approaches suffer from a fundamental context coupling problem: the standard code editing interface conflates code inspection, modification planning, and edit execution within a single context window, forcing agents to interleave exploratory viewing with strictly formatted edit generation. This causes irrelevant information to accumulate and degrades agent performance. To address this, we propose SWE-Edit, which decomposes code editing into two specialized subagents: a Viewer that extracts task-relevant code on demand, and an Editor that executes modifications from high-level plans--allowing the main agent to focus on reasoning while delegating context-intensive operations to clean context windows. We further investigate what makes an effective editing model: observing that the prevalent find-and-replace format is error-prone, we train Qwen3-8B with GRPO to adaptively select editing modes, yielding improved editing efficiency over single-format baselines. On SWE-bench Verified, SWE-Edit improves resolved rate by 2.1% while reducing inference cost by 17.9%. We additionally propose a code editing benchmark that reliably predicts downstream agentic performance, providing practical guidance for editing model selection. Our code is publicly available at https://github.com/microsoft/SWE-Edit.

---

## 论文详细总结（自动生成）

这篇论文提出了 **SWE-Edit**，一个旨在解决软件工程（SWE）智能体在代码编辑过程中“上下文耦合”问题的两阶段优化框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题：上下文耦合（Context Coupling）。** 现有的 SWE 智能体界面将代码检查（查看文件）、修改规划（思考逻辑）和编辑执行（生成特定格式代码）混杂在同一个上下文窗口中。
*   **研究动机：**
    *   **上下文污染：** 智能体在调试时需要查看大量文件，这些无关代码会残留在窗口中，干扰后续的推理和编辑。
    *   **格式敏感性：** 像 `find-replace` 这种主流编辑格式对空格和缩进极其敏感，即使是推理能力极强的模型（如 GPT-5 或 o1）也常因格式错误导致编辑失败。
    *   **效率低下：** 强推理模型生成长代码块或频繁重试会产生高昂的 Token 成本。

### 2. 方法论
SWE-Edit 通过**脚手架（Scaffolding）**和**模型（Model）**两个层面的解耦来实现优化：

*   **脚手架优化（子智能体解耦）：**
    *   **Viewer 子智能体：** 负责接收自然语言查询，从完整文件中提取并仅返回与任务相关的代码片段（逻辑块+上下文），从而保持主智能体上下文的整洁。
    *   **Editor 子智能体：** 接收主智能体的自然语言指令，在独立的干净上下文中执行具体的代码修改。主智能体只需负责“做什么”，而 Editor 负责“怎么写”。
*   **模型优化（自适应编辑策略）：**
    *   **自适应模式选择：** 观察到 `find-replace` 适合局部小改动，而“全文件重写”适合复杂重构。研究者训练模型根据任务复杂度自适应选择模式。
    *   **GRPO 强化学习：** 使用 Qwen3-8B 作为骨干，通过 GRPO 算法进行训练。
    *   **规范化匹配奖励（Normalized Match Reward）：** 在计算奖励时，先通过算法去除代码中的注释、规范化空格，再进行匹配。这作为一种执行无关的代理指标，能有效引导模型学习正确的编辑逻辑。

### 3. 实验设计
*   **数据集/场景：**
    *   **SWE-bench Verified：** 包含 500 个真实 GitHub 问题的核心基准测试。
    *   **PR-Edit（新提出）：** 包含 3.5K 个从真实 PR 中提取的实例，用于快速评估编辑器模型的性能。
*   **对比方法：**
    *   **基线：** 采用 Anthropic 风格的单智能体架构（基于 OpenHands）。
    *   **消融实验：** 分别测试“仅 Viewer”、“仅 Editor”以及两者结合的效果。
    *   **泛化性测试：** 在 Kimi-K2、MiniMax-M2.1、GLM-4.7 等多种推理模型上验证框架的有效性。
    *   **检索对比：** 将 Viewer 与传统的 BM25 和向量检索（Dense Retrieval）进行对比。

### 4. 资源与算力
*   **模型使用：** 主智能体默认使用 GPT-5（推理努力程度设为高），子智能体默认使用 GPT-5-mini。模型训练基于 Qwen3-8B。
*   **训练细节：** 训练运行了 520 个 rollout steps，每个 step 采样 32 个实例，每个实例生成 8 个候选输出。
*   **算力说明：** 论文**未明确说明**具体的 GPU 型号、数量或总训练时长，但提到了使用 Slime 框架进行 RL 训练。

### 5. 实验数量与充分性
*   **实验规模：** 进行了大规模的端到端测试（SWE-bench Verified 全量 500 例，每组运行 3 次以减少方差）。
*   **充分性：** 实验设计非常全面，不仅验证了性能提升，还深入分析了 Token 消耗、编辑成功率、检索召回率等细分指标。
*   **客观性：** 通过在多个不同厂商的推理模型上进行交叉验证，证明了该框架的增益具有普适性，而非仅针对特定模型优化。

### 6. 主要结论
*   **性能与成本双赢：** 在 SWE-bench Verified 上，解决率提升了 **2.1%**，同时推理成本降低了 **17.9%**。
*   **可靠性显著提升：** 编辑成功率（无格式错误）提升了 **3.5%**；在某些推理模型上，编辑成功率的提升甚至超过 15%。
*   **协同效应：** Viewer 减少了上下文污染，抵消了 Editor 因增加探索而带来的额外成本，使系统进入了高精度、低成本的象限。
*   **基准有效性：** 证明了 PR-Edit 分数与下游任务表现强相关，可作为编辑器模型选择的可靠指标。

### 7. 优点
*   **架构优雅：** 通过“认知解耦”而非简单的任务拆分，解决了 LLM 在长上下文中处理精确格式的固有弱点。
*   **实用性强：** 显著降低了使用昂贵 API 模型的成本，对工业界部署 SWE 智能体具有直接参考价值。
*   **自适应性：** 引入强化学习让模型学会选择编辑策略，比硬编码逻辑更具鲁棒性。

### 8. 不足与局限
*   **训练闭环缺失：** 目前 Editor 是独立训练的，未来若能将其置于主智能体的端到端反馈循环中（即根据任务最终是否解决来提供奖励），效果可能更佳。
*   **代理指标风险：** PR-Edit 使用 GPT Grader 作为评分手段，虽然相关性高，但仍可能存在 LLM 评估的固有偏差。
*   **系统复杂性：** 引入多个子智能体增加了系统的工程实现难度和维护成本。

（完）
