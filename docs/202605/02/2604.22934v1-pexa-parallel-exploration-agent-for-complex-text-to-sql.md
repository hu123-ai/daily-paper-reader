---
title: "PExA: Parallel Exploration Agent for Complex Text-to-SQL"
title_zh: PExA：用于复杂 Text-to-SQL 的并行探索智能体
authors: "Tanmay Parekh, Ella Hofmann-Coyle, Shuyi Wang, Sachith Sri Ram Kothur, Srivas Prasad, Yunmo Chen"
date: 2026-04-24
pdf: "https://arxiv.org/pdf/2604.22934v1"
tags: ["query:malm"]
score: 7.0
evidence: 用于复杂推理的并行探索智能体框架
tldr: "针对复杂Text-to-SQL任务中延迟与性能的权衡问题，本文提出PExA框架。该方法借鉴软件测试覆盖率理念，将复杂查询分解为多个简单的原子SQL测试用例并并行执行，以确保语义覆盖。在获取足够信息后生成最终SQL，显著提升了生成质量。在Spider 2.0基准测试中达到70.2%的执行准确率，刷新了SOTA记录。"
selection_source: fresh_fetch
motivation: 现有的基于大模型的Text-to-SQL智能体在提升性能时往往面临高延迟的挑战，难以平衡效率与准确性。
method: 将Text-to-SQL生成重构为软件测试覆盖问题，通过并行执行一组原子SQL测试用例来探索语义信息，并据此生成最终SQL。
result: "在Spider 2.0基准测试中取得了70.2%的执行准确率，创下了新的行业领先水平。"
conclusion: PExA通过并行探索策略有效解决了复杂SQL生成的性能瓶颈，证明了分解与并行验证在提升LLM推理能力方面的潜力。
---

## 摘要
基于大语言模型（LLM）的 Text-to-SQL 智能体通常在延迟与性能的权衡中挣扎，性能的提升往往以延迟为代价，反之亦然。我们从软件测试覆盖率的角度重新构建了 Text-to-SQL 生成任务，即为原始查询准备一组包含更简单、原子化 SQL 的测试用例，这些 SQL 并行执行，共同确保对原始查询的语义覆盖。在迭代测试用例覆盖率后，仅在收集到足够信息时才生成最终 SQL，并利用已探索的测试用例 SQL 来引导最终生成。我们在最先进的 Text-to-SQL 基准测试 Spider 2.0 上验证了我们的框架，以 70.2% 的执行准确率达到了新的最先进水平。

## Abstract
LLM-based agents for text-to-SQL often struggle with latency-performance trade-off, where performance improvements come at the cost of latency or vice versa. We reformulate text-to-SQL generation within the lens of software test coverage where the original query is prepared with a suite of test cases with simpler, atomic SQLs that are executed in parallel and together ensure semantic coverage of the original query. After iterating on test case coverage, the final SQL is generated only when enough information is gathered, leveraging the explored test case SQLs to ground the final generation. We validated our framework on a state-of-the-art benchmark for text-to-SQL, Spider 2.0, achieving a new state-of-the-art with 70.2% execution accuracy.