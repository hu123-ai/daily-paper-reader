---
title: "From Intent to Execution: Composing Agentic Workflows with Agent Recommendation"
title_zh: 从意图到执行：通过智能体推荐构建智能体工作流
authors: "Kishan Athrey, Ramin Pishehvar, Brian Riordan, Mahesh Viswanathan"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03986v1"
tags: ["query:malm"]
score: 10.0
evidence: 多智能体系统的自动化构建框架
tldr: 本研究针对多智能体系统（MAS）构建过程中依赖人工规划和选型的局限，提出了一种自动化构建框架。该框架集成了LLM规划器、动态调用图和基于两阶段检索（快速检索+LLM重排序）的智能体推荐系统。通过引入批评智能体对推荐结果进行全局审核，实现了从用户意图到执行流的端到端自动化，显著提升了系统构建的效率、召回率和可扩展性。
selection_source: fresh_fetch
motivation: 传统多智能体系统的构建高度依赖人工进行任务规划、智能体选择和执行图创建，难以应对复杂且多变的规模化应用需求。
method: 提出一种包含LLM规划器、任务编排器及两阶段智能体推荐系统的自动化框架，并引入批评智能体对任务与智能体的匹配进行全局优化。
result: 实验表明，该方法在召回率上优于现有技术，且在处理大规模智能体注册库时表现出更强的鲁棒性和可扩展性。
conclusion: 自动化规划与精准的智能体推荐是构建高效多智能体系统的关键，而批评机制的引入能进一步确保任务执行的准确性。
---

## 摘要
使用 AI 智能体构建的多智能体系统 (MAS) 能够满足多种用户意图，可用于设计和构建一系列相关应用。然而，此类 MAS 的创建目前涉及手动制定计划、手动选择合适的智能体以及手动创建执行图。本文介绍了一个用于自动创建多智能体系统的框架，该框架用自动化流程取代了多个手动步骤。所提出的框架由软件模块和工作流组成，用于编排所需的特定任务应用。这些模块包括：一个基于大语言模型 (LLM) 的规划器、一组以自然语言描述的任务、一个动态调用图、一个用于将智能体映射到任务的编排器，以及一个从本地和全局智能体注册表中寻找最合适智能体的智能体推荐器。该智能体推荐器采用了一个两阶段信息检索 (IR) 系统，由一个快速检索器和一个基于 LLM 的重排序器组成。我们进行了一系列实验，探索了嵌入器、重排序器、智能体描述增强以及监督评价智能体 (critique agent) 的选择。我们对该系统进行了端到端基准测试，评估了所提方法在规划、智能体选择和任务完成方面的综合表现。实验结果表明，我们的方法在召回率方面优于现有技术，且与以往方法相比更具鲁棒性和可扩展性。评价智能体根据整体计划对智能体和工具的推荐进行全面重新评估。我们展示了引入评价智能体能进一步提高召回率得分，证明了对基于任务的智能体选择进行全面审查和修正是构建端到端多智能体系统的关键步骤。

## Abstract
Multi-Agent Systems (MAS) built using AI agents fulfill a variety of user intents that may be used to design and build a family of related applications. However, the creation of such MAS currently involves manual composition of the plan, manual selection of appropriate agents, and manual creation of execution graphs. This paper introduces a framework for the automated creation of multi-agent systems which replaces multiple manual steps with an automated framework. The proposed framework consists of software modules and a workflow to orchestrate the requisite task- specific application. The modules include: an LLM-derived planner, a set of tasks described in natural language, a dynamic call graph, an orchestrator for map agents to tasks, and an agent recommender that finds the most suitable agent(s) from local and global agent registries. The agent recommender uses a two-stage information retrieval (IR) system comprising a fast retriever and an LLM-based re-ranker. We implemented a series of experiments exploring the choice of embedders, re- rankers, agent description enrichment, and supervising critique agent. We benchmarked this system end-to-end, evaluating the combination of planning, agent selection, and task completion, with our proposed approach. Our experimental results show that our approach outperforms the state-of-the- art in terms of the recall rate and is more robust and scalable compared to previous approaches. The critique agent holistically reevaluates both agent and tool recommendations against the overall plan. We show that the inclusion of the critique agent further enhances the recall score, proving that the comprehensive review and revision of task-based agent selection is an essential step in building end-to-end multi-agent systems.

---

## 论文详细总结（自动生成）

这篇论文由思科系统（Cisco Systems）的研究团队撰写，提出了一种名为 **AutoMAS** 的框架，旨在实现从用户自然语言意图到多智能体系统（MAS）执行流的端到端自动化构建。

以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：当前多智能体系统的构建高度依赖人工干预，包括任务规划、合适的智能体选型以及执行图（Execution Graph）的创建。
*   **研究动机**：
    *   **规模化挑战**：随着专用智能体（Agents）数量激增，在海量注册库中寻找“最合适”的智能体变成了“大海捞针”的问题。
    *   **局限性**：传统的关键词搜索或将所有智能体描述放入 LLM 上下文的方法，在处理成千上万个智能体时，会面临上下文长度限制、高昂的 Token 成本和推理延迟。
    *   **动态性需求**：现有的静态工作流难以根据运行时的成本、延迟或环境变化（如某个智能体离线）进行自我调整。

### 2. 方法论
AutoMAS 框架通过以下关键技术模块实现自动化：
*   **LLM 规划器（Planner）**：将用户意图解析为自然语言描述的子任务列表，并将其表示为**有限状态机（FSM）**，以处理非线性逻辑和冗余路径。
*   **两阶段智能体推荐器（Agent Recommender, AR）**：
    *   **第一阶段（检索器）**：使用混合搜索（向量嵌入 + 关键词匹配）从注册库中快速筛选出高召回率的候选集。
    *   **第二阶段（重排序器）**：利用 LLM 对候选智能体进行深度语义评估和排序，提升精确度。
*   **智能体描述增强（Enrichment）**：在索引阶段，利用 LLM 为每个智能体生成多个合成查询（Synthetic Queries），并将其与原始描述合并后取嵌入向量的平均值，以增强检索的鲁棒性。
*   **变量调用图（Variable Call Graph, VCG）**：一种重路由机制，根据用户约束（如最低成本、最低延迟）从推荐器提供的多个候选者中动态选择执行路径。
*   **评价机制（Critique Mechanism）**：
    *   **任务特定评价**：检查推荐的智能体是否满足当前子任务。
    *   **全局评价**：从整体工作流角度评估输入输出兼容性、安全性及全局最优性。

### 3. 实验设计
*   **数据集**：
    *   **ToolE (MetaTool)**：包含 199 个工具和 20,550 个查询对，用于优化和评估智能体推荐算法。
    *   **TaskBench**：包含 Dailylife、Hugging Face 和 Multimedia 三个领域的 API（共 103 个工具，约 17,000 个查询），用于评估端到端的规划与组合能力。
*   **对比方法**：
    *   基准检索模型（Retriever-only）。
    *   **Re-Invoke**：一种现有的工具检索重写方法。
    *   不同组合的嵌入模型（text-embedding-3-small/large）和重排序模型（GPT-4o, o1）。
*   **评估指标**：nDCG、Recall@K、mAP、Rouge、BERTScore，以及创新的 **LLM-as-a-Judge**（评估语义一致性）和结构化预测指标（工具名称 F1、序列相似度等）。

### 4. 资源与算力
*   **算力说明**：论文**未明确指出**具体的硬件配置（如 GPU 型号或数量）。
*   **模型使用**：实验主要基于 OpenAI 的 API 进行，提到了使用 `gpt-4o`、`o1-preview` 以及 `text-embedding-3-large` 等模型。由于是基于 API 的框架研究，重点在于算法流程而非底层算力训练。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对推荐系统进行了多维度消融实验，包括不同嵌入模型对比、重排序器对比、合成查询数量（3, 5, 10 个）的影响。
    *   针对端到端任务，在 TaskBench 的三个子集上进行了详尽的测试。
*   **充分性评估**：实验设计较为充分。通过数万个查询对进行测试，保证了统计意义上的可靠性。引入“评价智能体”前后的对比实验，客观地证明了多阶段审核的必要性。

### 6. 主要结论与发现
*   **性能领先**：AutoMAS 在召回率和精确度上显著优于 Re-Invoke 等现有技术。
*   **两阶段优势**：重排序阶段能大幅提升 Top-1 的准确率，而描述增强技术（Enrichment）能有效弥补原始文档描述不足的问题。
*   **评价机制的价值**：引入评价智能体后，Recall@1 提升了约 3.5%，证明了全局审查对纠正局部错误的重要性。
*   **可扩展性**：相比于将所有工具塞进 Prompt 的方法，AutoMAS 的检索架构在处理大规模智能体库时更具成本效益和速度优势。

### 7. 优点
*   **端到端自动化**：实现了从模糊意图到可执行图的完整闭环。
*   **鲁棒性设计**：通过 VCG 和冗余路径设计，增强了系统在复杂环境下的容错能力。
*   **检索增强**：创新的“描述增强”方法在不增加检索时延迟的前提下，显著提升了匹配质量。
*   **评估严谨**：结合了语义指标（LLM-as-a-Judge）和结构化指标，解决了传统 NLP 指标（如 Rouge）在代码/工具调用评估上的失真问题。

### 8. 不足与局限
*   **LLM 固有偏见**：实验发现重排序器有时会表现出对低成本智能体的偏好，即使指令要求优先考虑性能，这反映了模型预训练带来的偏见。
*   **评价机制的闭环限制**：目前的评价智能体只能在检索出的候选集中进行重排，如果检索阶段（第一阶段）漏掉了正确的智能体，评价机制无法“创造”出新的选项。
*   **指令遵循能力**：在处理极其复杂的约束（如多重性能指标权衡）时，LLM 的指令遵循能力仍有提升空间。

（完）
