---
title: "MAP-Law: Coverage-Driven Retrieval Control for Multi-Turn Legal Consultation"
title_zh: MAP-Law：面向多轮法律咨询的覆盖率驱动检索控制
authors: "Qinchuan Cheng, Ruixuan Xie, Jiaqi Liu, Xiaoya Yuan, Yuxin Liu"
date: 2026-05-02
pdf: "https://arxiv.org/pdf/2605.01486v1"
tags: ["query:q2"]
score: 9.0
evidence: 多轮法律咨询智能体的检索控制框架
tldr: 针对多轮法律咨询中检索控制不精准的问题，本文提出 MAP-Law 框架。该框架通过构建包含问题、法律要素和证据节点的结构化状态图，引入要素覆盖率、证据覆盖率和边际收益等指标，将检索停止决策从固定参数转变为可解释的覆盖驱动过程。实验表明，MAP-Law 在显著减少检索次数和证据量的同时，保持了极高的法律要素覆盖率，提升了咨询的效率与准确性。
selection_source: fresh_fetch
motivation: 现有的多轮法律咨询智能体多采用固定检索深度或粗糙启发式控制，导致法律证据支持不足或上下文信息冗余。
method: 提出一种覆盖驱动的检索控制框架，利用结构化状态图和覆盖率信号动态决定继续检索、重定向搜索或生成最终回复。
result: "在劳动法场景下的实验显示，MAP-Law 仅需平均 2.9 轮检索即可达到 0.860 的要素覆盖率，比固定轮次基准减少了 80% 以上的证据量。"
conclusion: MAP-Law 证明了将检索停止决策与法律论证结构对齐，能有效平衡法律咨询的全面性、可解释性与处理效率。
---

## 摘要
法律咨询是一项高风险、知识密集型的任务，要求智能体识别相关的法律问题，检索权威支持，并确定证据何时足以给出建议。尽管检索增强生成（RAG）提高了法律问答的可靠性，但许多多轮法律智能体仍依赖于固定的检索深度或粗粒度的启发式控制。这通常会导致关键法律要素的支持不足，或者过度检索增加了上下文负担并削弱了回答的焦点。我们提出了 MAP-Law，这是一个用于多轮法律咨询中检索控制的覆盖率驱动框架。MAP-Law 将咨询建模为一个在由问题节点、法律要素节点和证据节点组成的联合结构化状态上的受控检索过程。在每一轮检索后，智能体计算要素覆盖率（Element Coverage）、证据覆盖率（Evidence Coverage）和边际收益（Marginal Gain），并利用这些信号决定是继续检索、重定向搜索还是生成最终回复。通过这种方式，MAP-Law 将停止决策从一个固定的超参数转变为一个与法律论证结构相一致的、可解释且可审计的决策。在涵盖八个劳动法场景、包含 50 个案例的自建数据集上的实验表明，以 DeepSeek 作为动作选择器的 MAP-Law 仅需平均 2.9 轮检索和 5.8 条证据，即可实现 0.860 的要素覆盖率。与固定的七轮基准相比，它减少了超过 80% 的证据量和 58% 的检索轮数。消融实验结果进一步证实了覆盖率驱动停止、联合图表示和基于大语言模型（LLM）的动作选择的独立贡献。

## Abstract
Legal consultation is a high-stakes, knowledge-intensive task that requires agents to identify relevant legal issues, retrieve authoritative support, and determine when evidence is sufficient for a recommendation. Although retrieval-augmented generation has improved grounding in legal question answering, many multi-turn legal agents still rely on fixed retrieval depth or coarse heuristic control. This often leads to either insufficient support for key legal elements or excessive retrieval that increases context burden and weakens answer focus.   We propose MAP-Law, a coverage-driven framework for retrieval control in multi-turn legal consultation. MAP-Law models consultation as a controlled retrieval process over a joint structured state consisting of issue nodes, legal element nodes, and evidence nodes. After each retrieval round, the agent computes Element Coverage, Evidence Coverage, and Marginal Gain, and uses these signals to decide whether to continue retrieval, redirect the search, or generate the final response. In this way, MAP-Law turns stopping from a fixed hyperparameter into an interpretable and auditable decision aligned with legal argumentative structure.   Experiments on a self-constructed dataset of 50 cases across eight labor-law scenarios show that MAP-Law with DeepSeek as the action selector achieves an Element Coverage of 0.860 using only 2.9 retrieval rounds and 5.8 evidence pieces on average. Compared with a fixed seven-round baseline, it reduces evidence volume by over 80% and retrieval rounds by 58%. Ablation results further confirm the independent contributions of coverage-driven stopping, joint graph representation, and LLM-based action selection.

---

## 论文详细总结（自动生成）

这是一份关于论文《MAP-Law: Coverage-Driven Retrieval Control for Multi-Turn Legal Consultation》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：法律咨询是一项高风险、知识密集型任务。目前的法律检索增强生成（RAG）系统在多轮对话中，通常采用固定的检索深度或简单的启发式控制。
*   **核心痛点**：检索过浅会导致关键法律要素（Legal Elements）缺乏证据支持；检索过深则会引入冗余信息，增加上下文负担并干扰回答准确性。
*   **研究目标**：探索如何让智能体根据“证据是否充分”来科学、动态地决定何时停止检索，从而平衡法律论证的严谨性与系统的处理效率。

### 2. 方法论
MAP-Law（Mindmap-Augmented Planning）框架的核心思想是将检索控制建模为基于法律要素覆盖率的规划问题。
*   **联合图表示（Joint Graph）**：
    *   **计划图（Plan Graph）**：编码法律问题与所需法律要素之间的依赖关系。
    *   **证据图（Evidence Graph）**：记录已检索到的法律权威（法条、案例）及其与法律要素之间的支持链接。
*   **覆盖率驱动指标**：
    *   **要素覆盖率 (EC)**：已获得充分证据支持的法律要素比例。
    *   **证据覆盖率 (EVC)**：检索到的证据中被最终回答实际引用的比例（衡量效率）。
    *   **边际收益 (MG)**：新一轮检索对未解决要素提供的增量支持强度。
*   **动作选择与停止规则**：
    *   系统使用 LLM（如 DeepSeek）作为动作选择器，在结构化状态（而非原始文本）上决策：是继续检索、补充搜索方向，还是生成最终建议。
    *   **停止逻辑**：当 EC 和 EVC 达到阈值，或 MG 连续两轮低于阈值，或达到最大轮次限制时，系统强制停止检索并生成回答。

### 3. 实验设计
*   **数据集**：自建劳动法咨询数据集，包含 50 个复杂案例，涵盖劳动关系认定、工伤赔偿、竞业限制等 8 个典型场景。
*   **Benchmark**：以人工标注的法律要素、支持性证据和参考答案作为金标准。
*   **对比方法**：
    *   **固定轮次基准**：Fixed-3、Fixed-5、Fixed-7（分别固定执行 3/5/7 轮检索）。
    *   **消融变体**：MAP-Rule（规则驱动动作选择）、MAP-no-stopping（无停止机制）、MAP-no-graph（无图结构状态）。

### 4. 资源与算力
*   **模型使用**：检索端使用 `hfl/chinese-bert-wwm-ext` 进行向量编码；动作选择和生成端调用了 DeepSeek API。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量及训练/推理的总时长。由于主要依赖 API 调用和预训练编码器，其本地算力需求主要集中在向量检索和图状态维护上。

### 5. 实验数量与充分性
*   **实验规模**：测试了 50 个案例，虽然样本绝对数量不多，但每个案例涉及多轮交互和复杂的要素对齐。
*   **充分性**：实验设计较为全面，包含了主实验、消融实验（针对停止规则、图结构、LLM 策略）、案例研究以及失败案例分析。
*   **客观性**：引入了三名具有法律背景的标注员进行交叉验证（Fleiss’ κ=0.82），确保了评估标准的专业性和客观性。

### 6. 主要结论与发现
*   **效率显著提升**：MAP-Law (DeepSeek) 仅需平均 2.9 轮检索即可达到 0.860 的要素覆盖率。相比 Fixed-7 基准，证据量减少了 80% 以上，检索轮数减少了 58%。
*   **质量更优**：过度检索（如 MAP-no-stopping）不仅增加成本，反而可能因为引入噪声导致要素覆盖率下降（从 0.860 降至 0.780）。
*   **结构化状态的必要性**：消融实验显示，去掉计划图后要素覆盖率直接降至 0，证明了显式建模法律要素是实现精准控制的前提。

### 7. 优点
*   **可解释性强**：停止决策不再是黑盒，而是基于 EC、MG 等可量化的法律支持指标，符合法律论证的逻辑。
*   **自适应性**：能够根据案例的简单或复杂程度自动调整检索深度，避免了“一刀切”的检索策略。
*   **架构优雅**：将 LLM 的语义推理能力与结构化的图表示相结合，既利用了模型智能，又通过结构化约束提升了可靠性。

### 8. 不足与局限
*   **领域局限性**：实验仅限于劳动法领域，在其他法律领域（如刑法、民法典其他分编）的泛化能力尚待验证。
*   **上游依赖**：系统性能高度依赖于初始的“法律要素提取”质量，若要素识别错误，后续的覆盖率计算将失去意义。
*   **阈值敏感性**：EC 和 MG 等停止阈值目前是基于经验设定的，在不同风险等级的法律任务中可能需要更动态的调整机制。
*   **样本量限制**：50 个案例的评估规模对于证明系统在极端或罕见法律场景下的鲁棒性略显不足。

（完）
