---
title: "Autonomous LLM Agent Worms: Cross-Platform Propagation, Automated Discovery and Temporal Re-Entry Defense"
title_zh: 自主 LLM 代理蠕虫：跨平台传播、自动化发现与时域重入防御
authors: "Mingming Zha, Xiaofeng Wang"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02812v1"
tags: ["query:malm"]
score: 9.0
evidence: 多智能体大语言模型生态系统与跨智能体传输
tldr: 本研究揭示了自主LLM智能体在持久化存储机制下的新型蠕虫传播风险。攻击者可将恶意内容注入智能体状态，实现跨平台自动传播与提权。作者开发了SSCGV分析器识别注入点，并利用SRPO优化器生成抗摘要攻击载荷。在三大主流框架的测试中验证了零点击传播的危害，并提出了RTW-A防御框架，通过限制写后读重入和能力衰减等机制，在保留正常功能的同时消除了此类蠕虫威胁。
selection_source: fresh_fetch
motivation: 自主LLM智能体的持久化工作空间和自动加载任务状态的特性，为恶意代码在智能体间的自动化传播和持久化攻击提供了新途径。
method: 开发了SSCGV源码分析器定位数据流注入点，并设计了SRPO载荷优化器以确保攻击载荷在多轮摘要和改写后仍能保持活性。
result: 在三个生产级智能体框架上成功实现了零点击自主传播、跨平台传输及权限提升，证明了现有生态系统在持久化攻击面前的脆弱性。
conclusion: 提出的RTW-A防御框架通过密封配置、类型化内存提升和能力衰减等机制，能够有效阻断蠕虫的传播链条并保障系统完整性。
---

## 摘要
自主 LLM 代理作为长期运行的进程，具有持久化工作区、内存文件、计划任务状态和消息集成功能。这些特性带来了一种新的传播风险：受攻击者影响的内容可能被写入持久化的代理状态，通过计划的自动加载重新进入 LLM 决策上下文，并驱动包括配置更改和跨代理传输在内的高风险操作。我们提出了首个用于自动化分析基于文件的多代理 LLM 生态系统中持久性蠕虫传播的系统框架。我们的自动化源代码图分析器 SSCGV 能够追踪从文件 I/O 到 LLM 上下文注入点的数据流，并在无需人工分析的情况下根据上下文注入位置对载体进行排序。我们的摘要抗性有效载荷优化器 SRPO 能够生成在多跳通信中对 LLM 介导的摘要和改写具有鲁棒性的蠕虫有效载荷。在三个生产级代理框架上的评估表明，我们实现了零点击自主传播、无需平台特定适配的 3 跳跨平台传输、代理间权限提升以及数据外泄。我们发现了两个经验性见解：用户提示词载体比系统提示词载体具有更高的攻击合规性，且读取操作是 LLM 介导系统中的主要完整性威胁。为了防御此类攻击，我们开发了 RTW-A，并在正式的“无持久性蠕虫传播”定理下得到了证明。RTW 阻断了“暴露读取前写入”的重入；密封配置保护静态文件；类型化内存提升防止不受信任的摘要进入受信任内存；能力衰减限制了外部读取后的高风险操作。这些机制在保留普通工作流的同时，消除了持久性、重入和动作链。受影响的系统已匿名化，等待协同披露。

## Abstract
Autonomous LLM agents operate as long-running processes with persistent workspaces, memory files, scheduled task state, and messaging integrations. These features create a new propagation risk: attacker-influenced content can be written into persistent agent state, re-enter the LLM decision context through scheduled autoloading, and drive high-risk actions including configuration changes and cross-agent transmission. We present the first systematic framework for automated analysis of persistent worm propagation in file-backed multi-agent LLM ecosystems. SSCGV, our automated source-code graph analyzer, traces data flow from file I/O to LLM context injection points and ranks carriers by context injection position without manual analysis. SRPO, our summary-resilient payload optimizer, generates worm payloads robust to LLM-mediated summarization and paraphrasing across multi-hop communication. Evaluated on three production agent frameworks, we demonstrate zero-click autonomous propagation, 3-hop cross-platform transmission without platform-specific adaptation, inter-agent privilege escalation, and data exfiltration. We identify two empirical insights: user prompt carriers achieve higher attack compliance than system prompt carriers, and read operations represent the primary integrity threat in LLM-mediated systems. To defend against this class of attacks, we develop RTW-A, proven under a formal No Persistent Worm Propagation theorem. RTW blocks write-before-exposed-read re-entry; sealed configuration protects static files; typed memory promotion prevents untrusted summaries from entering trusted memory; and capability attenuation limits high-risk actions after external reads. These mechanisms eliminate the persistence, re-entry, action chain while preserving ordinary workflows. Affected systems are anonymized pending coordinated disclosure.

---

## 论文详细总结（自动生成）

这是一份关于论文《Autonomous LLM Agent Worms: Cross-Platform Propagation, Automated Discovery and Temporal Re-Entry Defense》的深度结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
随着自主 LLM 智能体（Agent）从简单的“请求-响应”模式转向具有持久化工作区、长期记忆和自动任务调度能力的长期运行进程，其安全边界发生了根本性变化。
*   **研究动机**：现有的 LLM 蠕虫研究多依赖于针对特定平台的硬编码攻击，且无法应对 Agent 通信中常见的“语义降级”（如摘要、改写）。
*   **核心问题**：攻击者如何利用 Agent 的持久化存储机制（文件、配置、任务状态），在无需人工干预的情况下实现跨平台、自动化的恶意载荷传播与持久化重入？
*   **安全哲学转变**：论文指出在 LLM 介导的系统中，**“读”操作可能比“写”操作更危险**，因为读取受控内容会直接污染 Agent 的决策大脑（LLM 上下文）。

### 2. 论文提出的方法论
论文提出了一个包含攻击分析与防御的完整框架，核心技术包括：
*   **SSCGV (静态源码图漏洞分析器)**：
    *   **核心思想**：通过构建代码属性图（CPG），追踪从文件 I/O 到 LLM 上下文注入点（系统提示词、用户提示词、内存加载）的数据流。
    *   **关键细节**：自动识别“暴露读取”（Exposed Read）路径，并根据注入位置对载体进行排序，无需平台特定知识。
*   **SRPO (摘要抗性载荷优化器)**：
    *   **核心思想**：利用三个 LLM 角色（生成者、模拟器、裁判）进行对抗性优化。
    *   **目标**：确保蠕虫载荷在经过 Agent 的摘要、压缩或改写后，仍能保持持久化、传播和执行恶意操作的语义能力。
*   **RTW-A 防御框架**：
    *   **核心逻辑**：基于“无持久性蠕虫传播”定理，强制执行 **RTW（Read-after-Tainted-Write）** 约束，即禁止在写入受污染内容后立即进行暴露读取。
    *   **多层防御**：包括密封静态配置、类型化内存提升（防止自由文本进入受信任内存）以及在外部读取后的能力衰减（限制高风险动作）。

### 3. 实验设计
*   **实验对象**：三个开源生产级 Agent 框架（匿名化为 Framework A、B、C），均具备持久化工作区和消息通道集成。
*   **实验场景**：
    *   **零点击传播**：初始注入后，Agent 通过心跳机制自动触发传播。
    *   **跨平台传输**：验证载荷能否从框架 A 的 Agent 传播到框架 B 的 Agent。
    *   **恶意行为测试**：包括权限提升（低权限 Agent 诱导高权限 Agent 执行代码）和数据外泄。
*   **Benchmark**：以现有的文本传播方法为基准，对比 SRPO 优化后的载荷在语义转换下的存活率。

### 4. 资源与算力
*   **模型后端**：实验主要使用了 **GPT-4o-mini** 和 **Gemini-2.5-Flash** 作为 Agent 的推理引擎。
*   **算力说明**：论文**未明确提及**具体的 GPU 硬件型号或训练时长。由于该研究侧重于系统架构分析和推理侧的攻击验证，而非模型训练，其算力消耗主要集中在 LLM API 的调用上。

### 5. 实验数量与充分性
*   **实验规模**：
    *   SSCGV 在 3 个框架中发现了 31 个可注入载体。
    *   进行了 3 跳（3-hop）跨平台传播实验。
    *   针对不同注入位置（用户提示词 vs 系统提示词）进行了合规性对比实验。
*   **充分性评价**：实验设计较为全面，涵盖了从漏洞发现、载荷优化到端到端攻击验证的全流程。通过跨模型（GPT 和 Gemini）的测试，证明了攻击的通用性。
*   **客观性**：采用了受控测试环境，未涉及真实用户数据，且在披露前已通知相关厂商，符合负责任的漏洞披露原则。

### 6. 论文的主要结论与发现
*   **注入位置决定论**：注入到“用户提示词”位置的载体比“系统提示词”具有更高的攻击成功率，因为 LLM 倾向于将用户输入视为直接指令。
*   **读/写威胁倒置**：在 Agent 环境中，读取受污染文件是导致决策权被劫持的关键点，颠覆了传统安全中“写权限是主要威胁”的认知。
*   **跨平台脆弱性**：蠕虫无需针对特定平台适配即可在异构 Agent 生态中传播。
*   **防御有效性**：RTW-A 框架在不破坏正常工作流的前提下，能从数学上证明阻断持久化传播链。

### 7. 优点
*   **自动化程度高**：SSCGV 实现了零人工干预的漏洞挖掘。
*   **语义鲁棒性**：SRPO 解决了 LLM 交互中载荷易被摘要算法破坏的痛点。
*   **理论完备**：防御部分不仅有工程实现，还提供了形式化定理证明（Theorem 1）。
*   **实战价值**：首次展示了生产级 Agent 框架下的零点击、跨平台蠕虫攻击。

### 8. 不足与局限
*   **闭源系统限制**：SSCGV 依赖源码分析，对于完全闭源且无检测接口的系统难以直接应用。
*   **防御开销**：虽然论文声称保留了正常工作流，但“类型化内存提升”可能会限制 Agent 记忆的灵活性和表达力。
*   **实验覆盖面**：虽然测试了三个框架，但全球 Agent 框架种类繁多，不同架构（如基于数据库而非文件系统的 Agent）的适用性有待进一步验证。
*   **算力细节缺失**：未提供具体的推理延迟或防御机制带来的性能损耗数据。

（完）
