---
title: "Say the Mission, Execute the Swarm: Agent-Enhanced LLM Reasoning in the Web-of-Drones"
title_zh: 任务下达，集群执行：无人机网中智能体增强的大语言模型推理
authors: "Andrea Iannoli, Lorenzo Gigli, Luca Sciullo, Angelo Trotta, Marco Di Felice"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03788v1"
tags: ["query:malm"]
score: 9.0
evidence: 用于集群控制的智能体增强型大语言模型框架
tldr: 本研究针对无人机集群管理中接口异构和实时执行难的问题，提出了一种基于代理增强的LLM框架。该框架结合模型上下文协议（MCP）与万维网物联网（WoT）标准，将无人机抽象为标准化对象，通过工具化交互实现自然语言任务的自主执行。实验表明，该方法显著提升了LLM在复杂集群任务中的推理可靠性与执行稳健性。
selection_source: fresh_fetch
motivation: 现有的LLM在处理无人机集群任务时面临接口不统一、缺乏环境感知及难以实现闭环实时控制的挑战。
method: 提出一种结合LLM代理核心、MCP网关和W3C WoT标准的架构，通过标准化工具调用而非代码生成来实现无人机控制。
result: 在ArduPilot模拟环境下的测试显示，通用LLM在缺乏显式感知和执行支持时难以完成任务，而特定工具和安全护栏能显著提高成功率。
conclusion: 任务特定的规划工具和运行时安全机制是提升LLM在网络无人机系统中执行可靠性的关键，而非单纯依赖模型规模或Token消耗。
---

## 摘要
大语言模型（LLMs）正越来越多地被探索作为信息物理系统的高级推理引擎，但由于异构接口、有限的具身落地（grounding）以及对长期闭环执行的需求，其在实时无人机（UAV）集群管理中的应用仍具挑战。本文提出了一种任务无关的、智能体增强的 LLM 框架，用于无人机集群控制。在该框架中，用户以自然语言表达任务目标，系统通过具身的实时交互自主执行任务。所提出的架构将基于 LLM 的智能体核心与模型上下文协议（MCP）网关以及基于 W3C 物联网（WoT）标准的无人机网抽象相结合。通过将无人机、传感器和服务暴露为标准化的 WoT 物（Things），该框架实现了结构化的基于工具的交互、持续的状态观测和安全的动作执行，而无需依赖代码生成。我们使用基于 ArduPilot 的仿真，在四种集群任务和六种最先进的 LLM 上对该框架进行了评估。结果表明，尽管具有强大的推理能力，但在没有明确的具身落地和执行支持的情况下，当前的通用 LLM 即使在处理简单的集群任务时，仍难以实现可靠的执行。特定任务的规划工具和运行时护栏显著提高了鲁棒性，而 Token 消耗量本身并不能指示执行质量或可靠性。

## Abstract
Large Language Models (LLMs) are increasingly explored as high-level reasoning engines for cyber-physical systems, yet their application to real-time UAV swarm management remains challenging due to heterogeneous interfaces, limited grounding, and the need for long-running closed-loop execution. This paper presents a mission-agnostic, agent-enhanced LLM framework for UAV swarm control, where users express mission objectives in natural language and the system autonomously executes them through grounded, real-time interactions. The proposed architecture combines an LLM-based Agent Core with a Model Context Protocol (MCP) gateway and a Web-of-Drones abstraction based on W3C Web of Things (WoT) standards. By exposing drones, sensors, and services as standardized WoT Things, the framework enables structured tool-based interaction, continuous state observation, and safe actuation without relying on code generation. We evaluate the framework using ArduPilot-based simulation across four swarm missions and six state-of-the-art LLMs. Results show that, despite strong reasoning abilities, current general-purpose LLMs still struggle to achieve reliable execution - even for simple swarm tasks - when operating without explicit grounding and execution support. Task-specific planning tools and runtime guardrails substantially improve robustness, while token consumption alone is not indicative of execution quality or reliability.

---

## 论文详细总结（自动生成）

这篇论文《Say the Mission, Execute the Swarm: Agent-Enhanced LLM Reasoning in the Web-of-Drones》探讨了如何利用大语言模型（LLM）实现无人机（UAV）集群的自主管理。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：无人机集群在农业、搜救等领域应用广泛，但传统的管理方式依赖于高度耦合、特定应用的集成，缺乏灵活性。
*   **核心挑战**：
    1.  **异构性**：不同厂商的无人机接口、协议各异，难以统一控制。
    2.  **具身落地（Grounding）不足**：LLM 难以实时获取物理世界状态，且传统的“一次性代码生成”模式无法应对动态环境。
    3.  **安全性与可靠性**：LLM 的非确定性可能导致碰撞或任务失败。
*   **研究目标**：构建一个任务无关（Mission-agnostic）的框架，让用户通过自然语言下达指令，系统通过智能体（Agent）增强的推理能力，实现闭环、实时的集群控制。

### 2. 方法论
*   **核心思想**：放弃传统的“LLM 生成代码”模式，转而采用“推理-执行-监控”的闭环工具调用模式。
*   **关键技术架构**：
    *   **W3C 物联网（WoT）标准**：将每台无人机、传感器和服务抽象为“物（Thing）”，通过“物描述（TD）”定义其属性和动作，实现硬件无关的互操作性。
    *   **模型上下文协议（MCP）网关**：作为 LLM 与物理世界之间的唯一桥梁，将 WoT 的能力暴露为 LLM 可调用的结构化工具。
    *   **智能体核心（Agent Core）**：
        *   **推理循环**：LLM 根据任务目标和实时遥测数据，决定下一步动作。
        *   **运行时护栏（Guardrails）**：动态注入提示词片段，纠正 LLM 的错误行为（如未降落就终止任务）。
        *   **辅助工具（Helper Tools）**：为小型模型提供执行快捷方式（如同步多机指令），减少推理负担。

### 3. 实验设计
*   **实验环境**：基于 ArduPilot SITL（软件在环仿真）的容器化部署，模拟 10 台多旋翼无人机。
*   **任务场景**：
    1.  **区域覆盖（有/无规划工具）**：测试 LLM 的空间规划能力及对外部工具的依赖度。
    2.  **阵型控制**：要求无人机组成星形阵型，并人为设置冲突路径以测试避障和协同能力。
    3.  **智能灌溉**：根据分布式传感器的实时数据（湿度、温度）进行逻辑决策和数据采集。
*   **对比基准**：选取了 6 种最先进的 LLM，包括 GPT-5.2、DeepSeek V3.2、GLM 4.7、Grok 4.1 Fast、Claude Haiku 4.5 和 Qwen3 8B。
*   **评估指标**：成功率、执行时间、电池消耗、碰撞次数、Token 消耗量。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号或数量）。由于实验主要涉及 LLM 的 API 调用和 ArduPilot 仿真，其算力消耗主要集中在模型推理（由云端供应商提供）和轻量级的物理仿真容器上。

### 5. 实验数量与充分性
*   **实验规模**：每种模型在每种任务配置下独立重复运行 10 次，以应对 LLM 的非确定性。
*   **充分性评价**：实验设计较为充分。通过消融实验（如移除规划工具）验证了工具支持的重要性；涵盖了空间、逻辑、协同等多个维度的任务；对比了不同规模和架构的模型，实验结果具有较强的客观性和说服力。

### 6. 主要结论与发现
*   **工具依赖性**：大多数模型（如 GLM、Grok）高度依赖特定任务的规划工具，移除工具后成功率大幅下降。
*   **模型表现差异**：**GLM 4.7** 在各项任务中表现最稳健，成功率高且 Token 消耗相对较低；**GPT-5.2** 推理能力强但在复杂工具交互中易出错；**Qwen3 8B** 等小模型难以处理长程、多步骤的任务。
*   **Token 消耗与质量**：Token 消耗量与任务成功率之间**没有直接相关性**。高效的模型能用更少的对话轮次完成任务。
*   **安全性**：运行时护栏和标准化接口能有效减少碰撞和非法指令，但 LLM 在处理高密度冲突路径时仍存在局限。

### 7. 优点
*   **标准化程度高**：引入 W3C WoT 和 MCP 协议，解决了机器人领域长期存在的异构接口难题。
*   **闭环控制**：相比静态代码生成，实时状态反馈和动态推理显著增强了系统对环境变化的适应性。
*   **任务无关性**：框架不针对特定任务硬编码，具有极强的通用扩展性。

### 8. 不足与局限
*   **仿真与现实的差距**：实验完全在仿真环境中进行，未考虑现实中的通信延迟、风力干扰及传感器噪声。
*   **实时性限制**：依赖云端 LLM 推理可能导致控制频率受限，难以处理需要毫秒级响应的极端避障场景。
*   **规模化压力**：随着无人机数量增加，上下文窗口（Context Window）可能会因遥测数据过多而面临压力。

（完）
