---
title: "NeuroAgent: LLM Agents for Multimodal Neuroimaging Analysis and Research"
title_zh: NeuroAgent：用于多模态神经影像分析与研究的大语言模型智能体
authors: "Lujia Zhong, Yihao Xia, Jianwei Zhang, Shuo huang, Jiaxin Yue, Mingyang Xia, Yonggang Shi"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06584v1"
tags: ["query:malm"]
score: 9.0
evidence: 用于神经影像分析的分层多智能体框架
tldr: 针对神经影像分析中预处理复杂、工具链异构等挑战，本文提出 NeuroAgent 框架。该框架利用大语言模型驱动的层次化多智能体架构，通过“生成-执行-验证”引擎自动化处理 sMRI、fMRI 等多种模态数据。实验表明，NeuroAgent 能高效完成预处理并支持交互式分析，在阿尔茨海默病分类中取得 0.9518 的 AUC，显著降低了科研门槛并提升了分析的自动化水平。
selection_source: fresh_fetch
motivation: 多模态神经影像分析涉及复杂的预处理流程和异构工具链，给研究人员带来了极高的技术门槛和重复性劳动。
method: 开发了一个基于大语言模型的层次化多智能体框架，采用反馈驱动的“生成-执行-验证”引擎来自动化代码生成、错误恢复和结果校验。
result: "在 1,470 名受试者的数据测试中，该系统实现了极高的预处理正确率，并在多模态阿尔茨海默病分类任务中达到了 0.9518 的 AUC。"
conclusion: NeuroAgent 证明了利用大语言模型自动化端到端神经影像分析流程的可行性，能有效减少人工干预并促进可重复性研究。
---

## 摘要
多模态神经影像分析通常涉及复杂的、特定模态的预处理工作流，这些工作流需要在异构工具链之间进行仔细的配置、质量控制和协调。除了预处理之外，下游统计分析和疾病分类通常需要特定任务的代码、评估协议和数据格式约定，这在原始数据采集与可重复的科学分析之间增加了额外的障碍。我们提出了 NeuroAgent，这是一个由大语言模型（LLM）驱动的智能体框架，它能够自动执行异构神经影像数据（包括 sMRI、fMRI、dMRI 和 PET）的关键预处理和分析步骤，并通过自然语言查询支持交互式下游分析。NeuroAgent 采用分层多智能体架构，并配备反馈驱动的“生成-执行-验证”（Generate-Execute-Validate）引擎：智能体自主生成可执行的预处理代码，检测并从运行时错误中恢复，并验证输出的完整性。我们在汇总了所有 ADNI 阶段的 1,470 名受试者（CN=1,000，AD=470）上评估了该系统，其中所有受试者均拥有 sMRI 和表格数据，部分子集还拥有 Tau-PET (n=469)、fMRI (n=278) 和 DTI (n=620)。跨多个 LLM 后端的流水线消融研究表明，性能较强的模型可以达到 100% 的意图解析准确率，其中最强的后端（Qwen3.5-27B）达到了 84.8% 的端到端预处理步骤正确率。自动恢复功能将人工干预限制在需要通过“人机回环”（Human-In-The-Loop）界面进行人工审查的边缘情况。对于使用自动预处理的多模态数据进行阿尔茨海默病分类，我们的智能体集成在四种模态下实现了 0.9518 的 AUC，优于所有单模态基准。这些结果表明，NeuroAgent 可以减少神经影像预处理所需的人工工作量，并为神经影像研究实现端到端的自动化分析流水线。

## Abstract
Multimodal neuroimaging analysis often involves complex, modality-specific preprocessing workflows that require careful configuration, quality control, and coordination across heterogeneous toolchains. Beyond preprocessing, downstream statistical analysis and disease classification commonly require task-specific code, evaluation protocols, and data-format conventions, creating additional barriers between raw acquisitions and reproducible scientific analysis. We present NeuroAgent, an LLM-driven agentic framework that automates key preprocessing and analysis steps for heterogeneous neuroimaging data, including sMRI, fMRI, dMRI, and PET, and supports interactive downstream analysis through natural-language queries. NeuroAgent employs a hierarchical multi-agent architecture with a feedback-driven Generate-Execute-Validate engine: agents autonomously generate executable preprocessing code, detect and recover from runtime errors, and validate output integrity. We evaluate the system on 1,470 subjects pooled across all ADNI phases (CN=1,000, AD=470), where all subjects have sMRI and tabular data, with subsets also having Tau-PET (n=469), fMRI (n=278), and DTI ($n=620$). Pipeline ablation studies across multiple LLM backends show that capable models reach up to 100% intent-parsing accuracy, with the strongest backend (Qwen3.5-27B) reaching 84.8% end-to-end preprocessing step correctness. Automated recovery limits manual intervention to edge cases where human review is required via the Human-In-The-Loop interface. For Alzheimer's Disease classification using automatically preprocessed multimodal data, our agent ensemble achieves an AUC of 0.9518 with four modalities, outperforming all single-modality baselines. These results show that NeuroAgent can reduce the manual effort required for neuroimaging preprocessing and enable end-to-end automated analysis pipelines for neuroimaging research.

---

## 论文详细总结（自动生成）

这是一份关于论文《NeuroAgent: LLM Agents for Multimodal Neuroimaging Analysis and Research》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：多模态神经影像分析（如 sMRI、fMRI、dMRI、PET）面临极高的技术门槛。其预处理流程复杂且高度依赖特定模态，涉及 FSL、FreeSurfer、ANTs 等多个异构工具链。
*   **核心挑战**：传统的自动化工具（如 Nipype、fMRIPrep）在处理非标准数据、缺失元数据或运行错误时仍需大量人工干预。此外，从预处理到下游统计分析和疾病分类的衔接存在断层，限制了研究的可重复性和效率。
*   **整体含义**：本文提出了 **NeuroAgent**，一个由大语言模型（LLM）驱动的自主智能体框架，旨在实现从原始影像数据到科学分析结果的端到端自动化，降低神经科学研究的专业门槛。

### 2. 方法论
*   **核心思想**：采用分层多智能体架构，将高层认知规划与底层技术执行分离，并通过反馈驱动的闭环机制确保执行的鲁棒性。
*   **关键技术细节**：
    *   **分层架构**：
        *   **中央编排器（Central Orchestrator）**：负责意图解析（Intent Parsing）、任务分解和工作流调度（构建 DAG 依赖图）。
        *   **模态专家智能体（Specialized Modality Agents）**：针对 sMRI、fMRI、PET、dMRI 各自的专业知识库，调用特定工具（如 FreeSurfer 进行皮层重建，FSL 进行扩散张量拟合）。
    *   **“生成-执行-验证”引擎（Generate-Execute-Validate）**：
        *   **代码生成**：智能体根据数据状态生成 Python 脚本。
        *   **沙箱执行与自纠错**：在受控环境中运行，若报错则捕获日志并反馈给 LLM 进行代码重写和重试。
        *   **结构验证**：执行后检查输出是否符合 BIDS 等标准规范。
    *   **人机回环（HITL）**：对于无法自动修复的边缘情况，系统会通过交互界面请求人类专家干预。

### 3. 实验设计
*   **数据集**：使用了阿尔茨海默病神经影像计划（ADNI）的汇总数据，涵盖 ADNI1 到 ADNI4 阶段。
    *   **总样本量**：1,470 名受试者（1,000 名认知正常 CN，470 名阿尔茨海默病 AD）。
    *   **模态分布**：所有受试者均有 sMRI 和表格数据，子集包含 Tau-PET (469)、fMRI (278) 和 DTI (620)。
*   **Benchmark 与对比方法**：
    *   **流水线消融研究**：对比了 Qwen3.5 (27B/9B/4B/2B/0.8B)、Qwen3 (30B/14B/8B/4B/1.7B)、GLM-4.7-Flash 和 GPT-OSS 等多个 LLM 后端。
    *   **评估指标**：意图解析准确率（EM）、代码生成正确率（Syntax/Tool/Path/Step Const.）、数据集成保真度。
    *   **下游分类对比**：对比了单模态（sMRI、Tau-PET）与多模态融合（四模态集成）的分类效果，基准模型包括 3D ResNet-18、DenseNet-121 和 EfficientNet-B0。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或模型训练的总时长。
*   **运行效率提及**：文中提到单个受试者的完整预处理大约需要 **4 到 8 小时**（取决于模态和集群可用性），并指出 LLM API 的调用会产生额外的通信开销。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **意图解析**：18 组复杂提示词测试。
    *   **预处理代码生成**：33 个跨模态步骤案例。
    *   **数据集成**：8 种不同缺失情况的场景。
    *   **下游任务**：在 1,470 名受试者的大规模队列上进行了 5 折交叉验证。
*   **充分性评价**：实验设计较为全面，涵盖了从底层的代码生成到高层的科学发现（如皮层厚度统计分析）。通过对不同参数规模的 LLM 进行消融，客观揭示了模型能力与任务成功率的关系。但由于未直接对比“纯手动脚本预处理”的质量差异，其在科学严谨性上的绝对优势仍有待进一步量化。

### 6. 主要结论与发现
*   **LLM 能力边界**：较强的后端（如 Qwen3.5-27B）在意图解析上可达 100% 准确率，在端到端预处理步骤正确率上达到 84.8%。
*   **模型规模并非唯一因素**：Qwen3:4B 在某些任务上优于 14B/8B 版本，说明小模型在结构化输出（JSON）的稳定性上可能更具优势。
*   **多模态优势**：通过 NeuroAgent 自动预处理的数据，在 AD 分类任务中，四模态融合的智能体集成模型达到了 **0.9518 的 AUC**，显著优于任何单模态基准。
*   **科学发现一致性**：智能体能够通过自然语言指令自主复现临床已知的现象（如 AD 患者内嗅皮层变薄、DTI 指标随年龄下降）。

### 7. 优点
*   **高度自动化**：通过自纠错机制极大地减少了研究人员在繁琐预处理上的时间投入。
*   **交互式分析**：支持自然语言驱动的统计绘图和报告生成，使非编程背景的临床医生也能进行复杂的数据挖掘。
*   **灵活性与可扩展性**：分层架构允许轻松集成新的影像工具或深度学习模型。

### 8. 不足与局限
*   **数据集成瓶颈**：在多模态数据集成阶段，开源模型的“全通过率”仅为 37.5%–50%，说明处理复杂的主体-访问对应关系仍具挑战。
*   **领域特定失败的识别**：目前的自纠错主要针对语法和路径错误，对于深层的影像伪影（如严重的运动伪影）可能仍需更专业的质量控制工具集成。
*   **泛化性验证**：ADNI 数据相对标准化，在更具异构性的真实世界临床数据（不同扫描仪、协议）上的表现尚未验证。
*   **依赖性**：系统性能高度依赖于底层 LLM 的推理能力，且存在 API 调用成本和隐私合规性考量。

（完）
