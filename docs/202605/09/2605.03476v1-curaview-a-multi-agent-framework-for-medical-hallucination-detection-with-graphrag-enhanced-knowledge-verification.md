---
title: "CuraView: A Multi-Agent Framework for Medical Hallucination Detection with GraphRAG-Enhanced Knowledge Verification"
title_zh: CuraView：一种基于 GraphRAG 增强知识验证的多智能体医疗幻觉检测框架
authors: "Severin Ye, Xiao Kong, Xiaopeng He, Guangsu Yan, Dongsuk Oh"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03476v1"
tags: ["query:malm"]
score: 9.0
evidence: 具有 GraphRAG 增强验证的多智能体框架
tldr: 针对大语言模型在生成出院小结时易产生医疗幻觉的问题，本文提出CuraView多智能体框架。该框架利用GraphRAG技术从电子健康档案中构建知识图谱，通过闭环生成-检测流程实现句子级的幻觉检测与证据解释。实验表明，该方法在安全性关键指标上表现优异，显著提升了临床文档的事实可靠性，并能生成高质量的标注数据集。
selection_source: fresh_fetch
motivation: 解决大语言模型在自动生成出院小结时产生的忠实性幻觉，以确保医疗文档的准确性与患者安全。
method: 提出基于GraphRAG增强的知识验证框架，通过多智能体协作和四级证据分类实现句子级的幻觉检测与解释。
result: "微调后的模型在关键安全指标E4上的F1值达到0.831，召回率达90.9%，显著优于现有的RAGTruth和QAGS基线。"
conclusion: 基于证据链的图检索验证能有效提升临床文档的事实可靠性，并为医疗大模型的训练与蒸馏提供高质量数据支持。
---

## 摘要
出院小结需要从冗长的电子健康记录（EHR）中提取关键信息，手动执行这一过程非常耗费人力。大语言模型（LLM）可以提高生成效率；然而，它们容易产生忠实度幻觉（即与源记录相矛盾的陈述），从而对患者安全构成直接风险。为解决这一问题，我们提出了 CuraView，这是一个用于出院小结中忠实度幻觉的句子级检测及基于证据解释的多智能体框架。CuraView 从患者层级的 EHR 中构建基于 GraphRAG 的知识图谱，并实现了一个闭环的生成-检测流水线，该流水线包含句子级的证据检索和跨越四个证据等级（从强支持到直接矛盾，E1-E4）的分类，从而产生结构化且可解释的证据链。我们在 Discharge-Me 基准测试集的 250 名患者子集上评估了 CuraView，其中保留了 50 名患者用于测试。我们微调后的 Qwen3-14B 检测模型在安全关键的 E4 指标上达到了 0.831 的 F1 分数（召回率 90.9%，精确率 76.5%），在 E3+E4 指标上达到了 0.823 的 F1 分数，相比基础模型实现了 50.0% 的相对提升，并优于 RAGTruth 风格和 QAGS 风格的基准模型。这些结果表明，基于证据链的图检索验证显著提高了临床文档的事实可靠性，同时为下游模型的训练和蒸馏生成了可重复使用的标注数据集。

## Abstract
Discharge summaries require extracting critical information from lengthy electronic health records (EHRs), a process that is labor-intensive when performed manually. Large language models (LLMs) can improve generation efficiency; however, they are prone to producing faithfulness hallucinations, statements that contradict source records, posing direct risks to patient safety. To address this, we present CuraView, a multi-agent framework for sentence-level detection and evidence-grounded explanation of faithfulness hallucinations in discharge summaries. CuraView constructs a GraphRAG-based knowledge graph from patient-level EHRs and implements a closed-loop generation-detection pipeline with sentence-level evidence retrieval and classification spanning four evidence grades from strong support to direct contradiction (E1-E4), yielding structured and interpretable evidence chains.   We evaluate CuraView on a subset of 250 patients from the Discharge-Me benchmark, with 50 patients held out for testing. Our fine-tuned Qwen3-14B detection model achieves an F1 of 0.831 on the safety-critical E4 metric (90.9% recall, 76.5% precision) and an F1 of 0.823 on E3+E4, representing a 50.0% relative improvement over the base model and outperforming RAGTruth-style and QAGS-style baselines. These results demonstrate that evidence-chain-based graph retrieval verification substantially improves the factual reliability of clinical documentation, while simultaneously producing reusable annotated datasets for downstream model training and distillation.

---

## 论文详细总结（自动生成）

### CuraView：基于 GraphRAG 增强知识验证的多智能体医疗幻觉检测框架

#### 1. 核心问题与整体含义
*   **研究动机**：大语言模型（LLM）在自动生成出院小结时，极易产生“忠实性幻觉”（Faithfulness Hallucinations），即生成的陈述与原始电子健康档案（EHR）不符。在医疗场景下，这种错误（如药量错误、误诊）可能直接威胁患者生命。
*   **核心问题**：现有的幻觉检测方法存在四个局限：缺乏系统性的错误生成机制、缺失患者特定的上下文、证据支持不足（缺乏可解释性）以及缺乏端到端的生成-检测闭环。
*   **研究目标**：开发一个能够实现句子级检测、并提供基于证据的结构化解释的框架，以确保临床文档的事实可靠性。

#### 2. 方法论
CuraView 是一个多智能体框架，其核心思想是利用 **GraphRAG**（图检索增强生成）技术将非结构化的 EHR 数据转化为患者专属的知识图谱，并结合对抗性智能体进行闭环验证。
*   **GraphRAG 知识图谱构建**：
    *   从多表 EHR 中提取 9 类实体（如诊断、药物、实验室结果）和 10 类关系。
    *   **领域定制优化**：通过实验室检查面板归一化、患者实体唯一性强制执行等策略，将实体数量减少了 75.8%，并实现了全图连通。
    *   使用 Leiden 算法进行社区检测，实现从局部到全局的分层检索。
*   **多智能体协作流程**：
    *   **生成智能体**：基于 7 种临床幻觉类型（诊断、药物、检查结果、时间、数值、否定、虚构事实）系统性地改写出院小结，生成带标注的训练/评估样本。
    *   **检测智能体**：将待测句子输入 GraphRAG 检索相关证据链，通过 LLM 进行推理判断。
*   **证据分级系统（E1-E4）**：
    *   **E1/E2**：强/弱支持。
    *   **E3**：无支持（虚构事实）。
    *   **E4**：直接矛盾（安全关键型错误）。
*   **可靠性保障**：采用三层架构（Pydantic 模式约束、JSON 修复、语义一致性验证）确保 14B 规模的模型能稳定输出结构化数据。

#### 3. 实验设计
*   **数据集**：使用 **Discharge-Me** 基准测试集（源自 MIMIC-IV 数据库）。
*   **样本规模**：选取 250 名患者，其中 200 名用于训练/验证，50 名（包含 1,103 个句子）作为独立测试集。
*   **Benchmark 与对比方法**：
    *   **API 基准**：Qwen3-Plus。
    *   **本地模型**：Qwen3-14B（Base 版、原始微调版、精选数据微调版）。
    *   **文献基准**：重现了 **RAGTruth-style**（扁平文本检索）和 **QAGS-style**（问答一致性）两种主流检测框架。

#### 4. 资源与算力
*   **推理算力**：单张 **NVIDIA RTX 4090 (24GB VRAM)**，采用 8-bit 量化部署 14B 模型。
*   **训练算力**：单张 **NVIDIA H200**，使用 LoRA 技术进行微调。
*   **效率**：250 名患者的 GraphRAG 索引构建耗时约 30 分钟。

#### 5. 实验数量与充分性
*   **实验覆盖**：
    *   **主实验**：对比了不同模型在 E4 和 E3+E4 指标下的表现。
    *   **消融实验**：分析了微调对不同幻觉类型的影响、数据质量（精选 vs 原始）的影响、以及 GraphRAG 领域定制化的增益。
    *   **泛化性实验**：对 Meditron-7B 生成的真实报告进行了案例研究。
*   **充分性评价**：实验设计较为全面，样本量（>1000 句）在临床 NLP 领域属于充足水平。通过对比 API 模型和多种 RAG 变体，客观地证明了 GraphRAG 和领域微调的必要性。

#### 6. 主要结论与发现
*   **性能提升**：精选微调后的 Qwen3-14B 在安全关键的 E4 指标上 F1 达到 **0.831**，召回率高达 **90.9%**，较基础模型提升了 **50.0%**。
*   **GraphRAG 的优越性**：相比扁平检索（RAGTruth 风格），CuraView 的 F1 值提升了 **0.192**，证明了图结构在处理跨表医疗逻辑时的关键作用。
*   **数据质量至上**：在精选数据上微调的效果显著优于在全量原始数据上微调，说明高质量标注对医疗安全任务至关重要。
*   **泛化发现**：在对 Meditron-7B 的检测中发现，真实模型的幻觉以“虚构事实”为主（占比 84.4%）。

#### 7. 优点
*   **患者专属图谱**：避免了通用知识图谱无法处理个体差异的问题。
*   **可解释性强**：不仅给出判断，还提供 E1-E4 分级和具体的证据链，符合临床审计需求。
*   **闭环生态**：生成智能体不仅用于评估，还能产生高质量的蒸馏数据，辅助小模型迭代。

#### 8. 不足与局限
*   **数据局限性**：仅基于 MIMIC-IV 单中心数据，且仅支持英文，跨机构和跨语言的泛化性待验证。
*   **语义盲区**：对于主观修饰词（如“严重的” vs “轻微的”）缺乏客观锚点，容易产生漏报。
*   **推理深度**：目前主要侧重于事实匹配，对于需要复杂因果推理（如“由于 A 导致了 B”）的验证能力有限。
*   **时间维度**：虽然有时间错误检测，但对复杂时序逻辑（如术后第几天）的建模仍有提升空间。

（完）
