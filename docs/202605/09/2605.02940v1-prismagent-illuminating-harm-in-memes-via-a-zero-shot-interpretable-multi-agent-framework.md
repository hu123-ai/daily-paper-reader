---
title: "PrismAgent: Illuminating Harm in Memes via a Zero-Shot Interpretable Multi-Agent Framework"
title_zh: PrismAgent：通过零样本可解释多智能体框架揭示模因中的危害
authors: "Zihan Ding, Ziyuan Yang, Yi Zhang"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.02940v1"
tags: ["query:malm"]
score: 9.0
evidence: 包含调查和判决专门智能体的多智能体框架
tldr: 针对有害模因检测中对标注数据依赖性强且泛化能力差的问题，本文提出了PrismAgent，一个零样本、可解释的多智能体框架。该框架模拟刑事调查流程，通过分析、调查、起诉和审判四个阶段，利用专门的智能体协作分析模因意图并检索证据。实验表明，PrismAgent在三个公开数据集上显著优于现有的零样本检测方法，并提供了透明的推理过程。
selection_source: fresh_fetch
motivation: 现有的有害模因检测方法过度依赖大规模标注数据，导致训练成本高且难以应对新出现的有害内容。
method: 提出模拟刑事调查流程的多智能体框架，通过分析意图、检索证据、初步研判和最终裁决四个阶段实现零样本检测。
result: 在三个公开数据集上的实验结果显示，PrismAgent的性能显著超过了现有的零样本检测方法。
conclusion: PrismAgent通过结构化的多智能体协作和显式推理链，实现了高效且具有高度可解释性的有害模因检测。
---

## 摘要
模因（memes）的快速传播使得有害内容检测变得日益重要，因为有效的识别可以遏制误导信息的流传。然而，现有方法严重依赖大量标注数据，这导致了高昂的训练成本和有限的泛化能力。为了应对这些挑战，我们提出了 PrismAgent，一个零样本、多智能体且可解释的框架。PrismAgent 将该任务概念化为刑事案件调查，在结构化的协作工作流中采用了四个专门的智能体，分别负责分析、调查、起诉和审判阶段。在第一阶段，分析智能体在善意和恶意假设下对每个模因进行改写，以探究其潜在意图。随后，调查智能体从无标注数据集中检索支持性证据，并为该模因及其变体构建上下文解释。接着，起诉智能体通过将原始模因与三种解释分别配对，进行三次独立的初步判定。最后，审判智能体对所有证据进行审议，并给出最终裁决。此外，PrismAgent 显式的多阶段推理链使模型具有内在的可解释性，因为每个中间步骤都被明确解释，而不仅仅是产生最终的检测结果。在三个公开数据集上的广泛实验表明，PrismAgent 显著优于现有的零样本检测方法。

## Abstract
The rapid spread of memes makes harmful content detection increasingly crucial, as effective identification can curb the circulation of misinformation. However, existing methods rely heavily on high-volume annotated data, which leads to substantial training costs and limited generalization. To address these challenges, we propose PrismAgent, a zero-shot, multi-agent, interpretable framework. PrismAgent conceptualizes this task as a criminal case investigation, employing four specialized agents responsible for the analysis, investigation, prosecution, and judgment stages within a structured collaborative workflow. In the first stage, the analyst agent paraphrases each meme under benevolent and malicious assumptions to probe its underlying intent. The investigator agent then retrieves supporting evidence from an unannotated dataset and constructs contextual interpretations for the meme and its variants. Next, the prosecutor agent performs three independent preliminary judgments by pairing the original meme with each of the three interpretations. Finally, the judge agent deliberates across all evidence to render a final verdict. Moreover, PrismAgent's explicit multi-stage reasoning chain makes the model inherently interpretable, as every intermediate step is explicitly explained rather than only producing a final detection result. Extensive experiments on three public datasets show that PrismAgent significantly outperforms existing zero-shot detection methods.

---

## 论文详细总结（自动生成）

### PrismAgent：通过零样本可解释多智能体框架揭示模因中的危害

#### 1. 核心问题与整体含义
*   **研究背景**：随着社交媒体的发展，模因（Memes）成为一种普遍的多模态沟通方式，但常被滥用于传播误导信息、仇恨言论和偏见。
*   **核心痛点**：
    1.  **数据依赖性强**：传统检测方法依赖大规模标注数据，训练成本高且难以应对快速演变的模因内容。
    2.  **语义隐晦性**：模因的含义通常隐藏在文化背景、视觉象征或多模态讽刺中，而非字面意思，模型难以捕捉其真实意图。
*   **研究目标**：提出一个名为 **PrismAgent** 的零样本（Zero-shot）、多智能体协作框架，旨在不依赖标注数据的情况下，通过结构化的推理链实现高效且可解释的有害模因检测。

#### 2. 方法论
PrismAgent 模拟了刑事案件的调查流程，将检测任务分解为四个阶段，由四个专门的智能体协作完成：
*   **分析智能体 (Analyst Agent)**：通过“意图想象”策略，在**善意**和**恶意**两种对立假设下对原始模因进行改写。这一步旨在放大模因潜在的隐晦意图，使其语义更显性。
*   **调查智能体 (Investigator Agent)**：利用 CLIP 提取多模态嵌入，在无标注数据集中检索 Top-k 个相似模因作为证据，并为原始模因及其改写版本构建上下文解释。
*   **起诉智能体 (Prosecutor Agent)**：基于调查智能体提供的不同视角解释，对模因进行三次独立的初步判定（原始版、善意版、恶意版），并给出理由。
*   **审判智能体 (Judge Agent)**：如果起诉阶段的判定存在冲突，审判智能体将综合所有证据、核心特征（Core Representation）以及各方矛盾点，进行最终裁决。
*   **核心技术细节**：采用多模态嵌入融合公式 $Emb = \alpha \cdot VE(V) + \beta \cdot TE(T)$ 进行相似度检索，其中 $\alpha=0.8, \beta=0.2$。

#### 3. 实验设计
*   **数据集**：使用了三个公开的 Benchmark 数据集：
    1.  **HarM**：与 COVID-19 相关的有害模因。
    2.  **FHM (Facebook Hateful Memes)**：仇恨言论检测。
    3.  **MAMI**：针对女性的厌女症模因检测。
*   **对比方法**：
    *   **有监督方法**：Late Fusion, MOMENTA。
    *   **零样本/闭源模型**：GPT-4o, Gemini-2.0-Flash。
    *   **开源多模态模型 (VLM)**：LLaVA 系列 (1.5-7B/13B, 1.6-34B), InstructBLIP, MiniGPT-v2, OpenFlamingo。
    *   **现有智能体框架**：MIND (2025 年最新的 SOTA 框架)。

#### 4. 资源与算力
论文明确列出了不同规模模型所使用的硬件资源：
*   **LLaVA-1.5-7B**：使用 1 台 NVIDIA RTX 4090 (24GB)。
*   **LLaVA-1.5-13B**：使用 2 台 NVIDIA RTX 4090 (24GB)。
*   **LLaVA-1.6-34B**：使用 1 台 NVIDIA H800 (80GB)。
*   **推理成本**：由于是零样本框架，无需训练时长，但单条数据的平均推理时间约为 33.2 秒（显著高于单次 Prompt 的 5.3 秒）。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，包括：
    *   **主性能对比**：在三个数据集上验证了超越 MIND 等 SOTA 方法的性能。
    *   **泛化性实验**：将框架应用于不同底座模型（开源与闭源），均有显著提升。
    *   **消融实验**：逐一验证了四个智能体及“核心表示”模块的必要性。
    *   **鲁棒性测试**：包括对抗攻击（FGSM 图像攻击、GCG 文本攻击）、跨领域检索和时间偏移（模拟动态数据库）测试。
    *   **可解释性评估**：通过 Qwen3.5-flash 对推理链的忠实度、逻辑连贯性等五个维度进行评分。
*   **评价**：实验设计非常充分且客观，通过多维度的压力测试证明了框架的稳健性。

#### 6. 主要结论与发现
*   **性能卓越**：PrismAgent 在零样本设置下显著优于现有方法，甚至在某些指标上接近有监督训练的模型。
*   **意图揭示有效**：通过对立假设改写模因，能有效解决模因语义隐晦的问题，纠正初始的误判。
*   **可解释性高**：多阶段推理链提供了透明的决策过程，评分显示其在逻辑深度和表达清晰度上优于基准模型。
*   **检索增强**：引入无标注的相似样本作为“证据”，能显著提升模型对新兴模因趋势的适应能力。

#### 7. 优点
*   **创新的架构设计**：将复杂的模因理解任务转化为结构化的法律调查流程，逻辑严密。
*   **无需标注数据**：完全基于零样本学习，极大地降低了实际应用中的数据准备成本。
*   **内在可解释性**：不同于黑盒分类器，该框架能提供详细的“思想链”解释，便于人工审核。
*   **强大的鲁棒性**：在面对对抗样本和领域迁移时表现出比同类框架更好的稳定性。

#### 8. 不足与局限
*   **推理延迟**：多智能体协作涉及多次 LLM 调用和检索操作，导致单次推理时间较长，难以满足超大规模实时流数据的检测需求。
*   **计算开销**：相比于单次推理，该框架对 API 调用或本地算力的消耗更高。
*   **底座依赖**：尽管框架本身很强，但最终的判断质量仍高度依赖于底层多模态大模型（如 LLaVA 或 GPT-4o）的基础认知能力。

（完）
