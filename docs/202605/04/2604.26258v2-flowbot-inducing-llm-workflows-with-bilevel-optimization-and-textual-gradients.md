---
title: "FlowBot: Inducing LLM Workflows with Bilevel Optimization and Textual Gradients"
title_zh: FlowBot：利用双层优化和文本梯度归纳 LLM 工作流
authors: "Hongyeon Yu, Young-Bum Kim, Yoon Kim"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26258v2"
tags: ["query:malm"]
score: 9.0
evidence: 自动诱导智能体和大语言模型工作流
tldr: 针对现有大语言模型（LLM）工作流依赖人工设计、难以自动化的瓶颈，本文提出 FlowBot 框架。该框架将工作流归纳建模为双层优化问题：外层循环优化工作流的高层结构草图，内层循环通过“文本梯度”逐层反向传播来优化单个 LLM 调用。实验表明，FlowBot 自动发现的工作流在性能上可媲美甚至超越人工设计或生成的强基准方案，为构建高效、自动化的 AI 系统提供了新路径。
selection_source: fresh_fetch
motivation: 现有 LLM 工作流主要依赖人工设计的管道和提示词，限制了其在现实世界中的大规模部署和自动化。
method: 提出一种基于双层优化的数据驱动方法，利用文本梯度分别优化工作流的宏观结构（外层）和具体的 LLM 调用（内层）。
result: FlowBot 自动生成的工作流在多个任务中表现出色，其性能与经过人工精心设计或生成的强基准方案相当。
conclusion: 通过双层优化和文本梯度反向传播，可以有效地从数据中自动诱导并优化复杂的 LLM 工作流。
---

## 摘要
LLM 工作流通过协调对单个 LLM/智能体（agents）的结构化调用来实现特定目标，为构建能够处理多样化任务的强大 AI 系统提供了一条充满前景的路径。然而，现有的构建此类工作流的方法通常依赖于人工设计的工作流管道和提示词，这在实际部署中构成了重大瓶颈。我们如何以数据驱动的方式自动归纳基于 LLM 的智能体和工作流？本文描述了一种简单的、用于自动归纳智能体和 LLM 工作流的数据驱动方法。我们将工作流归纳建模为一个双层优化问题：外层循环优化工作流的高层草图（特别是 LLM 调用的结构化方式），内层循环则逐一优化每个单独的 LLM 调用。两个循环都使用“文本梯度”进行优化，其中内层循环通过逐层“反向传播”文本梯度，以模块化的方式优化每个组件。我们发现，通过 FlowBot（通过双层优化和文本梯度进行工作流归纳）方法发现的 LLM 工作流，其性能与使用人工设计或生成的工作流的强基准模型相比具有竞争力。

## Abstract
LLM workflows, which coordinate structured calls to individual LLMs/agents to achieve a particular goal, offer a promising path towards building powerful AI systems that can tackle diverse tasks. However, existing approaches for building such workflows generally rely on human-crafted pipelines and prompts, which presents a substantial bottleneck in real world deployment. How can we automatically induce LLM-based agents and workflows in a data-driven way? This paper describes a simple data-driven approach for automatically inducing agents and LLM workflows. We formulate workflow induction as a bilevel optimization problem: an outer loop which optimizes a high-level sketch of the workflow (in particular how the LLM calls should be structured), and an inner loop which optimizes each individual LLM call one-by one. Both loops are optimized with ``textual gradients'' where for the inner loop we optimize each component in a modular way through ``backpropagating'' textual gradients layer-by-layer. We find that LLM workflows discovered through our \textsc{FlowBot} (work\textbf{flow} induction through \textbf{b}ilevel \textbf{o}ptimization and \textbf{t}extual gradients) approach performs competitively against strong baselines that make use of human-crafted or generated workflows.

---

## 论文详细总结（自动生成）

### FlowBot: 利用双层优化和文本梯度归纳 LLM 工作流

#### 1. 核心问题与整体含义
随着大语言模型（LLM）的应用从单次调用转向复杂的“复合 AI 系统”，如何设计高效的工作流（即 LLM 调用的结构化序列）成为了核心挑战。目前，大多数工作流依赖人工手动设计管道和提示词，这不仅耗时耗力，且难以跨领域扩展。论文的核心问题是：**如何以数据驱动的方式，自动诱导（Induce）并优化 LLM 工作流的结构及其内部组件的提示词？**

#### 2. 方法论
论文提出了 **FlowBot** 框架，将工作流的生成建模为一个**双层优化（Bilevel Optimization）**问题：

*   **外层循环（结构优化）：** 优化工作流的“草图”（Sketch），即决定包含哪些步骤、步骤的顺序、是否需要循环或条件判断，以及是否调用外部工具。
*   **内层循环（组件优化）：** 在固定结构下，优化每个步骤的具体提示词（Prompt）。
*   **核心技术：文本梯度（Textual Gradients）：**
    *   借鉴神经网络的反向传播思想，FlowBot 利用 LLM 生成自然语言形式的反馈（即“文本梯度”）。
    *   **逐层反向传播：** 从最终输出的损失（Loss）开始，将错误信息逆向传递给最后一个步骤，再由最后一个步骤传递给前一个步骤。每个步骤都会收到关于“为了让下游表现更好，本步骤输出应如何改进”的建议。
    *   **更新机制：** 收集一个 Batch 的文本梯度后，利用优化器 LLM 对工作流结构（外层）或提示词（内层）进行迭代更新。

#### 3. 实验设计
*   **数据集：** 涵盖了 10 个主流基准测试，包括多跳问答（HotpotQA, HoVer）、指令遵循（IFBench）、数学推理（GSM8K, MATH）、代码生成（HumanEval, MBPP）和阅读理解（DROP）等。
*   **Benchmark 与对比方法：**
    *   **提示词优化基准：** 对比了 GEPA（目前最强的进化算法提示词优化）、Trace、MIPROv2 和 TextGrad。
    *   **工作流生成基准：** 对比了 AFlow（基于代码图搜索的工作流优化）和 ADAS。
    *   **基础模型：** 主要实验使用 GPT-4o mini 作为执行器和优化器，同时也测试了 Qwen 系列（4B 到 397B）等开源模型。

#### 4. 资源与算力
*   **算力说明：** 论文未明确提及具体的 GPU 硬件型号或训练小时数，因为其核心实验是基于 OpenAI 的 API（GPT-4o mini）进行的。
*   **成本分析：** 论文详细记录了 API 调用次数和费用。例如，在 6 个任务的总测试中，FlowBot 消耗了约 5.47 万次 API 调用，总成本约为 47.67 美元，显著低于对比方法 AFlow（33.8 万次调用，130.95 美元）。

#### 5. 实验数量与充分性
*   **实验规模：** 论文在 10 个不同性质的数据集上进行了测试，每组实验包含训练（诱导）和测试阶段。
*   **消融实验：** 进行了多组消融研究，验证了“双层优化”架构和“逐层反向传播”机制的必要性。
*   **泛化性测试：** 验证了在不同规模的开源模型（如 Qwen 系列）上的表现，并尝试了“统一工作流”（一个工作流处理所有任务）的扩展实验。
*   **评价：** 实验设计较为充分且客观，通过跨任务对比和成本效益分析，证明了该方法的实用性。

#### 6. 主要结论与发现
*   **自动化能力：** FlowBot 能够从零开始自动发现与人工设计高度相似甚至更优的工作流结构。
*   **性能优越：** 在多个任务上，FlowBot 自动生成的工作流性能超过了基于人工设计结构的提示词优化方法（如 GEPA）和现有的自动工作流生成方法（如 AFlow）。
*   **效率高：** 相比于基于蒙特卡洛树搜索或其他搜索算法的方法，FlowBot 的文本梯度反向传播机制更加数据高效，且 API 成本更低。
*   **模型依赖：** 强大的“元 LLM”（负责生成梯度和更新提示词的模型）能显著提升较弱“执行器 LLM”的表现。

#### 7. 优点
*   **创新的优化范式：** 成功将深度学习中的“反向传播”和“双层优化”概念引入到离散的文本空间和工作流结构设计中。
*   **模块化反馈：** 逐层传递文本梯度解决了长链条工作流中“信用分配”（Credit Assignment）的问题，能精准定位错误步骤。
*   **高灵活性：** 不仅能优化提示词，还能动态增删工作流步骤，适应性极强。

#### 8. 不足与局限
*   **计算开销：** 虽然比部分基准便宜，但优化过程仍需大量 LLM 调用，对于简单任务可能存在“过度设计”的问题。
*   **收敛风险：** 实验观察到模型在训练早期会迅速达到性能瓶颈，提示目前的学习机制在利用大规模数据方面仍有提升空间。
*   **结构限制：** 目前主要集中在链式或简单的分支结构，对于极其复杂的循环或动态图结构的探索尚处于初步阶段。

（完）
