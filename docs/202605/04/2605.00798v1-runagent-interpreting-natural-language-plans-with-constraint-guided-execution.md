---
title: "RunAgent: Interpreting Natural-Language Plans with Constraint-Guided Execution"
title_zh: RunAgent：通过约束引导执行解释自然语言计划
authors: "Arunabh Srivastava, Mohammad A., Khojastepour, Srimat Chakradhar, Sennur Ulukus"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00798v1"
tags: ["query:malm"]
score: 9.0
evidence: 多智能体计划执行平台
tldr: 针对大语言模型在执行结构化工作流时不可靠的问题，本文提出 RunAgent 平台。它通过引入显式控制结构的代理语言，将自然语言计划与编程确定性结合，利用约束引导和动态工具调用实现逐步执行与自动验证，显著提升了复杂任务的执行准确性。
selection_source: fresh_fetch
motivation: 大语言模型在执行自然语言描述的复杂结构化计划时，往往缺乏可靠性和确定性。
method: 提出一种包含显式控制结构的代理语言，通过自主推导约束、动态选择推理模式及过滤上下文历史来确保执行正确性。
result: 在 Natural-plan 和 SciBench 数据集上的评估显示，RunAgent 的表现优于基准 LLM 和最先进的 PlanGEN 方法。
conclusion: RunAgent 有效桥接了自然语言的表达力与编程的确定性，为可靠的自动化计划执行提供了新方案。
---

## 摘要
人类通过执行有针对性的计划来解决问题，然而大语言模型（LLMs）在结构化工作流执行方面仍然不够可靠。我们提出了 RunAgent，这是一个多智能体计划执行平台，它在解释自然语言计划的同时，通过约束和准则强制执行逐步操作。RunAgent 通过一种具有显式控制结构（如 IF、GOTO、FORALL）的智能体语言，将自然语言的表达能力与编程的确定性联系起来。除了根据每个步骤的具体指令对步骤输出进行语法和语义验证外，RunAgent 还会根据任务描述及其在每个步骤中的实例，自主推导并验证约束条件。RunAgent 还能在基于 LLM 的推理、工具使用以及代码生成与执行（例如 Python）之间进行动态选择，并结合纠错机制以确保正确性。最后，RunAgent 在执行每个步骤期间，通过仅保留相关信息来过滤上下文历史记录。在 Natural-plan 和 SciBench 数据集上的评估表明，RunAgent 的性能优于基准 LLM 和最先进的 PlanGEN 方法。

## Abstract
Humans solve problems by executing targeted plans, yet large language models (LLMs) remain unreliable for structured workflow execution. We propose RunAgent, a multi-agent plan execution platform that interprets natural-language plans while enforcing stepwise execution through constraints and rubrics. RunAgent bridges the expressiveness of natural language with the determinism of programming via an agentic language with explicit control constructs (e.g., \texttt{IF}, \texttt{GOTO}, \texttt{FORALL}). Beyond verifying syntactic and semantic verification of the step output, which is performed based on the specific instruction of each step, RunAgent autonomously derives and validates constraints based on the description of the task and its instance at each step. RunAgent also dynamically selects among LLM-based reasoning, tool usage, and code generation and execution (e.g., in Python), and incorporates error correction mechanisms to ensure correctness. Finally, RunAgent filters the context history by retaining only relevant information during the execution of each step. Evaluations on Natural-plan and SciBench Datasets demonstrate that RunAgent outperforms baseline LLMs and state-of-the-art PlanGEN methods.

---

## 论文详细总结（自动生成）

### 论文总结：RunAgent —— 约束引导执行的自然语言计划解释平台

#### 1. 核心问题与整体含义（研究动机和背景）
尽管大语言模型（LLM）在推理和对话方面表现出色，但在执行复杂的、多步骤的结构化工作流时仍然不可靠。主要挑战在于：
*   **缺乏确定性**：自然语言描述的计划往往存在歧义，LLM 难以严格遵循逻辑跳转（如循环和条件分支）。
*   **执行偏差**：在执行过程中，LLM 容易忽略隐含的约束条件或在长序列任务中产生累积误差。
*   **能力错配**：LLM 在处理算法/数学计算时不如代码执行稳定，而在处理创意任务时又优于硬编码。
**RunAgent 的目标**是构建一个多智能体平台，通过一种结合了自然语言表达力与编程语言确定性的“代理语言（Agentic Language）”，实现对计划的精确解释、约束校验和动态执行。

#### 2. 方法论：核心思想与关键技术
RunAgent 的核心是一个三阶段的架构：**初始化与暂存（Staging）**、**编译器（Compiler）**和**执行器（Executor）**。

*   **代理语言（Agentic Language）**：引入了显式的控制流关键字，如 `IF`（条件）、`GOTO`（跳转）、`FORALL`（循环迭代）、`PYTHON`（强制代码执行）、`LLM`（强制模型推理）和 `TOOL`（强制工具调用）。
*   **约束自动生成与校验**：
    *   在任务开始前，系统自动从任务描述和实例中提取“原子约束”（不可分割的逻辑条件）。
    *   在每一步执行后，执行器会提取与当前步骤相关的约束子集，通过“推理者-裁判（Reasoner-Judge）”双模型机制验证输出是否合规。
*   **动态执行模式选择**：系统自主判断当前步骤最适合的执行方式（LLM 推理、生成并运行 Python 代码、或调用外部工具）。
*   **错误修正与上下文过滤**：
    *   **纠错机制**：若步骤未通过约束校验，系统会将错误原因反馈给模型并重新尝试（最多 N 次），失败则回退至 Fallback LLM。
    *   **上下文过滤**：仅保留与当前步骤相关的历史信息和“运行状态摘要（Running State Summary）”，减少冗余 Token 干扰。

#### 3. 实验设计
*   **数据集**：
    *   **Natural-plan**：日历调度（Calendar Scheduling）和旅行规划（Trip Planning）。
    *   **SciBench**：大学级科学问题解决能力测试，涵盖统计（Stat）、微积分（Calc）和微分方程（Diff）。
*   **对比基准（Benchmarks）**：
    *   **Vanilla LLM**：直接使用 GPT-4o 解决问题。
    *   **GPT-4o Plan Implementation**：让 GPT-4o 按照生成的计划自行执行（不使用 RunAgent 框架）。
    *   **State-of-the-art (SOTA)**：PlanGEN 方法（基于 Gemini-1.5-Pro 和 Gemini-2.0-Flash）。
*   **评估指标**：准确率（Accuracy）和精确匹配准确率（Exact Match Accuracy）。

#### 4. 资源与算力
论文**未明确说明**具体的硬件算力（如 GPU 型号或数量）。由于该研究主要基于 OpenAI 的 API（GPT-4o）进行多智能体编排，其核心开销在于 API 调用产生的 Token 消耗和推理延迟，而非本地模型的训练算力。

#### 5. 实验数量与充分性
*   **实验规模**：在日历调度任务中使用了 1000 个问题（分为 10 个难度等级），在 SciBench 的三个子集中也进行了详尽测试。
*   **消融实验**：进行了“无约束校验”版本的对比，证明了约束引导对性能的显著提升（从 75.4% 提升至 81.1%）。
*   **统计显著性**：使用了 **McNemar 检验**和 **Bootstrapping** 方法计算 95% 置信区间，证明 RunAgent 显著优于基准方法（p-value 极低，达 $5.29 \times 10^{-56}$）。
*   **客观性**：实验涵盖了不同难度和不同领域的任务，对比了当前最先进的商业模型和学术框架，实验设计较为客观、公平。

#### 6. 主要结论与发现
*   **执行框架优于纯模型推理**：RunAgent 在日历调度任务上达到了 81.1% 的准确率，远超 GPT-4o 基准（58.3%）和 PlanGEN SOTA（68.9%）。
*   **计划执行的悖论**：实验发现，如果只是给 LLM 一个计划而不配合强制执行框架，性能反而可能下降（因为模型注意力被分散）。
*   **约束的重要性**：自动生成的原子约束能有效拦截 LLM 在执行过程中的逻辑跳跃和事实错误。
*   **循环处理能力**：通过 `FORALL` 关键字，解决了 LLM 在处理集合操作时经常遗漏项的顽疾。

#### 7. 优点与亮点
*   **混合语言设计**：巧妙地平衡了自然语言的灵活性和编程语言的严谨性，降低了非专业人员定义智能体的门槛。
*   **闭环验证**：引入了实时的约束检查和纠错循环，而非简单的单向执行。
*   **状态摘要机制**：通过维护一个精简的“运行状态摘要”，既节省了 Token，又防止了模型在验证环节产生过度推理。

#### 8. 不足与局限
*   **依赖基础模型能力**：RunAgent 的表现高度依赖于底层 LLM（如 GPT-4o）的指令遵循能力，若底层模型太弱，框架效果会打折扣。
*   **评估指标局限**：在某些任务中，RunAgent 给出了正确但与“标准答案”格式不符的解，导致 EM 准确率可能低估了其实际表现（手动检查显示实际准确率更高）。
*   **延迟与成本**：由于引入了多次 LLM 调用（推理、判断、校验、纠错），其完成任务的时间延迟和 API 成本会显著高于单次调用。
*   **多模态缺失**：目前主要处理文本和代码，未涉及包含图像或复杂多模态输入的计划执行。

（完）
