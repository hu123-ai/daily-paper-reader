---
title: Self-Adaptive Multi-Agent LLM-Based Security Pattern Selection for IoT Systems
title_zh: 面向物联网系统的基于大语言模型（LLM）的多智能体自适应安全模式选择
authors: "Saeid Jamshidi, Foutse Khomh, Carol Fung, Kawser Wazed Nafi"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00741v1"
tags: ["query:malm"]
score: 9.0
evidence: 基于大语言模型的自适应多智能体安全选择
tldr: 针对物联网边缘设备在资源受限环境下安全机制选择困难的问题，本文提出ASPO框架。该框架结合大语言模型（LLM）的多智能体推理与确定性执行核心，在MAPE-K闭环中实现自适应安全模式选择。通过将随机决策生成与确定性约束强制分离，ASPO在保证资源可行性和无冲突执行的同时，显著降低了尾部延迟和能耗，为物联网系统提供了高效且安全的运行时防御方案。
selection_source: fresh_fetch
motivation: 现有的物联网安全防御机制在资源受限的边缘环境下难以兼顾决策的可行性、冲突安全性和执行正确性。
method: 提出ASPO框架，利用LLM智能体生成候选防御组合，并通过确定性优化核心强制执行闭环动作完整性、无冲突组合及资源约束。
result: "实验表明ASPO实现了100%的无冲突激活和资源可行性，并将尾部延迟和能耗分别降低了21.9%和23.1%。"
conclusion: 该研究证明了将LLM的推理能力与确定性约束机制相结合，能有效提升物联网系统在动态环境下的自适应安全防御效能。
---

## 摘要
智能架构网络边缘的物联网（IoT）系统应用正迅速增长，这加剧了对兼具自适应性和资源效率的安全机制的需求。在此类环境中，运行时防御机制不再局限于检测，而是演变为一项受资源约束的缓解措施选择任务。安全控制必须在延迟、能源和计算约束下进行仔细选择、组合和执行，同时防止控制措施之间产生不安全的交互。现有方法主要依赖于静态规则集和学习策略，在资源受限的边缘环境中，这些方法在可行性、冲突安全性以及执行正确性方面的保障有限。为解决这一局限性，我们提出了 ASPO，这是一种自适应多智能体安全模式选择方案，它在 MAPE-K 控制循环中集成了基于大语言模型（LLM）的推理与确定性执行。ASPO 明确地将随机决策生成与执行分离：LLM 智能体提出候选缓解组合，而确定性优化核心则在每个决策周期内强制执行封闭世界动作完整性、无冲突组合以及资源可行性。我们在分布式边缘网关测试平台上部署了 ASPO，并使用重放的物联网攻击流量，在包含 500 次和 1000 次运行时安全决策的两个工作负载下对其进行了评估。此外，结果展示了不变的安全性属性，包括 100% 的无冲突激活、跨工作负载的一致资源可行性，以及具有完美排名保持的稳定模式主导地位。重要的是，更深层次的决策探索降低了极端情况下的执行成本，在不增加平均能耗的情况下，分别将尾部延迟和能源开销压缩了 21.9% 和 23.1%。

## Abstract
The adoption of Internet of Things (IoT) systems at the network edge of smart architectures is increasing rapidly, intensifying the need for security mechanisms that are both adaptive and resource-efficient. In such environments, runtime defence mechanisms are no longer limited to detection alone but become a resource-constrained task of selecting mitigation actions. Security controls must be carefully selected, combined, and executed under latency, energy, and computational constraints, while preventing unsafe interactions between controls. Existing approaches predominantly rely on static rule sets and learned policies, which provide limited guarantees of feasibility, conflict safety, and execution correctness in resource-constrained edge settings. To address this limitation, we introduce ASPO, a self-adaptive multi-agent security pattern selection that integrates Large Language Model (LLM)-based reasoning with deterministic enforcement within a MAPE-K control loop. ASPO explicitly separates stochastic decision generation from execution: LLM agents propose candidate mitigation portfolios, while a deterministic optimisation core enforces closed-world action integrity, conflict-free composition, and resource feasibility at every decision epoch. We deploy ASPO on a distributed edge-gateway testbed and evaluate it across two workloads, each comprising 500 and 1000 runtime security decisions, using replayed IoT attack traffic. In addition, the results demonstrate invariant safety properties, including 100% conflict-free activation, consistent resource feasibility across workloads, and stable pattern dominance with perfect rank preservation. Importantly, deeper decision exploration reduces extreme-case execution costs, compressing tail latency and energy overheads by 21.9% and 23.1%, respectively, without increasing mean energy consumption.

---

## 论文详细总结（自动生成）

这是一份关于论文《Self-Adaptive Multi-Agent LLM-Based Security Pattern Selection for IoT Systems》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着物联网（IoT）在边缘计算中的普及，边缘网关面临着严峻的安全挑战。核心问题在于：在资源受限（CPU、内存、延迟、能耗）的边缘环境下，如何动态、安全且高效地选择和组合安全防御模式（如黑名单、流量分割等）。
*   **研究动机**：现有的深度强化学习（DRL）方法缺乏显式推理能力且难以保证硬性的安全约束；而大语言模型（LLM）虽然具备推理能力，但其输出具有随机性，可能产生不可行或冲突的防御计划。
*   **整体含义**：论文旨在构建一个既能利用 LLM 的上下文推理能力，又能通过确定性机制强制执行安全约束的自适应防御框架。

### 2. 方法论：核心思想与关键技术
论文提出了 **ASPO** 框架，其核心思想是将**随机的智能体推理**与**确定性的约束执行**分离，并嵌入到 **MAPE-K**（监控-分析-计划-执行-知识）控制循环中。
*   **多智能体推理（Plan 阶段）**：采用 5 种专门的 LLM 智能体（上下文、推理、约束、计划、审计智能体）。它们负责理解威胁上下文并从包含 10 种安全模式的“封闭世界”目录中提出候选组合。
*   **确定性优化核心**：这是 ASPO 的关键。它接收 LLM 提出的候选方案，通过数学优化模型（基于适用性评分、资源成本、冲突矩阵和协同矩阵）计算出最优且无冲突的防御组合。
*   **安全门控（Execute 阶段）**：在执行前，通过确定性验证核心（Security Gate）强制检查动作完整性、资源可行性和执行顺序，确保不符合约束的计划被拦截并回退到安全基准。

### 3. 实验设计
*   **数据集与场景**：使用 **Bot-IoT 数据集**，涵盖 DoS、DDoS、僵尸网络、暴力破解和端口扫描等攻击。
*   **实验平台**：由 10 台 **Raspberry Pi 4 Model B** 组成的分布式边缘网关测试床，模拟真实的嵌入式资源约束环境。
*   **对比维度**：
    *   **工作负载规模**：对比了 500 次和 1000 次运行时决策的稳定性。
    *   **LLM 后端**：对比了 DeepSeek v3、GPT-4o-mini 和 LLaMA-3.1-8B-Instruct。
    *   **指标**：审批通过率、决策延迟（中位数及 P99 尾部延迟）、能耗、冲突率、模式排名稳定性。

### 4. 资源与算力
*   **边缘端**：使用树莓派 4（四核 ARM Cortex-A72, 4GB RAM），并显式建模了热限值和功耗缩放。
*   **云端（LLM）**：调用了云端托管的 DeepSeek v3、GPT-4o-mini 等模型。
*   **训练说明**：论文未提及模型训练过程，主要侧重于**推理阶段**的智能体编排与确定性算法的集成。

### 5. 实验数量与充分性
*   **实验规模**：总计进行了 1500 次运行时安全决策实验（500+1000 轮次）。
*   **充分性**：实验涵盖了多种攻击类型和不同的 LLM 后端，并使用了 Fisher 精确检验、KS 检验和 Wilcoxon 符号秩检验等统计学方法验证结果的显著性。
*   **客观性**：通过在物理硬件（树莓派）而非纯仿真环境中运行，增强了结果的真实性和说服力。

### 6. 主要结论与发现
*   **安全性保障**：ASPO 实现了 **100% 的无冲突激活**和一致的资源可行性。确定性门控拦截了超过 99% 的不合格 LLM 提议。
*   **性能优化**：随着决策探索的深入（从 500 增至 1000 轮），**尾部延迟压缩了 21.9%**，**尾部能耗降低了 23.1%**，且未增加平均能耗。
*   **稳定性**：防御模式的选择频率表现出极高的稳定性，跨工作负载的模式排名相关性（Spearman Rank）达到了 **1.0**（完美保持）。

### 7. 优点
*   **架构创新**：成功解决了 LLM 随机性与工业/物联网安全确定性需求之间的矛盾。
*   **资源感知**：将延迟和能耗作为硬性约束纳入优化模型，非常契合边缘计算场景。
*   **可审计性**：基于 MAPE-K 的结构化设计和审计智能体，使得防御决策过程透明、可追溯。

### 8. 不足与局限
*   **实时性挑战**：单次决策延迟约为 20 秒以上（受限于云端 LLM 调用），对于某些毫秒级响应要求的攻击（如高速流量洪泛）可能过慢。
*   **外部依赖**：高度依赖云端 LLM 服务，在网络连接不稳定或断网的边缘环境下无法工作。
*   **扩展性限制**：目前基于 10 种预定义模式的“封闭世界”目录，对于未知威胁或需要自定义防御动作的场景扩展性有限。

（完）
