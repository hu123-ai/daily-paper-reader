---
title: A Self-Healing Framework for Reliable LLM-Based Autonomous Agents
title_zh: 一种用于可靠的大语言模型（LLM）自主智能体的自修复框架
authors: "Cheonsu Jeong, Younggun Shin"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06737v1"
tags: ["query:malm"]
score: 9.0
evidence: 可靠的大语言模型自主智能体自愈框架
tldr: 针对大语言模型（LLM）智能体在复杂系统中因幻觉和执行错误导致的可靠性挑战，本文提出了一种自修复框架。该框架集成了故障检测、量化可靠性评估和自动恢复机制，通过定义故障分类、识别异常执行模式以及实施自适应重规划策略，实现了智能体的动态纠错。实验表明，该方法在多智能体工作流中显著提升了任务成功率和系统鲁棒性，为LLM在生产环境的稳定应用提供了重要支撑。
selection_source: fresh_fetch
motivation: 旨在解决LLM智能体在复杂软件系统中因幻觉、执行错误和推理不一致而导致的可靠性不足问题。
method: 提出一种集成故障检测、可靠性评估及基于自适应重规划和纠错提示的自动恢复机制的自修复框架。
result: 实验证明该框架显著提高了任务成功率，有效遏制了故障传播，并增强了多智能体系统的整体稳定性。
conclusion: 通过结合内部推理监控与外部执行反馈，该研究为提升自主系统的可靠性及推动LLM在生产环境的落地提供了有效方案。
---

## 摘要
基于大语言模型（LLM）的自主智能体正越来越多地被应用于复杂的软件系统中。然而，由于幻觉、执行错误和推理不一致等不可预测的故障，可靠性仍然是一个重大挑战。本文提出了一种针对基于 LLM 的软件智能体的可靠性感知自修复框架。该框架集成了故障检测、可靠性评估和自动恢复机制。首先，我们定义了故障类型的分类法，并引入了定量可靠性评估模型。其次，我们提出了一种故障检测方法，该方法根据执行模式和输出一致性来识别异常的智能体行为。最后，我们设计了一种自修复机制，通过自适应重新规划和纠错提示策略从故障中动态恢复。所提出的框架在多智能体工作流环境中实现，并使用真实世界的任务场景进行了评估。实验结果表明，与现有方法相比，我们的方法显著提高了任务成功率，减少了故障传播，并增强了系统的整体鲁棒性。特别是，本研究的独特之处在于建立了一个综合监控系统，将智能体的内部推理过程与外部执行结果相结合。这些发现有望为保障先进自主系统的稳定性做出贡献，并降低在生产环境中采用 LLM 的门槛。

## Abstract
Autonomous agents based on Large Language Models (LLMs) are increasingly being utilized in complex software systems. However, reliability remains a significant challenge due to unpredictable failures such as hallucinations, execution errors, and inconsistent reasoning. This paper proposes a reliability-aware self-healing framework for LLM-based software agents. The framework integrates failure detection, reliability assessment, and automated recovery mechanisms. First, we define a taxonomy of failure types and introduce a quantitative reliability assessment model. Next, we propose a failure detection method that identifies abnormal agent behavior based on execution patterns and output consistency. Finally, we design a self-healing mechanism that dynamically recovers from failures through adaptive replanning and corrective prompting strategies. The proposed framework was implemented in a multi-agent workflow environment and evaluated using real-world task scenarios. Experimental results demonstrate that our approach significantly increases task success rates, reduces failure propagation, and enhances overall system robustness compared to existing methods. In particular, this study distinguishes itself by establishing an integrated monitoring system that combines the agent's internal reasoning process with external execution results. These findings are expected to contribute to securing the stability of advanced autonomous systems and lowering the barriers to LLM adoption in production environments.

---

## 论文详细总结（自动生成）

这篇论文提出了一种针对大语言模型（LLM）自主智能体的自修复框架，旨在解决其在复杂软件系统应用中的可靠性瓶颈。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：尽管 LLM 智能体在推理和任务规划方面表现出色，但在实际生产环境中仍面临**可靠性挑战**，主要表现为幻觉（Hallucination）、工具调用错误、推理不一致以及错误在多步工作流中的级联传播。
*   **研究背景**：现有的改进方法（如提示工程、自我修正或一致性解码）大多是零散的、针对静态推理的，缺乏一个将故障检测、定量评估和自动恢复集成在一起的系统级运行时框架。

### 2. 核心方法论
论文提出了一个集成化的自修复架构，其核心流程遵循“任务执行 -> 故障检测 -> 可靠性评估 -> 自动修复 -> 重新执行”的闭环：
*   **故障分类法（Taxonomy）**：定义了四类故障：F1（幻觉错误）、F2（执行错误/API失败）、F3（推理不一致）、F4（工作流传播错误）。
*   **定量可靠性模型**：引入可靠性分数 $R = \omega_1 C + \omega_2 S + \omega_3 E$。
    *   $C$（一致性）：通过多次执行轨迹的归一化编辑距离衡量。
    *   $S$（语义正确性）：通过 RAG 或领域知识库验证。
    *   $E$（执行成功率）：工具/API 调用的成功比例。
*   **混合检测机制**：结合执行模式分析（监控异常序列）和输出一致性检查，当 $R$ 低于预设阈值 $\theta$ 时触发修复。
*   **自修复策略**：
    *   **自适应重规划**：当原计划失败时重新生成任务分解。
    *   **提示词修正**：动态修改 Prompt 以消除歧义。
    *   **工具重选**：当特定 API 失效时，寻找替代工具或调整重试策略。

### 3. 实验设计
*   **实验场景**：模拟真实软件自动化任务的多智能体工作流，包括：
    1.  多步推理任务。
    2.  API 编排工作流。
    3.  文档处理流水线。
*   **基准模型与对比方法**：
    *   **基础模型**：OpenAI GPT-5。
    *   **对比基准（Baselines）**：标准 LLM 智能体（ReAct 模式）、简单重试机制（Retry-based）、自我修正方法（Self-Refine）、基于一致性的选择方法。
*   **评估指标**：任务成功率（TSR）、故障检测准确率（FDA）、恢复成功率（RSR）和执行开销（EO）。

### 4. 资源与算力
*   **模型使用**：明确提到使用了 **OpenAI GPT-5** 作为基础 LLM。
*   **算力细节**：论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）或训练/推理的总时长。由于该框架侧重于运行时（Runtime）的逻辑编排而非模型训练，其算力消耗主要体现在 API 调用频次和推理延迟上。

### 5. 实验数量与充分性
*   **实验规模**：针对三种任务类型各构建了 100 个测试用例，总计 **300 个任务实例**。
*   **实验设计**：
    *   进行了 **3 次重复实验**以确保结果稳定性。
    *   采用了**故障注入（Fault Injection）**技术，以 30% 的随机概率人工注入 F1-F4 类故障，验证框架的鲁棒性。
    *   使用 **Wilcoxon 符号秩检验**（p < 0.05）进行统计显著性分析。
*   **充分性评价**：实验设计较为严谨，包含了消融实验和统计检验，能够客观反映框架在处理动态故障时的有效性。

### 6. 主要结论与发现
*   **性能提升**：该框架显著提高了任务成功率（TSR），并能有效遏制错误在多智能体系统中的传播。
*   **恢复能力**：通过将底层基础设施异常（如 API 超时）转化为结构化文本反馈给智能体，智能体能够表现出“智能恢复”行为（如选择低资源消耗的替代工具）。
*   **系统集成价值**：证明了将内部推理监控与外部执行反馈相结合是提升自主系统稳定性的关键。

### 7. 论文优点
*   **系统性强**：不同于以往只关注 Prompt 优化的研究，该框架从软件工程角度构建了完整的自愈生命周期。
*   **闭环反馈**：将底层执行异常（Exception）引入 LLM 的推理环路，实现了软硬件层面的协同修复。
*   **定量化**：提出了明确的可靠性量化公式，为 LLM 智能体的运行时监控提供了标准。

### 8. 不足与局限
*   **参数敏感性**：可靠性公式中的权重（$\omega$）和阈值（$\theta$）需要根据不同领域手动调优，缺乏自动化的参数自适应机制。
*   **环境局限**：实验主要在模拟环境（Simulation-based）中进行，在极端复杂或高并发的真实生产环境中的表现仍需进一步验证。
*   **评估维度**：任务成功率采用二元判断（成功/失败），未能充分衡量“部分成功”或任务完成的质量细节。
*   **成本开销**：由于涉及多次推理和一致性检查，可能会显著增加 API 调用成本和响应延迟。

（完）
