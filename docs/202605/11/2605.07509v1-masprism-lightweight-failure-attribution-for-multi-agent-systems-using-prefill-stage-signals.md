---
title: "MASPrism: Lightweight Failure Attribution for Multi-Agent Systems Using Prefill-Stage Signals"
title_zh: MASPrism：利用预填充阶段信号的轻量级多智能体系统故障归因
authors: "Yang Liu, Hongjiang Feng, Junsong Pu, Zhuangbin Chen"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07509v1"
tags: ["query:malm"]
score: 9.0
evidence: 多智能体系统的故障归因
tldr: MASPrism是一个针对多智能体系统（MAS）故障归因的轻量级框架。针对长执行日志中故障定位难、成本高的问题，该方法利用小语言模型（SLM）在预填充阶段产生的负对数似然和注意力权重等信号，无需解码即可识别并排序故障源。实验表明，MASPrism在多个基准测试上显著优于现有方法和大型闭源模型，且推理速度提升了6.69倍，为长序列执行日志的故障诊断提供了高效实用的方案。
selection_source: fresh_fetch
motivation: 现有的多智能体系统故障归因方法面临执行步骤多、故障证据延迟以及现有诊断方法成本高昂且效率低下的挑战。
method: 提出一种基于小模型预填充信号的框架，通过分析Token级的负对数似然和注意力权重来识别故障症状并对候选故障源进行排序。
result: "在Who&When和TRAIL数据集上，MASPrism的表现优于Gemini-2.5-Pro等大型模型，Top-1准确率显著提升，且处理速度比基准快6.69倍。"
conclusion: 该研究证明了利用轻量级模型的内部信号可以在不进行文本解码的情况下，高效且精准地完成复杂多智能体系统的故障归因任务。
---

## 摘要
基于大语言模型（LLM）的多智能体系统中的故障归因旨在识别导致执行失败的步骤。这一任务仍然具有挑战性，因为单次执行可能包含大量的智能体动作和工具调用，故障证据可能在原始错误发生后的许多步骤才出现，且现有方法通常依赖于昂贵的智能体工作流、重放或在合成故障日志上进行训练。为了应对这些挑战，我们提出了 MASPrism，这是一个利用小语言模型（SLM）的预填充阶段信号进行故障归因的轻量级框架。MASPrism 首先在预填充过程中提取标记级（token-level）负对数似然和注意力权重，以识别症状类步骤和更早的候选源，而无需进行解码。然后，它重构一个聚焦的诊断提示词，并进行第二次预填充以对故障源候选进行排序。使用 Qwen3-0.6B 作为 SLM，MASPrism 在 Who&When 和 TRAIL 的四个评估子集中的三个上取得了最佳性能，在 Who&When-HC 上的 Top-1 准确率比最佳基线提高了 33.41%。在 TRAIL 上，MASPrism 的表现优于包括 Gemini-2.5-Pro 在内的强力专有 LLM，相对提升高达 89.50%。MASPrism 平均处理每条轨迹仅需 2.66 秒，比单次提示（single-pass prompting）基线实现了 6.69 倍的加速，且输出标记数为零。这些结果表明，MASPrism 为长多智能体执行日志中的故障归因提供了一个有效且实用的框架。

## Abstract
Failure attribution in LLM-based multi-agent systems aims to identify the steps that contribute to a failed execution. This task remains difficult because a single execution can contain many agent actions and tool calls, failure evidence can appear many steps after the original mistake, and existing methods often rely on costly agent workflows, replay, or training on synthetic failure logs. To address these challenges, we propose MASPrism, a lightweight framework that performs failure attribution using prefill-stage signals from a small language model (SLM). MASPrism first extracts token-level negative log-likelihood and attention weights during a prefill pass to identify symptom-like steps and earlier candidate sources, without decoding. It then reconstructs a focused diagnostic prompt and performs a second prefill pass to rank failure-source candidates. Using Qwen3-0.6B as the SLM, MASPrism achieves the best performance on three of the four evaluated subsets across Who&When and TRAIL, improving Top-1 accuracy on Who&When-HC by 33.41% over the best baseline. On TRAIL, MASPrism outperforms strong proprietary LLMs, including Gemini-2.5-Pro, with up to 89.50% relative improvement. MASPrism processes each trace in 2.66 seconds on average, achieving a 6.69$\times$ speedup over the single-pass prompting baseline, with zero output tokens. These results show that MASPrism provides an effective and practical framework for failure attribution in long multi-agent execution logs.

---

## 论文详细总结（自动生成）

以下是对论文《MASPrism: Lightweight Failure Attribution for Multi-Agent Systems Using Prefill-Stage Signals》的结构化总结：

### 1. 核心问题与整体含义
论文关注的是**基于大语言模型（LLM）的多智能体系统（MAS）的故障归因**问题。当多智能体系统执行任务失败时，开发者需要从冗长的执行日志（包含智能体动作、消息、工具调用等）中找出导致失败的根源步骤。
*   **研究动机**：现有的归因方法（如基于诊断智能体、执行重放或合成数据训练）在处理长序列日志时，面临计算成本高、延迟大、依赖正确答案（Oracle）或泛化性差等挑战。
*   **核心目标**：提出一种无需解码生成文本、无需重放、无需特定任务训练的轻量级故障归因框架。

### 2. 方法论
MASPrism 的核心思想是利用**小语言模型（SLM）在预填充（Prefill）阶段产生的内部信号**（而非生成阶段的文本）来定位故障。
*   **关键信号**：
    *   **负对数似然（NLL）**：衡量模型对当前 Token 的“惊讶”程度。高 NLL 通常对应症状步骤（如显式错误或逻辑不一致）。
    *   **注意力权重（Attention）**：衡量 Token 间的关联。症状步骤对早期步骤的注意力可以作为追溯故障源的路由信号。
*   **算法流程（两阶段管线）**：
    1.  **过滤阶段（Filtering）**：将长日志进行步骤级截断（仅保留前缀），通过一次预填充计算 NLL 识别“症状步骤”，并利用注意力权重筛选出潜在的“候选故障源”。
    2.  **诊断阶段（Diagnosis）**：重构提示词，恢复症状步骤和候选源的完整内容（其余步骤保持压缩），进行第二次预填充。通过结合 NLL 对比度和多症状共识机制（Multi-Symptom Consensus），对候选源进行最终排序并输出归因链路。

### 3. 实验设计
*   **数据集/场景**：
    *   **Who&When**：包含手工制作（HC）和算法生成（AG）子集，侧重于单步根因识别。
    *   **TRAIL**：包含 GAIA（开放搜索）和 SWE-bench（软件补丁）子集，侧重于长序列中的多处错误定位。
*   **基准方法（Baselines）**：
    *   **通用 LLM 诊断**：GPT-4o（采用全量、逐步、二分查找等策略）、OpenAI o3、Claude-3.7-Sonnet、Gemini-2.5-Pro。
    *   **专用归因工具**：A2P（基于 GLM-5.1）、AgenTracer（基于 Qwen3-8B 强化学习微调）。
*   **评估指标**：Top-1 准确率（Who&When）和位置准确率（Location Accuracy, TRAIL）。

### 4. 资源与算力
*   **硬件**：使用一台配备 **NVIDIA GeForce RTX 5090** GPU 的服务器。
*   **模型**：主要使用 **Qwen3-0.6B** 作为诊断模型，同时也测试了 Qwen3-1.7B、Llama-3.2-1B 和 3B 版本。
*   **训练时长**：该方法属于 **Zero-shot（零样本）** 推理框架，不需要针对诊断任务进行额外的模型训练或微调。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个主流子集上进行了端到端对比，涵盖了从短序列到超长序列（SWE-bench）的场景。
*   **消融实验**：对比了“无诊断阶段”、“无提示词恢复”、“无过滤阶段”等变体，验证了两阶段设计的必要性。
*   **统计显著性**：对注意力信号的有效性进行了配对单侧 Wilcoxon 符号秩检验（p-value 远小于 0.05），证明了信号的可靠性。
*   **充分性评价**：实验设计较为全面，不仅对比了准确率，还详细分析了延迟、Token 消耗和模型鲁棒性，实验结果客观且具有说服力。

### 6. 主要结论与发现
*   **性能卓越**：仅使用 0.6B 参数的模型，MASPrism 在长序列任务（Who&When-HC 和 TRAIL）上显著优于 GPT-4o 和 Gemini-2.5-Pro 等顶级闭源模型。
*   **高效低耗**：相比 A2P 基线，实现了 **6.69 倍的加速**，且由于无需解码，**输出 Token 数为零**，大幅降低了推理成本。
*   **长文本处理能力**：通过两阶段的截断与恢复机制，MASPrism 能够处理超出普通模型上下文限制的超长日志（如 SWE-bench 轨迹）。

### 7. 优点
*   **轻量高效**：利用 SLM 的内部信号，避开了昂贵的文本生成过程。
*   **无需 Oracle**：在不知道正确答案的情况下即可进行诊断，符合真实调试场景。
*   **可解释性**：通过“症状-源”的注意力链路提供归因依据，而非仅给出一个预测结果。
*   **通用性强**：在不同规模和家族的小模型（Qwen, Llama）上均表现稳健。

### 8. 不足与局限
*   **模型访问限制**：该方法依赖于获取模型的内部信号（NLL 和 Attention），因此**无法应用于闭源 API 服务**（如 OpenAI API），仅限于开源或可控权重的模型。
*   **应用范围**：目前主要针对已确定失败的日志进行归因，尚未深入探讨在成功/失败混合流中的实时异常检测表现。
*   **超参数依赖**：症状识别比例等超参数可能需要根据不同领域的日志特征进行微调。

（完）
