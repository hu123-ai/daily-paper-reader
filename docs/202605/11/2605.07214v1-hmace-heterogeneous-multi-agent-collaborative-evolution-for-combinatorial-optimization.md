---
title: "HMACE: Heterogeneous Multi-Agent Collaborative Evolution for Combinatorial Optimization"
title_zh: HMACE：用于组合优化的异构多智能体协同演化
authors: "Yuping Yan, Jirui Han, Fei Ming, Yuanshuai Li, Yaochu Jin"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07214v1"
tags: ["query:malm"]
score: 9.0
evidence: 异构多智能体协作进化框架
tldr: HMACE是一个针对组合优化问题（COP）的异构多智能体协作进化框架。针对现有大模型启发式设计方法中工作流僵化、易陷入局部最优的问题，该框架将进化过程分解为由提议者、生成者、评估者和反思者组成的自主协作环路。通过行为感知检索和轻量级过滤，HMACE在TSP和在线BPP等任务上以更低的Token消耗实现了优于现有单/多智能体基准的性能。
selection_source: fresh_fetch
motivation: 现有的基于大模型的启发式设计方法受限于僵化的单体工作流，导致探索不足且容易过早收敛到局部最优。
method: 提出一种异构多智能体协作进化框架，通过提议、生成、评估和反思四个角色的协同工作，结合行为感知检索和存档更新来引导启发式搜索。
result: 在TSP、在线BPP等多个典型组合优化问题上，HMACE以显著更少的Token消耗达到了最低的平均误差间隙。
conclusion: HMACE通过组织化设计和多智能体协作，为自动化启发式设计提供了一种高效且具备良好质量-效率权衡的新范式。
---

## 摘要
大语言模型（LLMs）最近已成为解决 NP 难组合优化问题（COPs）中自动化启发式设计的有前景的范式。尽管取得了这些进展，现有的基于 LLM 的方法通常依赖于受刚性模板限制的单一工作流，从而限制了记忆引导的探索，并导致过早收敛到局部最优。为了设计一种自主且协作的架构，我们引入了 HMACE，这是一个异构多智能体协同演化框架，它将启发式搜索重新概念化为一个组织设计问题。HMACE 将每一代演化分解为一个自主的、角色专门化的循环，包含四个协调的智能体：用于策略探索的建议者（Proposer）、用于可执行启发式合成的生成者（Generator）、用于经验评估的评估者（Evaluator）以及用于基于存档的记忆更新的反思者（Reflector）。通过结合行为感知检索、轻量级候选过滤和基于适应度的存档更新，HMACE 引导搜索向多样化且有前景的启发式行为发展，同时避免冗余评估。在包括 TSP、在线 BPP、MKP 和 PFSP 在内的代表性组合优化问题上的广泛评估表明，与最先进的单智能体和多智能体基准相比，HMACE 实现了良好的质量-效率权衡。在匹配的 LLM 驱动参考对比中，HMACE 在 TSP 和在线 BPP 上实现了最低的平均差距（分别为 0.464% 和 0.223%），而这两项任务仅分别需要 0.13M 和 0.42M 的 token，显著少于对比的基准。

## Abstract
Large Language Models have recently emerged as a promising paradigm for automated heuristic design for NP-hard combinatorial optimization problems. Despite this progress, existing LLM-based methods typically rely on monolithic workflows constrained by rigid templates, thereby restricting memory-guided exploration and triggering premature convergence to local optima. To design an autonomous and collaborative architecture, we introduce HMACE, a Heterogeneous Multi-Agent Collaborative Evolution framework that reconceptualizes heuristic search as an organizational design problem. HMACE decomposes each evolutionary generation into an autonomous, role-specialized loop with four coordinated agents: a Proposer for strategy exploration, a Generator for executable heuristic synthesis, an Evaluator for empirical assessment, and a Reflector for archive-backed memory update. By coupling behavior-aware retrieval, lightweight candidate filtering, and fitness-grounded archive updates, HMACE guides the search toward diverse and promising heuristic behaviors while avoiding redundant evaluations. Extensive evaluations on representative COPs, including TSP, Online BPP, MKP, and PFSP, show that HMACE achieves a favorable quality-efficiency trade-off compared to state-of-the-art single-agent and multi-agent baselines. In the matched LLM-driven reference comparison, HMACE achieves the lowest average gaps on TSP and Online BPP (0.464\% and 0.223\%, respectively), while requiring only 0.13M and 0.42M tokens for the two tasks, substantially fewer than the compared baselines.

---

## 论文详细总结（自动生成）

以下是对论文《HMACE: Heterogeneous Multi-Agent Collaborative Evolution for Combinatorial Optimization》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的基于大语言模型（LLM）的自动化启发式算法设计方法主要依赖于单一、僵化的工作流（单体智能体），这导致了两个瓶颈：一是缺乏系统性的探索机制，容易陷入局部最优和过早收敛；二是复杂的组合优化问题（COP）需要多种认知操作（规划、生成、评估、反思），强行让单一智能体承担所有角色会导致“角色干扰”。
*   **整体含义**：论文将启发式搜索重新定义为一个**组织设计问题**，提出了 HMACE 框架。通过异构多智能体协作，将搜索过程分解为专门的角色，利用行为感知记忆引导探索，旨在提高搜索的效率和解的质量。

### 2. 方法论：核心思想与算法流程
*   **核心思想**：将每一代演化分解为四个专门化角色的协作循环，并引入基于存档（Archive）的记忆机制。
*   **关键技术细节**：
    *   **提议者 (Proposer)**：基于当前种群和从存档中检索到的多样化示例，提出自然语言形式的改进策略。
    *   **生成者 (Generator)**：将策略转化为可执行的 Python 代码，并遵循特定的输入输出契约。
    *   **评估者 (Evaluator)**：在训练集上运行代码，输出两个信号：**适应度得分 (Fitness)**（衡量质量）和**行为向量 (Behavior Vector)**（衡量执行特征）。
    *   **反思者 (Reflector)**：维护一个基于 CVT-MAP-Elites 的存档，根据行为向量将启发式算法分类到不同的“生态位”，并更新最优解，引导后续检索。
    *   **轻量级过滤 (Filtering)**：在昂贵的评估之前，通过语法检查和去重剔除无效候选。
*   **算法流程**：初始化种群 -> 检索多样化示例 -> 提议策略 -> 生成代码 -> 预过滤 -> 经验评估（得分+行为） -> 更新存档与种群 -> 循环直至收敛或预算耗尽。

### 3. 实验设计
*   **数据集/场景**：
    *   **TSP (旅行商问题)**：使用 TSPLIB 实例，规模从 50 到 1000 城市。
    *   **Online BPP (在线装箱问题)**：使用 Weibull-5k 分布。
    *   **MKP (多维背包问题)**：10 个合成实例，规模从 10 到 100 个背包。
    *   **PFSP (置换流水车间调度问题)**：使用 Taillard 基准实例。
*   **对比基准 (Baselines)**：
    *   **经典算法**：Concorde, OR-Tools, Best Fit, NEH 等。
    *   **单智能体 LLM 方法**：FunSearch, EoH。
    *   **搜索控制器改进方法**：ReEvo, HSEvo, MCTS-AHD, MoH。
    *   **多智能体方法**：CORAL, DRAGON。

### 4. 资源与算力
*   **硬件**：服务器配备 NVIDIA L40 GPU 和 Intel Gold 6330 CPU (2.00GHz)。
*   **模型**：主要实验使用 **GPT-5.4**（注：论文中多次提及 GPT-5.4，可能指代特定版本或未来预期版本，或为笔误/特定代号）。
*   **时间预算**：每组实验设置了 1 小时的全局墙钟时间限制。
*   **并行度**：使用 12 个评估器工作进程并行运行。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 种不同类型的经典 COP 问题，每种问题包含多个规模等级。
*   **消融实验**：针对检索机制（随机 vs 行为感知）、过滤机制（无过滤、LLM 评审、代理模型评审）以及单体 vs 多体结构进行了详细对比。
*   **充分性与客观性**：每组实验运行 3 个随机种子并报告均值和标准差；使用了统一的 Token 消耗和运行时间记录，对比维度全面（质量、效率、成本），实验设计较为客观公平。

### 6. 主要结论与发现
*   **质量-效率权衡**：HMACE 在 TSP 和 Online BPP 上达到了最低的平均误差间隙（Gap），同时 Token 消耗极低。
*   **成本优势**：在 TSP 任务中，HMACE 仅需 0.13M Token，而对比方法如 CORAL 需要 0.94M；在 BPP 中，HMACE 的 Token 消耗仅为 CORAL 的 1/13。
*   **收敛性**：HMACE 表现出更快的收敛速度，能够通过行为感知检索有效地跳出局部最优。

### 7. 优点
*   **角色解耦**：通过异构分工消除了单体智能体的认知负担和角色干扰。
*   **行为感知记忆**：引入行为向量而非仅仅依赖适应度，确保了搜索空间的多样性覆盖（Quality-Diversity）。
*   **高性价比**：通过轻量级过滤和精准检索，极大地降低了调用昂贵 LLM API 的成本。

### 8. 不足与局限
*   **工作流固定**：目前的四阶段流水线是预定义的，缺乏根据搜索动态自动调整角色分配或工作流的灵活性。
*   **长期记忆管理**：随着搜索进行，存档数据会累积，如何动态精简记忆以避免上下文碎片化和信息过载仍是挑战。
*   **应用范围**：目前主要集中在经典离散 COP 问题，尚未在连续优化或更复杂的实时动态优化场景中验证。

（完）
