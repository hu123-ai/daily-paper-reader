---
title: Learning and Reusing Policy Decompositions for Hierarchical Generalized Planning with LLM Agents
title_zh: 面向大语言模型智能体的分层泛化规划：学习与重用策略分解
authors: "Shirin Sohrabi, Haritha Ananthakrishnan, Harsha Kokel, Kavitha Srinivas, Michael Katz"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06957v1"
tags: ["query:malm"]
score: 9.0
evidence: LLM智能体的分层泛化规划
tldr: 本研究提出HCL-GP框架，旨在提升LLM智能体在复杂任务中的泛化规划能力。该方法结合了分层任务分解，通过从成功执行中自动提取并参数化可重用的策略组件，构建动态组件库。通过语义搜索实现高效检索，解决了组件学习、泛化与复用的挑战。实验表明，该方法在AppWorld基准测试中表现优异，显著提升了闭源及开源模型在未知应用场景下的任务成功率。
selection_source: fresh_fetch
motivation: 针对LLM智能体在处理复杂任务时难以有效复用已有经验和在未知场景下泛化能力差的问题。
method: 提出HCL-GP方法，通过自动分解任务、学习参数化策略并构建可检索的组件库来实现分层泛化规划。
result: "在AppWorld测试中，该方法在挑战性任务上取得97.8%的准确率，相比静态合成方法在复杂场景下提升了15.8个百分点。"
conclusion: 证明了将经典规划中的分层分解与策略复用思想引入LLM智能体，能显著增强其处理复杂任务的效率与鲁棒性。
---

## 摘要
我们提出了一种动态策略学习方法，该方法结合了面向大语言模型（LLM）智能体的泛化规划与分层任务分解。我们的方法，即面向泛化策略的分层组件学习（HCL-GP），学习可在不同任务实例间泛化的参数化策略，并从成功的执行中自动提取可重用组件，将其组织成组件库以进行组合式策略生成。我们解决了三个挑战：（1）通过自动分解学习组件；（2）泛化组件以最大化重用；（3）通过语义搜索进行高效检索。在 AppWorld 基准测试上的评估结果显示，我们的方法在常规任务上达到了 98.2% 的准确率，在包含未见应用的挑战性任务上达到了 97.8%，在挑战性场景下比静态合成提高了 15.8 个百分点。对于开源模型，动态重用实现了 62.5% 的成功率，而没有重用时成功率接近于零。这表明经典规划概念可以有效地与 LLM 智能体集成，从而提高准确性和效率。

## Abstract
We present a dynamic policy-learning approach that combines generalized planning and hierarchical task decomposition for LLM-based agents. Our method, Hierarchical Component Learning for Generalized Policies (HCL-GP ), learns parameterized policies that generalize across task instances and automatically extracts reusable components from successful executions, organizing them into a component library for compositional policy generation. We address three challenges: (1) learning components through automated decomposition, (2) generalizing components to maximize reuse, and (3) efficient retrieval via semantic search. Evaluated on the AppWorld benchmark, our approach achieves 98.2% accuracy on normal tasks and 97.8% on challenge tasks with unseen applications, improving 15.8 points over static synthesis on challenging scenarios. For open-source models, dynamic reuse enables 62.5% success versus near-zero without reuse. This demonstrates that classical planning concepts can be effectively integrated with LLM agents for improved accuracy and efficiency.

---

## 论文详细总结（自动生成）

这篇论文由 IBM 研究团队提出，探讨了如何通过结合经典规划理论与大语言模型（LLM）智能体，实现复杂任务的知识积累与复用。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：当前的 LLM 智能体在解决实际任务（如 API 调用、代码生成）时，通常将每个任务视为孤立事件。这种方式导致智能体不断重复发现相同的解决方案模式，无法积累可重用的程序性知识。
*   **核心问题**：如何在没有符号化领域模型或预定义任务网络的情况下，让 LLM 智能体动态地学习、泛化并复用执行策略？
*   **整体含义**：论文提出了 **HCL-GP**（分层组件学习泛化策略）框架，旨在通过“泛化规划”和“分层任务分解”的思想，使智能体能够从成功经验中提取可执行的代码组件，构建动态组件库，从而提高处理新任务的效率和准确性。

### 2. 方法论：核心思想与关键技术
HCL-GP 框架包含三个核心阶段，形成一个闭环的动态学习系统：
*   **策略生成（Policy Generation）**：
    *   **任务抽象与参数化**：将自然语言指令转化为高层步骤、策略签名（函数接口）和具体的参数绑定。
    *   **组件检索**：利用语义搜索（基于 BAAI 嵌入模型）从已验证的组件库中检索最相关的代码片段。
    *   **迭代验证与调试**：生成 Python 策略代码并在模拟器中执行，根据反馈（错误信息、执行轨迹）进行自我修复。
*   **组件学习（Learning Components）**：
    *   **自动分解**：当策略成功解决任务后，分解智能体（Decomposition Agent）分析代码，提取出具有内聚逻辑的子任务片段。
    *   **验证提取**：确保提取出的组件在原任务中依然有效。
*   **组件泛化（Generalizing Components）**：
    *   **聚类与去重**：使用语义嵌入对候选组件进行聚类，识别功能相似的片段。
    *   **抽象化**：将特定领域的函数（如 `login_to_venmo`）泛化为通用函数（如 `login_to_app`），并通过验证循环确保泛化后的组件不破坏原有功能。

### 3. 实验设计
*   **基准测试（Benchmark）**：使用 **AppWorld**，这是一个模拟真实应用环境（如 Gmail, Spotify, Amazon）的复杂基准，要求智能体通过 API 调用解决多步任务。
*   **场景设置**：
    *   **Normal Test**：包含与训练集相似的应用。
    *   **Challenge Test**：包含训练集中未见过的应用（如 Amazon, Gmail），测试跨领域迁移能力。
*   **对比方法**：
    *   **GP（Baseline）**：不带组件复用的泛化规划。
    *   **Leaderboard 顶尖方法**：如 AgentRL (Qwen3-14B)、IBM CUGA (GPT-4.1)、LOOP (Qwen2.5-32B) 等。
    *   **不同模型**：Claude Sonnet 4.6（闭源强模型）和 GPT-OSS 120B（开源模型）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件资源（如 GPU 型号或数量）。
*   **模型调用**：实验主要通过 API 调用 Claude Sonnet 4.6 和开源模型（GPT-OSS 120B）。由于是基于推理的框架，其核心开销在于 LLM 的 Token 消耗。
*   **成本分析**：文中详细分析了推理成本（美元）与准确率之间的权衡，证明了 HCL-GP 在相同预算下能获得更高的成功率。

### 5. 实验数量与充分性
*   **实验规模**：在 30 个训练场景上生成初始组件库，并在 Normal 和 Challenge 测试集（共计数百个任务实例）上进行验证。
*   **充分性**：
    *   **消融实验**：对比了有无组件复用（HCL-GP vs GP）的效果。
    *   **跨模型验证**：在闭源和开源模型上均验证了方法的有效性。
    *   **效率分析**：通过“调试迭代次数 vs 准确率”曲线展示了收敛速度。
*   **客观性**：使用了标准化的 AppWorld 评估指标（TGC 和 SGC），并与排行榜上的最新方法进行了直接对比，实验设计较为公平。

### 6. 主要结论与发现
*   **性能卓越**：HCL-GP 在 Claude Sonnet 4.6 上实现了 **98.2%**（常规）和 **97.8%**（挑战）的场景成功率，显著优于所有基准方法。
*   **开源模型救星**：对于开源模型 GPT-OSS 120B，在没有复用时成功率接近 0%，而引入 HCL-GP 后提升至 **62.5%**。
*   **跨领域迁移**：即使在挑战集中遇到了未见过的应用，学习到的通用组件（如身份验证、数据过滤逻辑）依然能显著提升性能（比基线高出 15.8 个百分点）。
*   **效率提升**：动态复用减少了冗余的策略合成和调试，使智能体能以更少的迭代次数和更低的成本解决复杂任务。

### 7. 优点
*   **理论结合**：成功将经典规划（HTN, Generalized Planning）的结构化思维引入到现代 LLM 智能体中。
*   **动态演进**：组件库不是静态预设的，而是通过执行反馈动态生长和优化的，具有很强的自适应性。
*   **模型无关性**：框架不依赖于特定的 API 或符号模型，具有较好的通用性。

### 8. 不足与局限
*   **元域依赖**：该方法假设存在一个定义良好的“元域”（Meta-domain），即固定的操作空间和验证机制，在完全开放、无反馈的环境中可能难以实施。
*   **可扩展性挑战**：随着组件库规模增大，语义检索的精度和 LLM 处理长上下文的能力（在泛化阶段）可能成为瓶颈。
*   **场景划分**：实验依赖于 AppWorld 提供的场景划分（Scenario），在现实世界中，如何自动识别哪些任务属于同一“领域”仍是一个难题。

（完）
