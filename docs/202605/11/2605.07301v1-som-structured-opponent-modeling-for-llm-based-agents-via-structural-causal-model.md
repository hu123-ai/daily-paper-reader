---
title: "SOM: Structured Opponent Modeling for LLM-based Agents via Structural Causal Model"
title_zh: SOM：基于结构因果模型的 LLM 智能体结构化对手建模
authors: "Shiyue Cao, Pei Xu, Likun Yang, Lei Cui, Xiaotang Chen, Kaiqi Huang"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07301v1"
tags: ["query:malm"]
score: 9.0
evidence: 多智能体环境中大语言模型智能体的对手建模
tldr: 针对大语言模型智能体在多智能体环境中难以准确预测对手行为的问题，本文提出结构化对手建模（SOM）框架。该框架利用结构因果模型（SCM）将对手建模与预测分离，通过构建观察与行动间的有向依赖图，引导模型进行结构化推理。实验证明，SOM在多个基准测试中显著提升了预测准确性和决策稳定性，增强了智能体在动态交互中的适应能力。
selection_source: fresh_fetch
motivation: 现有方法将对手建模与预测混为一谈，依赖隐式上下文推理，导致在动态交互中缺乏适应性。
method: 提出两阶段框架，先利用结构因果模型构建显式的对手表示，再引导大模型沿因果路径进行结构化推理预测。
result: 在多种多智能体基准测试中，SOM的表现一致优于现有的先进大模型推理基准。
conclusion: 显式的结构化建模能有效提升大模型智能体在复杂博弈环境中的对手预测能力和战略决策水平。
---

## 摘要
在多智能体和博弈论环境中，从交互中准确预测对手行为是基于大语言模型（LLM）的智能体的一项基本能力。现有方法通常将对手建模与预测纠缠在一起，依赖隐式的上下文推理，限制了在动态交互中的适应性。为此，我们提出了结构化对手建模（SOM），这是一个将对手模型构建与对手预测明确分离的两阶段对手建模框架。在构建阶段，SOM 采用结构因果模型（SCM）——一种用于表示变量间依赖关系的基于图的形式化方法——来捕捉对手观测与行动之间的有向联系，从而产生显式且结构化的对手表示。在预测阶段，LLM 沿着源自 SCM 的清晰路径进行结构化推理，提高了预测的准确性和稳定性。在多种多智能体基准测试上的广泛实验表明，SOM 一致优于最先进的基于 LLM 的推理基线，能够在复杂且动态的多智能体交互中实现更准确、更具适应性的策略决策。

## Abstract
Accurately predicting opponents' behavior from interactions is a fundamental capability for large language model (LLM)-based agents in multi-agent and game-theoretic environments. Existing approaches often entangle opponent modeling with prediction, relying on implicit contextual reasoning and limiting adaptability in dynamic interactions. To this end, we propose Structured Opponent Modeling (SOM), a two-stage opponent modeling framework that distinctly separates opponent model construction and opponent prediction. At the construction stage, SOM employs a Structural Causal Model (SCM), a graph-based formalism for representing dependencies among variables, to capture directed links between opponents' observations and actions, yielding an explicit and structured opponent representation. At the prediction stage, the LLM performs structured reasoning along clear pathways derived from the SCM, improving both prediction accuracy and stability. Extensive experiments on diverse multi-agent benchmarks demonstrate that SOM consistently outperforms state-of-the-art LLM-based reasoning baselines, enabling more accurate and adaptable strategic decision-making in complex and dynamic multi-agent interactions.

---

## 论文详细总结（自动生成）

### 论文总结：SOM：基于结构因果模型的 LLM 智能体结构化对手建模

#### 1. 核心问题与整体含义
在多智能体博弈环境中，基于大语言模型（LLM）的智能体需要准确预测对手行为以制定最优策略。然而，现有方法（如 CoT、ToT 等）通常将“对手建模”（理解对手决策逻辑）与“行为预测”混为一谈，依赖于 LLM 的隐式上下文推理。这种方式缺乏可控的推理路径，容易导致模型忽略关键信息、产生幻觉，且在面对动态变化的对手策略时适应性较差。本文提出的 **SOM (Structured Opponent Modeling)** 旨在通过**结构因果模型（SCM）**将建模与预测解耦，为智能体提供显式、可解释且可进化的对手表示。

#### 2. 方法论
SOM 采用两阶段框架，将对手建模转化为结构化推理过程：

*   **阶段一：动态 SCM 构建与精炼（建模）**
    *   **初始化：** 构建一个包含观测变量和对手动作的最小有向无环图（DAG）。
    *   **反思与提取：** 在交互后，LLM 对对手行为进行反思，假设导致该行为的中间推理变量（如对手的意图、信念）。
    *   **图更新与剪枝：** 将提取的中间节点加入 SCM。通过维护节点强化计数，保留高频、可靠的因果路径，剪掉低频或被证伪的假设，确保图结构的简洁与准确。
*   **阶段二：基于推理的预测与适配（预测）**
    *   **拓扑推理：** 按照 SCM 的拓扑顺序，逐节点推导变量值。
    *   **示例引导：** 为每个因果链路维护一个“对手特定示例池”。在推理时，检索相似的成功推理历史作为 Few-shot 示例，模拟 SCM 中的结构方程。
    *   **个性化适配：** 针对不同对手维护独立的示例池，实现对特定对手行为模式的快速捕捉。

#### 3. 实验设计
*   **实验场景：**
    1.  **G0.8A (猜数字游戏变体)：** 预测群体行为的均值，考察战略前瞻性。
    2.  **SAG (生存拍卖游戏)：** 资源竞标与生存博弈，考察动态预算管理与对手心理建模。
    3.  **Undercover (谁是卧底)：** 社会推断游戏，考察语言层面的欺骗检测与角色推理。
*   **对比基线：**
    *   基础 LLM (LLM-only)
    *   思维链 (CoT)、思维树 (ToT)
    *   K-Level 推理 (K-R)：递归模拟对手信念。
    *   Reflexion：基于语言反馈的自我强化。
    *   **Mixed Opponent：** 混合多种策略的对手，模拟高度不确定的真实环境。

#### 4. 资源与算力
*   **模型：** 主要实验基于 **GPT-4o**。在知识迁移实验中使用了 **LLaMA-3-8B** 和 **Mixtral-8B**。
*   **算力说明：** 论文未明确提及具体的 GPU 型号、数量或训练时长。由于该方法主要基于 Prompting 和上下文学习（In-context Learning），不涉及大规模参数微调，其核心开销在于 **Token 消耗**。
*   **Token 分析：** 论文附录详细对比了 Token 消耗，指出 SOM 在动态构建阶段开销较高，但在结构稳定后的“固定 SCM 模式”下，Token 消耗与 K-R 或 Reflexion 相当。

#### 5. 实验数量与充分性
*   **实验规模：** 在三个迥异的游戏环境中进行了测试，涵盖了数值博弈、资源博弈和语言博弈。
*   **消融实验：** 详细评估了静态图、中间节点提取、图精炼和推理示例池等组件的贡献，证明了每个模块的必要性。
*   **充分性评价：** 实验设计较为全面。特别是引入了“混合对手”设置和“跨模型知识迁移”实验，验证了该方法在复杂环境下的鲁棒性以及结构化知识的通用性，实验结果具有较高的客观性和说服力。

#### 6. 主要结论与发现
*   **性能领先：** SOM 在所有测试环境中均显著优于 CoT、ToT 和 K-R 等基线，尤其在面对混合策略对手时表现出极强的适应性。
*   **学习进化：** 随着交互轮数的增加，SOM 的预测偏差持续下降，胜率稳步上升，证明了其动态精炼机制的有效性。
*   **知识可迁移：** 由 GPT-4o 构建的 SCM 结构和示例池可以直接提升 LLaMA-3 等较弱模型的表现，证明了结构化对手模型具有超越单一模型的知识价值。

#### 7. 优点
*   **显式建模：** 将隐式的黑盒推理转变为显式的因果图路径，极大地增强了智能体决策的可解释性。
*   **解耦设计：** 建模与预测的分离使得智能体能够有针对性地更新对对手的认知，而不受当前任务推理的干扰。
*   **即插即用与迁移性：** 结构化知识可以跨回合、跨模型复用，为构建通用的对手建模库提供了可能。

#### 8. 不足与局限
*   **因果真实性：** 论文承认 SOM 发现的“因果”结构本质上是从观测数据中推断的功能依赖，而非严格意义上对手认知的真实因果机制。
*   **计算成本：** 在初始构建阶段，频繁的 LLM 反思和图更新会导致较高的 Token 成本和推理延迟。
*   **模型依赖：** 尽管具有迁移性，但 SCM 的构建质量高度依赖于底层 LLM（如 GPT-4o）的反思和提取能力，弱模型可能难以自主构建高质量的图结构。

（完）
