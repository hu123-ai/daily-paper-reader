---
title: Active Learning for Communication Structure Optimization in LLM-Based Multi-Agent Systems
title_zh: 基于大语言模型的多智能体系统通信结构优化的主动学习
authors: "Huchen Yang, Xinghao Dong, Dan Negrut, Jin-Long Wu"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.05703v2"
tags: ["query:malm"]
score: 9.0
evidence: 大语言模型多智能体系统中的通信结构优化
tldr: 针对大语言模型多智能体系统（LLM-MAS）通信结构优化中随机采样任务效率低、不稳定的问题，本文提出一种基于主动学习的任务选择框架。该框架利用集成卡尔曼反演估计任务的信息量，通过识别最具价值的任务来优化通信拓扑。实验证明，该方法在有限预算下能显著提升系统性能并增强鲁棒性。
selection_source: fresh_fetch
motivation: 现有的通信结构优化方法依赖随机采样，忽略了不同任务在难度和领域上的差异，导致在有限预算下优化效果不稳定。
method: 提出一种基于集成卡尔曼反演的信息论任务选择框架，结合嵌入表示选择和批量汤普森采样，主动识别对优化通信结构最有价值的任务。
result: 在良性环境和存在智能体攻击的场景下，该方法均能以更低的计算成本实现更优的通信结构优化。
conclusion: 主动学习能有效提升LLM多智能体系统通信结构的优化效率和稳定性，是解决黑盒复杂系统优化问题的有效途径。
---

## 摘要
优化基于大语言模型的多智能体系统（LLM-MAS）的通信结构已被证明可以提高下游性能并减少 Token 使用量。现有方法通常依赖于随机采样的训练任务。然而，任务在难度和领域上可能存在显著差异，因此它们在更新通信结构方面提供的信息量并不相等，这使得在有限的训练预算下，优化过程往往不稳定且对特定训练集高度敏感。为了主动识别对通信结构优化最有价值的任务，我们提出了一个基于集成的、信息论驱动的任务选择框架。该方法通过候选任务对图参数分布的改变程度来估计任务的信息量，并使用集成卡尔曼反演（Ensemble Kalman Inversion）作为相应贝叶斯更新的一种高效且无导数的近似。由此产生的估计器特别适用于黑盒且含有噪声的多智能体系统。为了增强可扩展性，我们通过基于嵌入的代表性选择构建了一个紧凑的候选池，并将信息量选择与代理模型及批量汤普森采样相结合。我们在良性设置和存在智能体攻击的设置下验证了我们的方法，证明了其在受限计算预算下进行通信结构优化的有效性。

## Abstract
Optimizing the communication structure of large language model based multi-agent systems (LLM-MAS) has been shown to improve downstream performance and reduce token usage. Existing methods typically rely on randomly sampled training tasks. However, tasks may differ substantially in difficulty and domain, and thus they are not equally informative for updating communication structure, making optimization under limited training budgets often unstable and highly sensitive to the particular training set. To actively identify the most valuable tasks for communication-structure optimization, we propose an ensemble-based information-theoretic task selection framework. The proposed method estimates task informativeness by how much a candidate task changes the distribution over graph parameters, using ensemble Kalman inversion as an efficient and derivative-free approximation of the corresponding Bayesian update. The resulting estimator is especially suitable for black-box and noisy multi-agent systems. To enhance scalability, we construct a compact candidate pool through embedding-based representative selection and combine the informative selection with surrogate modeling and batch Thompson sampling. We validate our method in both benign settings and settings with agent attacks, demonstrating its effectiveness for communication-structure optimization under constrained computational budgets.

---

## 论文详细总结（自动生成）

### 论文结构化深度总结：基于主动学习的 LLM 多智能体系统通信结构优化

#### 1. 核心问题与研究动机
*   **核心问题**：如何高效且稳定地优化大语言模型多智能体系统（LLM-MAS）的通信拓扑结构？
*   **研究动机**：
    *   **现有缺陷**：目前的通信结构优化方法（如剪枝、拓扑搜索）通常依赖于从任务池中随机采样训练任务。然而，不同任务在难度和领域上差异巨大，对优化通信结构的贡献并不均等。
    *   **不稳定性**：在有限的训练预算下，随机采样会导致优化结果高度不稳定，下游任务表现波动大。
    *   **黑盒挑战**：LLM-MAS 是随机且不可导的黑盒系统，直接计算任务对参数更新的“信息增益”在计算上极其昂贵。

#### 2. 提出的方法论
该论文提出了一种基于**主动学习（Active Learning）**的任务选择框架，旨在识别对通信结构参数更新最具信息量的任务。
*   **核心思想**：利用信息论准则，选择那些能使通信图参数（Logits）的后验分布产生最大改变的任务。
*   **关键技术细节**：
    *   **两阶段选择策略**：
        1.  **代表性选择**：利用语义嵌入（Embedding）和贪婪最远点采样，从原始大任务池中筛选出具有多样性的候选子集。
        2.  **信息量选择**：从子集中挑选能提供最大信息增益的任务。
    *   **集成卡尔曼反演（EKI）**：这是本文的核心数学工具。它将贝叶斯更新近似为一种无导数（Derivative-free）的集成演化过程，通过一组“粒子”模拟图参数的分布，避免了在黑盒 LLM 系统中计算复杂的端到端梯度。
    *   **代理模型与汤普森采样**：为了进一步提升效率，引入高斯过程（GP）作为代理模型来预测任务的信息增益，并结合汤普森采样进行序列化任务获取，减少了昂贵的 MAS 实际运行次数。

#### 3. 实验设计
*   **数据集**：
    *   **MMLU**：涵盖多学科的综合推理任务。
    *   **GSM8K**：数学应用题推理任务。
*   **实验场景**：
    *   **良性设置（Benign）**：所有智能体均正常协作。
    *   **对抗设置（Adversarial/Attack）**：系统中存在恶意智能体，故意提供错误推理和误导信息。
*   **对比方法（Baselines）**：
    *   **无训练（No Train）**：使用全连接或预定义结构。
    *   **随机训练（Random）**：随机选择任务进行结构优化。
    *   **其他主动学习准则**：如期望梯度长度（EGL）、期望模型改变（EMC）、基于 Fisher 信息的 Coreset 方法。

#### 4. 资源与算力
*   **智能体模型**：实验使用了 **GPT-4.1-nano** 作为基础智能体模型。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **成本度量**：论文主要以 **Token 使用量**作为衡量计算预算的核心指标，并展示了在相同 Token 预算下，主动学习比随机训练能获得更高的精度。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   针对每个设置进行了多轮独立运行（每组实验约 10-23 次独立运行），并提供了自助法（Bootstrap）置信区间。
    *   进行了**消融实验**，验证了“代表性选择”和“信息量选择”各自的贡献。
    *   进行了**敏感性分析**，探讨了集成规模（Ensemble Size）和 EKI 迭代步数对结果的影响。
*   **评价**：实验设计较为充分且客观。通过引入对抗攻击场景，增强了方法在极端情况下的说服力。对比实验涵盖了主流的主动学习基准，公平性较好。

#### 6. 主要结论与发现
*   **稳定性提升**：主动学习显著降低了结构优化结果的方差，尤其是在“最差 25%”的任务表现上，提升幅度远超平均值，证明了其强大的鲁棒性。
*   **精度增益**：在对抗攻击场景下，该方法在 MMLU 上比随机训练平均提升了 **1.45%** 的精度，在 GSM8K 上提升了 **0.93%**。
*   **防御能力**：优化后的通信结构能有效识别并阻断恶意智能体的信息传播路径（如从恶意智能体到正常智能体的边被剪除）。

#### 7. 优点与亮点
*   **无导数优化**：巧妙地将 EKI 引入 LLM-MAS 领域，解决了 LLM 系统不可导导致的优化难题。
*   **双重鲁棒性**：既提升了优化过程本身的数值稳定性，也增强了多智能体系统抵御对抗攻击的能力。
*   **效率优化**：通过代理模型和重用策略（Reuse Strategy），大幅降低了评估任务信息增益时的 Token 开销。

#### 8. 不足与局限
*   **计算开销**：虽然比全量评估高效，但 EKI 仍需维护多个集成粒子，在初始阶段的 Token 消耗仍高于简单的随机采样。
*   **假设限制**：目前的 EKI 近似基于高斯假设，对于高度非线性的复杂后验分布，其近似精度可能受限。
*   **场景局限**：实验中恶意智能体的身份是预先固定的，未探讨动态变化的攻击者身份或更复杂的共谋攻击模式。
*   **模型依赖**：主要在 GPT-4 系列模型上验证，在开源小模型上的泛化能力有待进一步观察。

（完）
