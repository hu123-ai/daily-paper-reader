---
title: "Towards Security-Auditable LLM Agents: A Unified Graph Representation"
title_zh: 迈向可安全审计的大语言模型智能体：一种统一的图表示
authors: "Chaofan Li, Lyuye Zhang, Jintao Zhai, Siyue Feng, Xichun Yang, Huahao Wang, Shihan Dou, Yu Ji, Yutao Hu, Yueming Wu, Yang Liu, Deqing Zou"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06812v1"
tags: ["query:malm"]
score: 9.0
evidence: 多智能体协作审计的统一图表示
tldr: 针对LLM智能体在执行复杂任务时存在的语义鸿沟和审计困难问题，本文提出了Agent-BOM，一种统一的图表示结构。该方法通过分层属性有向图将静态能力与动态运行时状态解耦，并利用图查询实现路径级风险评估。实验证明，Agent-BOM能有效重构包括内存投毒和工具滥用在内的多种复杂攻击链，为智能体生态系统的安全审计提供了坚实基础。
selection_source: fresh_fetch
motivation: 现有的审计机制如SBOM和运行时日志过于碎片化，无法捕捉LLM智能体在执行过程中的认知演变和复杂的风险传播路径。
method: 提出Agent-BOM模型，通过分层属性有向图将静态能力基座与动态语义状态关联，并基于图查询实现针对OWASP Top 10风险的审计。
result: 在真实攻击场景下的评估表明，Agent-BOM能有效重构跨会话内存投毒、供应链劫持和多智能体生态滥用等隐蔽攻击链。
conclusion: Agent-BOM为复杂LLM智能体系统的根因分析和安全裁决提供了一个统一且具备可审计性的基础框架。
---

## 摘要
基于大语言模型（LLM）的智能体系统正迅速演进，通过动态工具调用、有状态的记忆管理以及多智能体协作来执行复杂的自主任务。然而，这种语义驱动的执行范式在底层物理事件与高层执行意图之间造成了严重的语义鸿沟，使得事后安全审计变得极其困难。现有的表示机制（包括静态软件物料清单 SBOM 和运行时日志）仅能提供碎片化的证据，无法捕捉认知状态演变、能力绑定、持久性记忆污染以及交互智能体之间的级联风险传播。为了弥补这一鸿沟，我们提出了 Agent-BOM，一种用于智能体安全审计的统一结构化表示。Agent-BOM 将智能体系统建模为分层属性有向图，将静态能力基础（如模型、工具和长期记忆）与动态运行时语义状态（如目标、推理轨迹和动作）分离开来。这些层通过语义边和安全属性相连，将碎片化的执行轨迹转化为可查询的审计路径。基于 Agent-BOM，我们开发了一种基于图查询的路径级风险评估范式，并结合 OWASP Agentic Top 10 进行了实例化。我们进一步在 OpenClaw 环境中实现了一个审计插件，用于从实时执行中构建 Agent-BOM。对代表性真实世界智能体攻击场景的评估表明，Agent-BOM 能够重建隐蔽的攻击链，包括跨会话记忆投毒与工具滥用、能力供应链劫持与非预期代码执行、多智能体生态系统劫持以及权限与信任滥用。这些结果证明，Agent-BOM 为复杂智能体生态系统中的根因分析和安全裁定提供了统一且可审计的基础。

## Abstract
LLM-based agentic systems are rapidly evolving to perform complex autonomous tasks through dynamic tool invocation, stateful memory management, and multi-agent collaboration. However, this semantics-driven execution paradigm creates a severe semantic gap between low-level physical events and high-level execution intent, making post-hoc security auditing fundamentally difficult. Existing representation mechanisms, including static SBOMs and runtime logs, provide only fragmented evidence and fail to capture cognitive-state evolution, capability bindings, persistent memory contamination, and cascading risk propagation across interacting agents. To bridge this gap, we propose Agent-BOM, a unified structural representation for agent security auditing. Agent-BOM models an agentic system as a hierarchical attributed directed graph that separates static capability bases, such as models, tools, and long-term memory, from dynamic runtime semantic states, such as goals, reasoning trajectories, and actions. These layers are connected through semantic edges and security attributes, transforming fragmented execution traces into queryable audit paths. Building on Agent-BOM, we develop a graph-query-based paradigm for path-level risk assessment and instantiate it with the OWASP Agentic Top 10. We further implement an auditing plugin in the OpenClaw environment to construct Agent-BOM from live executions. Evaluation on representative real-world agentic attack scenarios shows that Agent-BOM can reconstruct stealthy attack chains, including cross-session memory poisoning and tool misuse, capability supply-chain hijacking and unexpected code execution, multi-agent ecosystem hijacking, and privilege and trust abuse. These results demonstrate that Agent-BOM provides a unified and auditable foundation for root-cause analysis and security adjudication in complex agentic ecosystems.

---

## 论文详细总结（自动生成）

### 论文总结：迈向可安全审计的大语言模型智能体：一种统一的图表示

#### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）智能体从简单的文本生成器演变为具备自主决策、工具调用、长期记忆和多智能体协作能力的系统，其安全边界发生了根本性变化。
*   **语义鸿沟**：传统的安全审计依赖于底层物理事件（如系统日志、API调用），但智能体行为是由动态语义状态（目标、推理、决策）驱动的。同一物理动作（如删除文件）在不同语义背景下可能合法也可能属于攻击。
*   **现有机制局限**：现有的软件物料清单（SBOM）是静态的，而运行时日志又过于碎片化，无法捕捉认知状态的演变、跨层能力绑定、持久性记忆污染或多智能体间的风险传播。
*   **研究目标**：建立一种统一的结构化表示，将碎片化的执行轨迹转化为可查询、可解释、可裁定的审计路径。

#### 2. 方法论：核心思想与关键技术
论文提出了 **Agent-BOM**（智能体物料清单），将其建模为一个**分层属性有向图** $B_S = (A, V, E, \alpha)$：
*   **分层节点架构 ($V$)**：
    *   **静态能力层**：锚定模型（LLMNode）、工具（ToolNode）、提示词（PromptNode）和长期记忆（LTMNode）等基础对象。
    *   **运行时语义层**：捕捉瞬时状态，包括目标（GoalNode）、上下文（ContextNode）、推理（ReasoningNode）、决策（DecisionNode）和动作（ActionNode）。
*   **语义边 ($E$)**：连接节点以表示时间演化、跨层能力绑定（如决策选择工具）以及跨智能体传播（消息传递、任务委派）。
*   **安全属性 ($\alpha$)**：在节点和边上附加元数据，如来源信任度、完整性状态、权限范围和用户确认状态。
*   **审计范式**：定义了一个 4 元组审计规则 $R = \langle v_{entry}, P_{back}, P_{fwd}, C \rangle$，包括：
    1.  **入口定位**：识别异常语义或行为的起始点。
    2.  **回溯追踪**：寻找风险源头（如投毒的输入）。
    3.  **前向追踪**：评估风险对后续决策和环境的影响范围。
    4.  **属性裁定**：结合拓扑和安全属性进行最终风险判定。

#### 3. 实验设计
*   **实验环境**：在 **OpenClaw**（一个开源智能体运行环境）中实现审计插件。
*   **测试场景**：基于 **OWASP Agentic Top 10** 风险分类，构建了四个复杂的复合攻击场景：
    1.  **跨会话记忆投毒与工具滥用**：攻击者诱导智能体存储恶意指令，在后续会话中触发。
    2.  **供应链劫持与非预期代码执行**：通过恶意第三方插件（Skill）窃取数据。
    3.  **多智能体生态系统劫持**：篡改智能体间通信，导致目标漂移和级联失效。
    4.  **权限与信任滥用**：利用伪造的系统提示词绕过用户确认执行高危操作。
*   **Benchmark**：直接采用 OWASP 发布的 2026 年智能体十大安全风险作为评估标准。

#### 4. 资源与算力
*   **硬件/系统**：实验运行在 Ubuntu 22.04.5 LTS 系统上。
*   **模型**：使用了 **GPT-5** 作为基础大语言模型。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量或训练时长。由于该研究侧重于审计框架的构建和推理路径的提取，而非模型训练，其算力消耗主要集中在智能体运行和图查询处理上。

#### 5. 实验数量与充分性
*   **实验规模**：论文通过 4 个深度案例研究覆盖了 OWASP 定义的全部 10 种高危风险。
*   **充分性评价**：实验设计较为**充分且具有代表性**。它不仅验证了单点风险，还通过复合场景展示了风险在时间（跨会话）和空间（跨智能体）上的传播。
*   **客观性**：实验在真实的智能体框架（OpenClaw）中运行，并使用了业界公认的风险分类法，具有较高的客观性。但由于是案例驱动，缺乏大规模自动化测试的统计数据。

#### 6. 主要结论与发现
*   **有效性**：Agent-BOM 能够成功重构隐蔽的攻击链，将孤立的日志转化为连贯的因果路径。
*   **根因分析**：通过回溯追踪，审计员可以清晰地识别出风险是源于恶意输入、受损的插件还是模型自身的推理偏差。
*   **统一性**：该框架证明了异构的智能体风险（从提示词注入到权限提升）可以在同一个图拓扑结构下进行统一建模和查询。

#### 7. 优点
*   **填补空白**：首次提出了专门针对智能体语义执行特征的 BOM 式表示模型。
*   **解耦设计**：将静态能力与动态状态分离，既能追踪供应链风险，也能监控运行时偏差。
*   **可扩展性**：审计规则模板化，能够灵活适应未来新出现的智能体攻击类型。
*   **跨智能体支持**：原生支持多智能体协作场景下的风险传播追踪。

#### 8. 不足与局限
*   **依赖插桩质量**：图的完整性高度依赖于底层框架（如 OpenClaw）暴露运行数据的粒度，若数据缺失则会导致路径断裂。
*   **存储与计算开销**：对于长期运行或极其复杂的智能体系统，维护全量语义图可能会带来显著的存储压力。
*   **自动化程度**：虽然提供了查询范式，但目前在复杂语义冲突的自动判定上仍可能需要人工介入或更高层的策略定义。
*   **应用限制**：主要针对应用层逻辑审计，对于底层模型权重被篡改等物理层攻击覆盖有限。

（完）
