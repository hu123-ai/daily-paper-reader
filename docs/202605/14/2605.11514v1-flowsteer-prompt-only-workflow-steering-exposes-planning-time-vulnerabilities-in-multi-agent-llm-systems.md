---
title: "FlowSteer: Prompt-Only Workflow Steering Exposes Planning-Time Vulnerabilities in Multi-Agent LLM Systems"
title_zh: FlowSteer：仅通过提示词的工作流引导揭示了多智能体大语言模型系统在规划阶段的漏洞
authors: "Fanxiao Li, Jiaying Wu, Tingchao Fu, Natasha Jaques, Wei Zhou, Min-Yen Kan"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11514v1"
tags: ["query:malm"]
score: 9.0
evidence: 多智能体大语言模型系统中的规划-执行架构
tldr: 本研究揭示了多智能体系统（MAS）在规划阶段的安全漏洞，即攻击者可通过提示词操纵工作流的形成。作者提出了FlowSteer攻击方法，利用工作流位置的影响力和谄媚性框架来放大恶意信号的传播。实验证明该攻击能显著提升恶意任务成功率，且具有跨系统迁移性。为此，研究还提出了FlowGuard防御机制，强调了MAS协作逻辑层面的安全性。
selection_source: fresh_fetch
motivation: 多智能体系统灵活的规划器-执行器架构使得攻击者可以通过提示词操纵任务分解和路由，从而在工作流形成阶段引入新的攻击面。
method: 提出了FlowSteer攻击，通过将恶意信号与高影响力任务组件对齐，并引导规划器生成有利于信号传播的依赖关系和拓扑结构。
result: "FlowSteer在实验中将攻击成功率提升了高达55%，且在黑盒环境下依然有效，而配套的FlowGuard防御机制能有效降低攻击成功率。"
conclusion: 工作流形成过程是多智能体系统安全的新前沿，研究强调了在规划阶段针对智能体协作逻辑进行安全防御的必要性。
---

## 摘要
由大语言模型（LLM）驱动的多智能体系统（MAS）越来越多地采用“规划器-执行器”架构，其中规划器将提示词转换为子任务、角色、依赖关系和路由路径。这种灵活性实现了自适应协作，但也暴露了工作流形成过程中的攻击面：提示词可以在不修改 MAS 基础设施的情况下塑造智能体的组织形式。我们通过社会影响探测工作流来研究这一风险，以识别高影响力的子任务和恶意信号的传播。分析揭示了两个漏洞：工作流中的位置可以放大或抑制恶意信号，而谄媚式的框架使下游智能体更有可能转发该信号。我们将这些发现转化为 FlowSteer，这是一种仅通过提示词的工作流引导攻击，它将漏洞先验知识转化为一个精心设计的提示词。FlowSteer 将恶意信号与具有影响力的任务组件对齐，并引导重新规划转向能够维持传播的依赖关系。实验表明，与朴素提示词相比，FlowSteer 将恶意攻击的成功率提高了多达 55%，可在不同的 MAS 设置中迁移，并且在黑盒拓扑推理下依然有效。由于 FlowSteer 偏置了生成工作流的规划信号，仅检查生成工作流的 MAS 防御措施提供的保护有限。因此，我们引入了 FlowGuard，这是一种输入侧防御，在保持提示词效用的同时，将恶意攻击成功率降低了多达 34%。我们的研究结果将工作流形成定位为多智能体 LLM 系统的一个新安全前沿，从规划阶段的安全视角探讨了智能体协作本身如何被攻击和防御。

## Abstract
Multi-agent systems (MAS) powered by large language models (LLMs) increasingly adopt planner--executor architectures, where planners convert prompts into subtasks, roles, dependencies, and routing paths. This flexibility enables adaptive coordination, but exposes an attack surface in workflow formation: prompts can shape agent organization without modifying MAS infrastructure. We study this risk through social influence probing workflows to identify high-impact subtasks and malicious-signal propagation. The analysis reveals two vulnerabilities: workflow position can amplify or suppress a malicious signal, and sycophantic framing makes downstream agents more likely to relay it. We translate these findings into FlowSteer, a prompt-only workflow steering attack that converts vulnerability priors into one crafted prompt. FlowSteer aligns a malicious signal with influential task components and guides replanning toward dependencies that preserve propagation. Experiments show that FlowSteer increases malicious success by up to 55% over naive prompting, transfers across MAS setups, and remains effective with black-box topology inference. As FlowSteer biases the planning signals that generate the workflow, MAS defenses that inspect only the generated workflow provide limited protection. As such, we introduce FlowGuard, an input-side defense that reduces malicious success by up to 34% while preserving prompt utility. Our results position workflow formation as a new safety frontier for multi-agent LLM systems, opening a planning-time security perspective on how agent coordination itself can be attacked and defended.

---

## 论文详细总结（自动生成）

这篇论文对多智能体大语言模型（MAS）系统的安全性进行了深入探讨，以下是详细的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着 LLM 智能体向多智能体协作系统演进，**“规划器-执行器”（Planner-Executor）**架构成为主流。在这种架构中，规划器根据用户提示词动态生成子任务、分配角色并构建通信拓扑。
*   **研究动机**：现有的 MAS 安全研究多集中在执行阶段（如劫持执行智能体、毒化内存或工具调用）。本文提出了一个新的攻击面——**工作流形成阶段（Workflow Formation）**。
*   **核心问题**：攻击者能否仅通过提示词（Prompt-Only），在不干预系统内部组件的情况下，操纵规划器的协作逻辑，从而引导整个系统产生恶意输出？

### 2. 方法论：核心思想与关键技术
论文提出了攻击框架 **FlowSteer** 和防御框架 **FlowGuard**。

#### **FlowSteer 攻击方法**
其核心思想是利用“社会影响”诊断工作流漏洞，并通过提示词进行结构化引导：
1.  **漏洞探测（Vulnerability Probing）**：通过离线分析，识别工作流中哪些子任务节点对最终结果影响最大（高影响力节点），以及哪些通信路径最有利于恶意信号传播。
2.  **任务感知型谄媚论据（Task-Aware Sycophantic Argument）**：将恶意信号伪装成与高影响力子任务高度相关的专业论据，并使用“谄媚式”框架（如权威语气、合规性暗示）诱导下游智能体采纳。
3.  **依赖引导的工作流转向（Dependency-Guided Steering）**：在提示词中加入自然语言形式的“结构建议”，诱导规划器在重新规划时保留有利于攻击的路径，抑制可能纠偏的路径。

#### **FlowGuard 防御方法**
这是一种输入侧防御机制，旨在规划开始前拦截攻击：
1.  **意图分选（Intent Triage）**：将输入提示词分解为任务意图、方法论意图和框架意图。
2.  **意图去污染（Intent Decontamination）**：重写提示词，中和强制性的结构指令和断言式的恶意声明，同时保留原始任务目标。

### 3. 实验设计
*   **数据集/场景**：
    *   **MisinfoTask**：包含 108 个受限分析任务，带有特定的恶意目标和参考答案。
    *   **ASB-Bench**：基于 Agent Security Bench 构建，包含 100 个涵盖金融、法律等 10 个领域的开放式专业任务。
*   **模型家族**：测试了 GPT-4o、Gemini-2.5、Qwen-3.5 和 DeepSeek-V3 四个模型家族。
*   **对比方法**：
    *   **攻击对比**：朴素恶意提示（NM）、直接目标注入（DGI）。
    *   **防御对比**：ARGUS（基于图的纠偏）、G-Safeguard（拓扑引导的防御）。
*   **配置方案**：设置了 G1（规划器与执行器同级）、G2（强化规划器）、G3（强化执行器）三种能力配置。

### 4. 资源与算力
*   论文**未明确说明**具体的 GPU 训练算力（如型号或数量），因为该研究主要基于商业 LLM 的 API 调用。
*   **成本分析**：文中提供了 API 调用成本分析，指出 FlowSteer 的攻击成本极低（10 个样本约 0.10 美元），FlowGuard 防御成本约为 0.13 美元，证明了攻击的实用性和防御的轻量化。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 种主流模型家族、2 个基准数据集、3 种智能体能力配置。
*   **消融实验**：详细分析了结构先验、任务感知论据、谄媚框架等组件对攻击成功率的贡献。
*   **充分性与客观性**：实验设计考虑了**黑盒环境**（攻击者不知道内部拓扑，仅靠推理），并验证了攻击在多轮对话中的**鲁棒性**。使用了“LLM-as-a-Judge”并配合人工校验（一致性达 95% 以上），确保了评估的客观性。

### 6. 主要结论与发现
*   **工作流位置决定影响力**：同一恶意信号进入工作流的位置不同，其最终破坏力有显著差异。
*   **攻击效能显著**：FlowSteer 相比朴素提示词，将恶意攻击成功率（MASR）提升了高达 **55%**。
*   **跨模型迁移性**：在 GPT-4o 上发现的工作流漏洞先验，可以有效迁移至 Gemini 或 DeepSeek 系统。
*   **现有防御失效**：仅在工作流生成后进行检查的防御措施（如 ARGUS）难以应对 FlowSteer，因为规划阶段的逻辑已被偏置。
*   **输入侧防御有效**：FlowGuard 能在不损害任务效用的前提下，将攻击成功率降低约 **34%**。

### 7. 优点
*   **视角新颖**：首次将 MAS 的安全边界定义在“规划阶段”和“工作流形成”过程。
*   **攻击隐蔽性强**：FlowSteer 不需要劫持任何内部组件，仅通过普通用户接口即可实现系统级控制。
*   **诊断工具化**：引入“社会影响”作为工作流安全性的诊断指标，具有理论参考价值。

### 8. 不足与局限
*   **依赖离线剖析**：虽然证明了黑盒推理的可行性，但最强的攻击效果仍依赖于对工作流结构的离线先验获取。
*   **架构局限性**：研究主要针对“规划器-执行器”架构，对于完全去中心化或静态拓扑的 MAS 系统的适用性有待进一步验证。
*   **防御的自适应性**：FlowGuard 虽有效，但面对能够巧妙隐藏意图的自适应攻击者，其分选逻辑可能面临挑战。

（完）
