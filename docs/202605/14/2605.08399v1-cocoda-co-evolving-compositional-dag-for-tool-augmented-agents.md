---
title: "CoCoDA: Co-evolving Compositional DAG for Tool-Augmented Agents"
title_zh: CoCoDA：面向工具增强型智能体的协同演化组合式 DAG
authors: "Ziyang Yu, Qiyue Li, Liang Zhao"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.08399v1"
tags: ["query:malm"]
score: 9.0
evidence: 基于组合 DAG 的工具增强型智能体框架
tldr: 针对工具增强模型在扩展工具库时面临的检索效率低和上下文限制，本文提出CoCoDA框架。它通过组合式代码有向无环图（DAG）协同演化规划器与工具库，利用类型化DAG检索优化推理效率，并通过DAG诱导奖励机制强化训练。实验证明，CoCoDA能显著提升小模型性能，使8B模型在数学推理等任务上比肩32B模型，有效解决了工具库扩展带来的复杂性挑战。
selection_source: fresh_fetch
motivation: 现有工具增强模型在扩展工具库时，面临检索效率低下、上下文开销随库增大而增加以及忽视代码组合结构的问题。
method: 提出CoCoDA框架，利用组合式代码DAG结构协同演化规划器和工具库，并结合类型化检索与DAG诱导奖励机制。
result: CoCoDA使8B参数的学生模型在GSM8K和MATH等基准测试中达到或超过了32B教师模型的水平。
conclusion: 通过结构化的组合工具库和协同演化策略，可以显著提升小型语言模型在复杂任务中的工具调用能力和检索效率。
---

## 摘要
工具增强型语言模型可以通过外部可执行技能扩展小语言模型，但扩展工具库带来了一个耦合的挑战：随着新的可重用子程序的出现，工具库必须与规划器同步演化，同时从不断增长的库中进行检索必须保持在固定的上下文预算内。现有的工具使用和技能库方法通常将工具视为扁平的或以文本索引的记忆，导致提示词成本随库规模增长，并掩盖了可执行代码的有类型、组合式结构。我们提出了 CoCoDA，这是一个通过单一的代码原生结构——组合式代码 DAG——来协同演化规划器和工具库的框架。节点是原始工具或复合工具，边编码调用依赖关系，每个节点存储有类型的签名、描述、前置/后置条件规范以及工作示例。在推理时，“有类型 DAG 检索”通过符号签名统一剪枝候选对象，按描述对幸存者进行排序，按行为规范对其进行过滤，并利用示例消除歧义，从而在逐渐缩小的候选集上保持昂贵的上下文实例化。在训练时，成功的轨迹被折叠成经过验证的复合工具，同时规划器使用 DAG 诱导的奖励进行更新，该奖励根据复合工具的原始扩展规模给予信用。我们提供的理论结果展示了检索成本的降低、亚线性检索时间、成形奖励下的组合优势、保守更新下的单调协同演化以及 DAG 的良构性。在数学推理、表格分析和代码任务基准测试中，CoCoDA 使 8B 参数的学生模型在 GSM8K 和 MATH 上达到或超过了 32B 参数的教师模型，并始终优于强大的工具使用和库学习基线。

## Abstract
Tool-augmented language models can extend small language models with external executable skills, but scaling the tool library creates a coupled challenge: the library must evolve with the planner as new reusable subroutines emerge, while retrieval from the growing library must remain within a fixed context budget. Existing tool-use and skill-library methods typically treat tools as flat or text-indexed memories, causing prompt cost to grow with library size and obscuring the typed, compositional structure of executable code. We propose CoCoDA, a framework that co-evolves the planner and tool library through a single code-native structure: a compositional code DAG. Nodes are primitive or composite tools, edges encode invocation dependencies, and each node stores a typed signature, description, pre/post-condition specification, and worked examples. At inference time, Typed DAG Retrieval prunes candidates by symbolic signature unification, ranks survivors by descriptions, filters them by behavioral specifications, and disambiguates with examples, keeping expensive context materialization on progressively smaller candidate sets. At training time, successful trajectories are folded into validated composite tools, while the planner is updated with a DAG-induced reward that credits composites by their primitive expansion size. We provide theoretical results showing retrieval cost reduction, sublinear retrieval time, compositional advantage under the shaped reward, monotone co-evolution under conservative updates, and DAG well-formedness. Across mathematical reasoning, tabular analysis, and code task benchmarks, CoCoDA enables an 8B student to match or exceed a 32B teacher on GSM8K and MATH and consistently improves over strong tool-use and library-learning baselines.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **CoCoDA**（Co-evolving Compositional DAG）的框架，旨在解决小型语言模型（SLM）在利用外部工具库时面临的效率与演化难题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心挑战**：小型语言模型（SLM）依赖外部工具来增强能力，但随着工具库规模的扩大，面临两个耦合的难题：
    1.  **检索成本与上下文限制**：传统的扁平化或文本索引工具库导致检索成本随库规模线性增长，容易超出 SLM 有限的上下文窗口。
    2.  **库与策略的协同演化**：工具库需要随规划器（Planner）的探索而进化（将重复的子轨迹抽象为可重用的复合工具），否则规划器会不断重复低效的原始计算。
*   **研究背景**：现有方法通常将工具视为纯文本记忆，忽视了代码本身具有的**类型化、组合式结构**。

### 2. 方法论：CoCoDA 框架
CoCoDA 通过一个单一的**代码原生 DAG（有向无环图）**结构来同步管理工具库的增长与检索。

*   **核心结构：组合式代码 DAG**
    *   **节点**：分为原始工具（Primitives）和复合工具（Composites）。
    *   **边**：编码调用依赖关系。
    *   **四层元数据记录**：每个节点包含 $L_1$ 类型签名、$L_2$ 语义描述、$L_3$ 前/后置条件规范、$L_4$ 工作示例。
*   **推理技术：类型化 DAG 检索（Typed DAG Retrieval, TDR）**
    *   采用从廉价到昂贵的四级级联过滤：
        1.  **$L_1$ 符号剪枝**：通过类型统一（Unification）快速排除不匹配工具（零 LLM 成本）。
        2.  **$L_2$ 语义排序**：对幸存者进行描述匹配。
        3.  **$L_3$ 规范过滤**：检查行为规范的兼容性。
        4.  **$L_4$ 示例消歧**：利用具体示例进行最终重排序。
*   **训练技术：协同演化与 DAG 诱导奖励**
    *   **工具插入**：将成功的执行轨迹折叠为经过验证的复合工具，并插入 DAG。
    *   **结构化奖励（$R_{comp}$）**：在 GRPO 强化学习中，根据复合工具所代表的原始调用次数给予奖励（即“节省调用次数”奖励），鼓励模型使用更高层次的抽象。

### 3. 实验设计
*   **数据集（6个基准测试，涵盖3大类）**：
    *   **数学/逻辑推理**：GSM8K, MATH。
    *   **表格分析**：WikiTableQuestions (WTQ), FinQA。
    *   **代码任务**：EvalPlus (HumanEval+), MBPP。
*   **对比方法**：
    *   **无工具基线**：Qwen3 (0.6B-8B) 的 CoT，以及 32B 教师模型的 CoT。
    *   **固定工具库方法**：ReAct, ToRL, ReTool。
    *   **库学习方法**：CREATOR, TroVE。
*   **模型设置**：使用 Qwen3-32B 作为教师模型进行工具抽象，Qwen3 系列（0.6B, 1.7B, 4B, 8B）作为学生模型。

### 4. 资源与算力
*   **硬件环境**：云端虚拟机，配备 **4 张 NVIDIA H200 (80GB) GPU**。
*   **训练时长**：
    *   0.6B 模型总计约 35 GPU 小时。
    *   8B 模型（使用 LoRA）总计约 98 GPU 小时。
*   **软件框架**：基于 PyTorch 2.5, DeepSpeed ZeRO-3, 以及 verl 强化学习框架。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 0.6B 到 8B 的四个模型尺寸，并在 6 个不同领域的基准测试上进行了验证。
*   **消融实验**：非常充分。分别验证了组合 DAG 结构（CTD）、类型化检索（TDR）和图感知奖励（GAR）的贡献。
*   **客观性**：所有实验均运行 3 个随机种子并取平均值，提供了延迟与准确率的权衡曲线（Pareto 前沿分析），对比了文本分层检索（RAPTOR）与代码分层检索的差异，实验设计严谨且公平。

### 6. 主要结论与发现
*   **性能跨越**：CoCoDA 使 **8B 学生模型在 GSM8K 和 MATH 上达到或超过了 32B 教师模型**的水平（例如 GSM8K 达到 93.67%）。
*   **检索效率**：相比扁平化检索，CoCoDA 的 TDR 机制将提示词成本降低了约 11.4 倍，且检索时间随库规模呈亚线性增长。
*   **协同效应**：随着训练进行，工具库深度增加（平均 3-4 层），模型调用工具的步数减少，证明了“更深”的库能同时降低检索成本和推理步数。

### 7. 优点与亮点
*   **代码原生性**：不同于将代码视为文本的 RAG 方法，CoCoDA 充分利用了代码的静态类型和调用图结构。
*   **亚线性检索**：通过符号化预过滤和级联机制，解决了大规模工具库下的上下文溢出问题。
*   **结构化奖励设计**：巧妙地将“代码压缩”转化为强化学习的奖励信号，使模型自发学习使用高级抽象。

### 8. 不足与局限
*   **任务限制**：该框架目前仅适用于**结果可确定性验证**的任务（如数学、代码、SQL），对于主观性较强的开放域任务（如创意写作）可能难以定义执行奖励。
*   **教师依赖**：工具的初始抽象和验证仍依赖于一个较强的教师模型（如 32B 或更高），在完全脱离强模型的情况下，SLM 自主构建库的能力尚待验证。
*   **静态假设**：实验假设工具的功能是静态的，未讨论当底层原始工具发生 API 变更时，DAG 如何进行增量更新或维护。

（完）
