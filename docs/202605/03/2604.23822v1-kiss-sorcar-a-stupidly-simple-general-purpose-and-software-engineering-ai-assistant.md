---
title: "KISS Sorcar: A Stupidly-Simple General-Purpose and Software Engineering AI Assistant"
title_zh: KISS Sorcar：一个极其简单的通用型软件工程 AI 助手
authors: Koushik Sen
date: 2026-04-26
pdf: "https://arxiv.org/pdf/2604.23822v1"
tags: ["query:malm"]
score: 9.0
evidence: 五层智能体层级框架
tldr: KISS Sorcar 是一个基于极简框架（约1850行代码）开发的通用软件工程 AI 助手。它通过五层代理架构解决了上下文限制、错误累积和代码质量差等痛点，支持自动总结、并行子代理及 Git 工作区隔离。该工具以 VS Code 插件形式开源，强调通过自我验证（如运行测试和检查器）提升输出质量，在 Terminal Bench 2.0 测试中表现优于 Claude Code 等主流工具。
selection_source: fresh_fetch
motivation: 针对现有 AI 编程助手在长上下文处理、错误恢复及生成代码质量方面存在的缺陷，寻求一种简单且稳健的解决方案。
method: 采用五层代理架构，结合 ReAct 模式、自动摘要、并行子代理、历史召回及 Git 工作树隔离技术，并优先考虑自我验证而非响应速度。
result: "在 Terminal Bench 2.0 基准测试中，KISS Sorcar 取得了 62.2% 的通过率，超越了 Claude Code 和 Cursor Composer 2。"
conclusion: 证明了通过极简的框架设计和对输出质量的深度验证，可以构建出比复杂系统更高效、更可靠的软件工程 AI 助手。
---

## 摘要
大语言模型能够以惊人的流畅度生成代码并调用工具，但将其部署为实用的软件工程助手时，仍面临一些棘手的差距：有限的上下文窗口、导致整个会话脱轨的单一错误、陷入死循环的智能体、AI 废话（AI slop），以及难以审查或撤销的生成变更。我们介绍了 KISS Sorcar，这是一个构建在 KISS Agent 框架之上的通用助手和集成开发环境（IDE）。KISS Agent 是一个代码量约为 1,850 行的极简 AI 智能体框架。该框架通过强大的系统提示词和五层智能体层级结构来解决这些问题，每一层仅增加一个关注点：带有预算追踪的 ReAct 执行、通过摘要实现跨子会话的自动续接、带有并行子智能体的代码和浏览器工具、具有历史召回功能的持久多轮对话，以及 git 工作树隔离（确保每个任务都在独立分支上运行）。为了评估 KISS 智能体框架的能力，我们将 KISS Sorcar 实现为一个免费、开源的 Visual Studio Code 扩展，它可以在本地高效运行长程任务，并支持浏览器自动化、多模态输入和 Docker 容器。在本研究中，我们刻意将输出质量置于延迟之上：给予前沿模型充足的时间来验证其自身输出（运行 linter、类型检查器和测试），从而显著减少了那些困扰追求速度但不够严谨的智能体的低质量代码。整个系统是在 4.5 个月内使用其自身构建的，这提供了一个持续的压力测试，即智能体引入的任何错误都会立即损害其自身的工作能力。在 Terminal Bench 2.0 上，KISS Sorcar 配合 Claude Opus 4.6 实现了 62.2% 的总通过率，优于 Claude Code (58%) 和 Cursor Composer 2 (61.7%)。

## Abstract
Large language models can generate code and call tools with remarkable fluency, yet deploying them as practical software engineering assistants still expose stubborn gaps: finite context windows, single mistakes that derail entire sessions, agents that get stuck in dead ends, AI slop, and generated changes that are difficult to review or revert.   We present KISS Sorcar, a general-purpose assistant and integrated development environment (IDE) built on top of the KISS Agent Framework, a stupidly-simple AI agent framework of roughly 1,850 lines of code. The framework addresses these gaps using a robust system prompt and through a five-layer agent hierarchy in which each layer adds exactly one concern: budget-tracked ReAct execution, automatic continuation across sub-sessions via summarization, coding, and browser tools with parallel sub-agents, persistent multi-turn chat with history recall, and git worktree isolation so every task runs on its own branch.   To assess the power of the KISS agent framework, we implemented KISS Sorcar as a free, open-source Visual Studio Code extension that runs locally and effectively for long-horizon tasks, and supports browser automation, multimodal input, and Docker containers.   In this research, we deliberately prioritize output quality over latency: giving a frontier model adequate time to validate its own output -- running linters, type checkers, and tests -- dramatically reduces the low-quality code that plagues faster but less thorough agents. The entire system was built using itself in 4.5 months, providing a continuous stress test in which any agent-introduced bug immediately impairs its own ability to work. On Terminal Bench 2.0, KISS Sorcar achieves a 62.2% overall pass rate with Claude Opus 4.6, comparing favorably to Claude Code (58%) and Cursor Composer 2 (61.7).

---

## 论文详细总结（自动生成）

以下是对论文《KISS Sorcar: A Stupidly-Simple General-Purpose and Software Engineering AI Assistant》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：尽管大语言模型（LLM）在代码生成和工具调用方面表现出色，但在实际软件工程应用中仍存在显著缺陷：有限的上下文窗口、单一错误导致整个会话崩溃、智能体陷入死循环、生成低质量的“AI 废话”（AI slop），以及生成的代码难以审查或撤销。
*   **核心目标**：遵循“KISS”（Keep It Simple, Stupid）原则，构建一个极简、稳健且通用的软件工程助手，通过简单的架构解决上述复杂问题，并优先保证输出质量而非响应速度。

### 2. 方法论
*   **核心思想**：采用五层智能体层级架构，每一层仅负责解决一个核心关注点，通过严格的继承链实现功能叠加。
*   **关键技术细节（五层架构）**：
    1.  **KISS Agent**：最内层执行单元，实现标准的 ReAct 循环，具备原生函数调用、步骤/Token/预算追踪及错误重试机制。
    2.  **Relentless Agent**：处理长程任务。当上下文耗尽时，自动生成结构化摘要并开启新子会话，实现跨会话的任务延续。
    3.  **Sorcar Agent**：集成核心工具集，包括 Shell 执行、文件读写/编辑、基于 Playwright 的浏览器自动化，以及并行子智能体执行。
    4.  **Chat Sorcar Agent**：提供持久化的多轮对话管理，支持历史记录召回和任务元数据持久化。
    5.  **Worktree Sorcar Agent**：最外层，利用 Git Worktree 实现任务隔离。每个任务在独立分支运行，确保用户主工作区安全，支持冲突处理和崩溃恢复。
*   **系统提示词（System Prompt）**：设计了极其详尽的指令，强制执行工程纪律（如：修改前必读、修复前必写测试、100% 分支覆盖率、禁止使用 Mock 等）。

### 3. 实验设计
*   **数据集/场景**：使用 **Terminal Bench 2.0**，包含 89 个复杂的终端编程任务（如构建编译器、服务器配置、密码分析、机器学习模型训练等）。
*   **对比方法**：
    *   **Claude Code** (基于 Claude Opus 4.6)
    *   **Cursor Composer 2** (经过大规模强化学习微调的定制模型)
*   **实验设置**：每个任务进行 5 次独立试验，使用 Claude Opus 4.6 作为底层模型。

### 4. 资源与算力
*   **算力设备**：实验在一台 **2025 款 MacBook Air 15"** 上本地运行。
*   **训练时长**：该项目不涉及模型训练，而是基于 API 的框架开发。整个系统（框架、插件、提示词）由作者利用系统自身在 **4.5 个月**内迭代构建完成（自托管开发）。
*   **成本**：实验记录了详细的 API 成本，单次试验的中位成本为 0.45 美元，平均成本为 0.90 美元。

### 5. 实验数量与充分性
*   **实验规模**：共进行了 89 组任务，每组 5 次重复，总计 445 次试验。
*   **充分性评价**：实验设计较为充分。它不仅在标准基准测试上与顶级工业界产品进行了量化对比，还通过“自托管”开发过程进行了长达数月的持续压力测试。作者还对失败任务进行了分类讨论（如多媒体限制、资源密集型构建等），表现出较高的客观性。

### 6. 主要结论与发现
*   **性能卓越**：KISS Sorcar 在 Terminal Bench 2.0 上取得了 **62.2%** 的总通过率，优于 Claude Code (58%) 和 Cursor Composer 2 (61.7%)。
*   **质量优先**：通过给予模型充足时间进行自我验证（运行 Linter、类型检查和测试），可以显著消除低质量代码。
*   **极简有效**：仅用约 **1,850 行代码**的框架即可在复杂软件工程任务中达到甚至超越经过重度微调的复杂系统。

### 7. 优点
*   **架构优雅**：五层解耦设计使得系统极易维护和扩展，每个模块功能单一且明确。
*   **安全性高**：通过 Git Worktree 强制隔离，解决了 AI 助手可能破坏用户本地代码库的痛点。
*   **透明度高**：内置实时的 Token 和预算追踪，让用户对成本有明确预期。
*   **自我进化**：通过 `USER_PREFS.md` 实现跨会话的偏好学习和冲突解决。

### 8. 不足与局限
*   **延迟问题**：由于强调质量和多次自我验证，任务完成的耗时较长（平均 446 秒），不适合追求即时反馈的场景。
*   **模型依赖**：高度依赖前沿模型（如 Claude Opus 4.6）的推理能力，在弱模型上的表现尚未验证。
*   **摘要瓶颈**：虽然目前未遇到，但对于跨度极长的任务，累积的摘要可能会变得臃肿，影响上下文效率。
*   **环境限制**：在处理需要图形界面（GUI）或超大规模计算资源的特定任务时仍会失败。

（完）
