---
title: "AgentVisor: Defending LLM Agents Against Prompt Injection via Semantic Virtualization"
title_zh: AgentVisor：通过语义虚拟化防御大语言模型智能体的提示注入攻击
authors: "Zonghao Ying, Haozheng Wang, Jiangfan Liu, Quanchen Zou, Aishan Liu, Jian Yang, Yaodong Yang, Xianglong Liu"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24118v1"
tags: ["query:malm"]
score: 9.0
evidence: 大语言模型智能体及其架构的安全框架
tldr: 针对大语言模型智能体面临的提示注入攻击，本文受操作系统虚拟化启发，提出AgentVisor防御框架。该框架通过语义虚拟化实现特权分离，将智能体视为非受信客户，利用受信语义监控器拦截并审计工具调用，并引入自我纠正机制。实验表明，该方法在保持极高实用性的同时，能显著降低攻击成功率，为智能体安全提供了新思路。
selection_source: fresh_fetch
motivation: 现有的LLM智能体防御手段在安全性与实用性之间平衡性较差，且难以有效应对复杂的间接提示注入攻击。
method: 借鉴操作系统虚拟化思想，通过语义监控器拦截工具调用，并结合审计协议与一键式自我纠正机制来防御攻击。
result: "实验显示AgentVisor将攻击成功率降至0.65%，且相比无防御状态仅损失1.45%的实用性，表现优于现有方法。"
conclusion: AgentVisor通过语义特权分离有效解决了智能体的提示注入问题，实现了安全防御与任务效能的高度平衡。
---

## 摘要
大语言模型（LLM）智能体越来越多地被用于自动化复杂的工作流，但将不可信的外部数据与特权执行相结合，使其面临严重的安全风险，特别是直接和间接提示注入。现有的防御措施在平衡安全性与效用方面面临重大挑战，通常需要在严格保护导致的过度防御与微妙间接注入绕过检测之间进行权衡。受操作系统虚拟化的启发，我们提出了 AgentVisor，这是一种强制执行语义特权分离的新型防御框架。AgentVisor 将目标智能体视为不可信的访客（guest），并通过一个可信的语义监控器（visor）拦截工具调用。我们方法的核心是一个基于经典操作系统安全原语的严格审计协议，旨在系统地缓解直接和间接注入攻击。此外，我们引入了一种单次自纠正机制，将安全违规转化为建设性的反馈，使智能体能够从攻击中恢复。大量实验表明，AgentVisor 将攻击成功率降低至 0.65%，在实现这种强力防御的同时，相对于无防御场景，效用平均仅下降 1.45%，证明了其优于现有防御方法的性能。

## Abstract
Large Language Model (LLM) agents are increasingly used to automate complex workflows, but integrating untrusted external data with privileged execution exposes them to severe security risks, particularly direct and indirect prompt injection. Existing defenses face significant challenges in balancing security with utility, often encountering a trade-off where rigorous protection leads to over-defense, or where subtle indirect injections bypass detection. Drawing inspiration from operating system virtualization, we propose AgentVisor, a novel defense framework that enforces semantic privilege separation. AgentVisor treats the target agent as an untrusted guest and intercepts tool calls via a trusted semantic visor. Central to our approach is a rigorous audit protocol grounded in classic OS security primitives, designed to systematically mitigate both direct and indirect injection attacks. Furthermore, we introduce a one-shot self-correction mechanism that transforms security violations into constructive feedback, enabling agents to recover from attacks. Extensive experiments show that AgentVisor reduces the attack success rate to 0.65%, achieving this strong defense while incurring only a 1.45% average decrease in utility relative to the No Defense scenario, demonstrating superior performance compared to existing defense methods.

---

## 论文详细总结（自动生成）

这是一份关于论文《AgentVisor: Defending LLM Agents Against Prompt Injection via Semantic Virtualization》的结构化总结：

### 1. 核心问题与整体含义
随着大语言模型（LLM）智能体在自动化工作流中的普及，它们面临着严重的**提示注入（Prompt Injection）**风险。攻击者可以通过直接指令（用户恶意输入）或间接指令（外部网页、邮件中的隐藏命令）劫持智能体的工具调用权限。
*   **研究动机**：现有的防御手段（如输入过滤、提示词加固）往往在安全性与实用性之间难以平衡——要么防御过当导致正常功能失效，要么防御太弱被轻易绕过。
*   **核心目标**：建立一种既能系统性拦截恶意指令，又能保持智能体任务执行能力的防御架构。

### 2. 方法论
论文受操作系统虚拟化技术启发，提出了 **AgentVisor** 框架，将智能体视为“非受信客户机（Guest）”，将防御层视为“受信语义监控器（Visor）”。
*   **核心思想**：通过**语义虚拟化**实现特权分离。Visor 拦截 Guest 的所有工具调用请求，并根据受信上下文进行审计。
*   **STI 审计协议**：
    1.  **Suitability（适用性）**：核查工具调用是否符合系统预设角色和权限（防御权限提升）。
    2.  **Taint（污染分析）**：核查工具调用的目标是否与用户原始意图一致（防御目标劫持）。
    3.  **Integrity（完整性）**：核查工具参数是否被篡改或重定向（防御参数篡改）。
*   **陷阱-审计-恢复（Trap-Audit-Recover）流程**：
    *   **拦截（Trap）**：Guest 提出工具调用申请。
    *   **审计（Audit）**：Visor 依据 STI 协议进行检查。Visor 无法看到原始的不可信外部数据，仅基于受信指令和清理后的历史记录做判断。
    *   **恢复（Recover）**：若审计失败，Visor 不直接终止任务，而是注入一个**语义异常（Semantic Exception）**，告知 Guest 违规原因并要求其进行单次自纠正。

### 3. 实验设计
*   **数据集/场景**：
    *   **直接注入**：使用 OpenPromptInjection，包含 7 个 NLP 任务，共 4,900 个攻击案例。
    *   **间接注入**：使用 AgentDojo，涵盖银行、旅行、Slack 和工作场所 4 个交互式环境，共 629 个攻击案例。
*   **Benchmark 与对比方法**：
    *   对比了 8 种基准防御方法，包括基于缓解的（Sandwich, Reminder, Isolation 等）和基于检测的（DeBERTa, DataSentinel, MELON 等）。
    *   使用了 7 种代表性攻击策略（如 Ignore, Escape, FakeComp 等）。
*   **主模型（Backbones）**：采用 GPT-4o 和 GLM-4.7 作为目标智能体，使用 Gemini-2.5-Flash 作为 Visor。

### 4. 资源与算力
*   论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   由于该框架主要基于现有的闭源或开源模型进行推理侧的架构设计（API 调用为主），不涉及大规模的基础模型预训练，因此算力消耗主要体现在推理延迟和 Token 成本上。

### 5. 实验数量与充分性
*   **实验规模**：总计测试了超过 5,500 个攻击案例，覆盖了从单步到多步的复杂工作流。
*   **消融实验**：分别验证了 STI 协议中各组件（S、T、I）的作用，以及自纠正机制对实用性的贡献。
*   **鲁棒性测试**：进行了**自适应攻击（Adaptive Attack）**实验，模拟攻击者试图通过“忽略安全检查”等指令直接攻击防御层的情况。
*   **模型无关性测试**：在 Claude-Sonnet-4、Gemini-2.5-Pro 等多种模型上验证了框架的通用性。
*   **评价**：实验设计全面，涵盖了安全性（ASR）、基础效用（BU）和攻击下效用（UA）三个维度，对比客观且充分。

### 6. 主要结论与发现
*   **极高的安全性**：AgentVisor 将攻击成功率（ASR）降低至 **0.65%**（在某些场景下达到 0%）。
*   **极低的效用损失**：相比无防御状态，其任务完成率（Utility）平均仅下降 **1.45%**，远优于其他防御方法。
*   **自纠正的有效性**：单次自纠正足以让智能体从绝大多数攻击中恢复，多次纠正的边际收益递减。
*   **防御层轻量化**：即使使用较弱/较小的模型作为 Visor，也能提供极强的安全保障。

### 7. 优点
*   **架构创新**：首次将操作系统虚拟化的特权分离概念引入 LLM 智能体安全领域，提供了严谨的理论框架。
*   **上下文隔离**：Visor 架构上“屏蔽”了原始外部不可信数据，从根本上免疫了针对防御层本身的注入攻击。
*   **非破坏性防御**：通过语义异常注入而非直接拦截，最大限度保留了智能体在复杂环境下的可用性。

### 8. 不足与局限
*   **推理延迟**：引入 Visor 层会增加端到端延迟（良性条件下约 1.4 倍，攻击条件下约 1.7-2.3 倍）。
*   **长文本扩展性**：随着交互历史增长，Visor 处理长上下文的 Token 成本和分析精度可能受到限制。
*   **多模态限制**：目前仅针对文本提示注入，尚未扩展到视觉或音频等跨模态注入攻击的防御。

（完）
