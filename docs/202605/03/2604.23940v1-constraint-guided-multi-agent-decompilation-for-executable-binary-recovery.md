---
title: Constraint-Guided Multi-Agent Decompilation for Executable Binary Recovery
title_zh: 约束引导的多智能体反编译用于可执行二进制恢复
authors: "Yifan Zhang, Xiaohan Wang, Yueke Zhang, Kevin Leach"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.23940v1"
tags: ["query:malm"]
score: 9.0
evidence: 用于代码优化与验证的多智能体框架
tldr: 针对现有反编译器生成的代码难以编译或运行的问题，本文提出了一种基于多智能体框架的约束引导反编译方法（MCGD）。该方法通过语法、编译和行为等三层约束验证，利用大模型智能体对反编译代码进行迭代优化。实验表明，该框架在真实二进制文件上的可执行恢复率显著提升，超越了现有SOTA方法，为二进制代码恢复提供了高效且低成本的解决方案。
selection_source: fresh_fetch
motivation: 现有反编译器生成的源代码往往无法直接编译或运行，严重限制了其在安全分析和软件维护中的实际应用。
method: 提出一种多层级约束引导的反编译框架，通过语法解析、GCC编译和LLM生成的测试用例进行分层验证，并由多智能体协作迭代修复代码。
result: "在ExeBench数据集上实现了84-97%的可执行恢复率，相比基准提升了28-89个百分点，且在性能和成本上均优于LLM4Decompile等SOTA方法。"
conclusion: 约束引导的智能体优化能够有效弥补原始反编译输出与实用源代码之间的差距，证明了执行级验证在保证代码行为一致性中的关键作用。
---

## 摘要
反编译——从编译后的二进制文件中恢复源代码——对于安全分析、恶意软件逆向工程和遗留软件维护至关重要。然而，现有的反编译器生成的代码往往无法正确编译或执行，限制了它们的实际效用。我们提出了一个多智能体框架，通过多级约束引导反编译（MCGD）将反编译代码转换为可重新执行的源码。我们的方法采用了一个具有三个约束级别的分层验证流水线：(1) 通过解析实现的语法正确性，(2) 通过 GCC 实现的可编译性，以及 (3) 通过大语言模型（LLM）生成的测试用例实现的行为等价性。当验证失败时，专门的 LLM 智能体会利用结构化错误反馈迭代地优化代码。我们在来自 ExeBench 的 1,641 个真实二进制文件上评估了我们的框架，涵盖了三种反编译器（RetDec、Ghidra 和 Angr）。我们的框架实现了 84-97% 的可重执行率，比基准反编译器的输出提高了 28-89 个百分点。与使用相同 GPT-4o 骨干网络的先进基于 LLM 的反编译方法相比，我们的方法（84.1%）优于 LLM4Decompile（80.3%）、SK2Decompile（73.9%）和 SALT4Decompile（61.8%）。我们的消融研究表明，基于执行的验证至关重要：尽管编译率达到 91-99%，但仅靠编译的方法在行为正确性上为 0%。该系统收敛效率高，90% 以上的二进制文件在 2 次迭代内达到正确性，每个二进制文件的平均成本为 0.03-0.05 美元。我们的结果表明，约束引导的智能体优化可以弥合原始反编译器输出与实际有用的源代码之间的差距。

## Abstract
Decompilation -- recovering source code from compiled binaries -- is essential for security analysis, malware reverse engineering, and legacy software maintenance. However, existing decompilers produce code that often fails to compile or execute correctly, limiting their practical utility. We present a multi-agent framework that transforms decompiled code into re-executable source through Multi-level Constraint-Guided Decompilation (MCGD). Our approach employs a hierarchical validation pipeline with three constraint levels: (1) syntactic correctness via parsing, (2) compilability via GCC, and (3) behavioral equivalence via LLM-generated test cases. When validation fails, specialized LLM agents iteratively refine the code using structured error feedback. We evaluate our framework on 1,641 real-world binaries from ExeBench across three decompilers (RetDec, Ghidra, and Angr). Our framework achieves 84-97% re-executability, improving baseline decompiler output by 28-89 percentage points. In comparison with state-of-the-art LLM-based decompilation methods using the same GPT-4o backbone, our approach (84.1%) outperforms LLM4Decompile (80.3%), SK2Decompile (73.9%), and SALT4Decompile (61.8%). Our ablation study reveals that execution-based validation is critical: compile-only approaches achieve 0% behavioral correctness despite 91-99% compilation rates. The system converges efficiently, with 90%+ binaries reaching correctness within 2 iterations at an average cost of $0.03-0.05 per binary. Our results demonstrate that constraint-guided agentic refinement can bridge the gap between raw decompiler output and practically useful source code.

---

## 论文详细总结（自动生成）

### 论文总结：约束引导的多智能体反编译用于可执行二进制恢复

#### 1. 核心问题与整体含义
反编译是将二进制可执行文件恢复为高级源代码（如 C 语言）的关键技术，广泛应用于安全审计、逆向工程和遗留系统维护。然而，传统反编译器（如 Ghidra、Angr）生成的代码往往存在语法错误、类型不匹配或逻辑不一致，导致代码**无法重新编译或执行结果不正确**。现有的 LLM 反编译方法多为“单次生成”，缺乏验证与纠错机制。本文提出了 **Agent4Decompile**，旨在通过多智能体协作和多层级约束引导，将不可用的反编译输出转化为**可重执行（Re-executable）**且行为等价的源代码。

#### 2. 方法论：核心思想与算法流程
Agent4Decompile 的核心思想是将反编译视为一个**渐进式搜索空间缩减**的过程，通过三层约束引导 LLM 智能体进行迭代修复：

*   **核心流程**：
    1.  **初始反编译**：使用传统工具（Ghidra/Angr/RetDec）获取原始 C 代码。
    2.  **三层约束验证（L1-L3）**：
        *   **L1 语法约束**：使用 `gcc -fsyntax-only` 检查代码是否符合 C 语法。
        *   **L2 编译约束**：检查代码是否能成功编译并链接，识别类型错误或未定义符号。
        *   **L3 执行约束**：通过测试用例（I/O pairs）对比原始二进制与重编译代码的运行结果，确保行为等价。
    3.  **多智能体修复**：针对每一层级的失败，专门的 LLM 智能体接收结构化错误反馈（如 GCC 报错信息、失败的测试输入输出对比），并生成修复后的代码。
    4.  **迭代优化**：系统在约束层级间循环，直到代码通过所有验证或达到最大迭代次数（通常为 5-7 次）。

#### 3. 实验设计
*   **数据集**：
    *   **157-Binary Benchmark**：精选自 ExeBench 和自定义算法，用于深入分析和 SOTA 对比。
    *   **1,641-Binary Benchmark**：大规模评估，涵盖不同优化等级（O0-O3）和真实开源项目函数。
*   **基准工具（Decompilers）**：Ghidra（规则型）、Angr（提升型）、RetDec（ML/LLVM 型）。
*   **对比方法**：
    *   **DeepSeek Single Pass**：无迭代的单次 LLM 修复。
    *   **LLM4Decompile**：经过微调的专业反编译模型。
    *   **SK2Decompile**：侧重于可读性提升的两阶段修复方法。

#### 4. 资源与算力
*   **模型使用**：默认使用 **DeepSeek V3.2** API 作为底层 LLM 骨干。
*   **实现细节**：约 5,000 行 Python 代码，使用 `ProcessPoolExecutor` 进行多进程并行处理。
*   **算力说明**：论文**未明确提及**具体的 GPU 训练算力（如显卡型号或训练时长），因为该框架主要基于现有的 LLM API 进行推理和智能体调度，而非从头训练模型。实验中设置了 10 秒的执行超时和 256MB 的内存限制。

#### 5. 实验数量与充分性
*   **实验规模**：进行了超过 1,600 个二进制文件的测试，是目前反编译领域规模最大的实验之一。
*   **维度覆盖**：实验涵盖了 4 种优化等级（O0-O3）、3 种不同架构的反编译器、以及消融实验（验证 L1/L2/L3 各层贡献）。
*   **应用验证**：额外进行了安全性应用实验，包括漏洞模式保留测试（5 种漏洞类型）和模糊测试（Fuzzing）覆盖率分析。
*   **评价**：实验设计客观、公平，通过大规模数据和多维度对比，充分证明了迭代修复和执行反馈的必要性。

#### 6. 主要结论与发现
*   **性能飞跃**：Agent4Decompile 将可重执行率从基准的 22-26% 提升至 **43-50%**，相比 SOTA 方法提升了 15-38 个百分点。
*   **“编译-执行”鸿沟**：消融实验发现，仅通过编译验证（L1+L2）的代码虽然编译率接近 100%，但行为正确率仅为 32-42%。这证明了**执行反馈（L3）是捕获语义错误的关键**。
*   **收敛效率**：大多数成功的修复在 **2 次迭代内**即可完成，证明了反馈机制的精准性。
*   **安全性保持**：该方法能 100% 保留已知的漏洞模式，并使代码的“可模糊测试性”（Fuzzability）提升了 6.6 倍。

#### 7. 优点与亮点
*   **分层验证机制**：将复杂的语义修复分解为语法、编译、执行三个阶段，降低了 LLM 的处理难度。
*   **模型无关性**：作为一个 Agent 框架，它可以灵活更换底座模型（如 GPT-4, CodeLlama 等）。
*   **实用性强**：不仅关注代码的可读性，更关注代码的“功能性”，直接解决了反编译代码“看得到、用不了”的痛点。

#### 8. 不足与局限
*   **依赖测试用例**：L3 验证需要预先存在的 I/O 测试用例，对于完全未知的二进制文件，获取高质量测试用例仍具挑战。
*   **函数内联限制**：在 O3 优化下，编译器常将函数内联，导致反编译器无法提取独立的函数体，这是二进制分析的固有局限。
*   **语义漂移风险**：LLM 在修复过程中可能会为了通过测试用例而“过度拟合”逻辑，导致生成的代码虽然通过测试，但与原始逻辑存在细微偏差。
*   **范围限制**：目前主要针对单函数级别的 C 语言反编译，尚未扩展到全程序分析或复杂的 C++ 类结构。

（完）
