---
title: "Route by State, Recover from Trace: STAR with Failure-Aware Markov Routing for Multi-Agent Spatiotemporal Reasoning"
title_zh: 按状态路由，从轨迹恢复：具有故障感知马尔可夫路由的 STAR 多智能体时空推理框架
authors: "Ruiyi Yang, Lihuan Li, Hao Xue, Flora D. Salim"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10057v1"
tags: ["query:malm"]
score: 9.0
evidence: 用于时空推理的多智能体路由框架
tldr: 针对多智能体时空推理中复杂的失败恢复问题，本文提出STAR框架。它将智能体间的控制显式建模为基于状态的转移策略，通过“智能体路由矩阵”结合专家规则与从执行轨迹中学习到的恢复路径。STAR能识别不同类型的失败并采取针对性措施，在多个时空基准测试中显著提升了推理成功率和鲁棒性。
selection_source: fresh_fetch
motivation: 现有的多智能体系统通常将路由决策隐含在语言生成中，导致在面对不同类型的执行失败时难以进行解释和有效的恢复。
method: 提出STAR框架，利用基于状态的马尔可夫路由矩阵，结合专家定义的常规路径和从失败轨迹中学习到的恢复策略，通过共享黑板协议协调各专业智能体。
result: 在三个时空推理基准测试和八个主流大模型上的实验表明，STAR在处理偏离常规路径的复杂查询时表现优异，显著优于现有基线。
conclusion: 显式的、感知失败类型的路由机制是提升多智能体系统在复杂时空推理任务中鲁棒性和恢复能力的关键。
---

## 摘要
组合式时空推理通常需要系统调用多个异构专家，例如几何、时间、拓扑和轨迹智能体。一个核心问题是，当执行并非简单的成功或失败，而是以性质不同的方式失败时，此类系统应如何在专家之间进行路由。现有的工具增强型和多智能体大语言模型（LLM）系统通常将这种路由决策隐含在语言生成中，导致恢复过程具有随意性、难以解释且难以优化。本文提出了 STAR（时空智能体路由器），这是一种故障感知路由框架，它将智能体间的控制外化为基于当前智能体、任务类型和类型化执行状态的状态条件转移策略。STAR 的核心是一个智能体路由矩阵，它结合了专家指定的名义路线和从执行轨迹中学习到的恢复转移。由于该矩阵以不同的故障状态为条件，路由器可以针对格式错误的输出、缺失的依赖项以及工具与查询的不匹配做出不同的响应，而不是将它们统一归结为通用的重试信号。专家通过基于工具的“提取-计算-存储”协议执行，并将中间结果写入共享黑板以供下游融合。结果证明，在训练期间保留不成功的轨迹扩大了路由策略在错误状态上的支持集，从而实现了仅靠成功案例训练无法表示的恢复转移。在三个时空基准测试和八个骨干 LLM 上，STAR 优于多个基准模型，在执行偏离名义路由路径的查询上提升最为显著。针对路由器的消融实验和恢复分析进一步表明，类型化的故障感知路由，而非单纯的专家组合，是实现这些改进的关键因素。

## Abstract
Compositional spatiotemporal reasoning often requires a system to invoke multiple heterogeneous specialists, such as geometric, temporal, topological, and trajectory agents. A central question is how such a system should route among specialists when execution does not simply succeed or fail, but fails in qualitatively different ways. Existing tool-augmented and multi-agent LLM systems typically leave this routing decision implicit in language generation, making recovery ad hoc, difficult to interpret, and hard to optimize. This paper presents STAR (Spatio-Temporal Agent Router), a failure-aware routing framework that externalizes inter-agent control as a state-conditioned transition policy over the current agent, task type, and typed execution status. At the center of STARis an agent routing matrix that combines expert-specified nominal routes with recovery transitions learned from execution traces. Because the matrix conditions on distinct failure states, the router can respond differently to malformed outputs, missing dependencies, and tool--query mismatches, rather than collapsing them into a generic retry signal. Specialists execute through a tool-grounded extract--compute--deposit protocol and write intermediate results to a shared blackboard for downstream fusion. Results prove that retaining unsuccessful traces during training enlarges the support of the routing policy on error states, enabling recovery transitions that success-only training cannot represent. Across three spatiotemporal benchmarks and eight backbone LLMs, STAR improves over multiple baselines with the clearest gains on queries whose execution deviates from the nominal routing path. Router-specific ablations and recovery analyses further show that typed failure-aware routing, rather than specialist composition alone, is a key factor for these improvements.

---

## 论文详细总结（自动生成）

### 论文总结：STAR 故障感知马尔可夫路由框架

#### 1. 核心问题与整体含义（研究动机和背景）
时空推理（Spatiotemporal Reasoning）任务复杂，通常需要调用几何、时间、拓扑和轨迹分析等多个异构专家智能体。现有的多智能体 LLM 系统（如 AutoGen、MetaGPT）或工具增强系统（如 ReAct）通常将“下一步该调用谁”的路由决策隐含在自由文本生成中。
*   **研究动机**：当执行过程中出现不同性质的失败（如输出格式错误、缺少上游依赖、工具与查询不匹配）时，隐含路由难以进行针对性恢复，且不可解释、难以优化。
*   **核心目标**：将智能体间的控制逻辑显式化，构建一个能够感知不同失败状态并进行精准恢复的路由机制。

#### 2. 方法论：STAR 框架
STAR（Spatio-Temporal Agent Router）将多智能体协作建模为一个**状态条件转移策略**（State-conditioned Transition Policy）。

*   **核心思想**：引入一个显式的“智能体路由矩阵”，根据当前智能体、任务类型和执行状态（成功或特定类型的失败）决定下一个动作。
*   **关键技术细节**：
    *   **双系统路由内核**：
        *   **System 1（名义路由）**：由专家定义的标准执行路径，处理初始化（INIT）和成功（SUCC）状态。
        *   **System 2（恢复路由）**：从执行轨迹中学习到的转移矩阵，专门处理三种故障状态：**FAIL**（格式错误）、**BLOCK**（缺少依赖）、**MISS**（工具无法处理查询）。
    *   **提取-计算-存储协议（Extract-Compute-Deposit）**：智能体通过 LLM 提取参数，调用确定性工具进行计算，并将结果存入**共享黑板（Blackboard）**。
    *   **故障感知训练**：在构建路由矩阵时，不仅使用成功的执行轨迹，还保留不成功的轨迹（通过权重参数 $\alpha$ 调节）。论文通过理论证明（定理 1），保留失败轨迹能扩大路由矩阵在错误状态下的支持集，从而实现有效的恢复。
    *   **并行激活**：在推理时，若矩阵为多个候选智能体分配了高概率，系统支持并行执行多个专家。

#### 3. 实验设计
*   **数据集/基准**：
    *   **STARK**：包含 18 种任务类型，侧重时空知识检索。
    *   **STB24 (STBench)**：包含 13 种任务类型，侧重时空分析。
    *   **STB26 (ST-Bench 2026)**：包含 4 种任务类型，侧重因果推理和预测。
*   **对比方法**：
    *   **基础模型**：LLM-only (直接回答), LLM-only (CoT)。
    *   **智能体/提示框架**：Reflexion (自我反思), ReAct (推理+行动), Tree-of-Thought (思维树), Graph-of-Thought (思维图), Function-calling (原生函数调用)。
*   **骨干模型**：横跨 8 个 LLM，包括 Claude 3.5 系列、GPT-OSS-20B、Qwen3-8B、Llama-3.1-8B 等。

#### 4. 资源与算力
*   **硬件支持**：论文致谢中提到使用了 **NVIDIA H100 GPU**（由 Sharon AI 提供支持）。
*   **训练/推理细节**：文中未明确给出具体的训练总时长，但提供了推理时的平均延迟（如 Qwen3-8B 在 STAR 框架下平均延迟为 1.6-1.8 秒/查询）和 Token 消耗量。由于路由矩阵是基于轨迹计数的经验分布，其“训练”过程主要涉及轨迹收集和矩阵填充，计算开销远小于模型微调。

#### 5. 实验数量与充分性
*   **实验规模**：测试集包含超过 7,000 个查询。
*   **充分性**：
    *   **多模型验证**：在 8 个不同规模和家族的 LLM 上验证了通用性。
    *   **深度消融**：分别进行了框架级消融（移除黑板、工具等）和路由器级消融（移除状态感知、移除 System 2 等）。
    *   **恢复分析**：专门分析了在发生 MISS、FAIL、BLOCK 后的恢复成功率。
    *   **跨基准迁移**：测试了在一个数据集上学习的路由矩阵在另一个数据集上的表现。
*   **客观性**：使用了 95% Wilson 置信区间进行显著性标注，并对比了多种最先进的智能体框架，实验设计较为严谨。

#### 6. 主要结论与发现
*   **性能提升**：STAR 在所有基准测试中均显著优于基线，尤其在 STB24 上比 Reflexion 提升了近 40 个百分点。
*   **恢复机制的有效性**：在 STARK 中，进入 MISS 状态（工具不匹配）的查询经过路由恢复后的准确率（80.8%）甚至超过了从未发生故障的查询（75.3%），说明路由矩阵能纠正初始的错误分配。
*   **失败类型的重要性**：将不同失败模式坍缩为单一的“重试”信号会导致性能大幅下降（在某些任务上降幅达 33%），证明了类型化反馈的必要性。
*   **理论验证**：实验证实了定理 1，即包含失败轨迹对于构建鲁棒的路由策略至关重要。

#### 7. 优点
*   **显式控制**：将复杂的智能体协作逻辑从模糊的提示词转变为清晰的概率转移矩阵，增强了系统的可解释性。
*   **鲁棒性强**：通过区分失败类型，实现了比传统“重试”更智能的恢复路径（如从工具失败转向语义推理）。
*   **模块化与并行性**：基于黑板架构的“提取-计算-存储”协议支持专家智能体的解耦和并行加速。
*   **理论支撑**：结合了马尔可夫决策过程的视角，并提供了恢复可达性的理论证明。

#### 8. 不足与局限
*   **依赖任务分类**：系统性能高度依赖于 HEAD 智能体对任务类型的初始分类准确性。
*   **参数提取瓶颈**：FAIL 状态的恢复率相对较低，主要受限于 LLM 在提取工具参数时的固有错误，路由矩阵无法完全弥补底层的提取失败。
*   **冷启动问题**：构建 System 2 矩阵需要预先收集大量的执行轨迹，对于全新的、未见过的任务类型，初期恢复能力受限。
*   **应用限制**：目前主要针对时空推理任务，虽然具有通用性潜力，但在其他非结构化推理领域的表现尚未验证。

（完）
