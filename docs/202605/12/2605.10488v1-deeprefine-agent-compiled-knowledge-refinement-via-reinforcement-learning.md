---
title: "DeepRefine: Agent-Compiled Knowledge Refinement via Reinforcement Learning"
title_zh: DeepRefine：通过强化学习实现智能体编译的知识精炼
authors: "Haoyu Huang, Jiaxin Bai, Shujie Liu, Yang Wei, Hong Ting Tsang, Yisen Gao, Zhongwei Xie, Yufei Li, Yangqiu Song"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10488v1"
tags: ["query:malm"]
score: 9.0
evidence: 用于检索增强系统的智能体编译知识精炼
tldr: 针对LLM智能体构建的知识库中存在的缺失、错误和冗余问题，本文提出DeepRefine框架。该框架通过多轮交互和溯因诊断定位知识库缺陷，并执行针对性修复。利用Gain-Beyond-Draft奖励机制和强化学习，DeepRefine在无需标准参考的情况下优化精炼策略，显著提升了下游任务的检索精度和整体性能。
selection_source: fresh_fetch
motivation: 智能体自动生成的知识库常伴随不完整、不准确和冗余等缺陷，严重影响下游任务的检索质量和执行效果。
method: 提出一种基于强化学习的推理模型，通过多轮交互诊断知识库缺陷，并利用Gain-Beyond-Draft奖励机制进行端到端的精炼策略优化。
result: 实验结果表明，DeepRefine在多个知识密集型任务中均优于强基准模型，有效提升了知识库的质量和下游任务表现。
conclusion: DeepRefine证明了通过强化学习驱动的自动化精炼过程可以显著改善智能体知识库的实用性，为构建高质量外部知识源提供了新路径。
---

## 摘要
智能体编译的知识库为大语言模型（LLM）智能体在开放式、知识密集型下游任务中提供了持久的外部知识。然而，其质量受到不完整性、不准确性和冗余性的系统性限制，具体表现为证据或跨文档链接缺失、低置信度或不精确的断言，以及歧义或指代消解问题。这些缺陷在迭代使用中会不断累积，导致检索保真度和下游任务性能下降。我们提出了 DeepRefine，这是一种通用的基于 LLM 的推理模型，用于智能体编译的知识精炼，它能够根据用户查询改进任何预构建知识库的质量，使其更适用于下游任务。DeepRefine 与知识库进行多轮交互，并对交互历史进行溯因诊断，定位可能的缺陷，并执行有针对性的精炼动作以实现知识库的增量更新。为了在缺乏金标准参考的情况下优化 DeepRefine 的精炼策略，我们引入了“超越草案增益”（Gain-Beyond-Draft, GBD）奖励，并通过强化学习对推理过程进行端到端训练。广泛的实验表明，与强基线相比，该方法在下游任务中取得了持续的性能提升。

## Abstract
Agent-compiled knowledge bases provide persistent external knowledge for large language model (LLM) agents in open-ended, knowledge-intensive downstream tasks. Yet their quality is systematically limited by \emph{incompleteness}, \emph{incorrectness}, and \emph{redundancy}, manifested as missing evidence or cross-document links, low-confidence or imprecise claims, and ambiguous or coreference resolution issues. Such defects compound under iterative use, degrading retrieval fidelity and downstream task performance. We present \textbf{DeepRefine}, a general LLM-based reasoning model for \emph{agent-compiled knowledge refinement} that improves the quality of any pre-constructed knowledge bases with user queries to make it more suitable for the downstream tasks. DeepRefine performs multi-turn interactions with the knowledge base and conducts abductive diagnosis over interaction history, localizes likely defects, and executes targeted refinement actions for incremental knowledge base updates. To optimize refinement policies of DeepRefine without gold references, we introduce a Gain-Beyond-Draft (GBD) reward and train the reasoning process end-to-end via reinforcement learning. Extensive experiments demonstrate consistent downstream gains over strong baselines.

---

## 论文详细总结（自动生成）

这是一份关于论文《DeepRefine: Agent-Compiled Knowledge Refinement via Reinforcement Learning》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着大语言模型（LLM）智能体在知识密集型任务中的应用，由智能体自动编译生成的知识库（如 LLM-Wiki）成为重要的外部知识源。然而，这些知识库在构建过程中普遍存在**不完整性（缺失证据）、不准确性（错误断言）和冗余性（歧义或指代不清）**。
*   **研究动机**：现有的研究多关注如何优化知识库的“构建”过程，但对于已经部署的大规模知识库，重新构建成本极高且不切实际。
*   **核心问题**：如何在不重新构建的前提下，针对用户查询对现有知识库进行“后处理精炼”？这面临两个挑战：一是如何在海量数据中精确定位缺陷区域；二是在缺乏金标准（Golden Reference）的情况下如何优化精炼策略。

### 2. 方法论：核心思想与关键技术
DeepRefine 提出了一个基于强化学习（RL）的通用推理模型，其核心流程包含三个步骤：
1.  **可回答性判断循环（Answerability Judgement Loop）**：
    *   模型通过与知识库的多轮交互（子图扩展），判断当前检索到的知识是否足以回答用户查询。
    *   如果无法回答，则继续扩展检索范围，直到满足条件或达到最大跳数，从而锁定可能存在缺陷的局部邻域。
2.  **错误溯因（Error Abduction）**：
    *   基于交互历史，模型从不完整、错误和冗余三个维度分析导致无法回答的潜在原因。
3.  **精炼动作生成（Refinement Actions Generation）**：
    *   生成具体的编辑指令，包括 `insert_edge()`（补全）、`delete_edge()`（去错/去冗）和 `replace_node()`（消歧）。
4.  **策略优化（Policy Optimization）**：
    *   **GBD 奖励机制**：引入“超越草案增益”（Gain-Beyond-Draft）奖励，即：`精炼后回答准确度 - 精炼前回答准确度`。
    *   **算法**：采用 **GRPO（Group Relative Policy Optimization）** 算法进行端到端训练，使模型在没有标准答案参考的情况下，通过下游任务的反馈信号学习最优精炼策略。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：从 HotpotQA 中随机抽取 5,000 个样本。
    *   **评估集（OOD 场景）**：涵盖简单问答（NQ, PopQA）、多跳问答（2WikiMultihopQA, Musique）和长对话问答（LOCOMO）。
*   **基准（Benchmark）与对比方法**：
    *   **知识库构建器**：对比了 Naive（原生）、AR1（AutoGraph-R1 优化版）和 Graphify（智能体编译版）。
    *   **检索器**：对比了 Subgraph、ToG、HippoRAG 和 HippoRAG2。
    *   **对比维度**：原始知识库 vs. DeepRefine 精炼后的知识库。

### 4. 资源与算力
*   **硬件**：使用了 **8 张 NVIDIA A100-SXM4-80GB GPU**。
*   **模型架构**：以 Qwen3-8B 和 Qwen3-4B-Instruct 为骨干模型。
*   **训练细节**：
    *   4B 模型：Batch size 32，训练 60 步，学习率 5e-7。
    *   8B 模型：Batch size 64，训练 30 步，学习率 1e-7。
    *   使用 Qwen3-Embedding-0.6B 进行向量搜索，Qwen2.5-7B-Instruct 作为阅读器。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个不同的 OOD 数据集上进行了广泛测试，涵盖了多种知识库构建方法和检索算法的组合。
*   **消融实验**：专门对比了“有无强化学习（RL）”对精炼效果的影响，证明了 RL 训练是提升模型利用交互历史能力的关键。
*   **效率分析**：对比了 DeepRefine 与“重新构建知识库（AutoGraph-R1）”的时间消耗。
*   **客观性**：实验设置了严格的 OOD（分布外）环境，确保模型不是靠背诵训练集，而是学习到了通用的精炼逻辑。

### 6. 主要结论与发现
*   **持续增益**：DeepRefine 在几乎所有测试场景下都能稳定提升下游 RAG 任务的 F1 分数。
*   **优于重构**：在某些情况下，精炼后的原生知识库性能甚至超过了重新构建的优化知识库，且避免了重构可能引入的新噪声。
*   **高效性**：DeepRefine 的处理速度显著快于重构方法（如在多跳问答中，耗时仅为重构方法的约 1/3），因为它只关注与查询相关的局部区域。
*   **RL 的必要性**：未经 RL 优化的模型在处理冗余和复杂错误时表现疲软，而 GBD 奖励能显著增强模型的诊断深度。

### 7. 优点（亮点）
*   **首创性**：这是首个专门针对“智能体编译知识库”进行后处理精炼的通用推理模型。
*   **闭环反馈**：通过 GBD 奖励将知识库质量与下游任务表现直接挂钩，解决了缺乏精炼金标准的问题。
*   **通用性**：采用代码化的操作接口（Insert/Delete/Replace），使其可以适配任何类型的图数据库或结构化存储系统。

### 8. 不足与局限
*   **训练数据质量**：训练集仅基于 HotpotQA 的简单过滤，数据多样性仍有提升空间。
*   **系统稳定性**：虽然平均性能提升，但在极少数情况下仍可能出现性能微降，说明精炼动作有时会误删有用信息。
*   **应用限制**：目前主要针对基于三元组或图结构的知识库，对于纯非结构化文本块的精炼效果尚未深入探讨。

（完）
