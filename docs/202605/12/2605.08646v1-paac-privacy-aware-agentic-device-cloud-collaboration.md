---
title: "PAAC: Privacy-Aware Agentic Device-Cloud Collaboration"
title_zh: PAAC：隐私感知的智能体端云协作
authors: "Liangqi Yuan, Wenzhi Fang, Shiqiang Wang, Christopher G. Brinton"
date: 2026-05-09
pdf: "https://arxiv.org/pdf/2605.08646v1"
tags: ["query:malm"]
score: 9.0
evidence: 端云协作的隐私感知智能体框架
tldr: PAAC是一个隐私感知的端云协作智能体框架，旨在解决云端模型推理能力强但隐私泄露、端侧模型隐私性好但能力受限的矛盾。该框架将规划者-执行者架构与端云边界对齐，使云端在处理脱敏占位符的同时进行逻辑推理，而端侧负责敏感信息识别与任务执行。实验表明，PAAC在多个基准测试中显著提升了任务准确率并大幅降低了隐私泄露风险。
selection_source: fresh_fetch
motivation: 现有的端云协作方案在处理智能体任务时，难以在保持工具调用结构完整性的同时有效平衡云端推理能力与端侧隐私保护。
method: 通过将云端设为处理脱敏占位符的规划者，端侧设为执行具体任务并管理敏感信息的执行者，利用确定性注册表实现隐私数据的安全替换与还原。
result: "PAAC在隐私与准确率的权衡上达到最优，相比现有基准，准确率提升了15-36%，隐私泄露风险降低了2-6倍。"
conclusion: 该研究证明了通过角色专业化分工与端云边界对齐，可以构建出既具备强大推理能力又符合严格隐私要求的智能体系统。
---

## 摘要
大语言模型（LLM）智能体面临着一种结构性张力：云端智能体提供强大的推理能力但会暴露用户数据，而端侧智能体虽然保护了隐私，却以牺牲整体能力为代价。现有的端云设计将这一边界视为计算切分，而非适用于智能体工作负载的信任边界；且现有的脱敏器迫使开发者在策略灵活性与工具调用所需的结构保真度之间做出权衡。在这项工作中，我们开发了 PAAC，这是一个隐私感知的智能体框架，它将“规划者-执行者”分解与端云边界相对齐，使角色专业化本身成为一种隐私机制。云端智能体基于类型化的占位符标记进行推理，这些标记在丢弃敏感值内容的同时保留了其推理角色；而端侧智能体则负责识别敏感片段，并将每一步的执行结果提炼为简洁的关键发现。脱敏过程限制端侧 LLM 仅建议需要掩码的片段，而由确定性注册表执行所有的替换与还原操作，从而保持动作在端侧直接可执行。在严格隐私设置下的三个智能体基准测试中，PAAC 占据了隐私与准确率的帕累托前沿，相比最先进的端云基准方案，平均准确率提升了 15-36%，平均泄露量减少了 2-6 倍，且在固定实体分类法之外的隐私目标上优势最为显著。我们在涵盖数学、科学和金融等 10 个领域的另外 17 个基准测试中也发现了持续的改进。

## Abstract
Large language model (LLM) agents face a structural tension: cloud agents provide strong reasoning but expose user data, while on-device agents preserve privacy at the cost of overall capability. Existing device-cloud designs treat this boundary as a compute split rather than a trust boundary suited to agentic workloads, and existing sanitizers force a choice between policy flexibility and the structural fidelity tool calls require. In this work, we develop PAAC, a privacy-aware agentic framework that aligns planner--executor decomposition with the device-cloud boundary so that role specialization itself becomes the privacy mechanism. The cloud agent reasons over typed placeholder tokens that preserve each sensitive value's reasoning role while discarding its content, while the on-device agent identifies sensitive spans and distills each step's execution outcome into compact key findings. Sanitization confines the on-device LLM to proposing which spans to mask, while a deterministic registry performs all substitution and reversal, keeping actions directly executable on device. On three agentic benchmarks under strict privacy settings, PAAC dominates the Pareto frontier of privacy and accuracy, improving average accuracy by 15-36\% and reducing average leakage by 2-6$\times$ over state-of-the-art device-cloud baselines, with the largest margins on privacy targets outside fixed entity taxonomies. We find consistent improvements on 17 additional benchmarks spanning 10 domains, including math, science, and finance.

---

## 论文详细总结（自动生成）

### PAAC: 隐私感知的端云协作智能体框架总结

#### 1. 核心问题与整体含义
论文针对大语言模型（LLM）智能体在实际应用中的**结构性矛盾**展开研究：
*   **云端智能体**：推理和规划能力强，但处理用户私密数据（如邮件、财务记录）时存在严重的隐私泄露风险。
*   **端侧智能体**：隐私安全性高，但受限于计算资源，在复杂推理、多步规划及长上下文处理上表现较差。
*   **现有方案缺陷**：现有的端云协作设计多为简单的计算切分，且脱敏工具（Sanitizers）往往在“策略灵活性”与“工具调用所需的结构保真度”之间顾此失彼。

#### 2. 方法论
PAAC 提出了一种将**角色专业化分工与端云信任边界对齐**的架构，核心思想是“角色即隐私机制”：
*   **解耦架构（Decoupled Architecture）**：
    *   **云端（Reasoner）**：作为规划者，仅接触脱敏后的“类型化占位符”（如 `MONEY_1`, `ADDRESS_2`）。它负责高层逻辑推理和生成脱敏动作。
    *   **端侧（Executor/Judge）**：作为执行者和隐私守护者。负责识别敏感信息、执行工具调用、并将结果提炼为不含敏感内容的“关键发现”（Key Findings）。
*   **提案-验证-注册表脱敏机制**：
    *   **提案（Propose）**：端侧 LLM 仅建议哪些片段需要掩码。
    *   **验证（Verify）**：通过对齐检查确保脱敏后的文本可还原。
    *   **注册表（Registry）**：使用确定性的正则匹配（Regex）执行替换和逆向还原，确保工具调用的参数结构不被破坏，从而直接在端侧执行。
*   **共识终止机制**：云端根据逻辑进度、端侧根据执行结果质量，共同决定任务是否完成，解决了信息不对称导致的误判问题。

#### 3. 实验设计
*   **核心基准测试**：
    *   **$\tau^2$-Bench**（Airline 和 Retail 场景）：测试复杂业务规则下的多轮对话能力。
    *   **GAIA**：测试通用 AI 助手处理现实世界复杂任务的能力。
*   **跨领域验证**：在数学（GSM8K）、科学（SciBench）、金融（FinQA）、逻辑（CLUTRR）等 10 个领域的 17 个额外基准上进行了测试。
*   **对比方法**：
    *   单智能体基准（Qwen3-4B, Gemini 3 Flash）。
    *   端云协作基准：PAPILLON（基于重写）、PRISM（基于扰动）。
    *   脱敏基准：基于模式的替换（PBS）。
*   **隐私等级**：定义了 P0（无保护）到 P3（严格保护）四个等级，系统性评估隐私-准确率权衡。

#### 4. 资源与算力
*   **模型使用**：端侧使用 **Qwen3-4B-Instruct**，云端使用 **Gemini 3 Flash**。
*   **硬件环境**：端侧推理由 **NVIDIA A100 GPU** 上的 vLLM 框架驱动。
*   **训练时长**：论文未提及模型训练时长，因为 PAAC 主要是基于现成 LLM 的推理框架设计。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 21 个基准测试，实验组数非常充分。
*   **消融实验**：针对最大步数、智能体能力等级、共识终止机制、以及组件反射（Reflection）等进行了详尽的消融分析。
*   **客观性**：通过引入“隐私泄露率（Leakage Rate）”和“任务准确率（Accuracy）”双维度评估，并对比了帕累托前沿，实验设计客观且具有说服力。

#### 6. 主要结论与发现
*   **性能领先**：PAAC 在严格隐私设置下，准确率比现有端云基准提升 15-36%，隐私泄露降低 2-6 倍。
*   **独立增益**：架构解耦带来的性能提升与脱敏机制是独立的，即使在零保护（P0）下，PAAC 的架构也优于单智能体云端模型。
*   **鲁棒性**：在开放词汇（如非标准格式的订单号）识别上，PAAC 远优于基于规则的传统方法。
*   **成本稳定**：随着隐私策略收紧，PAAC 的 Token 消耗和准确率保持稳定，而基准方法通常会性能崩溃。

#### 7. 优点
*   **设计巧妙**：将复杂的隐私问题转化为端云角色分工问题，符合“最小权限原则”。
*   **结构保真**：通过确定性注册表解决了 LLM 脱敏容易破坏代码/工具调用语法的顽疾。
*   **高度灵活**：云端推理引擎（如 ReAct, Plan-and-Solve）可无缝替换，不影响隐私机制。

#### 8. 不足与局限
*   **工具环境依赖**：PAAC 假设端侧拥有完善的工具环境。如果某些任务必须依赖云端特有工具（且涉及敏感数据），该框架的优势会受限。
*   **知识隔离**：由于云端看不到具体实体（如具体地名），它无法利用其参数化知识库中关于该实体的特定信息（如某地的天气特征），只能进行通用逻辑推理。
*   **端侧开销**：虽然使用了小模型，但 4B 级别的模型在移动端实时运行仍有一定计算压力。

（完）
