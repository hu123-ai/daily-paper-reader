---
title: "PRISM: Generation-Time Detection and Mitigation of Secret Leakage in Multi-Agent LLM Pipelines"
title_zh: PRISM：多智能体大语言模型流水线中秘密泄露的生成时检测与缓解
authors: "Riya Tapwal, Abhishek Kumar, Carsten Maple"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10614v1"
tags: ["query:malm"]
score: 9.0
evidence: 多智能体大语言模型流水线中秘密泄露的检测与缓解
tldr: 针对多智能体大模型系统中敏感信息通过共享上下文传播导致的泄露风险（传播放大现象），本文提出PRISM防御框架。PRISM在生成过程中实时监测，结合词汇、结构及信息论等16种信号计算风险评分，通过识别熵坍缩等生成动力学变化，在令牌级别实现干预。实验表明，PRISM在保持高实用性的同时，实现了零泄露率，显著优于现有基准。
selection_source: fresh_fetch
motivation: 多智能体系统中敏感信息易在共享上下文中传播并被下游放大，而现有防御手段在实时性、准确性或延迟方面存在不足。
method: 提出PRISM框架，在解码每一步综合16种多维特征计算风险分值，利用熵坍缩和逻辑值集中等信号在秘密完全生成前进行分级干预。
result: "在包含2000个任务的对抗性基准测试中，PRISM达到了1.000的精确率和0%的任务级泄露率，F1分数达0.832，且保留了约89%的输出效用。"
conclusion: PRISM证明了通过监测生成过程中的动力学变化可以有效且实时地拦截秘密泄露，为多智能体系统的安全性提供了强有力的保障。
---

## 摘要
多智能体大语言模型（LLM）系统引入了一种安全风险，即一个智能体访问的敏感信息可能通过共享上下文传播，并在下游输出中重新出现，即使没有明确的对抗意图。我们将这种现象形式化为传播放大（propagation amplification），即随着敏感内容反复暴露给下游生成器，跨智能体边界的泄露风险会不断增加。现有的防御措施，包括基于提示词的保护、静态模式匹配和 LLM-as-judge 过滤，并非针对此场景设计：它们要么在生成后运行，要么主要依赖表面形式模式，或者在不建模生成过程本身的情况下增加了显著的延迟。为了解决这些问题，我们提出了 PRISM，这是一种实时防御机制，将凭据泄露视为生成过程中的序列风险累积问题。在每个解码步骤中，PRISM 结合了涵盖词汇、结构、信息论、行为和上下文特征的 16 种信号，生成一个校准后的风险评分，通过绿、黄、红风险区域实现逐 token 的干预。我们的核心观察是，凭据复现通常伴随着生成动力学的可测量转变，其特征是熵坍缩（entropy collapse）和 logit 集中度增加。当与标识符模式检测等文本结构线索相结合时，这些时间信号能在秘密被完全重构之前提供泄露的早期预警。在一个包含 13 个攻击类别、三个压力级别、涵盖 2,000 个任务的异构四智能体流水线对抗基准测试中，PRISM 实现了 F1 = 0.832、精确率 = 1.000 和召回率 = 0.712，同时在我们的基准测试中未产生观察到的泄露（任务级泄露率为 0.0%），并保持了 0.893 的输出效用。它显著优于最强的基准模型 Span Tagger，后者的 F1 为 0.719，任务级泄露率为 15.0%。

## Abstract
Multi-agent LLM systems introduce a security risk in which sensitive information accessed by one agent can propagate through shared context and reappear in downstream outputs, even without explicit adversarial intent. We formalise this phenomenon as propagation amplification, where leakage risk increases across agent boundaries as sensitive content is repeatedly exposed to downstream generators. Existing defences, including prompt-based safeguards, static pattern matching, and LLM-as-judge filtering, are not designed for this setting: they either operate after generation, rely primarily on surface-form patterns, or add substantial latency without modelling the generation process itself. To resolve these issues, we propose PRISM, a real-time defence that treats credential leakage as a sequential risk accumulation problem during generation. At each decoding step, PRISM combines 16 signals spanning lexical, structural, information-theoretic, behavioural, and contextual features into a calibrated risk score, enabling per-token intervention through green, yellow, and red risk zones. Our central observation is that credential reproduction is often preceded by a measurable shift in generation dynamics, characterised by entropy collapse and increasing logit concentration. When combined with text-structural cues such as identifier-pattern detection, these temporal signals provide an early warning of leakage before a secret is fully reconstructed. Across a 2,000-task adversarial benchmark covering 13 attack categories and three pressure levels in a heterogeneous four-agent pipeline, PRISM achieves F1 = 0.832 with precision = 1.000 and recall = 0.712, while producing no observed leakage on our benchmark (0.0% task-level leak rate) and preserving output utility of 0.893. It substantially outperforms the strongest baseline, Span Tagger, which achieves F1 = 0.719 with a 15.0% task-level leak rate.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **PRISM** 的防御框架，旨在解决多智能体大语言模型（LLM）流水线中敏感信息泄露的动态风险。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机：** 在多智能体系统中，智能体通过共享上下文进行协作。一旦某个智能体（如搜索智能体）获取了敏感信息（如 API 密钥），这些信息会进入共享上下文，并可能被下游智能体（如代码生成智能体）无意中复现并泄露给用户。
*   **核心概念：** 论文提出了**“传播放大”（Propagation Amplification）**现象，即泄露风险随流水线深度增加而累积。
*   **现有缺陷：** 传统的静态扫描器（如 TruffleHog）或生成后过滤（LLM-as-judge）要么无法处理 LLM 生成的自由文本，要么延迟过高，且无法在秘密被完全重构前进行拦截。

### 2. 方法论
*   **核心思想：** 将秘密泄露视为生成过程中的**序列风险累积问题**。PRISM 在 Token 生成的每一时刻进行实时监测，利用模型内部状态和文本特征进行早期预警。
*   **关键技术细节：**
    *   **多维特征提取（16 种信号）：** 涵盖信息论（熵、Logit 集中度）、结构化模式（标识符正则匹配）、行为信号（工具调用、风险轨迹）和上下文特征。
    *   **熵坍缩（Entropy Collapse）理论：** 论文通过定理证明，当模型从开放式生成转向确定性地复现一段记忆中的秘密字符串时，其 Token 级别的熵必然会大幅下降。这是 PRISM 实现早期拦截的理论基石。
    *   **风险评分与分级干预：**
        *   使用逻辑回归模型计算实时风险分值 $r_t = \sigma(w^\top f_t + b)$。
        *   **绿区（Green）：** 安全，正常输出。
        *   **黄区（Yellow）：** 疑似泄露，对当前 Token 进行掩码（Mask）处理。
        *   **红区（Red）：** 高危，立即终止生成并脱敏。
*   **算法流程：** 在每个解码步骤提取特征 -> 计算风险分 -> 根据阈值决定是继续生成、脱敏还是中断。

### 3. 实验设计
*   **数据集/场景：** 构建了一个包含 **2,000 个任务**的对抗性基准测试，涵盖 13 个攻击类别（如提示词注入、社会工程、配置审查等）。
*   **实验环境：** 采用异构四智能体流水线：Planner (LLaMA 3.1 8B)、Researcher (Gemma 2 9B)、Coder (Qwen 2.5 7B)、Executor (Mistral 7B)。
*   **对比方法（10 种）：** 包括静态扫描器（detect-secrets, TruffleHog）、PII 检测工具（Presidio）、监督学习模型（Span Tagger, GBT Classifier）以及基于提示词的防御和 LLM 裁判。

### 4. 资源与算力
*   **硬件设备：** 实验在 **NVIDIA DGX H200** 上运行。
*   **算力开销：** PRISM 的特征提取和评分基于 Python/NumPy 实现，每任务平均延迟仅为 **5.5 毫秒**，远低于基于 LLM 过滤的方法（后者可能需要数百秒）。
*   **训练细节：** 逻辑回归模型在 195 个任务的 Token 轨迹上进行训练，具有极高的参数效率。

### 5. 实验数量与充分性
*   **实验规模：** 2,000 组任务测试，包含 30,900 个秘密实例。
*   **消融实验：** 进行了时间信号 vs. 结构信号的消融、白盒 vs. 黑盒模式对比。
*   **鲁棒性测试：** 针对 8 种混淆手段（如 Base16 编码、ROT13、字符间插入空格等）进行了压力测试。
*   **客观性：** 训练集和测试集使用完全不同的秘密领域（医疗 vs. 电商），确保模型学习的是生成动力学特征而非死记硬背秘密内容。

### 6. 主要结论与发现
*   **零泄露率：** PRISM 是唯一在基准测试中实现 **0.0% 任务级泄露率**的方法，同时保持了 **89.3% 的输出效用**。
*   **性能领先：** F1 分数达到 0.832，显著优于最强基准 Span Tagger (F1=0.719, 泄露率 15%)。
*   **早期拦截：** 实验证明 PRISM 往往在秘密被完全拼凑出来之前就能通过熵坍缩信号触发拦截。
*   **抗混淆性：** 即使秘密被编码或格式转换，PRISM 的检测率仍保持在 95% 以上，而传统工具会迅速失效。

### 7. 优点
*   **实时性与低延迟：** 逐 Token 监控，适合流式输出场景。
*   **理论支撑：** 将安全问题转化为信息论中的熵分析，提供了较强的解释性。
*   **多智能体适配：** 专门针对共享上下文导致的“传播放大”风险设计，填补了该领域的空白。
*   **高鲁棒性：** 不依赖于秘密的具体表面形式，能有效对抗简单的混淆攻击。

### 8. 不足与局限
*   **白盒依赖：** 最强性能依赖于获取 Token 级别的 Logits（概率分布），在闭源 API（如 GPT-4）中应用受限（虽然论文提供了黑盒变体，但效果略有下降）。
*   **合成基准：** 实验基于合成的对抗基准，在真实生产环境（如极其复杂的企业级流水线）中的表现仍需进一步验证。
*   **高级对抗攻击：** 尚未测试针对熵信号的专门攻击（例如攻击者故意引入高熵噪声来掩盖泄露过程）。

（完）
