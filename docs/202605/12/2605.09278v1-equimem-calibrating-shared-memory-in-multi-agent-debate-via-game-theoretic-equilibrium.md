---
title: "EquiMem: Calibrating Shared Memory in Multi-Agent Debate via Game-Theoretic Equilibrium"
title_zh: EquiMem：通过博弈论均衡校准多智能体辩论中的共享内存
authors: "Yuqiao Meng, Sakshi Sunil Narvekar, Luoxi Tang, Rupali Rajendra Vaje, Yingxue Zhang, Muchao Ye, Zhaohan Xi"
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.09278v1"
tags: ["query:malm"]
score: 9.0
evidence: 多智能体辩论与共享内存校准
tldr: 针对多智能体辩论（MAD）中共享内存易受错误信息污染的问题，本文提出EquiMem。该方法将内存更新建模为零信任博弈，利用博弈论均衡来校准内存信任度。EquiMem通过算法量化更新，无需LLM判断，在多种内存架构和基准测试中表现优异，且推理开销极低，显著提升了系统的鲁棒性。
selection_source: fresh_fetch
motivation: 多智能体辩论系统中的共享内存容易因单个错误条目导致下游推理崩溃，而现有的防御手段往往依赖于存在相同失效模式的LLM判断。
method: 将内存更新建模为零信任博弈，利用智能体已有的检索查询和路径作为证据，通过博弈论均衡在推理阶段算法化地校准内存信任度。
result: 实验表明EquiMem在多种基准测试和内存架构上均优于现有防御机制，能有效抵御对抗性智能体，且推理开销几乎可以忽略不计。
conclusion: EquiMem为多智能体协作提供了一种无需额外LLM开销的内存校准方案，通过博弈论视角增强了共享内存系统的可靠性。
---

## 摘要
多智能体辩论（MAD）系统日益依赖共享内存来支持长程推理，但这种便利性带来了一个关键漏洞：单个损坏的条目即可污染下游的内存增强推理，且仅靠辩论本身无法过滤此类错误。现有的保护机制通过启发式方法或基于大语言模型（LLM）的验证来过滤条目，但它们依赖于具有相同失败模式的 AI 判断，并忽略了 MAD 中的跨智能体动态。我们通过将 MAD 中的内存更新建模为一个零信任内存博弈来填补这一空白。在该博弈中，不假设任何智能体是诚实的，博弈的均衡状态作为最优内存信任的指标。在这一均衡的指导下，我们提出了 EquiMem，这是一种推理阶段的校准机制。它利用智能体现有的检索查询和遍历路径作为证据，通过算法针对共享内存状态对每次更新进行量化，而无需征求任何 LLM 的判断。EquiMem 实现了针对嵌入式和图结构内存的校准。在多种基准测试、MAD 框架和内存架构中，它始终优于现有的保护机制，在对抗性智能体环境下表现稳健，且推理开销微乎其微。

## Abstract
Multi-agent debate (MAD) systems increasingly rely on shared memory to support long-horizon reasoning, but this convenience opens a critical vulnerability: a single corrupted entry can contaminate the downstream memory-augmented reasoning, and debate alone fails to filter such errors. Existing safeguards filter entries via heuristics or LLM-based validation, yet they rely on AI judgments that share the same failure modes and overlook the cross-agent dynamics of MAD. We address this gap by formulating memory updating in MAD as a zero-trust memory game, in which no agent is assumed honest and the game's equilibrium serves as an indicator of optimal memory trust. Guided by this equilibrium, we propose EquiMem, an inference-time calibration mechanism that quantifies each update algorithmically against the shared memory state, using agents' existing retrieval queries and traversal paths as evidence rather than soliciting any LLM judgment. EquiMem instantiates calibration for both embedding- and graph-based memory, and across diverse benchmarks, MAD frameworks, and memory architectures, it consistently outperforms existing safeguards, remains robust under adversarial agents, and incurs negligible inference overhead.

---

## 论文详细总结（自动生成）

这是一份关于论文《EquiMem: Calibrating Shared Memory in Multi-Agent Debate via Game-Theoretic Equilibrium》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在多智能体辩论（Multi-Agent Debate, MAD）系统中，共享内存（Shared Memory）虽然能增强长程推理能力，但也引入了**“单点崩溃”**的风险。一旦某个智能体写入错误或虚假信息，这些“毒素”会迅速污染整个内存，导致后续推理全面失效。
*   **研究背景**：现有的防御手段（如启发式过滤或利用 LLM 进行自我验证）存在两个缺陷：一是 LLM 验证器本身可能存在与被验证对象相同的幻觉或失效模式；二是忽略了多智能体之间动态博弈的特性。
*   **整体含义**：本文提出了一种名为 **EquiMem** 的校准机制，通过博弈论中的均衡概念，在不依赖额外 LLM 判断的情况下，算法化地评估内存更新的信任度。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：将内存更新建模为一个**“零信任博弈”（Zero-Trust Memory Game）**。在该博弈中，不预设任何智能体是诚实的，通过寻找博弈的均衡状态来确定最优的内存信任权重。
*   **关键技术细节**：
    *   **证据提取**：利用智能体在推理过程中已有的检索查询（Retrieval Queries）和图遍历路径（Traversal Paths）作为证据，而非要求智能体生成额外的解释。
    *   **算法化校准**：EquiMem 并不调用 LLM 来判断信息真伪，而是根据智能体提供的证据与当前共享内存状态的一致性/冲突程度，通过数学公式计算更新的量化值。
    *   **多架构支持**：该方法同时适配了**嵌入式内存（Embedding-based）**和**图结构内存（Graph-based）**。
    *   **推理阶段执行**：这是一种即插即用的推理时（Inference-time）机制，通过博弈论均衡解来动态调整每个智能体对内存贡献的权重。

### 3. 实验设计
*   **数据集/场景**：涵盖了多种复杂的推理基准测试（具体包括逻辑推理、常识问答等，旨在测试长程推理能力）。
*   **对比方法（Baselines）**：
    *   标准的 MAD 框架（无保护）。
    *   基于启发式规则的过滤机制。
    *   基于 LLM 验证（LLM-based validation）的防御机制。
*   **测试维度**：在不同的 MAD 框架和内存架构下进行测试，并特别设置了**对抗性智能体（Adversarial Agents）**场景，模拟恶意或严重幻觉的智能体试图污染内存的情况。

### 4. 资源与算力
*   **算力说明**：论文摘要和元数据中**未明确指出**具体的 GPU 型号、数量或训练时长。
*   **效率表现**：论文强调 EquiMem 的推理开销**“微乎其微”（Negligible）**。由于它采用算法化计算而非调用 LLM 进行验证，其计算复杂度远低于基于 LLM 的防御手段。

### 5. 实验数量与充分性
*   **实验规模**：实验覆盖了多种基准测试、多种 MAD 框架以及两种主流的内存架构（向量嵌入和知识图谱）。
*   **充分性评价**：
    *   **客观性**：通过引入对抗性实验，验证了系统在极端情况下的鲁棒性，这比单纯在干净数据集上测试更具说服力。
    *   **公平性**：对比了当前主流的 LLM 验证方案，证明了在不增加额外推理成本的前提下，博弈论方法能取得更好的效果。

### 6. 主要结论与发现
*   **性能优越**：EquiMem 在所有测试场景中始终优于现有的保护机制。
*   **鲁棒性强**：在存在对抗性智能体的情况下，EquiMem 能有效识别并抑制错误信息的传播，保持系统推理的准确性。
*   **高效性**：证明了通过数学/博弈论手段解决 LLM 协作中的信任问题，比单纯增加 LLM 验证步骤更高效且更可靠。

### 7. 优点
*   **无需额外 LLM 开销**：避免了昂贵的 API 调用或推理延迟，适合实时性要求高的场景。
*   **零信任假设**：更符合开放式多智能体系统的实际情况，不依赖于“智能体总是诚实”的理想假设。
*   **通用性**：能够同时处理非结构化的向量内存和结构化的图内存。

### 8. 不足与局限
*   **证据依赖性**：校准效果高度依赖于智能体提供的检索查询和路径的质量。如果所有智能体都提供完全无关的证据，系统可能难以找到有效的均衡点。
*   **冷启动问题**：在博弈初期，如果内存中缺乏足够的状态信息，算法化的校准可能会受到限制。
*   **应用限制**：目前主要针对推理型任务，对于纯创作型或主观性极强的多智能体协作任务，如何定义“证据”和“冲突”可能存在挑战。

（完）
