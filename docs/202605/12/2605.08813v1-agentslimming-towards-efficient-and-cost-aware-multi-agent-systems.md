---
title: "AgentSlimming: Towards Efficient and Cost-Aware Multi-Agent Systems"
title_zh: AgentSlimming：迈向高效且具备成本意识的多智能体系统
authors: "Yulang Chen, Haoxuan Peng, Jinyan Liu, Zichen Wen, Dongrui Liu, Linfeng Zhang"
date: 2026-05-09
pdf: "https://arxiv.org/pdf/2605.08813v1"
tags: ["query:malm"]
score: 9.0
evidence: 多智能体工作流的压缩框架
tldr: "针对大语言模型多智能体系统（MAS）中存在的结构冗余和高昂Token成本问题，本文提出了AgentSlimming框架。该框架借鉴神经网络的剪枝与量化思想，通过混合机制评估智能体重要性，并执行冗余剔除或低成本模型替换。实验证明，该方法在保持甚至提升性能的同时，最高可降低78.9%的Token消耗，实现了成本与质量的帕累托最优平衡。"
selection_source: fresh_fetch
motivation: 现有的多智能体系统设计往往导致结构臃肿和冗余，造成极高的计算成本和Token消耗。
method: 提出一种即插即用的压缩框架，通过混合机制评估智能体重要性，并结合剪枝和量化策略优化工作流。
result: "在多个任务中实现了高达78.9%的Token成本削减，且性能几乎无损，甚至在某些情况下有所提升。"
conclusion: AgentSlimming为构建高效、低成本的多智能体系统提供了一种有效的自动化压缩方案，达到了性能与成本的理想平衡。
---

## 摘要
基于大语言模型的多智能体系统（MAS）在处理复杂任务方面展现了卓越的能力。然而，手动设计最优通信拓扑非常耗费人力，而自动扩展方法往往会导致结构臃肿并产生冗余智能体，从而导致过度的 Token 消耗。为了解决这一问题，我们提出了 AgentSlimming，这是一种针对图结构多智能体工作流的即插即用压缩框架。受神经网络剪枝和量化的启发，AgentSlimming 通过混合机制首先评估每个智能体的重要性得分，然后移除冗余智能体或将其替换为低成本智能体，其中每项操作都使用基于基准锚定的接受规则进行验证，以防止性能崩溃。实验表明，AgentSlimming 在性能下降微乎其微的情况下，可将平均 Token 成本降低高达 78.9%，有时甚至能提高准确率，在成本与质量之间实现了强大的帕累托最优权衡。我们的代码已在 https://github.com/CitrusYL/AgentSlimming 公开。

## Abstract
Large Language Model-based Multi-Agent Systems (MAS) have demonstrated remarkable capabilities in complex tasks. However, manually designing optimal communication topologies is labor-intensive, while automated expansion methods often result in bloated structures with redundant agents, leading to excessive token consumption. To address this problem, we introduce \textbf{AgentSlimming}, a plug-and-play compression framework for graph-structured multi-agent workflows. Motivated by pruning and quantization in neural networks, AgentSlimming compresses workflows by first estimating the importance score of each agent with a hybrid mechanism, and then removes redundant agents or replaces them with low-cost ones, where each operation is validated using a baseline-anchored acceptance rule to prevent performance collapse. Experiments show that AgentSlimming reduces average token cost by up to 78.9\% with negligible performance degradation, and sometimes even improves accuracy, achieving a strong Pareto-optimal trade-off between cost and quality. \textit{Our code is publicly available at https://github.com/CitrusYL/AgentSlimming

---

## 论文详细总结（自动生成）

以下是对论文《AgentSlimming: Towards Efficient and Cost-Aware Multi-Agent Systems》的结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前基于大语言模型（LLM）的多智能体系统（MAS）虽然性能强大，但存在严重的**效率与成本问题**。手动设计的工作流耗时耗力，而自动生成的工作流（如 AFlow）往往结构臃肿，包含大量冗余智能体和密集的通信路径，且所有节点通常统一使用昂贵的模型（如 GPT-4），导致 Token 消耗呈二次方增长，难以大规模应用。
*   **整体含义**：论文提出了 **AgentSlimming**，这是首个针对图结构多智能体工作流的即插即用压缩框架。它借鉴了神经网络中的“剪枝”和“量化”概念，旨在在保持系统性能的同时，大幅降低推理成本，实现成本与质量的帕累托最优（Pareto-optimal）。

### 2. 方法论：核心思想与关键技术
AgentSlimming 包含三个主要阶段，通过一个**混合重要性评估机制**来指导优化：
*   **混合重要性评估**：结合了拓扑结构和功能贡献四个指标：
    1.  **度中心性（Degree Centrality）**：评估节点的局部连接强度。
    2.  **介数中心性（Betweenness Centrality）**：评估节点作为信息桥梁的重要性。
    3.  **近似 Shapley 值**：通过“留一法”（Leave-One-Out, LOO）估计移除该节点对整体性能的影响。
    4.  **Delta-Cost 信号**：评估移除该节点能节省的经济成本。
    *   **融合策略**：使用**倒数排名融合（RRF）**将上述异构信号整合为统一的重要性得分。
*   **核心优化流程**：
    1.  **AgentPruner（结构剪枝）**：根据重要性得分，迭代移除最不重要的智能体节点，并通过边缘修补（Edge Patching）保持图的连通性。
    2.  **AgentQuant（语义量化）**：将剩余低重要性节点中的高成本模型替换为低成本模型（如从 GPT-4.1-mini 替换为 GPT-4.1-nano）。
    3.  **AgentTuner（自适应微调）**：可选阶段，利用蒙特卡洛树搜索（MCTS）对压缩后的工作流进行局部微调（如提示词优化），以补偿性能损失。
*   **验证机制**：每一步操作都设有基于基准锚定的接受规则（阈值 $\tau$），若性能下降超过容忍度则触发回滚。

### 3. 实验设计
*   **数据集**：涵盖 8 个公开基准测试，分为标准任务（GSM8K, MBPP, HotpotQA, DROP, MATH）和高难度任务（AIME, MuSiQueAns, LiveCode）。
*   **对比方法**：
    *   手动提示策略：CoT, SC-CoT, Self-Refine, LLM-Debate。
    *   自动化工作流优化：ADAS, AFlow。
    *   成本意识变体：Cost-aware AFlow。
*   **模型使用**：主要使用 OpenAI 的 GPT-4.1 系列（mini 作为高性能模型，nano 作为低成本模型），并在消融实验中使用了 Qwen3 系列。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出具体的 GPU 型号或训练时长**，因为该框架是**无需训练（Training-free）**的。
*   **成本消耗**：实验主要通过调用 OpenAI API 完成。论文详细记录了每个问题的平均 API 成本（美元/题），并提供了“盈亏平衡分析”，计算了通过一次性搜索成本换取长期推理节省所需的调用次数。

### 5. 实验数量与充分性
*   **实验规模**：在 8 个不同领域的任务上进行了广泛测试，实验设计较为全面。
*   **充分性与客观性**：
    *   进行了**消融实验**，验证了剪枝与量化顺序的影响，以及不同重要性指标（RRF vs 单一指标）的有效性。
    *   进行了**泛化性验证**，包括跨模型架构（Qwen3）和跨数据集（GSM8K 与 MATH 互换结构）的测试。
    *   实验设置遵循了 AFlow 的基准配置，确保了对比的公平性。

### 6. 主要结论与发现
*   **显著降本**：在保持性能的前提下，AgentSlimming 最高可降低 **78.9%** 的 Token 成本。
*   **性能保持甚至提升**：在某些任务（如 MBPP 和 LiveCode）中，剪枝冗余节点反而减少了噪声，导致准确率提升。
*   **帕累托改进**：AgentSlimming 成功找到了比现有自动化方法更优的成本-质量平衡点。
*   **结构洞察**：实验发现，负责执行验证和聚合的节点（如 Test, ScEnsembler）通常适合量化，而核心生成节点则需保持高精度。

### 7. 优点
*   **即插即用**：不依赖特定模型或 MAS 框架，具有很强的通用性。
*   **无需训练**：通过结构优化而非参数更新实现压缩，极大地降低了优化门槛。
*   **多维评估**：结合了图论指标与博弈论（Shapley 值），比单一的启发式剪枝更科学。
*   **语义量化概念**：创新性地将模型替换类比为神经网络量化，为 MAS 优化提供了新视角。

### 8. 不足与局限
*   **实验覆盖**：虽然测试了 GPT 和 Qwen 系列，但尚未在最新的顶级模型（如 GPT-4o 或 Claude 3.5）上进行全面验证。
*   **粒度限制**：目前的优化是在任务级别（Task-level）进行的，尚未实现针对单个输入实例（Instance-level）的动态工作流调整。
*   **安全风险**：剪枝和替换节点可能会改变工作流的不可预测行为，在安全敏感领域（如医疗、法律）应用时需谨慎评估。
*   **搜索开销**：虽然推理成本降低了，但初期的结构搜索和重要性评估仍需一定的 API 调用成本。

（完）
