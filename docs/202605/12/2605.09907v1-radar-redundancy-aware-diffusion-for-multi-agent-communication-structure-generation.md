---
title: "RADAR: Redundancy-Aware Diffusion for Multi-Agent Communication Structure Generation"
title_zh: RADAR：面向多智能体通信结构生成的冗余感知扩散模型
authors: "Zhen Zhang, Wanjing Zhou, Juncheng Li, Hao Fei, Jun Wen, Wei Ji"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.09907v1"
tags: ["query:malm"]
score: 10.0
evidence: 多智能体系统通信结构生成
tldr: 针对大语言模型多智能体系统中通信拓扑固定或生成方式单一导致的效率与性能瓶颈，本文提出RADAR框架。该框架利用条件离散图扩散模型，根据查询需求逐步生成通信结构，并引入冗余感知机制以降低通信开销。实验证明，RADAR在提高任务准确率的同时显著减少了Token消耗，增强了系统在复杂场景下的鲁棒性。
selection_source: fresh_fetch
motivation: 现有多智能体系统通信拓扑往往固定或单步生成，难以兼顾简单任务的效率与复杂任务的性能。
method: 提出一种基于条件离散图扩散模型的生成框架，通过图的有效规模引导，实现查询自适应的通信结构逐步构建。
result: 在六个基准测试中，RADAR在准确率、Token消耗和鲁棒性方面均优于现有基准方法。
conclusion: RADAR通过冗余感知的扩散生成机制，为多智能体系统提供了一种高效且灵活的通信拓扑优化方案。
---

## 摘要
与单个智能体相比，基于大语言模型的多智能体系统在代码生成、数学推理和规划等多种任务中持续展现出强大的能力。尽管表现出色，但这些系统的有效性和鲁棒性在很大程度上依赖于其通信拓扑，而这种拓扑通常是固定的或通过单步生成的。这限制了细粒度的结构探索和灵活组合，导致在简单任务中消耗过多的 token，而在复杂任务中则限制了能力的发挥。为了缓解这一挑战，我们提出了 RADAR，这是一个冗余感知且查询自适应的生成框架，能够主动减少通信开销。受条件离散图扩散模型最新进展的启发，我们将通信拓扑设计建模为一个由图的有效规模引导的逐步生成过程。在六个基准测试上的全面实验表明，RADAR 始终优于近期的基准模型，在不同场景下实现了更高的准确率、更低的 token 消耗和更强的鲁棒性。我们的代码和数据可在 https://github.com/cszhangzhen/RADAR 获取。

## Abstract
Compared with individual agents, large language model based multi-agent systems have shown great capabilities consistently across diverse tasks, including code generation, mathematical reasoning, and planning, etc. Despite their impressive performance, the effectiveness and robustness of these systems heavily rely on their communication topology, which is often fixed or generated in a single step. This restricts fine-grained structural exploration and flexible composition, resulting in excessive token utilization on simple tasks while limiting capability on complicated tasks. To mitigate this challenge, we introduce RADAR, a redundancy-aware and query-adaptive generative framework that actively reduce communication overhead. Motivated by recent progress in conditional discrete graph diffusion models, we formulate communication topology design as a step-by-step generation process, guided by the effective size of the graph. Comprehensive experiments on six benchmarks demonstrate that RADAR consistently outperforms recent baselines, achieving higher accuracy, lower token consumption, and greater robustness across diverse scenarios. Our code and data are available at https://github.com/cszhangzhen/RADAR.

---

## 论文详细总结（自动生成）

这是一份关于论文《RADAR: Redundancy-Aware Diffusion for Multi-Agent Communication Structure Generation》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：基于大语言模型（LLM）的多智能体系统（MAS）的性能高度依赖于其**通信拓扑结构**。然而，现有的拓扑设计要么是静态手工制定的（如链式、星型），要么是单步生成的。
*   **研究动机**：
    *   **灵活性不足**：固定结构无法适应不同难度的任务（简单任务浪费Token，复杂任务能力受限）。
    *   **冗余开销**：复杂的交互结构虽然表达力强，但往往引入大量重复信息，导致Token消耗激增。
    *   **生成粒度粗**：单步生成图结构难以捕捉智能体之间细粒度的协作依赖。

### 2. 方法论
RADAR 提出了一种**冗余感知且查询自适应**的迭代生成框架，将通信拓扑设计建模为离散图扩散过程。
*   **核心思想**：利用图论中的“有效规模（Effective Size）”概念来量化冗余，并通过扩散模型逐步构建非冗余的通信图。
*   **关键技术细节**：
    *   **有效规模（Effective Size, ES）**：借鉴社会网络理论，定义节点 $v_k$ 的有效规模 $\phi(v_k)$，衡量其连接中非重叠信息的比例。公式结合了入度有效性和出度有效性，用于引导生成过程。
    *   **前向扩散过程**：通过一个“排序网络（Ordering Network）”根据有效规模优先级逐步掩码（Mask）节点及其连接。
    *   **反向去噪过程**：利用“去噪网络（Denoising Network）”在给定任务查询 $Q$ 的条件下，从空图开始逐步恢复节点角色和边缘连接。
    *   **优化策略**：结合强化学习（REINFORCE 算法）优化排序网络，并利用策略梯度（Policy Gradient）根据下游任务的实际效用（如准确率）直接优化去噪网络。

### 3. 实验设计
*   **数据集/场景**：涵盖三大类共 6 个基准测试：
    *   **通用推理**：MMLU。
    *   **数学解题**：GSM8K, MultiArith, SVAMP, AQuA。
    *   **代码生成**：HumanEval。
*   **对比方法**：
    *   **单智能体**：CoT, ComplexCoT, Self-Consistency (SC)。
    *   **固定/半自动 MAS**：LLM-Debate, AgentVerse, DyLAN, MacNet 等。
    *   **自动设计 MAS（SOTA）**：GPTSwarm, G-Designer, Agent-Prune, MaAS, ARG-Designer 等。

### 4. 资源与算力
*   **模型基础**：主要实验基于 `gpt-4o-mini`。
*   **算力细节**：
    *   **训练时长**：在 GSM8K 数据集上，训练时间约为 **2小时10分钟**。
    *   **推理时长**：单次推理（含图生成）约为 **17.55分钟**（针对整个测试集）。
    *   **网络参数**：去噪网络和排序网络均采用轻量级的 3 层图神经网络（RGCN/GAT），隐藏层维度仅为 32。
    *   **嵌入模型**：使用 `all-MiniLM-L6-v2` 生成查询向量（384维）。

### 5. 实验数量与充分性
*   **实验规模**：在 6 个数据集上进行了全面测试，每个任务采样 50 个查询进行训练。
*   **消融实验**：针对有效规模（ES）、任务效用损失、查询输入、排序/去噪网络架构等进行了多组对比。
*   **鲁棒性测试**：模拟了“系统提示词攻击”（将部分智能体变为“骗子”），验证系统在错误信息干扰下的稳定性。
*   **充分性评价**：实验设计较为全面，不仅关注准确率，还深入分析了 Token 消耗、图结构分布（密度、有效规模）以及不同 LLM 后座（Llama-3.1, Qwen3, DeepSeek-R1）的迁移能力，结果客观公平。

### 6. 主要结论与发现
*   **性能领先**：RADAR 在所有基准测试中均取得最优结果，平均性能优于最强基准（ARG-Designer）约 1.75%，相比单智能体提升显著。
*   **Token 高效**：在 GSM8K 上，RADAR 的 Token 消耗仅为 G-Designer 的一半左右，实现了极佳的性能-成本平衡。
*   **强鲁棒性**：在遭受提示词攻击时，RADAR 的性能几乎不下降，而完整图结构等基准方法性能大幅下滑。
*   **自适应性**：RADAR 生成的图结构随任务难度动态变化，简单任务使用较少智能体，复杂任务则激活更多协作路径。

### 7. 优点
*   **范式创新**：将图扩散模型引入 MAS 拓扑生成，实现了从“单步生成”到“迭代精修”的转变。
*   **冗余控制**：显式引入“有效规模”指标，从结构层面解决了多智能体通信中的“废话”问题。
*   **任务感知**：生成的拓扑结构高度依赖于具体 Query，实现了真正的“按需分配”计算资源。

### 8. 不足与局限
*   **推理延迟**：由于需要为每个查询实时生成拓扑结构，其推理时间略高于预定义结构的 MAS 方法。
*   **智能体规模限制**：实验中最大智能体数量固定为 5 个，虽然 RADAR 可以选择不激活某些节点，但在超大规模智能体集群（如数百个）中的扩展性尚未验证。
*   **依赖训练数据**：扩散模型的质量依赖于初始构建的基准拓扑数据集，若初始样本多样性不足，可能限制生成结构的创新性。

（完）
