---
title: "EvoMAS: Learning Execution-Time Workflows for Multi-Agent Systems"
title_zh: EvoMAS：为多智能体系统学习执行时工作流
authors: "Chengdong Xu, Kaiqiang Ke, Ziheng Liu, Jiaqi Wei, Zibo Shao, Weile Guo, Chao Yu"
date: 2026-05-09
pdf: "https://arxiv.org/pdf/2605.08769v1"
tags: ["query:malm"]
score: 10.0
evidence: 多智能体系统的执行时工作流
tldr: 针对大模型多智能体系统在长程任务中静态工作流难以适应动态需求的问题，本文提出EvoMAS框架。该框架将工作流构建视为元级序列决策问题，通过“规划-评估-更新”流水线构建任务状态，并利用学习到的工作流适配器在执行过程中动态实例化特定阶段的智能体协作模式。实验证明，EvoMAS在GAIA等复杂基准测试中优于现有静态及自动化设计方法，能有效根据任务演进调整协作策略。
selection_source: fresh_fetch
motivation: 现有的多智能体系统多采用静态或一次性优化的工作流，难以应对长程任务中不断变化的子目标和信息需求。
method: 提出一种执行时动态构建工作流的框架，通过显式任务状态建模和基于策略梯度训练的适配器，从候选池中动态选择并组织智能体。
result: 在GAIA、HLE和DeepResearcher等多个复杂任务基准上，EvoMAS的性能显著超过了单智能体基线和最新的自动化多智能体设计方法。
conclusion: 显式的任务状态构建与学习到的动态工作流适配具有互补作用，使系统能够随任务演进灵活调整智能体间的协作。
---

## 摘要
基于大语言模型（LLM）的多智能体系统通过智能体专业化、工具使用和协作推理，在复杂任务中展现出强大潜力。然而，大多数自动化的多智能体系统设计方法仍遵循“单次（one-shot）”范式：在执行前优化或选择工作流，并在整个任务过程中保持不变地重复使用。这种静态协作策略不适用于长程任务，因为这类任务的子目标、中间证据和信息需求会随着多个执行阶段而演变。我们提出了 EvoMAS，一个用于执行时多智能体工作流构建的框架。EvoMAS 将工作流构建建模为沿单一任务轨迹的元级序列决策问题。在每个阶段，它通过“规划器-评估器-更新器（Planner-Evaluator-Updater）”流水线构建显式的任务状态，并使用学习到的工作流适配器（Workflow Adapter）从固定的候选智能体池中实例化特定阶段的分层工作流。该适配器使用策略梯度进行训练，以稀疏且可验证的终端任务成功作为主要监督信号，同时在极难的稀疏奖励设置下单独分析了基于评估器的过程奖励。在 GAIA、HLE 和 DeepResearcher 上的实验表明，EvoMAS 优于单智能体基准和最近的自动化多智能体工作流设计方法。我们的分析进一步表明，显式任务状态构建和学习到的工作流适配提供了互补的优势。补充结果表明，当终端成功极其稀疏时，过程奖励最为有用；定性案例研究说明了 EvoMAS 能够随着任务状态的演变而调整智能体协作。

## Abstract
Large language model (LLM)-based multi-agent systems have shown strong potential on complex tasks through agent specialization, tool use, and collaborative reasoning. However, most automated multi-agent system design methods still follow a one-shot paradigm: a workflow is optimized or selected before execution and then reused unchanged throughout the task. This static coordination strategy is ill-suited for long-horizon tasks whose subgoals, intermediate evidence, and information needs evolve over multiple execution stages. We propose EvoMAS, a framework for execution-time multi-agent workflow construction. EvoMAS formulates workflow construction as a meta-level sequential decision problem along a single task trajectory. At each stage, it constructs an explicit task state through a Planner-Evaluator-Updater pipeline and uses a learned Workflow Adapter to instantiate a stage-specific layered workflow from a fixed pool of candidate agents. The adapter is trained with policy gradients using sparse, verifiable terminal task success as the main supervision signal, while evaluator-based process reward is analyzed separately under very-hard sparse-reward settings. Experiments on GAIA, HLE, and DeepResearcher show that EvoMAS outperforms single-agent baselines and recent automated multi-agent workflow design methods. Our analyses further show that explicit task-state construction and learned workflow adaptation provide complementary benefits. Additional results indicate that process reward is most useful when terminal success is extremely sparse, and qualitative case studies illustrate that EvoMAS adapts agent coordination as the task state evolves.

---

## 论文详细总结（自动生成）

以下是对论文《EvoMAS: Learning Execution-Time Workflows for Multi-Agent Systems》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的基于大语言模型（LLM）的多智能体系统（MAS）大多遵循“一次性（one-shot）”设计范式，即在任务执行前确定工作流（Workflow），并在执行过程中保持不变。
*   **研究动机**：长程任务（Long-horizon tasks）具有动态演进的特征，其子目标、中间证据和信息需求会随执行阶段而变化。静态的协作策略难以应对这种复杂性，因此需要一种能够在执行过程中根据当前任务状态动态调整智能体协作模式的机制。

### 2. 方法论：核心思想与关键技术
EvoMAS 将多智能体工作流的构建重新定义为一个**元级序列决策问题（Meta-level Sequential Decision Problem）**。
*   **核心思想**：在任务执行的每一个元步骤（Meta-step），系统根据当前的任务状态动态选择并组织智能体，形成特定阶段的工作流。
*   **关键技术组件**：
    1.  **任务状态构建（Planner-Evaluator-Updater）**：
        *   **Planner**：维护子任务计划，识别当前活跃子任务。
        *   **Evaluator**：评估上一步的执行结果，提供成功/失败/需精炼的判定及反馈。
        *   **Updater**：整合计划、评估反馈和执行记录，更新显式的结构化任务状态。
    2.  **工作流适配器（Workflow Adapter）**：
        *   基于当前任务状态的向量表示，通过交叉注意力机制（Cross-Attention）对候选智能体池（如搜索、推理、纠错等智能体）进行评分。
        *   采用累积概率采样（Cumulative-mass soft sampling）从池中选出智能体，构建多层有向无环图（DAG）工作流。
    3.  **策略优化**：
        *   使用强化学习中的 **REINFORCE 算法** 进行训练。
        *   **奖励信号**：主要使用可验证的终端任务成功（Terminal Success）作为稀疏奖励；在极难场景下引入基于评估器的过程奖励（PRM）辅助信用分配。

### 3. 实验设计
*   **数据集/场景**：
    *   **GAIA (Level 1 & 2)**：评估多步工具使用能力。
    *   **HLE (Humanity's Last Exam)**：评估知识密集型推理。
    *   **DeepResearcher**：评估研究型问题解决能力。
    *   **GAIA Level 3 & BrowserComp**：作为极难（Very-hard）诊断场景，分析稀疏奖励下的表现。
*   **对比方法**：
    *   **单智能体基线**：GPT-4o, GPT-4o-mini。
    *   **自动化 MAS 基线**：GPTSwarm, AFlow, G-Designer, MaAS（这些方法多为执行前优化静态工作流）。
*   **智能体池配置**：对比了 4 个智能体（EvoMAS-4）和 7 个智能体（EvoMAS-7）的候选池效果。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的本地硬件算力（如 GPU 型号、数量或具体训练时长）。
*   **模型调用**：实验主要基于 OpenAI 的 API 进行，使用了 `gpt-4o-mini` 和 `gpt-4o` 作为 Planner、Evaluator 和执行智能体的骨干模型（Backbone）。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个主流基准上进行了主实验，并在 2 个极难基准上进行了诊断分析。
*   **消融实验**：
    *   验证了显式任务状态构建的独立贡献。
    *   验证了工作流适配器在稀疏奖励下的学习曲线。
    *   分析了过程奖励（PRM）在不同难度任务中的必要性。
    *   测试了不同骨干模型（Backbone）对系统性能的影响。
*   **充分性评价**：实验设计较为全面，涵盖了从基础性能到深层机制分析的多个维度。通过对比多种最新的自动化 MAS 方法，证明了其“执行时动态适配”范式的优越性，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **性能领先**：EvoMAS-7 在所有基准上均显著优于单智能体和静态自动化 MAS 方法（平均得分 65.0 vs 最强基线 34.8）。
*   **动态适配的价值**：显式任务状态构建和学习到的工作流适配具有互补性。系统能随任务演进从“信息获取”转向“验证”和“聚合”。
*   **奖励信号的选择**：对于标准任务，稀疏的终端奖励已足够；但在终端成功率极低的极难任务中，过程奖励（PRM）是实现训练突破的关键。
*   **可扩展性**：更强的骨干模型能进一步提升 EvoMAS 的表现，显示出良好的正交性。

### 7. 优点（亮点）
*   **范式突破**：打破了预定义或预优化工作流的限制，实现了真正的执行时（Execution-time）动态协作。
*   **闭环学习**：通过强化学习使系统具备了从失败中学习并优化协作结构的能力。
*   **状态管理**：引入 Planner-Evaluator-Updater 流水线，解决了长程任务中上下文丢失和进度追踪难的问题。

### 8. 不足与局限
*   **候选池限制**：目前仍依赖于预定义的固定智能体池，尚未实现智能体的动态创建或开放式增长。
*   **搜索空间**：工作流的最大深度和层级结构是预设的，缺乏运行时的深度自适应调整。
*   **评估成本**：由于涉及多轮迭代和多个智能体协作，其推理成本和 API 调用次数可能显著高于单智能体方法（尽管论文提到在相同预算下对比，但实际部署成本仍需考虑）。
*   **偏差风险**：系统性能高度依赖于 Evaluator 的准确性，若 Evaluator 产生幻觉，可能导致任务状态更新错误。

（完）
