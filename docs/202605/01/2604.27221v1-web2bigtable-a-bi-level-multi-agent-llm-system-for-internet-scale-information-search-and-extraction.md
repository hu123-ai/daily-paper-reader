---
title: "Web2BigTable: A Bi-Level Multi-Agent LLM System for Internet-Scale Information Search and Extraction"
title_zh: Web2BigTable：一种用于互联网规模信息搜索与提取的双层多智能体大语言模型系统
authors: "Yuxuan Huang, Yihang Chen, Zhiyuan He, Yuxiang Chen, Ka Yiu Lee, Huichi Zhou, Weilin Luo, Meng Fang, Jun Wang"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.27221v1"
tags: ["query:malm"]
score: 10.0
evidence: 用于信息搜索和提取的双层多智能体大语言模型系统
tldr: Web2BigTable是一个双层多智能体框架，旨在解决互联网规模下的深度推理和广度结构化信息提取难题。它通过上层协调器分解任务与下层执行器并行处理，结合闭环验证、外部存储和共享工作区，实现了高效的搜索与数据聚合。该系统在WideSearch和XBench-DeepSearch基准测试中均取得SOTA性能，显著提升了复杂Web搜索任务的成功率和准确性。
selection_source: fresh_fetch
motivation: 现有Web搜索智能体在处理需要跨实体一致性的广度聚合任务和长路径深度推理任务时表现不佳。
method: 提出一种双层架构，利用上层协调器分解任务，下层智能体通过共享工作区和闭环验证机制并行执行并动态优化。
result: "在WideSearch基准上，成功率达到38.50%（是次优系统的7.5倍），并在深度搜索任务中达到73.0%的准确率。"
conclusion: Web2BigTable通过多智能体协同和自我演化机制，有效统一了互联网规模下的深度搜索与广度提取需求。
---

## 摘要
智能体网络搜索日益面临两种截然不同的需求：针对单一目标的深度推理，以及跨多个实体和异构来源的结构化聚合。当前系统在这两个方面都面临挑战。面向广度的任务要求输出符合模式、具有广泛覆盖范围和跨实体一致性，而面向深度的任务则需要对长且具有分支的搜索轨迹进行连贯推理。我们介绍了 Web2BigTable，这是一个支持上述两种模式的“网络到表格”搜索多智能体框架。Web2BigTable 采用双层架构，其中上层编排器将任务分解为子问题，下层工作智能体并行解决这些问题。通过“运行-验证-反思”的闭环过程，该框架利用持久且人类可读的外部存储器，随着时间的推移共同改进任务分解和执行，并对每个单一智能体进行自我进化更新。在执行过程中，工作智能体通过共享工作区进行协作，使部分发现可见，从而减少冗余探索、调和冲突证据，并适应新出现的覆盖缺口。Web2BigTable 在 WideSearch 上刷新了最先进的性能记录，Avg@4 成功率达到 38.50（是排名第二的 5.10 的 7.5 倍），行 F1 分数为 63.53（比第二名高出 25.03），项 F1 分数为 80.12（比第二名高出 14.42）。它还泛化到了 XBench-DeepSearch 上的深度导向搜索，达到了 73.0 的准确率。代码可在 https://github.com/web2bigtable/web2bigtable 获取。

## Abstract
Agentic web search increasingly faces two distinct demands: deep reasoning over a single target, and structured aggregation across many entities and heterogeneous sources. Current systems struggle on both fronts. Breadth-oriented tasks demand schema-aligned outputs with wide coverage and cross-entity consistency, while depth-oriented tasks require coherent reasoning over long, branching search trajectories. We introduce \textbf{Web2BigTable}, a multi-agent framework for web-to-table search that supports both regimes. Web2BigTable adopts a bi-level architecture in which an upper-level orchestrator decomposes the task into sub-problems and lower-level worker agents solve them in parallel. Through a closed-loop run--verify--reflect process, the framework jointly improves decomposition and execution over time via persistent, human-readable external memory, with self-evolving updates to each single-agent. During execution, workers coordinate through a shared workspace that makes partial findings visible, allowing them to reduce redundant exploration, reconcile conflicting evidence, and adapt to emerging coverage gaps. Web2BigTable sets a new state of the art on WideSearch, reaching an Avg@4 Success Rate of \textbf{38.50} ($7.5\times$ the second best at 5.10), Row F1 of \textbf{63.53} (+25.03 over the second best), and Item F1 of \textbf{80.12} (+14.42 over the second best). It also generalises to depth-oriented search on XBench-DeepSearch, achieving 73.0 accuracy. Code is available at https://github.com/web2bigtable/web2bigtable.

---

## 论文详细总结（自动生成）

以下是对论文《Web2BigTable: A Bi-Level Multi-Agent LLM System for Internet-Scale Information Search and Extraction》的深度结构化总结：

### 1. 核心问题与整体含义
论文针对当前大语言模型（LLM）智能体在网络搜索中的两大痛点：**深度推理（Deep Search）**和**广度聚合（Wide Search）**。
*   **研究动机**：现有的搜索智能体在处理单一复杂问题时表现尚可，但在面对需要从海量异构来源提取数百个实体并保持结构化一致性的“广度搜索”任务（如“列出2010-2025年泰勒·斯威夫特的所有演唱会信息”）时，往往会遇到上下文饱和、错误累积和缺乏动态调整能力的问题。
*   **核心目标**：构建一个能够同时兼顾搜索深度与广度，且具备自我进化能力的双层多智能体框架。

### 2. 方法论：核心思想与关键技术
Web2BigTable 采用了**双层架构（Bi-Level Architecture）**，通过外部存储实现非参数化的自我进化：
*   **双层分工**：
    *   **上层编排器（Orchestrator）**：负责任务分解。它根据查询类型（如按实体拆分、按时间拆分）将复杂任务划分为多个并行的子任务。
    *   **下层工作智能体（Workers）**：负责具体执行。多个 Worker 并行搜索、阅读并提取信息。
*   **双重存储机制**：
    *   **长期语义存储（Skill Banks）**：包含编排器技能（$S_o$）和工作者技能（$S_w$）。通过“运行-验证-反思”（Run-Verify-Reflect）闭环，将成功经验和错误教训转化为人类可读的 Markdown 技能文件，无需微调模型参数。
    *   **短期工作存储（Shared Workboard）**：一个基于 Markdown 的共享协作板。Worker 之间通过异步读写（利用文件锁和标签分区）共享进度，从而避免重复搜索、填补覆盖缺口并调和冲突证据。
*   **算法流程**：
    1.  **训练阶段**：在少量标注数据上运行，通过对比预测结果与金标准（Gold Standard），利用 LLM 生成反思报告并更新技能库。
    2.  **推理阶段**：冻结技能库，编排器路由任务，Worker 调用技能库中的工具并行执行，最终汇总为大表（BigTable）。

### 3. 实验设计
*   **数据集/基准测试**：
    *   **WideSearch**：侧重广度，包含 200 个手动策划的任务（中英各半），涵盖 15 个领域，要求提取多维原子数据。
    *   **XBench-DeepSearch**：侧重深度，要求多跳推理和跨源验证。
*   **对比方法**：
    *   **单智能体**：Claude 4.5/Thinking、GPT-5 High/mini、Gemini 3 Pro/Flash、DeepSeek-R1 等。
    *   **端到端系统**：OpenAI/Gemini/Claude 的官方深度搜索/研究产品。
    *   **多智能体框架**：基于上述前沿模型的标准分层编排框架。
*   **评价指标**：成功率（SR）、行级 F1（Row F1）、项级 F1（Item F1）以及准确率（Accuracy）。

### 4. 资源与算力
*   **模型使用**：编排器主要使用 **GPT-5 mini**，工作智能体使用 **Gemini 3 Flash**。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。由于该系统基于 API 调用且不涉及模型参数微调（Parameter-free），其资源消耗主要集中在推理过程中的 Token 消耗和并发处理上。

### 5. 实验数量与充分性
*   **实验规模**：在 WideSearch 上进行了 4 次独立运行并取平均值（Avg@4），在 XBench 上进行了完整测试。
*   **消融实验**：针对“学习到的编排技能”、“共享工作板”和“工作者技能进化”分别进行了消融，证明了各组件的必要性。
*   **充分性评价**：实验设计较为充分。通过跨越“广度”和“深度”两个截然不同的基准测试，验证了系统的通用性。对比实验涵盖了目前市面上最顶尖的闭源和开源模型，具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能飞跃**：在 WideSearch 上，Web2BigTable 的成功率达到 38.50%，是次优系统（5.10%）的 **7.5 倍**；行 F1 和项 F1 分别提升了 25.03% 和 14.42%。
*   **深度搜索优势**：在 XBench-DeepSearch 上达到 73.0% 的准确率，超越了 Minimax-M2 和 MiroFlow 等专门的深度研究系统。
*   **框架胜过模型**：实验证明，使用较弱的模型（如 GPT-5 mini + Gemini 3 Flash）配合该框架，性能远超直接使用最强单智能体模型（如 Claude 4.5）。

### 7. 优点（亮点）
*   **双层进化机制**：通过外部 Markdown 存储实现技能积累，既保证了可解释性，又避免了昂贵的模型微调。
*   **异步协作创新**：共享工作板（Workboard）解决了多智能体协作中的冗余和冲突问题，实现了类似“异步共识”的效果。
*   **任务自适应**：能够根据查询的结构特征（如实体数量、时间跨度）自动选择最优的分解策略。

### 8. 不足与局限
*   **依赖底层模型推理能力**：虽然框架提升了性能，但反思和验证环节仍高度依赖底层 LLM 的逻辑判断，若模型发生严重幻觉，技能库可能引入错误。
*   **Token 成本与延迟**：多智能体并行及多次反思循环虽然提升了质量，但相比单智能体，其 API 调用成本和整体响应延迟会显著增加。
*   **应用限制**：目前主要针对“网络到表格”的结构化任务，对于非结构化的开放式创意写作或纯逻辑证明任务的适用性尚未充分验证。

（完）
