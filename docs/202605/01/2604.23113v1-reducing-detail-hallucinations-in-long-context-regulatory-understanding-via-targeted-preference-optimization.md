---
title: Reducing Detail Hallucinations in Long-Context Regulatory Understanding via Targeted Preference Optimization
title_zh: 通过针对性偏好优化减少长上下文规制理解中的细节幻觉
authors: "Yang Liu, Bin Chong, Yuhan Lin, Chongyang Zhang, Hao Zheng, Ziyi Zhang, Jiayu Liang, Ran Ran, Qian Li, Kefu Xu"
date: 2026-04-25
pdf: "https://arxiv.org/pdf/2604.23113v1"
tags: ["query:profile-1"]
score: 9.0
evidence: 监管文件理解与幻觉减少
tldr: 针对大语言模型在处理长篇监管文档时容易产生细节幻觉（如数值、单位、范围等错误）的问题，本文提出了细粒度的细节错误分类法及基准测试集 DetailBench。通过引入针对性的偏好优化框架 DetailDPO，利用最小细节扰动构造对比对，使模型梯度集中在关键细节 Token 上。实验证明该方法在多个模型和长文本场景下显著降低了细节错误率，并具有良好的跨领域迁移能力。
selection_source: fresh_fetch
motivation: 大语言模型在长文本监管理解中常出现看似合理但关键参数错误的细节幻觉，严重影响安全性。
method: 提出了 DetailDPO 框架，通过构造仅在单一细节维度上存在差异的对比对，将优化信号集中于承载细节的 Token。
result: "在 Qwen2.5 和 Llama-3.1 系列模型上，DetailDPO 将细节错误率相对降低了 42% 至 61%，且在金融和医疗领域表现出跨域有效性。"
conclusion: 针对性的偏好优化能有效抑制长文本中的细节幻觉，提升模型在处理高精度监管文档时的可靠性。
---

## 摘要
大型语言模型（LLMs）在处理长篇规制文档时经常产生“细节幻觉”，包括在阈值、单位、范围、义务级别和条件方面出现的细微错误。这些错误在保持表面合理性的同时，破坏了安全关键参数。我们通过包含五种错误类型的细粒度“细节错误分类法”将这一现象形式化，并推出了 DetailBench。这是一个基于来自三个司法管辖区的 172 份真实规制文档和 150 份合成文档构建的基准，包含 13,000 个由人工标注的细节级真实偏好对。我们提出了 DetailDPO，这是一种针对性的偏好优化框架，通过构建仅在单个细节维度上存在差异的对比对，将 DPO 梯度信号集中在承载细节的标记（tokens）上。我们提供了理论分析，证明了在温和假设下，为什么“最小细节扰动”对会产生梯度集中。在 Qwen2.5 系列（7B、14B、72B）和 Llama-3.1-8B 上跨三个上下文长度层级（8K-64K tokens）的实验表明，DetailDPO 相对于基准模型将细节错误率降低了 42-61%，在所有五种错误类型上均取得了一致的提升，并展现出向金融和医疗文档的跨领域迁移能力。

## Abstract
Large language models (LLMs) frequently produce \emph{detail hallucinations} when processing long regulatory documents, including subtle errors in threshold values, units, scopes, obligation levels, and conditions that preserve surface plausibility while corrupting safety-critical parameters. We formalize this phenomenon through a fine-grained \emph{Detail Error Taxonomy} of five error types and introduce \textbf{DetailBench}, a benchmark built from 172 real regulatory documents and 150 synthetic documents spanning three jurisdictions, with human-annotated detail-level ground truth comprising 13,000 preference pairs. We propose \textbf{DetailDPO}, a targeted preference optimization framework that constructs contrastive pairs differing in exactly one detail dimension, concentrating DPO gradient signal on detail-bearing~tokens. We provide theoretical analysis showing why \emph{minimal detail perturbation} pairs yield gradient concentration under mild assumptions. Experiments on the Qwen2.5 family (7B, 14B, 72B) and Llama-3.1-8B across three context-length tiers (8K--64K tokens) show that DetailDPO reduces the Detail Error Rate by 42--61\% relative to baselines, with consistent gains across all five error types and cross-domain transfer to financial and medical documents.

---

## 论文详细总结（自动生成）

这篇论文针对大语言模型（LLM）在处理长篇监管文档时普遍存在的“细节幻觉”问题，提出了一套从分类法、基准测试到优化框架的完整解决方案。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在能源、金融和医疗等合规性敏感领域，LLM 处理长文档时常出现**细节幻觉（Detail Hallucination）**。这种幻觉不同于完全胡编乱造，它生成的回复在结构和叙述上高度合理，但会在关键参数（如压力阈值、计量单位、适用范围、法律义务等级、触发条件）上出现细微错误。
*   **研究动机**：现有的幻觉研究多关注粗粒度的事实一致性，而忽略了这种“表面合理但参数致命”的细节错误。随着上下文长度增加（32K-128K+），细节准确性的下降速度远快于整体任务表现。
*   **核心挑战**：缺乏细粒度的错误刻画、缺乏专门的细节级基准测试、缺乏针对细节忠实度的对齐方法。

### 2. 方法论：核心思想与技术细节
论文提出了 **DetailDPO** 框架，其核心思想是通过“最小细节扰动”构造偏好对，强制模型梯度集中在关键细节 Token 上。
*   **细节错误分类法（Taxonomy）**：定义了五种类型：
    1.  **阈值错误 ($\tau_1$)**：数值变动（如 70MPa 变为 75MPa）。
    2.  **单位错误 ($\tau_2$)**：单位替换（如 MPa 变为 bar）。
    3.  **范围错误 ($\tau_3$)**：适用对象扩大或缩小。
    4.  **等级错误 ($\tau_4$)**：义务强度改变（如“必须”变为“应该”）。
    5.  **条件错误 ($\tau_5$)**：遗漏或修改触发条款。
*   **最小细节扰动（Minimal Detail Perturbation）**：在构造 DPO 训练对时，被拒绝的回答（Rejected）与选中的回答（Chosen）在 Token 序列上几乎完全一致，仅在上述五个维度的某一个细节上存在差异。
*   **梯度集中理论**：论文通过数学证明（Proposition 3.1）指出，当偏好对仅在细节位置不同时，DPO 的梯度会高度集中在这些差异 Token 上，从而避免了学习信号被无关的文本差异稀释。
*   **证据引用机制**：在训练目标中加入证据监督项（$L_{evid}$），强制模型输出结构化的 JSON，并将结论绑定到原文的具体段落和引文。

### 3. 实验设计
*   **数据集（DetailBench）**：
    *   **来源**：172 份真实监管文件（中国 GB 标准、美国 CFR、欧盟 EUR-Lex）和 150 份合成文档。
    *   **规模**：13,000 个偏好对（10K 训练，1K 验证，2K 测试）。
    *   **层级**：按上下文长度分为 Short (8K-16K)、Medium (16K-32K)、Long (32K-64K)。
*   **对比方法**：
    *   **基础模型**：Qwen2.5 (7B/14B/72B) 和 Llama-3.1-8B。
    *   **基准线**：Zero-shot、RAG、SFT、RLHF (PPO)、通用 DPO、KTO、SimPO。
    *   **商业模型**：GPT-4o、Claude-3.5-Sonnet。

### 4. 资源与算力
论文明确列出了算力消耗：
*   **硬件**：使用 NVIDIA A100-80GB GPU。
*   **训练时长**：
    *   **7B 模型**：4 张 A100，约 18 小时。
    *   **14B 模型**：4 张 A100，约 30 小时。
    *   **72B 模型**：8 张 A100，配合 DeepSpeed ZeRO-3，约 72 小时。
*   **技术参数**：采用 LoRA 微调（r=16, alpha=32），最大序列长度 32,768。

### 5. 实验数量与充分性
实验设计非常充分且客观：
*   **多维度评估**：涵盖了合规准确率、细节错误率（DER）、证据 F1 值和证据一致性。
*   **消融实验**：验证了最小扰动策略、证据监督、合成数据和 LoRA 的各自贡献。
*   **跨领域迁移**：在监管数据上训练后，直接在金融（SEC 文件）和医疗（临床指南）领域测试，验证了方法的通用性。
*   **梯度分析**：通过实证数据可视化了梯度分布，证实了理论假设。

### 6. 主要结论与发现
*   **显著提升**：DetailDPO 将细节错误率（DER）相对降低了 **42% 至 61%**。
*   **以小博大**：经过 DetailDPO 优化的 7B 模型在细节准确性上超过了 Zero-shot 的 GPT-4o。
*   **长文本优势**：随着上下文长度增加，Zero-shot 的错误率剧增，而 DetailDPO 的领先优势在长文本场景下更加扩大。
*   **类型差异**：数值（阈值）和单位错误的改进最为明显，因为这些 Token 的位置最为固定。

### 7. 优点
*   **理论与实证结合**：不仅提出了方法，还给出了梯度集中的数学证明，并用实验验证了该证明。
*   **细粒度分类**：首次系统性地定义了监管理解中的五种细节错误，具有很强的行业实用价值。
*   **高质量基准**：DetailBench 结合了多国真实法规，填补了长文本细节忠实度评估的空白。

### 8. 不足与局限
*   **长文本瓶颈**：尽管有所改善，但在 64K 极长上下文下的 DER 仍高于短文本，说明长程注意力衰减问题依然存在。
*   **合成数据依赖**：为了覆盖所有错误类型，训练集依赖了一定比例的合成数据，可能存在分布偏差。
*   **安全底线**：论文指出即使 DER 降至 0.06（72B 模型），在安全关键任务中仍不能完全脱离人工审核。

（完）
