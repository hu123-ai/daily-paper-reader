---
title: "From Skill Text to Skill Structure: The Scheduling-Structural-Logical Representation for Agent Skills"
title_zh: 从技能文本到技能结构：智能体技能的调度-结构-逻辑表示
authors: "Qiliang Liang, Hansi Wang, Zhong Liang, Yang Liu"
date: 2026-04-27
pdf: "https://arxiv.org/pdf/2604.24026v2"
tags: ["query:malm"]
score: 9.0
evidence: 智能体技能与协作的结构化表示
tldr: 引入调度-结构-逻辑表示，以改进LLM智能体中的技能管理和推理。
selection_source: fresh_fetch
motivation: 智能体技能与协作的结构化表示。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## 摘要
大语言模型（LLM）智能体日益依赖可重用的技能，即结合了指令、控制流、约束和工具调用的能力包。然而，在大多数当前的智能体系统中，技能仍由以文本为主的工件表示，包括 SKILL.md 风格的文档和结构化记录，其机器可用的证据在很大程度上仍嵌入在自然语言描述中。这给以技能为中心的智能体系统带来了挑战：管理技能集合以及利用技能支持智能体，都需要对调用接口、执行结构和具体的副作用进行推理，而这些内容往往纠缠在单一的文本表层中。因此，对技能知识进行显式表示可能有助于使这些工件更易于机器获取和利用。借鉴 Schank 和 Abelson 关于语言知识表示的经典著作中的记忆组织包（Memory Organization Packets）、脚本理论（Script Theory）和概念依赖（Conceptual Dependency），我们引入了据我们所知首个针对智能体技能工件的结构化表示，该表示解耦了技能级调度信号、场景级执行结构以及逻辑级动作和资源使用证据：即调度-结构-逻辑（SSL）表示。我们使用基于 LLM 的归一化器实例化了 SSL，并在两个任务（技能发现和风险评估）的技能语料库上对其进行了评估，其表现优于纯文本基准：在技能发现中，SSL 将 MRR 从 0.573 提升至 0.707；在风险评估中，它将宏 F1 从 0.744 提升至 0.787。这些发现表明，显式的、基于源的结构使智能体技能更易于搜索和审查。它们还表明，SSL 最好被理解为迈向更具可检查性、可重用性和操作可行性的智能体系统技能表示的务实一步，而非一个最终标准或管理与使用技能的端到端机制。

## Abstract
LLM agents increasingly rely on reusable skills, capability packages that combine instructions, control flow, constraints, and tool calls. In most current agent systems, however, skills are still represented by text-heavy artifacts, including SKILL{.}md-style documents and structured records whose machine-usable evidence remains embedded largely in natural-language descriptions. This poses a challenge for skill-centered agent systems: managing skill collections and using skills to support agent both require reasoning over invocation interfaces, execution structure, and concrete side effects that are often entangled in a single textual surface. An explicit representation of skill knowledge may therefore help make these artifacts easier for machines to acquire and leverage. Drawing on Memory Organization Packets, Script Theory, and Conceptual Dependency from Schank and Abelson's classical work on linguistic knowledge representation, we introduce what is, to our knowledge, the first structured representation for agent skill artifacts that disentangles skill-level scheduling signals, scene-level execution structure, and logic-level action and resource-use evidence: the Scheduling-Structural-Logical (SSL) representation. We instantiate SSL with an LLM-based normalizer and evaluate it on a corpus of skills in two tasks, Skill Discovery and Risk Assessment, and superiorly outperform the text-only baselines: in Skill Discovery, SSL improves MRR from 0.573 to 0.707; in Risk Assessment, it improves macro F1 from 0.744 to 0.787. These findings reveal that explicit, source-grounded structure makes agent skills easier to search and review. They also suggest that SSL is best understood as a practical step toward more inspectable, reusable, and operationally actionable skill representations for agent systems, rather than as a finished standard or an end-to-end mechanism for managing and using skills.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **SSL（Scheduling-Structural-Logical）** 的智能体技能结构化表示方法，旨在解决当前大语言模型（LLM）智能体技能描述过于依赖非结构化文本、导致机器难以高效管理和利用的问题。

以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：目前的智能体技能通常以 `SKILL.md` 等文本文件形式存储。这种“重文本”的表示方式将调用接口、执行流程、约束条件和工具调用逻辑混杂在一起，导致机器在进行**技能发现（检索）**和**风险评估（安全审查）**时，必须从冗长且多噪声的自然语言中进行推理，存在严重的表示瓶颈。
*   **研究背景**：随着智能体从简单的工具调用转向复杂的工作流封装（即“技能”），需要一种能够解耦不同层级信息的中间表示，以便于机器自动化处理。

### 2. 方法论：SSL 表示架构
论文借鉴了 Schank 和 Abelson 的经典语言知识表示理论，提出了三层结构的 **SSL 表示**：
*   **调度层 (Scheduling Layer)**：
    *   **核心思想**：借鉴“记忆组织包（MOPs）”，定义技能的调用接口。
    *   **细节**：包含技能目标（Goal）、标签、输入/输出签名、控制流特征（是否有循环、分支）以及依赖项。
*   **结构层 (Structural Layer)**：
    *   **核心思想**：借鉴“脚本理论（Script Theory）”，将执行过程划分为有序的“场景（Scenes）”。
    *   **细节**：定义执行阶段（如准备、获取、动作、验证、恢复），描述场景间的转换规则。
*   **逻辑层 (Logical Layer)**：
    *   **核心思想**：借鉴“概念依赖（Conceptual Dependency）”，分解为原子动作。
    *   **细节**：记录具体的动作类型（READ, WRITE, CALL_TOOL 等）、操作对象、资源范围（内存、文件系统、网络、凭证）及其副作用。
*   **实现流程**：通过一个基于 LLM 的**归一化器（Normalizer）**，采用四阶段提示词流水线（提取记录 -> 分解场景 -> 扩展逻辑步 -> 验证一致性），将原始文本转换为符合 JSON Schema 的结构化图。

### 3. 实验设计
论文在两个关键下游任务上验证了 SSL 的有效性：
*   **任务一：技能发现 (Skill Discovery)**
    *   **场景**：从大规模技能库中根据用户请求检索最匹配的技能。
    *   **数据集**：包含 6,184 个公开智能体技能的语料库，以及 403 个涵盖功能、约束、组合等 5 种类型的任务查询。
    *   **对比方法**：仅描述（Desc_only）、全文本（Full SKILL.md）以及不同丰富程度的 SSL 增强版本。
*   **任务二：风险评估 (Risk Assessment)**
    *   **场景**：识别技能在执行过程中可能存在的安全风险。
    *   **数据集**：从语料库中采样 500 个技能，涵盖数据外泄、破坏性行为、权限提升等 6 个风险维度。
    *   **Benchmark**：使用 Gemini-3.1-pro、Claude-Sonnet-4.5 和 GPT-5 三模型投票产生的金标准。
    *   **对比方法**：仅描述、全文本、浅层 SSL、完整 SSL、全文本+SSL。

### 4. 资源与算力
*   **模型使用**：实验使用了多种先进模型，包括 Qwen3-Embedding-0.6B（用于检索向量化）、DeepSeek-V3.2（作为风险评估的判定器）、以及 GPT-5 等（用于生成金标准）。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量或训练/推理的总时长。这在基于 API 或现成模型推理的论文中较为常见。

### 5. 实验数量与充分性
*   **实验规模**：
    *   检索实验覆盖了 6,000+ 规模的候选池，具有较强的统计意义。
    *   风险评估实验对 500 个样本进行了 6 个维度的细粒度标注。
*   **充分性与公平性**：
    *   进行了详细的**消融实验**，验证了 SSL 不同层级（调度、结构、逻辑）对结果的贡献。
    *   使用了固定的 Embedding 模型和判定模型（DeepSeek），确保了对比的公平性。
    *   通过多模型投票和人工抽检确保了 Benchmark 的客观性。

### 6. 主要结论与发现
*   **检索性能提升**：在技能发现任务中，SSL 显著优于纯文本。最丰富的 SSL 视图将平均倒数排名（MRR）从 0.573 提升至 **0.707**。
*   **风险识别增强**：在风险评估任务中，“全文本 + SSL”的组合表现最佳，宏 F1 分数从 0.744 提升至 **0.787**。
*   **互补性**：实验发现 SSL 并不完全取代原始文档，而是作为一种“证据接口”。在需要深度语义理解的场景（如判断风险严重程度）下，SSL 与原始文本结合效果最好。

### 7. 优点与亮点
*   **理论支撑**：将 20 世纪 70 年代的经典认知科学/语言学理论（Schank & Abelson）成功应用于现代 LLM Agent 领域，具有深厚的学术底蕴。
*   **解耦清晰**：首次系统性地提出了三层解耦架构，为智能体技能的标准化提供了参考。
*   **实用性强**：不仅提出了表示法，还提供了配套的归一化工具和高质量的开源数据集（6,184 个技能及标注）。

### 8. 不足与局限
*   **静态分析限制**：SSL 目前主要基于静态文档提取，对于运行时动态生成的代码或条件触发的隐藏行为（如动态下载负载）难以完全捕捉。
*   **归一化器依赖**：SSL 的质量高度依赖于底层 LLM 归一化器的准确性，如果原始文档本身存在误导或模糊，SSL 可能会产生偏差。
*   **应用范围**：实验主要集中在管理和审查（离线任务），尚未直接评估 SSL 在智能体实时规划（Planning）和执行监控中的在线表现。

（完）
