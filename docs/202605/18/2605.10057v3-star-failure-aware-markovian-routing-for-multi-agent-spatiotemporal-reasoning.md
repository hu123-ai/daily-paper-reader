---
title: "STAR: Failure-Aware Markovian Routing for Multi-Agent Spatiotemporal Reasoning"
title_zh: STAR：面向多智能体时空推理的故障感知马尔可夫路由
authors: "Ruiyi Yang, Lihuan Li, Hao Xue, Flora D. Salim"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10057v3"
tags: ["query:malm"]
score: 9.0
evidence: 多智能体系统的故障感知路由框架
tldr: STAR是一个针对多智能体时空推理的故障感知路由框架。针对现有系统在处理不同类型执行失败时缺乏显式路由机制的问题，STAR将智能体间的控制建模为基于状态的转移策略。通过结合专家定义的常规路径与从执行轨迹中学习到的恢复路径，STAR能根据具体的失败状态（如格式错误、依赖缺失等）进行差异化响应。实验表明，该方法在处理偏离常规路径的复杂查询时表现优异，显著提升了系统的鲁棒性。
selection_source: fresh_fetch
motivation: 现有的多智能体系统通常将路由决策隐含在语言生成中，导致在面对不同类型的执行失败时难以进行解释、优化和有效恢复。
method: 提出STAR框架，利用包含专家规则和学习到的恢复转移的路由矩阵，根据当前智能体、任务类型及具体的失败状态进行显式路由。
result: 在三个时空推理基准测试和八种大语言模型上，STAR均优于基准方法，特别是在需要从执行错误中恢复的场景下提升显著。
conclusion: 显式的、区分故障类型的路由机制是提升多智能体系统在复杂时空推理任务中鲁棒性和成功率的关键。
---

## 摘要
组合式时空推理通常需要系统调用多个异构专家，例如几何、时间、拓扑和轨迹智能体。一个核心问题是，当执行并非简单的成功或失败，而是以性质截然不同的方式失败时，此类系统应如何在专家之间进行路由。现有的工具增强型和多智能体大语言模型（LLM）系统通常将这种路由决策隐含在语言生成中，导致恢复过程具有随意性、难以解释且难以优化。本文提出了 STAR（时空智能体路由器），这是一个故障感知路由框架，它将智能体间的控制外化为一种基于当前智能体、任务类型和类型化执行状态的状态条件转移策略。STAR 的核心是一个智能体路由矩阵，它结合了专家指定的名义路线和从执行轨迹中学习到的恢复转移。由于该矩阵以不同的失败状态为条件，路由器可以针对格式错误的输出、缺失的依赖项以及工具与查询的不匹配做出不同的响应，而不是将它们统统归结为通用的重试信号。专家通过基于工具的“提取-计算-存放”协议执行，并将中间结果写入共享黑板以供下游融合。结果证明，在训练期间保留不成功的轨迹扩大了路由策略在错误状态上的支持集，从而实现了仅靠成功轨迹训练无法表示的恢复转移。在三个时空基准测试和八个骨干 LLM 上，STAR 优于多个基准模型，在执行偏离名义路由路径的查询上提升最为显著。针对路由器的消融实验和恢复分析进一步表明，类型化的故障感知路由（而非仅仅是专家组合）是实现这些改进的关键因素。

## Abstract
Compositional spatiotemporal reasoning often requires a system to invoke multiple heterogeneous specialists, such as geometric, temporal, topological, and trajectory agents. A central question is how such a system should route among specialists when execution does not simply succeed or fail, but fails in qualitatively different ways. Existing tool-augmented and multi-agent LLM systems typically leave this routing decision implicit in language generation, making recovery ad hoc, difficult to interpret, and hard to optimize. This paper presents STAR (Spatio-Temporal Agent Router), a failure-aware routing framework that externalizes inter-agent control as a state-conditioned transition policy over the current agent, task type, and typed execution status. At the center of STARis an agent routing matrix that combines expert-specified nominal routes with recovery transitions learned from execution traces. Because the matrix conditions on distinct failure states, the router can respond differently to malformed outputs, missing dependencies, and tool--query mismatches, rather than collapsing them into a generic retry signal. Specialists execute through a tool-grounded extract--compute--deposit protocol and write intermediate results to a shared blackboard for downstream fusion. Results prove that retaining unsuccessful traces during training enlarges the support of the routing policy on error states, enabling recovery transitions that success-only training cannot represent. Across three spatiotemporal benchmarks and eight backbone LLMs, STAR improves over multiple baselines with the clearest gains on queries whose execution deviates from the nominal routing path. Router-specific ablations and recovery analyses further show that typed failure-aware routing, rather than specialist composition alone, is a key factor for these improvements.

---

## 论文详细总结（自动生成）

### 论文总结：STAR - 面向多智能体时空推理的故障感知马尔可夫路由

#### 1. 论文的核心问题与整体含义
时空推理（Spatiotemporal Reasoning）涉及几何定位、时间关系、路径规划等多种异构计算。现有的多智能体大语言模型（LLM）系统在处理此类复杂任务时，通常将“如何选择下一个专家智能体”这一路由决策隐含在自由文本生成中。当执行过程中出现不同性质的失败（如输出格式错误、工具不匹配、上游依赖缺失）时，现有系统往往只能进行盲目的重试，缺乏显式的、可解释的恢复机制。

本文的研究动机在于：**将智能体间的控制逻辑从语言生成中剥离出来，构建一个显式的、基于状态的路由策略**，使系统能够根据具体的失败类型采取差异化的恢复措施。

#### 2. 论文提出的方法论
STAR 框架的核心思想是将多智能体协作建模为一个**状态条件转移的马尔可夫过程**。

*   **核心组件：路由矩阵 (Routing Matrix)**
    路由决策由一个四维张量 $M[a, s, t, a']$ 决定，其中 $a$ 是当前智能体，$s$ 是执行状态，$t$ 是任务类型，$a'$ 是候选的下一个智能体。
*   **双系统路由机制**
    *   **System 1 (名义路由)**：遵循专家预设的逻辑路径，处理初始化（INIT）和成功（SUCC）状态。
    *   **System 2 (恢复路由)**：从执行轨迹中学习到的转移策略，专门处理三种类型的故障：**FAIL**（格式错误）、**BLOCK**（依赖缺失）、**MISS**（工具与查询不匹配）。
*   **执行协议：提取-计算-存放 (Extract–Compute–Deposit)**
    智能体不再直接生成答案，而是先从查询中**提取**参数，调用确定性工具进行**计算**，最后将结果**存放**到共享的“黑板”（Blackboard）架构中。
*   **故障感知训练**
    在构建路由矩阵时，STAR 不仅利用成功的执行轨迹，还保留了失败的轨迹（通过权重参数 $\alpha$ 调节）。这使得矩阵能够覆盖错误状态，从而学习到“当 A 失败时，应转由 B 处理”的恢复逻辑。

#### 3. 实验设计
*   **数据集/场景**：
    *   **STARK**：包含 26 种任务类型，涵盖空间/时间插值、预测和关系推理。
    *   **STB24 (STBench)**：包含 11 种任务类型，侧重于轨迹分析和城市功能识别。
    *   **STB26 (ST-Bench 2026)**：包含 4 种任务类型，侧重于因果推理和相关性分析。
*   **对比方法**：
    *   **基础模型**：LLM-only, LLM-only (CoT)。
    *   **智能体框架**：ReAct, Reflexion, Tree-of-Thought (ToT), Graph-of-Thought (GoT)。
    *   **功能调用**：标准的 Function-calling 模式。
*   **骨干模型**：测试了包括 Claude 3.5 Sonnet/Haiku、GPT-OSS-20B、Qwen3-8B、Llama-3.1-8B 等在内的 8 种闭源和开源模型。

#### 4. 资源与算力
*   **硬件支持**：论文在致谢部分提到使用了由 Sharon AI 提供的 **NVIDIA H100 GPU**。
*   **训练/推理细节**：文中未明确给出具体的训练总时长或使用的 GPU 数量，但提到 Qwen3-8B 作为默认骨干模型在推理时具有较低的延迟（平均每查询 1.6-1.8 秒）。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 3 个基准测试上对 8 个模型进行了全面评估，总计涉及 35 种任务类型。
*   **消融实验**：进行了框架级（移除黑板、工具等）和路由器级（移除状态感知、改变 $\alpha$ 权重等）的多维度消融。
*   **客观性**：所有实验结果均附带了 95% 的 Wilson 得分置信区间，并进行了跨基准的迁移实验（Transfer Learning），验证了路由矩阵的通用性。实验设计严谨，对比基准涵盖了当前主流的 Agent 架构，具有较高的说服力。

#### 6. 主要结论与发现
*   **性能提升**：STAR 在所有基准测试上均显著优于 LLM-only 和现有的 Agent 框架。在 STB24 上，相比 Reflexion 提升了约 39.1%。
*   **故障恢复的价值**：实验证明，当查询进入 MISS（工具不匹配）状态时，STAR 的恢复成功率甚至超过了无故障的基准线，说明路由矩阵能纠正初始的错误分配。
*   **失败数据的必要性**：理论证明并实验确认，在训练中包含失败轨迹对于构建完整的恢复路由至关重要（定理 1：恢复可达性优势）。
*   **并行性**：通过并行激活机制，系统能同时调用多个专家，显著提升了处理复合查询的效率。

#### 7. 优点
*   **显式控制**：将复杂的 Agent 协作逻辑矩阵化，提高了系统的可解释性和可调试性。
*   **细粒度故障处理**：区分了不同类型的失败，避免了盲目重试带来的资源浪费。
*   **模块化与解耦**：通过“黑板”架构实现了智能体间的松耦合，方便扩展新的专家智能体。
*   **鲁棒性**：在处理偏离常规路径的复杂、长程推理任务时表现出极强的韧性。

#### 8. 不足与局限
*   **依赖任务分类**：系统的第一步（HEAD 智能体）需要准确识别任务类型，若分类错误，后续路由可能失效。
*   **参数提取瓶颈**：虽然计算是确定性的，但 LLM 在从文本中提取参数（Extract 阶段）时仍存在错误风险，这是导致 FAIL 状态难以完全恢复的主要原因。
*   **任务分类体系限制**：目前的路由矩阵是基于预定义的任务分类法学习的，对于完全未见过的任务类型，其泛化能力仍有待进一步验证。

（完）
