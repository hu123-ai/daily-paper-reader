---
title: An Agent-Oriented Pluggable Experience-RAG Skill for Experience-Driven Retrieval Strategy Orchestration
title_zh: 一种面向智能体的可插拔 Experience-RAG 技能，用于经验驱动的检索策略编排
authors: "Dutao Zhang, Tian Liao"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03989v1"
tags: ["query:malm"]
score: 9.0
evidence: 面向智能体的检索策略编排层
tldr: 针对不同任务对检索策略需求各异的问题，本文提出Experience-RAG Skill，一种面向智能体的可插拔检索编排层。该方法通过分析场景并咨询经验记忆，动态选择最优检索策略，将检索策略选择封装为可重用的智能体技能，在多项基准测试中表现优于固定检索基准。
selection_source: fresh_fetch
motivation: 传统的RAG系统通常采用固定的检索流程，难以适应事实问答、多跳推理和科学验证等不同任务对检索策略的多样化偏好。
method: 提出一种可插拔的检索编排技能，通过分析当前场景并结合经验记忆，从检索池中动态选择并执行最合适的检索策略。
result: "在BeIR/nq、hotpotqa和scifact等数据集上，该方法取得了0.8924的nDCG@10评分，优于固定单检索器基准且与自适应路由方法相当。"
conclusion: 检索策略的选择可以被有效地封装为一种可重用的智能体技能，而非硬编码在顶层工作流中，从而提高系统的灵活性和性能。
---

## 摘要
检索增强生成（RAG）系统通常假设单一固定的检索流水线足以应对各种异构任务，然而事实型问答、多跳推理和科学验证表现出不同的检索偏好。我们提出了 Experience-RAG Skill，这是一种位于智能体与检索器池之间的面向智能体的可插拔检索编排层。该技能通过分析当前场景、咨询经验记忆、选择合适的检索策略，并向智能体返回结构化证据。在固定候选池的情况下，Experience-RAG Skill 在 BeIR/nq、BeIR/hotpotqa 和 BeIR/scifact 上实现了 0.8924 的总体 nDCG@10，优于固定的单检索器基线，并与 Adaptive-RAG 风格的路由保持相当的竞争力。结果表明，检索策略的选择可以有效地封装为可重用的智能体技能，而不是在上层工作流中进行硬编码。

## Abstract
Retrieval-augmented generation systems often assume that one fixed retrieval pipeline is sufficient across heterogeneous tasks, yet factoid question answering, multi-hop reasoning, and scientific verification exhibit different retrieval preferences. We present Experience-RAG Skill, an agent-oriented pluggable retrieval orchestration layer positioned between the agent and the retriever pool. The proposed skill analyzes the current scene, consults an experience memory, selects an appropriate retrieval strategy, and returns structured evidence to the agent. Under a fixed candidate pool, Experience-RAG Skill achieves an overall nDCG@10 of 0.8924 on BeIR/nq, BeIR/hotpotqa, and BeIR/scifact, outperforming fixed single-retriever baselines and remaining competitive with Adaptive-RAG-style routing. The results suggest that retrieval strategy selection can be productively encapsulated as a reusable agent skill rather than being hard-coded in the upper workflow.

---

## 论文详细总结（自动生成）

以下是对论文《An Agent-Oriented Pluggable Experience-RAG Skill for Experience-Driven Retrieval Strategy Orchestration》的结构化深度总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：传统的检索增强生成（RAG）系统通常采用固定的检索流水线。然而，不同类型的任务（如事实问答、多跳推理、科学验证）对检索策略有不同的偏好。例如，稠密检索擅长事实问答，而混合检索在多跳和科学证据匹配中更稳定。
*   **核心问题**：如何让智能体（Agent）能够根据当前任务场景，动态且自主地选择最合适的检索策略，而不是将检索逻辑硬编码在工作流中。
*   **整体含义**：本文提出将检索策略编排定义为一种“面向智能体的可插拔技能”（Experience-RAG Skill），作为智能体与检索器池之间的中间层，实现经验驱动的策略调度。

### 2. 论文提出的方法论
*   **核心思想**：通过场景分析、经验查找、策略路由和结果封装四个步骤，将复杂的检索决策过程封装为一个统一的技能接口。
*   **关键模块**：
    1.  **技能接口（Skill Interface）**：智能体调用的统一入口。
    2.  **场景分析器（Scene Analyzer）**：提取查询（q）、对话历史（h）和任务元数据（m）的特征（如任务类型、领域、复杂度、文档结构）。
    3.  **经验记忆（Experience Memory）**：存储历史记录，包括场景特征、各策略得分向量、最优策略及其领先幅度（Margin）。
    4.  **策略路由器（Strategy Router）**：根据经验决定路由。目前最强配置是基于规则的路由（如：直接任务指向稠密检索，多跳/科学任务指向混合 RRF 检索）。
    5.  **检索器池（Retriever Pool）**：包含 BM25、稠密检索、混合检索等多种工具。
    6.  **结果封装器（Result Packager）**：将检索到的证据标准化后返回给智能体。
*   **流程公式**：$s = Analyze(q, h, m)$；$r^* = Route(s, M)$；$o = RetrieveAndPackage(q, r^*)$。

### 3. 实验设计
*   **数据集**：选取了 BeIR 基准中的三个代表性子集：**BeIR/nq**（自然语言问答）、**BeIR/hotpotqa**（多跳推理）、**BeIR/scifact**（科学声明验证）。
*   **实验设置**：每个数据集采样 120 个查询，并使用采样后的候选语料库。
*   **对比方法**：
    *   **基础基线**：BM25、带重写的 BM25、稠密检索（Dense）、混合检索（Hybrid RRF）。
    *   **现代/先进基线**：Adaptive-RAG（自适应路由）、HyDE（假设文档嵌入）、LongRAG（长上下文 RAG）、RAPTOR（递归摘要树检索）。
*   **评价指标**：Recall@10、MRR@10、nDCG@10。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或训练时长。由于该研究侧重于策略编排层而非底层模型训练，且部分实验基于规则路由，其对算力的即时需求可能主要集中在 LLM 推理（用于场景分析和基线对比）上。

### 5. 实验数量与充分性
*   **实验规模**：
    *   主实验覆盖了 3 类异构任务。
    *   进行了消融实验（验证各固定策略与编排技能的差异）。
    *   探索了学习型路由（Learned Routing）与规则路由的对比。
    *   进行了定性的智能体工作流案例分析。
*   **充分性评价**：实验设计较为客观，对比了多种现代 RAG 变体。但由于使用了**采样语料库**而非全量数据，且每个数据集仅 120 个样本，实验的统计严密性仍有提升空间。

### 6. 论文的主要结论与发现
*   **性能优越性**：Experience-RAG Skill 实现了 0.8924 的 nDCG@10，优于所有固定的单检索器基线。
*   **路由有效性**：该技能在混合负载下的表现证明了其编排价值，其性能与复杂的 Adaptive-RAG 相当（差距极小），但接口更具通用性。
*   **学习型路由的局限**：在当前经验规模下，基于硬分类或评分回归的学习型路由器表现不如基于规则的路由，说明经验记忆的规模和质量对自动化学习至关重要。
*   **封装价值**：检索策略选择可以被有效地封装为可重用的智能体技能，简化了上层智能体的逻辑设计。

### 7. 优点
*   **架构解耦**：将检索策略从业务逻辑中抽离，使 RAG 系统更具模块化和可插拔性。
*   **经验驱动**：引入经验记忆（Experience Memory）的概念，为未来实现持续学习和自我进化的检索系统奠定了基础。
*   **场景感知**：能够识别任务异构性并据此优化，解决了“一套方案打天下”的低效问题。

### 8. 不足与局限
*   **语料规模限制**：实验基于采样语料库而非全规模工业级数据，可能无法完全反映真实环境中的检索挑战。
*   **路由算法尚浅**：目前最有效的仍是人工定义的规则路由，学习型路由尚未展现出超越规则的优势。
*   **候选池固定**：目前假设检索器池是预定义的，尚未解决动态接入新检索工具（Open-world candidate onboarding）的问题。
*   **评估维度**：缺乏端到端的智能体最终生成质量评估，目前的评估主要集中在检索阶段。

（完）
