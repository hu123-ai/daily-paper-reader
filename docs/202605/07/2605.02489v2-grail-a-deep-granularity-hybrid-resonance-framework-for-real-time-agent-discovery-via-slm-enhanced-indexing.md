---
title: "GRAIL: A Deep-Granularity Hybrid Resonance Framework for Real-Time Agent Discovery via SLM-Enhanced Indexing"
title_zh: GRAIL：一种基于 SLM 增强索引的实时智能体发现深粒度混合共振框架
authors: Jinliang Xu
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02489v2"
tags: ["query:malm"]
score: 9.0
evidence: 多智能体协作中的实时智能体发现框架
tldr: 针对大规模多智能体协作中智能体发现效率与精度难以兼得的问题，本文提出GRAIL框架。该框架通过轻量化小语言模型（SLM）进行意图解析，结合伪文档扩展增强语义密度，并利用MaxSim共振机制实现细粒度匹配。在AgentTaxo-9K数据集上的实验表明，GRAIL在保持高精度的同时，将发现延迟降低至400ms以内，比传统LLM方案快79倍以上，为实时“智能体互联网”提供了可扩展的工业级解决方案。
selection_source: fresh_fetch
motivation: 现有的智能体发现方法在重型LLM的高延迟与传统向量检索的低精度之间存在矛盾，难以满足大规模实时协作需求。
method: 采用微调的SLM进行快速能力标签预测，通过伪文档扩展增强智能体描述，并利用MaxSim机制计算查询与使用示例间的最大相似度。
result: "在包含9,240个智能体的数据集上，GRAIL实现了亚400毫秒的端到端延迟，比LLM基准快79倍，且召回率显著优于传统向量搜索。"
conclusion: GRAIL通过深度粒度的混合共振框架，成功解决了实时智能体发现中的性能瓶颈，是构建大规模智能体生态系统的关键技术。
---

## 摘要
随着基于大语言模型（LLM）的智能体生态系统迅速扩张，高效且准确的智能体发现已成为大规模多智能体协作的关键瓶颈。现有方法通常面临两难境地：要么依赖重量级 LLM 进行意图解析，导致极高的延迟（通常超过 30 秒）；要么使用单一的向量检索，为了速度而牺牲语义精度。为了弥补这一差距，我们提出了 GRAIL（Granular Resonance-based Agent/AI Link），这是一个在不牺牲准确性的情况下实现低于 400ms 发现延迟的新型框架。GRAIL 引入了三项关键创新：(1) SLM 增强预测，采用专门微调的小语言模型（SLM）取代通用 LLM 解析器，实现毫秒级的能力标签预测；(2) 伪文档扩展，通过合成查询增强智能体描述，以提升语义密度并实现鲁棒的稠密检索；(3) MaxSim 共振，一种细粒度的匹配机制，通过计算用户查询与离散智能体使用示例之间的最大相似度，有效缓解语义稀释。在包含 9,240 个智能体的新型大规模数据集 AgentTaxo-9K 上的验证结果显示，与基于 LLM 解析的基准相比，GRAIL 将端到端发现延迟降低了 79 倍以上，同时在 Recall@10 指标上显著优于传统的向量搜索。该框架为实时的“智能体互联网”提供了一种可扩展的工业级解决方案。

## Abstract
As the ecosystem of Large Language Model (LLM)-based agents expands rapidly, efficient and accurate Agent Discovery becomes a critical bottleneck for large-scale multi-agent collaboration. Existing approaches typically face a dichotomy: either relying on heavy-weight LLMs for intent parsing, leading to prohibitive latency (often exceeding 30 seconds), or using monolithic vector retrieval that sacrifices semantic precision for speed. To bridge this gap, we propose \textbf{GRAIL} (Granular Resonance-based Agent/AI Link), a novel framework achieving sub-400ms discovery latency without compromising accuracy. GRAIL introduces three key innovations: (1) \textbf{SLM-Enhanced Prediction}, replacing the generalized LLM parser with a specialized, fine-tuned Small Language Model (SLM) for millisecond-level capability tag prediction; (2) \textbf{Pseudo-Document Expansion}, augmenting agent descriptions with synthetic queries to enhance semantic density for robust dense retrieval; and (3) \textbf{MaxSim Resonance}, a fine-grained matching mechanism computing maximum similarity between user queries and discrete agent usage examples, effectively mitigating semantic dilution. Validated on \textbf{AgentTaxo-9K}, our new large-scale dataset of 9,240 agents, GRAIL reduces end-to-end discovery latency by over \textbf{79$\times$} compared to LLM-parsing baselines, while significantly outperforming traditional vector search in Recall@10. This framework offers a scalable, industrial-grade solution for the real-time ``Internet of Agents."

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **GRAIL** 的新型框架，旨在解决大规模多智能体系统（MAS）中“智能体发现”过程中的实时性与准确性矛盾。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在“智能体互联网”（Internet of Agents）愿景下，如何从成千上万个专业智能体中，实时（亚秒级）且精准地匹配符合用户意图的智能体。
*   **研究背景与动机**：
    *   **现有方案的局限性**：目前的智能体发现主要分为两类：一是**基于 LLM 的解析方案**，虽然意图理解深，但推理延迟极高（常超过 30 秒），无法满足实时协作需求；二是**单一向量检索方案**，虽然速度快，但将智能体复杂的能力压缩为单个向量会导致“语义稀释”（Semantic Dilution），导致检索精度低。
    *   **需求**：工业界需要一种既能保持 LLM 级别的语义精度，又能达到毫秒级响应速度的平衡方案。

### 2. 论文提出的方法论
GRAIL 框架采用了**深粒度混合共振**的思想，将发现过程分为离线索引和在线检索两个阶段：

*   **核心思想**：解耦智能体特征，从标签（Tags）、上下文（Context）和意图（Intent）三个维度构建索引，并引入小语言模型（SLM）加速解析。
*   **关键技术细节**：
    1.  **SLM 增强预测**：使用微调后的 3B 参数小模型（如 Qwen2.5-3B）代替重型 LLM，在 50ms 内预测用户查询对应的能力标签，实现快速候选过滤。
    2.  **伪文档扩展（PDE）**：利用 LLM 为智能体描述生成合成查询，扩充元数据的语义密度，解决用户表达与技术描述之间的词汇不匹配问题。
    3.  **MaxSim 共振机制**：这是核心创新点。不使用平均向量，而是保留智能体每个使用示例（Example）的独立向量，计算查询向量与示例矩阵之间的**最大相似度**。
*   **算法流程**：
    *   **离线**：构建倒排标签索引、扩展后的描述向量索引、以及示例嵌入矩阵。
    *   **在线**：SLM 预测标签 -> 混合召回（标签过滤 + 向量搜索）-> MaxSim 重排序（计算查询与示例的“共振”得分）-> 输出 Top-K。

### 3. 实验设计
*   **数据集/场景**：发布了 **AgentTaxo-9K**，这是一个包含 9,240 个智能体、27,720 条查询的大规模分层基准数据集。
*   **Benchmark 结构**：基于 OASF 标准，涵盖 154 个行业、924 个子领域，每个智能体包含名称、描述、标签和 3 个使用示例。
*   **对比方法**：
    1.  **MDR (Monolithic Dense Retrieval)**：传统的单一向量检索。
    2.  **SLM-Sort**：仅依赖 SLM 预测标签进行检索。
    3.  **LLM-Router**：使用重型 LLM（如 Gemini 3 级别）进行意图解析和路由。

### 4. 资源与算力
*   **硬件环境**：单台 Linux 服务器，配备 **1 张 NVIDIA T4 GPU (15GB VRAM)**，12.7GB 系统内存。
*   **软件环境**：Python 3.12, PyTorch 2.1。
*   **模型细节**：SLM 采用 Qwen2.5-3B-Instruct，使用 LoRA 技术进行微调；向量编码器使用 BGE-Small-EN-v1.5（33M 参数）。
*   **训练时长**：文中未明确给出具体的训练总时长，但提到了推理延迟的组成（SLM 推理约 370ms，向量计算约 24ms）。

### 5. 实验数量与充分性
*   **实验规模**：在 9,240 个智能体的全量池中进行检索测试，而非小规模子集，具有较强的说服力。
*   **实验类型**：
    *   **主实验**：对比了 Recall@1, Recall@10 和 MRR@10。
    *   **消融实验**：分别验证了 MaxSim 机制和 SLM 过滤器的贡献。
    *   **延迟分析**：详细拆解了端到端延迟的构成。
*   **评价**：实验设计较为充分，通过理论证明（定理 1）与实证结果相结合，客观地展示了 MaxSim 在解决语义稀释问题上的优越性。

### 6. 主要结论与发现
*   **性能卓越**：GRAIL 的 Recall@10 达到 **91.45%**，显著优于 MDR（76.77%）和 LLM-Router（78.95%）。
*   **极低延迟**：端到端延迟仅为 **394.48ms**，比 LLM 路由方案快了 **79 倍以上**。
*   **理论验证**：实验证实了“语义稀释”现象的存在，即当智能体功能增多时，平均向量表示会导致匹配失效，而 MaxSim 机制能有效保持信号强度。

### 7. 优点
*   **架构平衡**：巧妙地结合了 SLM 的速度和多维索引的精度，解决了工业界实时发现的痛点。
*   **理论支撑**：对 MaxSim 机制进行了数学证明，解释了为何细粒度匹配优于均值池化。
*   **高质量资源**：贡献了 AgentTaxo-9K 数据集，填补了大规模智能体发现基准的空白。

### 8. 不足与局限
*   **SLM 依赖性**：虽然 SLM 较快，但 370ms 的推理时间在极高并发场景下仍可能成为瓶颈。
*   **冷启动问题**：对于缺乏使用示例（Examples）的新智能体，MaxSim 机制的效果可能会打折扣。
*   **语言覆盖**：实验主要基于英文环境（BGE-Small-EN），在多语言或跨语言智能体发现上的表现尚待验证。
*   **动态更新**：论文未详细讨论当智能体库频繁更新时，索引维护的成本和一致性问题。

（完）
