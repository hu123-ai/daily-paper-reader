---
title: "Safe Multi-Agent Behavior Must Be Maintained, Not Merely Asserted: Constraint Drift in LLM-Based Multi-Agent Systems"
title_zh: 安全的多智能体行为必须得到维持，而非仅仅是断言：基于大语言模型的多智能体系统中的约束漂移
authors: "Tianxiao Li, Yixing Ma, Haiquan Wen, Zhenglin Huang, Qianyu Zhou, Zeyu Fu, Guangliang Cheng"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10481v1"
tags: ["query:malm"]
score: 9.0
evidence: 基于大语言模型的多智能体系统中的安全与约束漂移
tldr: 本文探讨了基于大语言模型的多智能体系统中的安全性挑战，提出了“约束漂移”概念，即安全约束在记忆、通信和工具调用等复杂执行过程中会逐渐失效。研究指出，仅靠初始提示或输出检查不足以确保安全，必须在整个执行轨迹中动态维护约束。为此，作者提出了“约束状态治理”范式，通过将约束作为显式执行状态并结合约束原生强化学习，确保智能体在安全边界内高效运行，为构建可审计且持续安全的智能体系统提供了新方向。
selection_source: fresh_fetch
motivation: LLM智能体在长程工作流中常因记忆、委派和通信等环节导致安全约束逐渐丢失或弱化，即产生“约束漂移”现象。
method: 提出“约束状态治理”范式，将安全约束作为显式的执行状态进行维护，并采用约束原生强化学习在安全边界内优化系统效用。
result: 界定了多智能体系统中安全失效的新模式，并构建了一套确保约束在执行过程中保持新鲜、可继承且可审计的理论框架。
conclusion: 多智能体系统的安全不能仅靠初始声明，而必须通过将安全性转化为贯穿执行全过程的可操作状态来动态维持。
---

## 摘要
现代基于大语言模型（LLM）的智能体不再是消极的文本生成器。它们读取代码库、调用工具、浏览网页、执行代码、维护记忆、与其他智能体通信，并通过长时程工作流采取行动。这种转变改变了安全性的基本单元。一个系统可能会产生合规的最终答案，但同时通过内部消息泄露私密信息、授权超出其原始范围、在带有敏感上下文的情况下调用外部工具，或者丢失重建为何允许某项操作所需的证据。我们认为，基于 LLM 的多智能体系统中出现的许多故障具有共同的结构：安全关键约束在整个执行轨迹中未能保持有效。我们将这种现象称为“约束漂移”：即约束在经过记忆、授权、通信、工具使用、审计和优化过程中发生的丢失、扭曲、削弱或松弛。本文持有的观点是，安全的多智能体行为必须得到维持，而不仅仅是断言。提示词、护栏、工具模式、访问控制和最终输出检查是必要的，但除非约束在执行过程中保持新鲜、可继承、可强制执行且可审计，否则它们是不充分的。我们提出“约束状态治理”作为基于 LLM 的多智能体系统的研究范式。在该范式中，安全关键约束被维护为显式的执行状态，而约束原生强化学习仅在维持的安全边界内提高效用。其目标不是将智能体系统冻结在僵化的规则之下，而是使安全性在现代智能体实际行动的轨迹中具备可操作性。

## Abstract
Modern LLM based agents are no longer passive text generators. They read repositories, call tools, browse the web, execute code, maintain memory, communicate with other agents, and act through long horizon workflows. This shift moves the unit of safety. A system may produce a compliant final answer while leaking private information through an internal message, delegating authority beyond its original scope, calling an external tool with sensitive context, or losing the evidence needed to reconstruct why an action was allowed. We argue that many emerging failures in LLM-based multi-agent systems share a common structure: safety critical constraints do not remain operative throughout the trajectory. We call this phenomenon constraint drift: the loss, distortion, weakening, or relaxation of constraints as they pass through memory, delegation, communication, tool use, audit, and optimization. The position taken here is that safe multi-agent behavior must be maintained, not merely asserted. Prompts, guardrails, tool schemas, access control, and final output checks are necessary, but they are insufficient unless constraints remain fresh, inherited, enforceable, and auditable across execution. We propose Constraint State Governance as a research paradigm for LLM-based multi-agent systems. In this paradigm, safety-critical constraints are maintained as explicit execution state, while constraint-native reinforcement learning improves utility only within maintained safety boundaries. The goal is not to freeze agentic systems under rigid rules, but to make safety operational across the trajectories through which modern agents actually act.

---

## 论文详细总结（自动生成）

这是一份关于论文《Safe Multi-Agent Behavior Must Be Maintained, Not Merely Asserted: Constraint Drift in LLM-Based Multi-Agent Systems》的结构化深入分析总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：现代基于大语言模型（LLM）的智能体已从单纯的文本生成器演变为复杂的执行系统，能够调用工具、操作代码库、维护记忆并进行多智能体协作。
*   **核心问题**：传统的安全性评估往往只关注最终输出（Final Answer），但在长程执行轨迹（Trajectory）中，安全约束往往会随着记忆更新、任务委派、工具调用等环节逐渐失效。
*   **核心概念——约束漂移（Constraint Drift）**：作者定义了这一新现象，即安全关键约束在执行过程中发生的丢失、扭曲、削弱或松弛。例如，一个禁止泄露 API 密钥的规则，可能在智能体总结上下文或进行内部通信时被忽略，导致虽然最终答案看似合规，但中间过程已造成实质性危害。

### 2. 论文提出的方法论
论文提出了 **约束状态治理（Constraint State Governance, CSG）** 范式，旨在将安全性从“口头声明”转变为“系统级维护”。

*   **核心思想**：将安全约束视为显式的、可验证的“执行状态”，而非仅仅是提示词（Prompt）的一部分。
*   **关键技术细节**：
    1.  **约束令牌化（Constraint Tokenization）**：将自然语言规则转化为带签名的令牌（Token），包含唯一标识、执行谓词、适用范围、优先级和有效期。
    2.  **状态维护**：系统实时维护一个包含活跃约束集、权限映射、敏感对象标签（Taint Tracking）和审计根的全局状态。
    3.  **准入控制算法（Algorithm 1）**：在执行任何关键动作前，必须通过多重检查：
        *   **新鲜度（Freshness）**：确保动作基于最新的约束状态。
        *   **权限与范围（Capability & Scope）**：验证智能体是否有权执行该操作。
        *   **流控（Flow）**：检查敏感信息是否跨越了禁止的通道。
        *   **证据（Evidence）**：确保破坏性操作有可追溯的授权证据。
*   **约束原生强化学习（Constraint Native RL）**：提出一种新的学习目标，将强制性约束定义为“准入边界”。只有在满足所有安全约束的轨迹（Admissible Set）中，才进行效用（Utility）优化，从而避免智能体通过牺牲安全性来换取任务成功率。

### 3. 实验设计
*   **数据集/场景**：使用了 **AgentLeak** 数据集，这是一个针对多智能体 LLM 系统隐私泄露的全栈基准测试。
*   **实验规模**：包含来自医疗、金融、法律和企业领域的 **4,979 条** 真实执行轨迹。
*   **对比方法**：
    1.  **无防御（No Defense）**：基准状态。
    2.  **仅输出过滤（Output Only Filter）**：仅检查最终返回给用户的答案。
    3.  **CSG Lite（本文方法）**：在中间通道（如智能体间消息、共享记忆）实施转换级别的准入控制。
*   **评估指标**：泄露率（C1 最终输出、C2 内部消息、C5 记忆写入）、审计事件数量、信息保留率（Retention）等。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **实验性质**：由于主要采用的是基于现有数据集的“反事实回放”（Counterfactual Replay）研究，其计算开销主要集中在对 4,979 条轨迹的重新扫描、谓词检查和离线策略搜索上，而非大规模的模型预训练。

### 5. 实验数量与充分性
*   **实验组数**：
    *   针对 4,979 条轨迹进行了完整的泄露防御对比实验。
    *   进行了离线策略搜索实验，对比了 5 种不同偏好的治理策略（如严格型、平衡型、字段感知型）。
*   **充分性与客观性**：实验涵盖了多个敏感领域，具有较强的代表性。通过对比“输出过滤”和“过程治理”，清晰地揭示了“约束漂移”的隐蔽性。虽然实验是基于回放而非实时动态交互，但作为验证“约束状态治理”必要性的初步研究，其逻辑是自洽且客观的。

### 6. 主要结论与发现
*   **输出检查的局限性**：仅靠过滤最终输出，虽然能将表面泄露率降至 2.9%，但内部通道（消息和记忆）的泄露率依然高达 68.8%。
*   **CSG 的有效性**：CSG Lite 能将内部总泄露率从 68.9% 降至 5.7%，并生成了近 2 万个可审计的决策事件。
*   **效用与安全的平衡**：通过约束原生 RL，可以在确保不违反安全边界的前提下，优化信息的保留率和任务效用，证明了安全治理不必以牺牲系统可用性为代价。

### 7. 优点（亮点）
*   **视角独特**：首次系统性地提出了“约束漂移”概念，准确捕捉了 LLM 智能体从文本到执行转变过程中的核心安全痛点。
*   **系统级方案**：不同于传统的 Prompt Engineering，CSG 借鉴了计算机系统安全（如能力访问控制、污点追踪）的成熟思想，方案更具鲁棒性。
*   **可审计性**：强调了安全不仅要被执行，还要能被“重建”和“审计”，这对于受监管行业的 AI 应用至关重要。

### 8. 不足与局限
*   **语义提取风险**：CSG 的效果高度依赖于对动作效果（EFFECT）的准确提取，如果模型无法准确理解某个工具调用的潜在副作用，治理层可能会失效。
*   **复杂约束的公式化**：并非所有人类规范都能轻易转化为可执行的谓词（Predicates），对于模糊的道德约束，该框架的适用性受限。
*   **性能开销**：在每一跳执行中引入签名验证和准入检查会增加系统延迟，论文未详细讨论在大规模实时系统中的性能损耗。
*   **回放实验局限**：回放实验无法模拟智能体在动作被拦截后的“重试”或“绕过”行为，真实的动态博弈可能更复杂。

（完）
