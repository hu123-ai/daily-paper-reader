---
title: "Orchard: An Open-Source Agentic Modeling Framework"
title_zh: Orchard：一个开源的智能体建模框架
authors: "Baolin Peng, Wenlin Yao, Qianhui Wu, Hao Cheng, Xiao Yu, Rui Yang, Tao Ge, Alessandrio Sordoni, Xingdi Yuan, Yelong Shen, Pengcheng He, Tong Zhang, Zhou Yu, Jianfeng Gao"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.15040v1"
tags: ["query:malm"]
score: 9.0
evidence: 用于可扩展智能体训练的开源智能体建模框架
tldr: Orchard 是一个开源的智能体建模框架，旨在解决开源社区在智能体基础设施和大规模训练方面的短板。该框架核心是 Orchard Env 环境服务，提供可复用的沙箱管理原语。研究团队基于此开发了针对软件工程、视觉计算机操作和个人助手的三个训练方案，通过蒸馏轨迹、改进的 SFT 和强化学习技术，在多个基准测试中刷新了开源模型的性能记录。
selection_source: fresh_fetch
motivation: 针对当前智能体研究过度依赖闭源系统且缺乏可扩展开源训练框架的问题，提出标准化的基础设施。
method: 引入 Orchard Env 环境服务实现沙箱管理，并结合信用分配 SFT、平衡自适应 Rollout RL 等技术优化不同领域的智能体训练。
result: "Orchard-SWE 在 SWE-bench Verified 达到 67.5%，Orchard-GUI 在 WebVoyager 等测试中成为性能最强的开源模型。"
conclusion: 统一且轻量级的环境层是实现跨领域智能体数据复用、高效训练和准确评估的关键。
---

## 摘要
智能体建模（Agentic modeling）旨在将大语言模型（LLMs）转化为能够通过规划、推理、工具使用以及与环境的多轮交互来解决复杂任务的自主智能体。尽管投入巨大，但开源研究仍受限于基础设施和训练方面的差距。许多高性能系统依赖于专有的代码库、模型或服务，而大多数开源框架侧重于编排和评估，而非可扩展的智能体训练。我们提出了 Orchard，一个用于可扩展智能体建模的开源框架。其核心是 Orchard Env，这是一个轻量级环境服务，为跨任务领域、智能体测试框架（harnesses）和流水线阶段的沙箱生命周期管理提供可重用的原语。在 Orchard Env 之上，我们构建了三种智能体建模方案。Orchard-SWE 针对编程智能体。我们从 MiniMax-M2.5 和 Qwen3.5-397B 中蒸馏了 107K 条轨迹，引入了信用分配（credit-assignment）有监督微调（SFT）以从未解决轨迹的高效片段中学习，并应用了平衡自适应展开（Balanced Adaptive Rollout）进行强化学习（RL）。以 Qwen3-30B-A3B-Thinking 为起点，Orchard-SWE 在 SFT 后在 SWE-bench Verified 上达到 64.3%，在 SFT+RL 后达到 67.5%，在同等规模的开源模型中创下了新的 SOTA。Orchard-GUI 仅使用 0.4K 条蒸馏轨迹和 2.2K 个开放式任务训练了一个 4B 参数的视觉语言计算机使用智能体。它在 WebVoyager、Online-Mind2Web 和 DeepShop 上分别实现了 74.1%、67.0% 和 64.0% 的成功率，使其成为最强的开源模型，同时保持了与专有系统的竞争力。Orchard-Claw 针对个人助理智能体。仅使用 0.2K 个合成任务进行训练，它在 Claw-Eval 上实现了 59.6% 的 pass@3，在与更强大的 ZeroClaw 测试框架配合时达到 73.9%。总之，这些结果表明，一个轻量级、开放且与测试框架无关的环境层能够实现跨领域的智能体数据、训练方案和评估的可重用性。

## Abstract
Agentic modeling aims to transform LLMs into autonomous agents capable of solving complex tasks through planning, reasoning, tool use, and multi-turn interaction with environments. Despite major investment, open research remains constrained by infrastructure and training gaps. Many high-performing systems rely on proprietary codebases, models, or services, while most open-source frameworks focus on orchestration and evaluation rather than scalable agent training. We present Orchard, an open-source framework for scalable agentic modeling. At its core is Orchard Env, a lightweight environment service providing reusable primitives for sandbox lifecycle management across task domains, agent harnesses, and pipeline stages. On top of Orchard Env, we build three agentic modeling recipes. Orchard-SWE targets coding agents. We distill 107K trajectories from MiniMax-M2.5 and Qwen3.5-397B, introduce credit-assignment SFT to learn from productive segments of unresolved trajectories, and apply Balanced Adaptive Rollout for RL. Starting from Qwen3-30B-A3B-Thinking, Orchard-SWE achieves 64.3% on SWE-bench Verified after SFT and 67.5% after SFT+RL, setting a new state of the art among open-source models of comparable size. Orchard-GUI trains a 4B vision-language computer-use agent using only 0.4K distilled trajectories and 2.2K open-ended tasks. It achieves 74.1%, 67.0%, and 64.0% success rates on WebVoyager, Online-Mind2Web, and DeepShop, respectively, making it the strongest open-source model while remaining competitive with proprietary systems. Orchard-Claw targets personal assistant agents. Trained with only 0.2K synthetic tasks, it achieves 59.6% pass@3 on Claw-Eval and 73.9% when paired with a stronger ZeroClaw harness. Collectively, these results show that a lightweight, open, harness-agnostic environment layer enables reusable agentic data, training recipes, and evaluations across domains.

---

## 论文详细总结（自动生成）

### Orchard: 开源智能体建模框架论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：尽管大语言模型（LLM）智能体研究投入巨大，但开源社区在**可扩展的智能体建模（Scalable Agentic Modeling）**方面仍面临基础设施和训练方案的严重短板。
*   **研究动机**：
    *   **基础设施瓶颈**：现有的高性能智能体系统多依赖闭源环境或专有服务。开源框架多关注“编排（Orchestration）”而非“训练（Training）”。
    *   **环境耦合**：环境管理层往往与特定的训练栈或测试框架（Harness）紧密耦合，导致数据、方案和评估难以跨领域复用。
    *   **训练挑战**：智能体训练面临监督信号有限（SFT数据稀缺）和奖励稀疏（RL难以收敛）的问题。

#### 2. 方法论：核心思想与关键技术
Orchard 提出了一个三层架构：**Orchard Env（环境层）**、**Open Training Recipes（训练方案层）**和**Domain-specific Agents（领域应用层）**。

*   **Orchard Env（核心基础设施）**：
    *   **Kubernetes 原生服务**：采用轻量级 REST API 管理沙箱生命周期。
    *   **运行时智能体注入（Agent Injection）**：通过 K8s 的 Init Container 将执行代理注入任意 Docker 镜像，无需重新构建镜像即可支持异构任务。
    *   **直接 Pod-IP 通信**：绕过 K8s API Server 的中转，直接与沙箱内的代理通信，将命令执行延迟降低至 0.28s。
*   **Orchard-SWE（软件工程方案）**：
    *   **信用分配 SFT（Credit-assignment SFT）**：利用老师模型对失败轨迹进行回顾性价值估计，提取出其中“有进展”的片段（Rise Segments）进行微调，变废为宝。
    *   **平衡自适应展开（Balanced Adaptive Rollout, BAR）**：在 RL 阶段，通过步进式采样确保每个训练批次中正负样本比例平衡，解决奖励极其稀疏的问题。
*   **Orchard-GUI（视觉导航方案）**：
    *   **多模态 ReAct 循环**：针对 4B 规模的视觉语言模型（VLM），采用通用的工具调用接口。
    *   **步长预算课程学习**：先在短路径任务上训练，再逐步增加交互步数上限。
*   **Orchard-Claw（个人助手方案）**：
    *   **跨测试框架训练**：研究智能体技能如何在不同交互框架（如 ZeroClaw）之间迁移。

#### 3. 实验设计
论文在三个核心领域进行了广泛评估：
*   **软件工程 (SWE)**：
    *   **数据集**：SWE-bench Verified (500个任务)、SWE-rebench、Scale-SWE。
    *   **对比方法**：Qwen 2.5/3 序列、DeepSeek-V3/R1、GLM-4/5、以及专有的 Claude/GPT 系列。
*   **视觉 GUI 导航**：
    *   **数据集**：WebVoyager、Online-Mind2Web、DeepShop。
    *   **对比方法**：UI-TARS、MolmoWeb、Fara、GPT-4o/o3、Gemini 3。
*   **个人助手 (Claw)**：
    *   **数据集**：Claw-Eval。
    *   **对比方法**：Nemotron-3、Qwen3-Coder 等同规模模型。

#### 4. 资源与算力
*   **系统测试环境**：使用了 8 个节点的集群（每个节点 32 vCPU, 128 GiB RAM）。
*   **训练框架**：基于 `slime` 框架，结合 Ray、Megatron-LM 和 SGLang 进行分布式训练。
*   **具体算力细节**：文中**未明确给出**具体的 GPU 型号（如 A100 或 H100）及总训练时长，但提到了全局 Batch Size 为 128，上下文窗口最高达 128K/256K。
*   **成本优势**：论文强调 Orchard Env 相比 E2B 或 Daytona 等商业方案，在相同负载下可降低 50%-90% 的成本（利用 K8s 竞价实例）。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   SWE 领域使用了 107K 条蒸馏轨迹。
    *   GUI 领域使用了 2.6K 个训练任务。
    *   进行了大量的**消融实验**，包括数据规模、数据选择策略、跨 Harness 泛化性、SFT 与 RL 的增益对比等。
*   **充分性与公平性**：实验设计较为客观。论文特别讨论了“测试框架锁定（Harness Lock-in）”现象，即模型在训练时使用的 Harness 上表现好，换一个就崩溃。Orchard 通过多 Harness 训练证明了其鲁棒性，这种对比在同类论文中较为罕见且具有说服力。

#### 6. 主要结论与发现
*   **性能突破**：
    *   **Orchard-SWE (30B)**：在 SWE-bench Verified 上达到 67.5%，超越了许多参数量大 10-30 倍的专家模型。
    *   **Orchard-GUI (4B)**：平均成功率 68.4%，成为目前最强的开源 GUI 智能体，甚至在某些指标上超过了其 235B 的老师模型。
*   **环境层的重要性**：一个轻量、解耦的环境层是实现智能体数据复用和大规模 RL 训练的基石。
*   **RL 的价值**：即使在高质量 SFT 之后，环境反馈驱动的 RL 仍能显著提升智能体处理长程任务和错误恢复的能力。

#### 7. 优点
*   **基础设施开源**：填补了开源社区缺乏高性能、低成本智能体训练环境服务的空白。
*   **算法创新**：提出的“信用分配 SFT”和“BAR 采样策略”有效解决了智能体训练中数据利用率低和奖励稀疏的痛点。
*   **泛化性强**：证明了同一套环境抽象和训练方案可以跨越文本编程、视觉导航和工具使用三个截然不同的领域。

#### 8. 不足与局限
*   **算力细节模糊**：未披露具体的 GPU 资源消耗，对于想要复现的开发者来说，难以预估硬件门槛。
*   **泛化瓶颈**：虽然提升了跨 Harness 的鲁棒性，但在完全未见的 Harness 或极端 OOD（分布外）任务上，性能仍有明显下降。
*   **依赖老师模型**：训练数据高度依赖于 MiniMax-M2.5 或 Qwen3.5 等顶级模型的蒸馏，若缺乏这些强力老师，方案的效果可能会打折扣。

（完）
