---
title: "MetaAgent-X : Breaking the Ceiling of Automatic Multi-Agent Systems via End-to-End Reinforcement Learning"
title_zh: MetaAgent-X：通过端到端强化学习突破自动多智能体系统的瓶颈
authors: "Yaolun Zhang, Yujie Zhao, Nan Wang, Yiran Wu, Jiayu Chang, Yizhao Chen, Qingyun Wu, Jishen Zhao, Huazheng Wang"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.14212v1"
tags: ["query:malm"]
score: 9.0
evidence: 自动多智能体系统的端到端强化学习
tldr: MetaAgent-X 提出了一个端到端强化学习框架，旨在解决现有自动多智能体系统（MAS）中设计者与执行者优化不协同的问题。通过联合优化 MAS 的设计与执行，并引入分层回滚和阶段性协同进化策略，该框架打破了固定执行器的性能瓶颈。实验证明，MetaAgent-X 在多个基准测试中显著优于现有方法，为构建自设计、自执行的智能体模型提供了新范式。
selection_source: fresh_fetch
motivation: 现有的自动多智能体系统通常只优化设计层而冻结执行层，导致系统性能受限于固定执行器的能力上限。
method: 提出 MetaAgent-X 框架，利用端到端强化学习同时优化设计者和执行者，并采用分层回滚和阶段性协同进化策略提升训练稳定性。
result: "MetaAgent-X 在性能上显著超越了现有的自动 MAS 基准，最高实现了 21.7% 的性能提升。"
conclusion: 端到端可训练的自动 MAS 是构建自设计与自执行智能体模型的一种实用且高效的范式。
---

## 摘要
自动多智能体系统旨在不依赖手动设计或固定编排的情况下实例化智能体工作流。然而，现有的自动多智能体系统（MAS）方法仍仅具有部分自适应性：它们要么执行无需训练的测试时搜索，要么在保持下游执行智能体冻结的情况下优化元级设计器，这造成了“冻结执行器”瓶颈，并使得自设计和自执行智能体模型的端到端训练仍未得到探索。为解决这一问题，我们引入了 MetaAgent-X，这是一个端到端强化学习框架，可联合优化自动 MAS 的设计与执行。MetaAgent-X 支持基于脚本的 MAS 生成、执行展开收集，以及针对设计器和执行器轨迹的信用分配。为了支持稳定且可扩展的优化，我们提出了执行器-设计器分层展开和阶段性协同演化，以提高训练稳定性并揭示设计器与执行器协同演化的动态过程。MetaAgent-X 的表现一贯优于现有的自动 MAS 基准，实现了高达 21.7% 的提升。全面的消融实验表明，设计器和执行器在整个训练过程中均有所提升，且有效的自动 MAS 学习遵循阶段性协同演化过程。这些结果确立了端到端可训练的自动 MAS 是构建自设计和自执行智能体模型的一种实用范式。

## Abstract
Automatic multi-agent systems aim to instantiate agent workflows without relying on manually designed or fixed orchestration. However, existing automatic MAS approaches remain only partially adaptive: they either perform training-free test-time search or optimize the meta-level designer while keeping downstream execution agents frozen, which creating a frozen-executor ceiling and leaving the end-to-end training of self-designing and self-executing agentic models unexplored. To address this, we introduce MetaAgent-X, an end-to-end reinforcement learning framework that jointly optimizes automatic MAS design and execution. MetaAgent-X enables script-based MAS generation, execution rollout collection, and credit assignment for both designer and executor trajectories. To support stable and scalable optimization, we propose Executor Designer Hierarchical Rollout and Stagewise Co-evolution to improve training stability and expose the dynamics of designer-executor co-evolution. MetaAgent-X consistently outperforms existing automatic MAS baselines, achieving up to 21.7% gains. Comprehensive ablations show that both designer and executor improve throughout training, and that effective automatic MAS learning follows a stagewise co-evolution process. These results establish end-to-end trainable automatic MAS as a practical paradigm for building self-designing and self-executing agentic models.

---

## 论文详细总结（自动生成）

以下是对论文《MetaAgent-X: Breaking the Ceiling of Automatic Multi-Agent Systems via End-to-End Reinforcement Learning》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机与背景）
*   **核心问题**：现有的自动多智能体系统（MAS）存在“部分自适应”的局限。它们要么在推理时进行无需训练的搜索，要么仅优化负责设计的“元智能体（Designer）”而冻结负责执行的“下游智能体（Executor）”。
*   **研究动机**：冻结的执行器限制了系统的性能上限，且设计者无法根据执行器的反馈诱导其产生特定的协作行为。
*   **整体含义**：论文提出了 **MetaAgent-X**，这是首个通过**端到端强化学习（RL）**同时优化 MAS 设计者和执行者的框架，旨在打破“冻结执行器”带来的性能瓶颈，实现设计与执行的深度协同进化。

### 2. 方法论：核心思想与关键技术
MetaAgent-X 将自动 MAS 建模为一个耦合的在线强化学习问题，核心包含以下三个关键设计：
*   **端到端在线 RL 流水线**：
    *   **设计者（Designer）**：根据任务查询生成轻量级 Python 脚本，定义角色、交互协议和工具调用。
    *   **执行者（Executor）**：运行生成的脚本，在环境中执行任务并收集轨迹。
    *   **优化目标**：使用 **GRPO（Group Relative Policy Optimization）** 算法，根据环境反馈的奖励信号更新策略参数。
*   **执行器-设计器分层回滚（Hierarchical Rollout）**：
    *   为了解决信用分配（Credit Assignment）难题（即失败是因为设计不好还是执行不力），采用树状采样结构：为每个问题采样 $M$ 个设计，每个设计进行 $N$ 次独立执行。
    *   **设计者奖励**：取 $N$ 次执行的平均结果，以平滑执行过程中的随机性。
    *   **执行者奖励**：基于单次执行的结果，但在问题级别进行归一化，以提供更稳定的训练信号。
*   **阶段性协同进化（Stagewise Co-evolution）**：
    *   为了避免设计者和执行者互为动态环境导致的训练不稳定，采用交替训练策略：固定 $K$ 个步骤只更新执行者，接下来的 $K$ 个步骤只更新设计者。

### 3. 实验设计
*   **数据集与场景**：
    *   **数学推理**：AIME 2024、AIME 2025、OlympiadBench。
    *   **代码生成**：APPS、LiveCodeBench-v6、CodeContests。
*   **对比基准（Baselines）**：
    *   **单智能体**：直接提示（SA）、SA + GRPO。
    *   **基于搜索的自动 MAS**：AFlow、ADAS。
    *   **基于 RL 的自动 MAS**：ScoreFlow、MaAS、AFM-Coder（Agent Foundation Model）。
*   **模型规模**：基于 Qwen3-4B 和 Qwen3-8B（非思考模式）进行实验。

### 4. 资源与算力
*   **硬件环境**：使用了单节点，配备 **8 张 NVIDIA H200 GPU**。
*   **训练细节**：
    *   **SFT 阶段**：使用 DeepSeek-V3.2 蒸馏的 3K 设计者数据和 8K 执行者数据进行冷启动。
    *   **RL 阶段**：学习率为 $5 \times 10^{-6}$，阶段长度 $K=30$，每个查询采样 $M=4$ 个设计且每个设计执行 $N=4$ 次。
    *   **上下文长度**：最大支持 8192 标记。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 6 个主流数学和代码基准测试，使用了两种不同规模的底座模型（4B 和 8B）。
*   **消融实验**：非常详尽，包括：
    1.  分层回滚配置（$M$ 与 $N$ 的比例）。
    2.  阶段性训练 vs. 耦合训练 vs. 仅训练单一角色。
    3.  共享策略参数 vs. 独立策略参数。
    4.  阶段长度 $K$ 的敏感度分析。
*   **客观性**：通过与多种最新（2024-2025年）的自动 MAS 框架对比，并使用统一的评估环境，实验结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能突破**：MetaAgent-X RL 在所有基准测试中均优于单智能体和现有的自动 MAS 方法，平均提升显著，最高在 AIME24 上比单智能体提升了 **21.7%**。
*   **协同进化效应**：消融实验证明，阶段性训练是成功的关键。设计者学会了根据任务难度选择结构（如难题选“反射”结构，简单题选“单智能体”），而执行者在相同结构下的解题能力也得到了增强。
*   **共享策略优势**：设计者和执行者共享模型参数比独立参数效果更好，表明两者之间存在知识迁移和互补。

### 7. 优点
*   **范式创新**：首次实现了自动 MAS 的端到端全参数微调，打破了以往方法中执行器性能受限的“天花板”。
*   **信用分配机制**：提出的树状分层回滚有效地解决了多智能体强化学习中设计与执行职责模糊的问题。
*   **任务自适应性**：系统能够根据任务特性动态生成 Python 脚本，比固定的工作流模板更具灵活性。

### 8. 不足与局限
*   **算力开销**：由于采用了 $M \times N$ 的采样策略，训练时的计算成本较高（尽管论文通过 GRPO 优化了显存，但采样量依然巨大）。
*   **模型规模限制**：实验主要集中在 4B 和 8B 规模，尚未在超大规模模型（如 70B+）上验证其扩展性（Scaling Law）。
*   **领域局限**：目前仅在数学和代码这两个具有明确客观评价指标（单元测试/标准答案）的领域进行了验证，在创意写作或开放式对话等主观领域的表现尚不明确。

（完）
