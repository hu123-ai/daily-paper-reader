---
title: "SkillFlow: Flow-Driven Recursive Skill Evolution for Agentic Orchestration"
title_zh: SkillFlow：面向智能体编排的流驱动递归式技能演化
authors: "Mingda Zhang, Tiesunlong Shen, Haoran Luo, Wenjin Liu, Zikai Xiao, Erik Cambria, Xiaoying Tang"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.14089v1"
tags: ["query:malm"]
score: 9.0
evidence: 基于大语言模型的智能体任务编排系统
tldr: 针对大语言模型智能体在任务编排中面临的策略崩溃、信用分配不透明及技能演化缺乏指导等挑战，本文提出 SkillFlow 框架。该框架采用基于流匹配的 Tempered Trajectory Balance (TTB) 损失函数，在保持策略多样性的同时实现透明的每步信用分配。通过递归技能演化机制，SkillFlow 能够自主决定技能的创建与修剪，在问答、推理和代码生成等 14 个数据集上显著优于现有基准。
selection_source: fresh_fetch
motivation: 现有的智能体编排方法存在策略单一化、信用分配模糊以及技能演化缺乏原理性指导信号的问题。
method: 提出基于流匹配的 SkillFlow 框架，利用 TTB 损失函数平衡探索与奖励，并通过递归机制实现技能库的自主优化。
result: 在涵盖数学推理、代码生成和交互式决策的 14 个基准测试中，SkillFlow 的表现均显著超越了现有主流方法。
conclusion: SkillFlow 通过将流诊断与技能演化相结合，成功闭合了从训练信号到自主能力增长的环路，提升了智能体编排的效率与多样性。
---

## 摘要
近年来，各种强大的基于大语言模型（LLM）的智能体系统已被应用于通过任务编排实现复杂任务的自动化。然而，现有的编排方法仍面临关键挑战，包括奖励最大化下的策略崩溃、信用分配不透明导致的高梯度方差，以及缺乏引导的技能演化（其决策通常通过直接提示 LLM 进行判断，而非源自原则性的训练信号）。为了应对这些挑战，我们提出了 SkillFlow，这是一个基于流（flow-based）的框架，它将可训练的监督者（Supervisor）作为智能体，并构建了一个包含动态技能库和冻结执行器的结构化环境，通过多轮交互实现任务编排的自动化。SkillFlow 采用了回火轨迹平衡（Tempered Trajectory Balance, TTB），这是一种基于回归的流匹配损失，它按奖励比例对轨迹进行采样，从而保留多样化的编排策略，避免坍缩至单一模式。同一流目标产生了一个联合学习的反向策略，在零额外推理成本的情况下提供了透明的每步信用分配。基于这些流诊断，递归式技能演化机制可以确定何时进行演化、创建或剪枝哪些技能，以及决策差距所在，从而闭合了从训练信号到自主能力增长的环路。在 14 个数据集上的实验结果表明，SkillFlow 在问答、数学推理、代码生成和现实世界交互式决策任务中显著优于基准模型。我们的代码可在 https://anonymous.4open.science/r/SkillFlow-E850 获取。

## Abstract
In recent years, a variety of powerful LLM-based agentic systems have been applied to automate complex tasks through task orchestration. However, existing orchestration methods still face key challenges, including strategy collapse under reward maximization, high gradient variance with opaque credit assignment, and unguided skill evolution whose decisions are typically made by directly prompting an LLM to judge rather than derived from principled training signals. To address these challenges, we propose SkillFlow, a flow-based framework that takes a trainable Supervisor as the agent and a structured environment with dynamic skill library and frozen executor, automating task orchestration through multi-turn interaction. SkillFlow employs Tempered Trajectory Balance (TTB), a regression-based flow-matching loss that samples trajectories proportional to reward, preserving diverse orchestration strategies rather than collapsing to a single mode. The same flow objective yields a jointly learned backward policy that provides transparent per-step credit assignment at zero additional inference cost. Building on these flow diagnostics, a recursive skill evolution mechanism determines when to evolve, what skills to create or prune, and where decision gaps lie -- closing the loop from training signal to autonomous capability growth. Experimental results on 14 datasets show that SkillFlow significantly outperforms baselines across question answering, mathematical reasoning, code generation, and real-world interactive decision making tasks. Our code is available at https://anonymous.4open.science/r/SkillFlow-E850.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **SkillFlow** 的框架，旨在解决大语言模型（LLM）智能体在复杂任务编排（Orchestration）中面临的策略单一、信用分配模糊以及技能演化缺乏指导等核心问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在复杂的端到端任务中，智能体需要通过编排原始动作和可重用技能来完成目标。现有的编排方法主要存在三个痛点：
*   **策略崩溃（Strategy Collapse）：** 传统的强化学习（如 REINFORCE 系列）倾向于收敛到单一的最优路径，导致智能体丧失了探索多种有效策略的能力，在环境变化时表现脆弱。
*   **信用分配不透明：** 仅依靠终端奖励（Terminal Reward）很难确定多步操作中哪一步是成功的关键，导致梯度方差大。
*   **技能演化缺乏引导：** 现有的动态技能库更新通常依赖于启发式规则或直接让 LLM 充当裁判，缺乏基于训练信号的严谨指导。

**SkillFlow 的核心意义**在于引入了**流网络（Flow Network）**理论，通过将编排过程建模为有向无环图（DAG）上的流，实现了奖励比例采样和自动化的技能库演化。

### 2. 方法论：核心思想与关键技术
SkillFlow 包含一个可训练的监督者（Supervisor）和动态演化的技能库。
*   **回火轨迹平衡（Tempered Trajectory Balance, TTB）：** 这是核心损失函数。它是一种回归风格的流匹配损失，强制轨迹的采样概率与奖励成正比（而非仅追求最大奖励）。这保证了策略的多样性，使多条高奖励路径都能被保留。
*   **联合学习的正向与反向策略：** 框架同时训练正向策略（选择动作）和反向策略（事后评估）。通过两者的比率定义“步骤重要性”（Step Importance），在不增加额外推理成本的情况下，实现了透明的每步信用分配。
*   **递归式技能演化机制：** 利用流诊断信号回答三个问题：
    *   **何时演化（When）：** 当 TTB 损失进入平台期，说明当前技能库限制了性能。
    *   **演化什么（What）：** 根据“技能边际流”排名，决定创建新技能或剪枝低效技能。
    *   **在哪里演化（Where）：** 定位步骤重要性高的决策点，针对性地生成新技能。

### 3. 实验设计
*   **数据集与场景：** 涵盖了 14 个基准测试，分为 **IID（分布内）**和 **OOD（分布外）**两组。
    *   任务类型包括：多跳问答（HotpotQA）、数学推理（AIME, MATH）、代码生成（HumanEval, SWE-bench）、交互式决策（WebShop, ALFWorld, ScienceWorld）等。
*   **对比基准（Baselines）：**
    *   **直接调用：** Qwen3.5-9B, GPT-4o-mini, Claude 4.5 等。
    *   **微调与强化学习：** SFT, GRPO, AgentFlow, FlowSteer, SkillRL。
    *   **搜索方法：** AFlow。
*   **骨干模型：** 主要使用 Qwen3.5-9B 作为监督者，并测试了 6 种闭源模型以验证迁移性。

### 4. 资源与算力
论文在附录 P 中明确说明了算力消耗：
*   **硬件：** 1 台服务器，配备 **4 张 NVIDIA A100-SXM4 (80GB) GPU**。
*   **训练时长：** 主实验运行约 **73 小时**（约 292 GPU 小时）。
*   **训练参数：** 250 个训练步数，使用 LoRA 适配器进行微调。

### 5. 实验数量与充分性
*   **实验规模：** 论文进行了极其详尽的实验，包括 14 个数据集的性能对比、6 种不同 LLM 骨干模型的迁移性测试、针对核心组件（TTB、反向策略、演化信号）的消融实验，以及与多种最新强化学习算法（GRPO, HCAPO 等）的对比。
*   **公平性与客观性：** 实验采用了均值±标准差的形式报告结果，涵盖了 IID 和 OOD 场景，验证了模型不仅是“记住了”任务，而是学会了可迁移的编排原语。对比实验均在相同骨干模型和训练数据下进行，具有较高的客观性。

### 6. 主要结论与发现
*   **性能领先：** SkillFlow 在所有 14 个基准测试中均优于现有方法，尤其在需要复杂编排的任务（如 SWE-bench）中提升显著。
*   **多样性保护：** 相比于 GRPO 等方法，SkillFlow 能够维持更高的策略熵，保留了多种解决问题的路径。
*   **自主演化有效：** 自动生成的技能库比手动设计的或启发式生成的更高效，且能显著降低推理成本（减少了不必要的步骤）。
*   **零成本信用分配：** 证明了利用反向策略进行步骤重要性评估的有效性，且该过程不产生额外的推理开销。

### 7. 优点
*   **理论严谨：** 将 GFlowNet 引入智能体编排，为技能演化提供了原则性的数学信号，而非依赖 LLM 的主观判断。
*   **闭环系统：** 实现了从训练信号到能力增长的完全自动化闭环。
*   **高效性：** 相比于其他技能演化方法，SkillFlow 在达到相同精度时消耗的 Token 更少，训练更高效。

### 8. 不足与局限
*   **长上下文依赖：** 编排过程会产生较长的交互历史，对骨干模型的长文本处理能力有较高要求。
*   **基础推理能力限制：** 框架虽然优化了编排，但如果骨干模型本身缺乏解决特定领域问题的基础推理能力，SkillFlow 也无法凭空创造出这些能力。
*   **超参数敏感性：** 流网络中的温度参数 $\beta$ 和奖励平滑项 $\epsilon$ 可能需要针对不同任务进行微调以达到最佳效果。

（完）
