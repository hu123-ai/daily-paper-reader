---
title: "SkillGen: Verified Inference-Time Agent Skill Synthesis"
title_zh: SkillGen：经过验证的推理阶段智能体技能合成
authors: "Yuchen Ma, Yue Huang, Han Bao, Haomin Zhuang, Swadheen Shukla, Michel Galley, Xiangliang Zhang, Stefan Feuerriegel"
date: 2026-05-09
pdf: "https://arxiv.org/pdf/2605.10999v1"
tags: ["query:malm"]
score: 9.0
evidence: 合成智能体技能的多智能体框架
tldr: SkillGen是一个多智能体框架，旨在通过推理时合成可验证的技能来提升LLM智能体能力。它利用对比归纳法分析成功与失败轨迹，识别成功模式并生成可审计的技能。该方法将技能视为干预措施进行实证验证，确保其对性能的净提升。实验表明，SkillGen在多个任务中显著优于现有基准，且生成的技能具备跨模型迁移性。
selection_source: fresh_fetch
motivation: 现有的高质量智能体技能大多依赖手动编写，缺乏自动化且可验证的合成方法。
method: 采用对比归纳法分析成功与失败轨迹，并结合迭代优化和干预式验证来合成可审计的技能。
result: SkillGen在多种智能体和数据集上显著提升了性能，优于现有基准，且生成的技能具有跨模型迁移能力。
conclusion: SkillGen证明了通过自动化合成和实证验证推理时技能，可以有效且可控地增强LLM智能体的任务处理能力。
---

## 摘要
技能是提高大语言模型（LLM）智能体能力的一种极具前景的方法，无需重新训练，同时能保持新增过程的可重用性和可控性。然而，高质量的技能目前在很大程度上仍依赖于手工编写。我们介绍了 SkillGen，这是一个多智能体框架，能够从基础智能体生成的轨迹中合成单一的可审计技能。其输出是人类可读的工件，在使用前可以进行检查。SkillGen 不仅仅是总结轨迹，而是利用对成功和失败轨迹的对比归纳（contrastive induction），来识别可重用的成功模式、反复出现的失败模式，以及在相近的成功案例中出现但在失败案例中缺失的行为。随后，SkillGen 生成候选技能并对其进行迭代优化。SkillGen 的一个关键创新在于，我们将智能体技能建模为干预措施，以实证验证技能对整体性能的净效应。具体而言，我们比较了在相同实例上使用和不使用该技能的结果，从而同时考虑了修复（技能修复了基准失败的情况）和退化（技能破坏了基准成功的情况）。在广泛的智能体和数据集上，SkillGen 持续提升了留出集（held-out）的性能，优于现有的技能生成基准，并产生了可跨模型迁移的技能。

## Abstract
Skills are a promising way to improve LLM agent capabilities without retraining, while keeping the added procedure reusable and controllable. However, high-quality skills are still largely written by hand. We introduce SkillGen, a multi-agent framework that synthesizes a single auditable skill from trajectories generated by a base agent. The output is a human-readable artifact that can be inspected before use. Rather than merely summarizing trajectories, SkillGen leverages contrastive induction over both successful and failed trajectories to identify reusable success patterns, recurring failure modes, and behaviors that appear in nearby successes but are missing from failures. SkillGen then generates candidate skills and iteratively refines the skill. A key novelty in SkillGen is that we model agent skills as interventions to empirically verify the net effect of skills on the overall performance. Specifically, we compare outcomes on the same instances with and without the skill, so that we account for both repairs (cases where the skill fixes a baseline failure) and regressions (cases where the skill breaks a baseline success). Across a broad range of agents and datasets, SkillGen consistently improves held-out performance, outperforms existing skill-generation baselines, and produces skills that transfer across models.

---

## 论文详细总结（自动生成）

### SkillGen: 经过验证的推理阶段智能体技能合成

#### 1. 核心问题与整体含义
*   **研究动机**：虽然大语言模型（LLM）智能体可以通过“技能”（推理时的指令、代码或知识）来增强能力，但目前高质量的技能大多依赖人工编写。
*   **现有挑战**：现有的自动化技能合成方法存在两个主要缺陷：
    1.  **忽视对比信号**：大多仅从成功轨迹中学习，或者孤立地分析失败，未能识别出导致成功与失败差异的关键行为（例如，成功案例中多出的一个验证步骤）。
    2.  **缺乏实证验证**：技能在修复某些错误的同时，可能会导致原本能做对的题目出错（即“退化”现象）。现有方法缺乏对技能“净效应”的量化评估。
*   **核心目标**：开发一个能够自动从智能体经验中合成、验证并优化“可审计技能”的多智能体框架。

#### 2. 方法论
*   **核心思想**：将技能合成建模为一个**干预问题**。通过对比成功与失败轨迹提取模式，并利用因果推断中的“潜在结果框架”来衡量技能对性能的净提升。
*   **算法流程**：
    1.  **基准诱导（Baseline Elicitation）**：在诱导集上运行基础智能体，收集成功（$I^+$）和失败（$I^-$）的轨迹。
    2.  **对比行为归纳（Contrastive Induction）**：
        *   **失败/成功分析**：对轨迹进行聚类，总结反复出现的失败根源和成功技术。
        *   **局部对比分析**：寻找失败案例在嵌入空间中最近的成功邻居，识别出“成功者做了而失败者没做”的具体动作。
    3.  **生成-验证-优化循环（Gen-Verify-Refine Loop）**：
        *   **生成**：将诊断结果转化为结构化技能（包含任务上下文、成功程序、失败教训）。
        *   **验证**：在验证集上对比“有技能”和“无技能”的表现，计算**净收益 $G_m = \text{修复数} - \text{退化数}$**。
        *   **优化**：根据验证反馈（保持、删除、添加、强调）进行迭代。
        *   **验证门（Verification Gate）**：只有当净收益超过特定阈值时，技能才会被激活，否则弃用。

#### 3. 实验设计
*   **数据集/场景**：涵盖了 10 个基准测试，包括交互式环境（ALFWorld, ScienceWorld）、编程（LiveCodeBench）、Web 导航（Mind2Web）、科学问答（PubMedQA）、工具使用（MCPBench, $\tau$-Bench）和化学预测（ChemLLMBench）。
*   **基准模型**：评估了 8 个主流模型，包括开源模型（Llama-3.1-8B, Qwen-2.5-7B, Gemma-4-26B, Mistral-Nemo）和闭源模型（GPT-5.4 系列, Claude-4.5-Haiku, Grok-4-Fast）。
*   **对比方法**：与四种最先进的技能生成基准进行了对比：**Trace2Skill**、**SkillX**、**EvoSkill** 和 **CoEvoSkills**。

#### 4. 资源与算力
*   **算力说明**：论文未提及具体的 GPU 硬件型号或训练时长，因为该方法主要基于 API 调用（推理阶段合成）。
*   **Token 成本**：论文提供了详细的 Token 消耗分析。构建一个技能的平均成本约为 **5.6M tokens**（以 GPT-5.4-Mini 为例，单次构建成本约 **8.2 美元**）。这属于一次性构建成本，后续推理时的额外开销（Overhead）中位数为每个调用约 1400 tokens。

#### 5. 实验数量与充分性
*   **实验规模**：共进行了 **80 组**“模型-数据集”组合的留出集评估，规模庞大。
*   **消融实验**：针对 ICL 示例、优化循环、验证门、失败教训组件、脚本组件等进行了多组消融实验，验证了各模块的必要性。
*   **客观性**：采用了配对评估（Paired Evaluation），即在完全相同的实例上对比有无技能的表现，有效排除了随机性干扰，实验设计严谨、公平。

#### 6. 主要结论与发现
*   **性能显著提升**：SkillGen 提升了所有 8 个基础模型的平均准确率，增幅在 **+3.27% 到 +10.08%** 之间。
*   **优于现有基准**：在所有对比测试中，SkillGen 的净收益均显著高于 Trace2Skill 和 EvoSkill 等方法。
*   **跨模型迁移性**：由一个模型（如 Qwen）生成的技能，往往可以直接提升另一个模型（如 Llama）的性能，证明了技能的通用性。
*   **验证的必要性**：实验显示，迭代优化过程并不总是单调提升的，如果没有“验证门”和“最佳轮次选择”，最新的候选技能往往会因为过拟合而导致性能下降。

#### 7. 优点
*   **对比学习机制**：通过分析“差一点就成功”的对比对，捕捉到了细粒度的行为差异。
*   **安全性与可控性**：引入“退化（Regressions）”概念，确保技能不会破坏模型原有的能力。
*   **人类可读**：输出的技能是 Markdown 格式，人类专家可以轻松审计、修改或手动干预。

#### 8. 不足与局限
*   **过拟合风险**：尽管有验证门，但在某些 OOD（分布外）任务上仍可能出现轻微的性能退化。
*   **任务依赖性**：对于纯知识检索型任务提升较小，更适用于具有明确操作流程的程序性任务。
*   **API 依赖**：构建过程依赖高质量的辅助模型（如 GPT-4 级别）进行归纳和反馈，对于完全本地化的部署可能存在成本或能力门槛。

（完）
