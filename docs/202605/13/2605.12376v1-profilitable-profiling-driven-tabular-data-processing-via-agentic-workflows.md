---
title: "ProfiliTable: Profiling-Driven Tabular Data Processing via Agentic Workflows"
title_zh: ProfiliTable：基于智能体工作流的剖析驱动型表格数据处理
authors: "Wei Liu, Yang Gu, Xi Yan, Zihan Nan, Beicheng Xu, Keyao Ding, Bin Cui, Wentao Zhang"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.12376v1"
tags: ["query:malm"]
score: 9.0
evidence: 用于表格数据处理的自主多智能体框架
tldr: 针对表格处理（清洗、转换等）中LLM生成代码语义错误的问题，本文提出ProfiliTable框架。该框架采用多智能体协作模式，通过动态分析构建执行上下文，集成了ReAct风格的数据探索、知识增强的代码生成以及基于反馈的闭环优化机制。实验证明，该方法在18种表格任务中表现优异，显著提升了复杂场景下的处理可靠性。
selection_source: fresh_fetch
motivation: 现有的LLM表格处理方法在面对模糊指令和复杂任务时，常因缺乏结构化反馈而生成语义错误的代码。
method: 提出一种由分析器、生成器和评估-总结器组成的自主多智能体框架，通过动态分析和迭代反馈来精炼执行上下文。
result: 在涵盖18种任务类型的基准测试中，ProfiliTable在复杂多步场景下一致优于现有强基准模型。
conclusion: 动态分析在将模糊的用户意图转化为稳健且符合规范的表格转换任务中起着至关重要的作用。
---

## 摘要
表格处理——包括清洗、转换、增强和匹配——是现实世界数据流水线中基础但易错的阶段。尽管近期基于大语言模型（LLM）的方法在自动化此类任务方面展现出潜力，但由于指令模糊、任务结构复杂以及缺乏结构化反馈，它们在实践中往往表现不佳，导致生成的代码虽然语法正确但在语义上存在缺陷。为应对这些挑战，我们提出了 ProfiliTable，这是一个以动态剖析为核心的自主多智能体框架，它通过交互式探索、知识增强合成和反馈驱动优化，构建并迭代完善统一的执行上下文。ProfiliTable 集成了：(i) 一个执行 ReAct 风格数据探索以构建语义理解的剖析器（Profiler）；(ii) 一个检索精选算子以合成任务感知代码的生成器（Generator）；以及 (iii) 一个注入执行评分和诊断见解以实现闭环优化的评估器-总结器（Evaluator-Summarizer）循环。在涵盖 18 种表格任务类型的多样化基准测试上进行的广泛实验表明，ProfiliTable 始终优于强基准模型，特别是在复杂的多步场景中。这些结果强调了动态剖析在将模糊的用户意图可靠地转化为稳健且符合治理要求的表格转换方面的关键作用。

## Abstract
Table processing-including cleaning, transformation, augmentation, and matching-is a foundational yet error-prone stage in real-world data pipelines. While recent LLM-based approaches show promise for automating such tasks, they often struggle in practice due to ambiguous instructions, complex task structures, and the lack of structured feedback, resulting in syntactically correct but semantically flawed code. To address these challenges, we propose ProfiliTable, an autonomous multi-agent framework centered on dynamic profiling, which constructs and iteratively refines a unified execution context through interactive exploration, knowledge-augmented synthesis, and feedback-driven refinement. ProfiliTable integrates (i) a Profiler that performs ReAct-style data exploration to build semantic understanding, (ii) a Generator that retrieves curated operators to synthesize task-aware code, and (iii) an Evaluator-Summarizer loop that injects execution scores and diagnostic insights to enable closed-loop refinement. Extensive experiments on a diverse benchmark covering 18 tabular task types demonstrate that ProfiliTable consistently outperforms strong baselines, particularly in complex multi-step scenarios. These results highlight the critical role of dynamic profiling in reliably translating ambiguous user intents into robust and governance-compliant table transformations.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **ProfiliTable** 的自主多智能体框架，旨在通过动态剖析（Dynamic Profiling）驱动的智能体工作流来解决表格数据处理中的复杂挑战。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：表格数据处理（清洗、转换、增强、匹配）是数据科学流水线中最耗时且易出错的阶段。
*   **核心问题**：现有的基于大语言模型（LLM）的自动化工具在处理现实世界表格时存在三大痛点：
    1.  **指令模糊**：用户意图难以直接转化为精确代码。
    2.  **缺乏语义理解**：仅依赖元数据（Schema）而未深入观察实际单元格值，导致生成的代码在逻辑上不符合数据特征。
    3.  **缺乏闭环反馈**：一次性生成代码往往因微小错误导致整个流水线失败。
*   **整体含义**：论文提出“动态剖析”不应是静态的元数据读取，而应是一个通过交互、反馈和知识整合不断演进的语义构建过程。

### 2. 方法论：核心思想与关键技术
ProfiliTable 采用多智能体协作架构，核心是一个**迭代演进的动态剖析上下文 ($P^{(t)}$)**，其公式化表达为包含当前剖析摘要、检索到的算子和反馈历史的集合。
*   **核心智能体角色**：
    *   **剖析器 (Profiler)**：采用 **ReAct 风格**的主动探索，通过采样、计算唯一值等操作形成对数据质量和结构的假设。
    *   **解释器与分解器 (Interpreter & Decompositer)**：解析意图并将复杂任务分解为原子子任务。
    *   **生成器 (Generator)**：利用 **RAG（检索增强生成）** 从精选算子库中提取经过验证的代码模板，结合剖析上下文合成代码。
    *   **评估器与总结器 (Evaluator & Summarizer)**：在沙盒中执行代码，评估器给出量化评分，总结器通过 ReAct 循环验证输出结果是否符合原始意图，并提供诊断见解。
*   **算法流程**：探索 $\rightarrow$ 假设 $\rightarrow$ 实现 $\rightarrow$ 验证 $\rightarrow$ 反思 $\rightarrow$ 精炼。如果评分未达标，则进入下一轮迭代，直到达到最大尝试次数或成功阈值。

### 3. 实验设计
*   **基准测试 (Benchmark)**：构建了一个包含 **18 种细粒度任务类型**的综合基准，涵盖清洗、转换、增强和匹配四大维度。
*   **对比方法**：
    *   **通用多智能体框架**：MetaGPT, CAMEL, ChatDev2.0。
    *   **表格专用智能体**：DataGovAgent, CleanAgent。
*   **评估指标**：平均任务得分 (ATS)、任务成功率 (TSR)、代码可运行率 (CRR) 以及任务级可运行率 (TRR) 等。
*   **基础模型**：使用了 GPT-4o 和 GPT-5.2（注：文中设定背景为 2026 年）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号或数量）。
*   **效率度量**：通过统计**平均 Token 消耗**和**平均墙钟时间 (Wall-clock time)** 来衡量效率。实验显示 ProfiliTable 在保持高准确率的同时，Token 消耗处于帕累托最优前沿（即在同等精度下成本较低）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了单步和多步复杂任务，进行了广泛的对比实验。
*   **消融实验**：非常充分。分别验证了剖析模块、反馈机制、RAG 整合以及分解器对最终性能的贡献。
*   **超参数分析**：对检索算子数量 $k$ 和相似度阈值 $\theta_{sim}$ 进行了敏感度分析。
*   **客观性**：通过任务特定的评估脚本（eval.py）进行自动化评分，减少了人工评价的偏差，实验设计较为客观公平。

### 6. 主要结论与发现
*   **性能领先**：ProfiliTable 在单步和多步任务中均显著优于所有基准模型。在多步任务中，其 ATS 得分比第二名高出约 10-15 个百分点。
*   **高可靠性**：它是唯一一个在多步设置下达到 **100% 任务级可运行率 (TRR)** 的方法，这对于生产环境部署至关重要。
*   **动态剖析的价值**：实验证明，主动的数据探索（而非被动读取元数据）能有效消除指令歧义，显著提升代码生成的语义正确性。

### 7. 优点
*   **主动探索机制**：引入 ReAct 风格的 Profiler，使智能体能像人类数据科学家一样“观察”数据后再写代码。
*   **闭环优化**：Summarizer 不仅报告错误，还通过观察处理后的表格给出具体的改进建议。
*   **帕累托效率**：在准确性和计算成本之间取得了极佳的平衡。
*   **模块化设计**：算子库与 RAG 的结合提高了代码生成的稳健性和可维护性。

### 8. 不足与局限
*   **超长流水线挑战**：在超过 7 步的极深任务中，性能会出现明显下降，存在错误累积风险。
*   **领域知识依赖**：对于完全缺失领域提示（Domain Cues）的极端模糊指令（例如未说明如何处理特定的缺失值占位符），智能体仍可能挣扎。
*   **实时性限制**：由于采用了多轮迭代和 ReAct 循环，其响应时间（Latency）高于一次性生成的方法，可能不适用于对实时性要求极高的场景。

（完）
