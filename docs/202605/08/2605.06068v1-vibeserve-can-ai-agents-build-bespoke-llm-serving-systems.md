---
title: "VibeServe: Can AI Agents Build Bespoke LLM Serving Systems?"
title_zh: VibeServe：AI 智能体能否构建定制化的 LLM 推理服务系统？
authors: "Keisuke Kamahori, Shihang Li, Simon Peter, Baris Kasikci"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06068v1"
tags: ["query:malm"]
score: 9.0
evidence: 用于合成定制化大模型推理系统的多智能体循环
tldr: 本研究挑战了传统通用型LLM推理系统的设计范式，提出了VibeServe，这是首个能够端到端自动合成定制化推理栈的多智能体系统。VibeServe通过内外双层循环进行系统设计规划、代码实现与性能评测。实验表明，VibeServe在标准场景下可媲美vLLM，而在非标准架构或特定硬件优化场景下，其生成的定制化系统性能优于通用系统，证明了“生成时定制”优于“运行时通用”的潜力。
selection_source: fresh_fetch
motivation: 传统的通用型LLM推理系统开发周期长且难以针对特定非标准场景进行深度优化。
method: 提出一种多智能体循环机制，通过外层循环规划设计空间，内层循环负责代码实现、正确性校验及性能评估。
result: VibeServe在标准部署中与vLLM性能相当，并在涉及非标准架构和硬件优化的六个特殊场景中表现更优。
conclusion: 基础设施软件的设计范式可以从追求运行时通用性转向利用AI智能体实现的生成时定制化。
---

## 摘要
多年来，我们像构建其他关键基础设施一样构建 LLM 推理服务系统：采用单一的通用技术栈，经过多年的人工调优，旨在支持所有模型和工作负载。在本文中，我们采取了相反的策略：利用多智能体循环为不同的使用场景自动合成定制化的推理服务系统。我们提出了 VibeServe，这是第一个端到端生成完整 LLM 推理服务栈的智能体循环。VibeServe 使用外层循环来规划和跟踪系统设计的搜索过程，并使用内层循环来实施候选方案、检查正确性以及在目标基准测试上衡量性能。在现有技术栈已高度优化的标准部署设置中，VibeServe 的表现与 vLLM 相当，这表明生成时专业化（generation-time specialization）并不一定以牺牲性能为代价。更具意义的是，在非标准场景下，VibeServe 通过利用通用系统在涉及非标准模型架构、工作负载知识和硬件特定优化的六个场景中所错失的机会，超越了现有系统。总之，这些结果为基础设施软件的设计空间提供了一个不同的视角：即生成时专业化而非运行时通用性。代码可在 https://github.com/uw-syfi/vibe-serve 获取。

## Abstract
For years, we have built LLM serving systems like any other critical infrastructure: a single general-purpose stack, hand-tuned over many engineer-years, meant to support every model and workload. In this paper, we take the opposite bet: a multi-agent loop that automatically synthesizes bespoke serving systems for different usage scenarios. We propose VibeServe, the first agentic loop that generates entire LLM serving stacks end-to-end. VibeServe uses an outer loop to plan and track the search over system designs, and an inner loop to implement candidates, check correctness, and measure performance on the target benchmark. In the standard deployment setting, where existing stacks are highly optimized, VibeServe remains competitive with vLLM, showing that generation-time specialization need not come at the cost of performance. More interestingly, in non-standard scenarios, VibeServe outperforms existing systems by exploiting opportunities that generic systems miss in six scenarios involving non-standard model architectures, workload knowledge, and hardware-specific optimizations. Together, these results suggest a different point in the design space for infrastructure software: generation-time specialization rather than runtime generality. Code is available at https://github.com/uw-syfi/vibe-serve.

---

## 论文详细总结（自动生成）

以下是对论文《VibeServe: Can AI Agents Build Bespoke LLM Serving Systems?》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：当前的 LLM 推理服务系统（如 vLLM、SGLang）遵循“通用型”设计范式，即通过一套代码库支持所有模型、硬件和工作负载。然而，随着非标准架构（如混合 SSM/Attention）、新型硬件（如 Apple Silicon）和特殊工作负载（如代码编辑预测输出）的涌现，通用系统往往难以提供最优性能，甚至无法运行。
*   **核心问题**：能否利用 AI 智能体（AI Agents）为每种特定的“模型-硬件-工作负载”组合自动合成一套**定制化（Bespoke）**的推理服务系统？
*   **整体含义**：论文提出了 **VibeServe**，这是首个能够端到端生成完整推理栈的智能体系统。它标志着基础设施软件设计范式的转变：从追求“运行时通用性”转向“生成时专业化”。

### 2. 方法论
VibeServe 采用双层循环架构，将长程（Long-horizon）系统设计任务分解：
*   **外层循环（规划者）**：
    *   负责跨轮次的全局规划。它维护一个基于 Git 的优化历史记录、一个结构化的“问题追踪器（Issue Tracker）”和长期记忆文件。
    *   决定下一轮优化的方向（如：引入连续批处理、优化 KV 缓存布局），并向内层循环分发具体任务。
*   **内层循环（执行者）**：
    *   由三个专门的智能体组成：**实现者（Implementer）**编写代码；**正确性裁判（Accuracy Judge）**通过参考实现校验输出；**性能评估者（Performance Evaluator）**利用分析工具（如 Nsight）测量指标。
    *   内层循环在独立的工作空间中运行，确保代码只有在通过正确性校验后才会提交给外层循环。
*   **关键技术细节**：
    *   **技能库（Skills Library）**：提供关于推理系统设计的结构化知识（如 PagedAttention 原理、特定硬件的 API 文档），供智能体检索。
    *   **输入工件**：用户仅需提供模型权重、参考实现（如 HF Transformers）、正确性检查脚本和性能基准脚本。

### 3. 实验设计
论文在六个具有代表性的场景中评估了 VibeServe：
*   **场景 A（标准场景）**：Llama-3.1-8B 在 H100 上运行。对比对象：vLLM, SGLang。
*   **场景 B（代码编辑）**：Qwen3-32B 在 H100 上利用预测输出进行投机采样。
*   **场景 C（混合架构）**：Olmo-Hybrid-7B 在 L4 GPU 上进行 Prompt 缓存。
*   **场景 D（流式 ASR）**：Moonshine 模型在 L4 上进行流式语音识别。
*   **场景 E（本地约束解码）**：Llama-3.1-8B 在 MacBook (M3 Pro) 上生成 JSON。
*   **场景 F（多模态生成）**：Show-o2 模型在 H100 和 MacBook 上运行。
*   **Benchmark**：吞吐量（Throughput）、首字延迟（TTFT）、每 token 延迟（TPOT）。

### 4. 资源与算力
*   **硬件环境**：使用了 NVIDIA H100 (80GB)、NVIDIA L4 (24GB) 以及 MacBook Pro (M3 Pro, 36GB 统一内存)。
*   **智能体算力消耗**：论文详细记录了每个场景的 LLM 调用时长（见附录 Table 1）。例如，生成标准 Llama-3.1 推理系统（60 轮迭代）总计耗时约 **25 小时** 的智能体活动时间；其他非标准场景耗时在 **3 到 14 小时** 不等。
*   **使用的模型**：智能体由 Codex CLI（基于 OpenAI 模型）驱动。

### 5. 实验数量与充分性
*   **实验规模**：共进行了 6 组深度实验，每组实验都展示了随迭代次数增加性能提升的曲线（通常在 15-60 轮迭代之间达到饱和）。
*   **充分性**：实验覆盖了从数据中心 GPU 到移动端 SoC 的硬件跨度，以及从纯文本到多模态的模型跨度。
*   **客观性**：通过与工业界最强的通用框架（vLLM, SGLang, MLX-LM）进行直接对比，证明了其在特定场景下的优越性。不过，论文也承认目前主要基于单次种子运行（Single-seed runs），可能存在随机性。

### 6. 主要结论与发现
*   **性能对齐**：在高度优化的标准场景下，VibeServe 生成的系统性能可与 vLLM 媲美（吞吐量持平，延迟误差在 5% 以内）。
*   **非标准场景获胜**：在通用系统未覆盖的领域，VibeServe 取得了显著加速。例如：
    *   代码编辑场景加速 **5.95×**。
    *   混合架构 Prompt 缓存吞吐量提升 **3.45×**。
    *   MacBook 上的多模态推理加速 **6.27×**。
*   **可行性验证**：证明了长程编码智能体已经具备构建复杂、多组件基础设施软件的能力。

### 7. 优点
*   **端到端合成**：不同于以往只优化单个算子（Kernel）的研究，VibeServe 生成的是包含调度、内存管理和 API 的完整系统。
*   **解耦设计**：内外循环的分离有效解决了智能体在长任务中的“上下文漂移”问题。
*   **灵活性**：通过技能库和用户提供的检查器，可以快速适配尚未被主流框架支持的新模型或新硬件。

### 8. 不足与局限
*   **计算成本**：为每个部署目标生成定制系统需要数小时甚至数十小时的 LLM 推理开销，成本较高。
*   **依赖检查器**：系统的正确性完全依赖于用户提供的检查脚本，如果检查脚本不严谨，生成的系统可能存在隐蔽的 Bug。
*   **搜索空间限制**：目前主要依赖现有的算子库（如 FlashInfer, MLX），尚未展示大规模自主编写高性能 CUDA 算子的能力。
*   **冷启动问题**：对于完全陌生的架构，可能需要较多的迭代轮次才能达到可用状态。

（完）
