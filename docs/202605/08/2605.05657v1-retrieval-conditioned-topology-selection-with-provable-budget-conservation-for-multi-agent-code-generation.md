---
title: Retrieval-Conditioned Topology Selection with Provable Budget Conservation for Multi-Agent Code Generation
title_zh: 面向多智能体代码生成的具有可证明预算守恒的检索条件拓扑选择
authors: "Abhijit Talluri, Pujith Anne, Bhagavan Choudary Pendiyala, Raghavendra Chilukuri"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.05657v1"
tags: ["query:malm"]
score: 10.0
evidence: 多智能体代码生成的检索引导自适应编排
tldr: 针对多智能体代码生成中拓扑选择与代码复杂度脱节的问题，本文提出RGAO架构。该架构通过分层代码索引提取复杂度向量来动态选择编排拓扑，并结合形式化资源代数，在Code-Agent框架下实现了可证明的预算守恒。研究贡献包括复杂度感知路由、预算代数及高效检索引擎，显著降低了路由错误率并保证了系统稳定性。
selection_source: fresh_fetch
motivation: 现有多智能体系统在选择协作拓扑时未参考代码库的结构复杂度，导致资源分配不均和路由效率低下。
method: 提出RGAO架构，利用分层代码索引提取复杂度向量指导拓扑选择，并引入形式化预算代数确保多智能体协作中的资源守恒。
result: "实验显示该方法将代理路由错误率从30.1%降至8.2%，并实现了亚毫秒级的DAG构建和线性扩展的树索引。"
conclusion: 通过将复杂度感知路由与形式化资源管理相结合，实现了高效且具备资源守恒保证的多智能体代码生成编排。
---

## 摘要
用于代码生成的多个大语言模型（LLM）系统面临一个基础的路由问题：最优的编排拓扑取决于待修改代码的结构复杂度，然而现有系统在选择拓扑时并未参考代码库。我们提出了检索引导的自适应编排（RGAO），这是一种通过在选择编排拓扑之前从分层代码索引中提取结构复杂度向量来闭合这一环路的架构。RGAO 在 Code-Agent 中运行，这是一个多智能体框架，其子智能体受具有六维预算向量的形式化合同约束。我们的核心贡献是结合了两个此前独立的研究方向——基于复杂度的 LLM 路由和形式化资源代数——从而产生了一个两者单独都无法实现的特性：在检索条件下的动态拓扑选择中实现可证明的预算守恒。具体而言，我们的贡献包括：(1) 一种基于复杂度的拓扑路由器，将代理测量的误路由率从 30.1% 降低到 8.2%；(2) 一种具有结构归纳守恒定理的预算代数；以及 (3) 一个分层代码检索引擎。实验评估表明，该系统实现了亚毫秒级的 DAG 构建和线性树索引可扩展性。

## Abstract
Multi-agent LLM systems for code generation face a fundamental routing problem: the optimal orchestration topology depends on the structural complexity of the code under modification, yet existing systems select topologies without consulting the codebase. We present Retrieval-Guided Adaptive Orchestration (RGAO), an architecture that closes this loop by extracting a structural complexity vector from a hierarchical code index before selecting the orchestration topology. RGAO operates within Code-Agent, a multi-agent framework whose sub-agents are governed by formal contracts with six-dimensional budget vectors. Our headline contribution is the composition of two previously separate lines of work -- complexity-conditioned LLM routing and formal resource algebras -- yielding a property neither admits alone: provable budget conservation under retrieval-conditioned dynamic topology selection. Concretely we contribute: (1) a complexity-conditioned topology router that reduces proxy-measured misrouting from 30.1% to 8.2%; (2) a budget algebra with a structural-induction conservation theorem; and (3) a hierarchical code retrieval engine. Empirical evaluation demonstrates sub-millisecond DAG construction and linear tree-index scalability.

---

## 论文详细总结（自动生成）

以下是对论文《Retrieval-Conditioned Topology Selection with Provable Budget Conservation for Multi-Agent Code Generation》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：多智能体（Multi-Agent）系统在处理复杂代码任务时表现优异，但其“编排拓扑”（即智能体如何协作的结构）通常是静态的，或者仅根据用户查询（Query）的表面特征来选择。
*   **核心矛盾**：现有的系统在选择拓扑时“不看代码”。一个简单的单文件修复和一个跨模块的重构如果仅凭查询文本可能无法区分，导致资源浪费（过度编排）或任务失败（编排不足）。
*   **整体含义**：本文提出了 **RGAO（检索引导的自适应编排）** 架构，主张在决定协作模式前，先通过检索提取代码库的“结构复杂度”，并结合形式化资源代数，确保在动态选择拓扑的同时，系统资源消耗（Token、时间等）是可预测且受限的。

### 2. 方法论
*   **核心思想**：将代码检索信号引入路由决策，并利用形式化合同（Contract）约束智能体行为。
*   **关键技术细节**：
    *   **复杂度向量提取 ($c$)**：从分层代码索引中提取五维向量，包括：依赖深度 ($d_{dep}$)、涉及文件数 ($n_f$)、符号密度 ($n_s$)、树深度 ($h_t$) 和跨模块耦合度 ($\rho_x$)。
    *   **拓扑路由器**：根据复杂度向量将任务路由至四种拓扑之一：
        1.  **FAST PATH**：单文件、低复杂度任务（单模型处理）。
        2.  **SUB AGENT**：中等复杂度（单个专家智能体）。
        3.  **MULTI AGENT**：高复杂度（流水线或群集协作）。
        4.  **DEEP RESEARCH**：高模糊度任务（多阶段检索重度模式）。
    *   **预算代数（Budget Algebra）**：定义了六维预算向量（迭代次数、调用数、Token数、秒数、重试数、移交数）。
    *   **算法流程**：
        1.  **检索层**：构建 `CodeIndexTree`，通过 LATTICE 和 KohakuRAG 等技术进行混合搜索。
        2.  **路由层**：计算复杂度向量 $c$，选择最优拓扑。
        3.  **执行层**：在执行前进行 $O(|V|+|E|)$ 的静态验证，确保子任务预算之和不超过父任务总预算（定理1：预算守恒定理）。

### 3. 实验设计
*   **数据集/场景**：
    *   **路由评估集**：构建了包含 250 个实例的标注数据集（任务描述 + 理想拓扑标签），由专家标注。
    *   **代理测试床（Proxy Harness）**：由于 SWE-bench 存在数据污染风险且成本极高，作者使用了 `swebench_proxy.py` 进行模拟评估。
*   **对比方法**：
    *   **基准路由**：基于正则表达式（Regex）的分类器。
    *   **主流框架**：与 MetaGPT、Magentic-One、OpenHands 等系统在任务解决率和 Token 效率上进行对比。
*   **评估指标**：误路由率（Misrouting Rate）、DAG 构建延迟、Token 消耗、Pass@1 解决率。

### 4. 资源与算力
*   **硬件环境**：实验在单台开发者工作站上完成（Apple M 系列芯片，32GB 内存）。
*   **算力消耗**：
    *   **模型调用**：使用了 GPT-4o 和 Claude-3.5-Sonnet 的 API。
    *   **成本**：代理评估的总 API 花费约为 18 美元。
    *   **未明确说明项**：论文明确指出，由于全量 SWE-bench 运行需要约 220 GPU 小时和 3400 美元成本，这部分实验被留作后续工作，本次仅报告代理测试结果。

### 5. 实验数量与充分性
*   **实验规模**：
    *   路由实验使用了 250 个样本，通过 McNemar 检验验证了统计显著性 ($p < 10^{-6}$)。
    *   微基准测试（Microbenchmarks）进行了 20 轮迭代，报告了中位数和绝对中位差（MAD）。
    *   进行了 7 种检索信号的消融实验。
*   **充分性评价**：在算法效率、路由准确性和预算守恒验证方面，实验非常充分且客观。但在“最终代码修复能力”上，由于缺乏全量标准 Benchmark（如 SWE-bench Pro）的端到端数据，其结论在工程落地效果上仍有待进一步验证。

### 6. 主要结论与发现
*   **路由优化**：引入代码结构信号后，误路由率从 30.1% 大幅降至 8.2%。
*   **性能开销**：RGAO 的所有核心操作（DAG 构建、预算检查、复杂度提取）均在亚毫秒级，远低于 LLM 推理延迟。
*   **资源效率**：通过精准路由，系统在保持高解决率的同时，Token 消耗比无约束的单智能体降低了 8-20 倍。
*   **安全性**：证明了在动态拓扑选择下，静态预算验证可以有效防止智能体陷入死循环或资源耗尽。

### 7. 优点
*   **理论严密性**：首次将形式化资源代数与多智能体路由结合，并给出了静态验证的复杂度证明。
*   **机制创新**：闭合了“检索-路由”环路，使系统能够根据代码库的实际状态（而非仅凭用户的一句话）来调整自身结构。
*   **工程实用性**：系统设计考虑了“多智能体陷阱”（如 Chattiness 话痨问题），并提供了三门禁协议（3-Gate Protocol）确保执行安全。

### 8. 不足与局限
*   **实验覆盖不足**：缺乏在 SWE-bench Verified/Pro 等权威榜单上的完整排名，主要依赖代理测试。
*   **阈值依赖**：RGAO 的路由决策目前依赖人工调优的阈值，虽然具有可解释性，但在极端分布外（OOD）场景下的泛化能力可能受限。
*   **语言限制**：复杂度提取依赖于 Tree-sitter 解析器，对于不支持的编程语言，系统退化为普通意图路由。
*   **确定性假设**：预算守恒定理在工具成本完全确定的假设下最强，在随机性成本（如采样温度 > 0）下仅能提供概率保证。

（完）
