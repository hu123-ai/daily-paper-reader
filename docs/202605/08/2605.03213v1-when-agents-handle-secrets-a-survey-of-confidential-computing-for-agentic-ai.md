---
title: "When Agents Handle Secrets: A Survey of Confidential Computing for Agentic AI"
title_zh: 当智能体处理秘密时：代理式人工智能机密计算综述
authors: "Javad Forough, Marios Kogias, Hamed Haddadi"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.03213v1"
tags: ["query:malm"]
score: 9.0
evidence: 智能体AI及对等协议机密计算综述
tldr: 随着Agentic AI在规划、工具调用和多智能体协作中的广泛应用，其面临的隐私泄露和凭据窃取风险日益严重。本文综述了机密计算（CC）如何利用硬件可信执行环境（TEE）为智能体提供隔离保护和远程度量。文章构建了涵盖感知、规划、内存和行动的威胁模型，对比了主流硬件平台，并指出了多跳智能体链度量等未来挑战，旨在为生产级智能体提供硬件级安全保障。
selection_source: fresh_fetch
motivation: 传统的软件防御无法有效抵御拥有高权限的系统级攻击者，且智能体在处理敏感上下文和跨平台协作时面临独特的安全风险。
method: 本文系统性地分类了六种主流TEE硬件平台，并针对智能体的感知、规划、记忆、行动和协调层构建了统一的威胁模型。
result: 综述了现有机密计算防御技术，区分了单次推理与复杂智能体设计的安全差异，并识别出复合度量和GPU-TEE性能等六大挑战。
conclusion: 尽管硬件信任原语已初步成熟，但仍需建立统一的端到端框架，以将机密计算整合为生产级Agentic AI的可靠安全基座。
---

## 摘要
代理式人工智能（Agentic AI）系统，特别是那些能够进行规划、调用工具、维护持久内存，并通过 MCP 和 A2A 等协议将任务委托给同行智能体的大语言模型（LLM）驱动的智能体，引入了与独立模型推理有本质区别的威胁面。智能体积累敏感上下文、持有凭据，并在没有任何一方能完全控制的流水线中运行，这使得提示词注入、上下文泄露、凭据窃取以及智能体间消息投毒成为可能。目前的防御措施完全在软件栈内运行，可能会被具有足够权限的对手（如被攻破的云运营商）静默绕过。机密计算（CC）提供了一种基于硬件根源的替代方案：可信执行环境（TEE）将智能体代码和数据与特权系统软件隔离，而远程度量则实现了跨分布式部署的可验证信任。本综述从四个部分综合了设计空间：(i) 涵盖六种 TEE 平台（Intel SGX、Intel TDX、AMD SEV-SNP、ARM TrustZone、ARM CCA 和 NVIDIA H100 CC）的统一分类法，涉及部署角色和性能权衡；(ii) 以智能体为中心的威胁模型，涵盖感知、规划、内存、行动和协调层，并映射到九个安全目标；(iii) 基于机密计算防御措施的比较研究，区分了从单次调用推理迁移而来的发现与需要全新代理式设计的发现；以及 (iv) 六个开放性挑战，包括多跳智能体链的复合度量以及大语言模型规模下的 GPU-TEE 性能。尽管几种硬件信任原语在针对性部署方面已趋于成熟，但目前尚未建立广泛认可的端到端框架，能将它们整合为生产级代理式人工智能的连贯安全基座。

## Abstract
Agentic AI systems, specifically LLM-driven agents that plan, invoke tools, maintain persistent memory, and delegate tasks to peer agents via protocols such as MCP and A2A, introduce a threat surface that differs materially from standalone model inference. Agents accumulate sensitive context, hold credentials, and operate across pipelines no single party fully controls, enabling prompt injection, context exfiltration, credential theft, and inter-agent message poisoning. Current defenses operate entirely within the software stack and can be silently bypassed by a sufficiently privileged adversary such as a compromised cloud operator. Confidential computing (CC) offers a hardware-rooted alternative: Trusted Execution Environments (TEEs) isolate agent code and data from privileged system software, while remote attestation enables verifiable trust across distributed deployments. This survey synthesizes the design space in four parts: (i) a unified taxonomy of six TEE platforms (Intel SGX, Intel TDX, AMD SEV-SNP, ARM TrustZone, ARM CCA, and NVIDIA H100 CC) covering deployment roles and performance tradeoffs; (ii) an agent-centric threat model spanning perception, planning, memory, action, and coordination layers mapped to nine security goals; (iii) a comparative survey of CC-based defenses distinguishing findings that transfer from single-call inference versus what requires new agentic designs; and (iv) six open challenges including compound attestation for multi-hop agent chains and GPU-TEE performance at LLM scale. While several hardware trust primitives appear mature enough for targeted deployments, no broadly established end-to-end framework yet binds them into a coherent security substrate for production agentic AI.

---

## 论文详细总结（自动生成）

这篇论文《When Agents Handle Secrets: A Survey of Confidential Computing for Agentic AI》对机密计算（Confidential Computing, CC）在代理式人工智能（Agentic AI）中的应用进行了全面且深入的综述。以下是详细总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的 LLM 推理是无状态的，但 **Agentic AI（代理式 AI）** 具有规划、调用工具、维护持久内存和多智能体协作的能力。这导致了更大的威胁面，如凭据窃取、内存泄露、恶意工具调用和智能体间消息投毒。
*   **研究动机**：现有的软件层防御（如提示词保护、沙箱）容易被拥有高权限的对手（如被攻破的云运营商、恶意管理程序）绕过。
*   **整体含义**：论文探讨了如何利用基于硬件的**机密计算（CC）**和**可信执行环境（TEE）**，为智能体提供硬件级的隔离和远程度量（Remote Attestation），从而在不可信的基础设施中建立信任根。

### 2. 论文提出的方法论
论文通过构建多维度的分类法和威胁模型来系统化该领域：
*   **五层智能体架构模型**：将智能体分解为感知（Perception）、规划（Planning）、内存（Memory）、行动（Action）和协调（Coordination）五层，并针对每一层分析威胁。
*   **九大安全目标**：定义了输入机密性、模型机密性、执行完整性、内存机密性、工具调用完整性、消息真实性、溯源性（Provenance）、新鲜度（Freshness）和侧信道抗性。
*   **TEE 平台分类**：详细对比了六种主流硬件平台：
    *   **Intel SGX**（进程级飞地，TCB 最小）。
    *   **Intel TDX / AMD SEV-SNP**（机密虚拟机 CVM，适合现有应用迁移）。
    *   **ARM TrustZone / CCA**（适用于边缘侧和移动端）。
    *   **NVIDIA H100 CC**（将信任边界扩展到 GPU 显存和计算）。
*   **核心思想**：通过硬件强制隔离代码和数据，并利用加密度量证明（Attestation）确保运行的是预期的、未被篡改的智能体逻辑。

### 3. 实验设计（综述性质）
由于本文是一篇**综述论文**，它本身并不包含新的实验数据，而是对现有研究进行了系统性总结：
*   **分析对象**：深入分析了多个代表性系统，如 **Omega**（基于 SEV-SNP 和 H100 的机密智能体运行时）、**AttestMCP**（扩展模型上下文协议以支持度量）、**CAEC**（ARM CCA 下的机密共享内存）等。
*   **Benchmark 提及**：引用了 **MCPSecBench**（针对工具调用的安全基准）和 **Toolsandbox**（有状态工具使用评估）来展示现有防御的效果。
*   **对比方法**：将 CC 与纯软件防御、全同态加密（FHE）和多方安全计算（MPC）进行了对比，强调 CC 在处理大模型工作负载时的性能优势。

### 4. 资源与算力
*   **文中提及**：论文讨论了不同硬件的性能开销。例如，引用实验表明 **NVIDIA H100 CC** 在机密推理时的吞吐量损失仅为 **4-8%**；而 **ARM CCA** 在边缘侧推理的开销约为 **22%**。
*   **算力细节**：文中未进行实际训练或推理实验，因此没有具体的 GPU 数量或训练时长记录，其重点在于硬件架构的性能权衡。

### 5. 实验数量与充分性
*   **文献覆盖**：论文引用了超过 100 篇核心文献，涵盖了从 2011 年（CryptDB）到 2026 年（注：文中日期标注为 2026 年，可能为前瞻性研究或特定预印本标注）的最前沿研究。
*   **分析深度**：对 7 个代表性系统（如 TEESlice, TEECHAT 等）进行了详细的横向对比（见表 III），涵盖了 TEE 基座、保护范围、操作类别等维度。
*   **客观性**：论文不仅讨论了 CC 的优点，还专门列出了 CC 无法解决的问题（如语义层面的提示词注入、可用性攻击等），评价客观公平。

### 6. 论文的主要结论与发现
*   **CC 是必要非充分的**：CC 能有效抵御基础设施级别的攻击（A3 级对手），但无法阻止语义层面的“提示词注入”（A1 级对手）。
*   **多智能体挑战**：在多跳智能体链中，如何实现“复合度量”（Compound Attestation）和信任传递是目前尚未解决的核心难题。
*   **GPU 隔离至关重要**：对于大模型智能体，仅靠 CPU 端的 TEE 是不够的，必须将信任边界扩展到 GPU 显存（如 H100 CC）。
*   **协议缺失**：现有的智能体通信协议（如 MCP, A2A）缺乏原生的机密计算支持，需要重新设计以包含度量原语。

### 7. 优点
*   **前瞻性强**：紧扣 Agentic AI 这一前沿趋势，而非局限于传统的 LLM 推理。
*   **结构严谨**：从硬件层到协议层再到应用层，构建了非常清晰的分类体系。
*   **问题导向**：明确提出了六大开放性挑战（如侧信道泄露、监管合规等），为后续研究指明了方向。

### 8. 不足与局限
*   **缺乏实测数据**：作为综述，没有提供统一基准下的各平台性能实测对比。
*   **应用限制**：目前许多提到的系统（如基于 ARM CCA 的方案）仍处于研究原型阶段，距离大规模商业化部署还有距离。
*   **侧信道风险**：论文承认 TEE 无法完全消除微架构侧信道攻击（如通过 Token 生成频率泄露信息），这在 LLM 场景下尤为突出。

（完）
