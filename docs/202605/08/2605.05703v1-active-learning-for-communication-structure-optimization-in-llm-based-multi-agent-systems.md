---
title: Active Learning for Communication Structure Optimization in LLM-Based Multi-Agent Systems
title_zh: 基于大语言模型的多智能体系统通信结构优化的主动学习
authors: "Huchen Yang, Xinghao Dong, Dan Negrut, Jin-Long Wu"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.05703v1"
tags: ["query:malm"]
score: 10.0
evidence: 优化基于大语言模型的多智能体系统通信结构
tldr: 本研究针对大语言模型多智能体系统（LLM-MAS）中通信结构优化效率低的问题，提出了一种基于主动学习的任务选择框架。该框架利用集成卡尔曼反演和信息论方法，主动识别对优化通信结构最有价值的任务，从而在有限的计算预算下提高优化效率和稳定性。实验证明，该方法在正常及受攻击环境下均能显著提升系统性能并降低Token消耗。
selection_source: fresh_fetch
motivation: 现有的通信结构优化方法依赖随机采样，忽略了不同任务在优化过程中的信息价值差异，导致在有限预算下优化效果不稳定。
method: 提出一种基于集成卡尔曼反演的信息论任务选择框架，结合嵌入表示选择和批量汤普森采样，以高效识别并利用最具信息量的任务。
result: 在多种实验设置（包括智能体受攻击场景）下，该方法均能以更低的计算成本实现更优的通信结构优化效果。
conclusion: 通过主动学习策略筛选关键任务，可以显著提升LLM多智能体系统通信结构优化的效率、鲁棒性和可扩展性。
---

## 摘要
优化基于大语言模型的多智能体系统（LLM-MAS）的通信结构已被证明可以提高下游性能并减少 Token 使用量。现有方法通常依赖于随机采样的训练任务。然而，任务在难度和领域上可能存在显著差异，因此它们在更新通信结构方面提供的信息量并不相等，这使得在有限的训练预算下，优化过程往往不稳定且对特定训练集高度敏感。为了主动识别对通信结构优化最有价值的任务，我们提出了一个基于集成的、信息论驱动的任务选择框架。该方法通过评估候选任务对图参数分布的改变程度来估计任务的信息量，并使用集成卡尔曼反演（Ensemble Kalman Inversion）作为相应贝叶斯更新的一种高效且无导数的近似。由此产生的估计器特别适用于黑盒且具有噪声的多智能体系统。为了增强可扩展性，我们通过基于嵌入的代表性选择构建了一个紧凑的候选池，并将信息量选择与代理模型和批量汤普森采样相结合。我们在良性设置和存在智能体攻击的设置下验证了我们的方法，证明了其在受限计算预算下进行通信结构优化的有效性。

## Abstract
Optimizing the communication structure of large language model based multi-agent systems (LLM-MAS) has been shown to improve downstream performance and reduce token usage. Existing methods typically rely on randomly sampled training tasks. However, tasks may differ substantially in difficulty and domain, and thus they are not equally informative for updating communication structure, making optimization under limited training budgets often unstable and highly sensitive to the particular training set. To actively identify the most valuable tasks for communication-structure optimization, we propose an ensemble-based information-theoretic task selection framework. The proposed method estimates task informativeness by how much a candidate task changes the distribution over graph parameters, using ensemble Kalman inversion as an efficient and derivative-free approximation of the corresponding Bayesian update. The resulting estimator is especially suitable for black-box and noisy multi-agent systems. To enhance scalability, we construct a compact candidate pool through embedding-based representative selection and combine the informative selection with surrogate modeling and batch Thompson sampling. We validate our method in both benign settings and settings with agent attacks, demonstrating its effectiveness for communication-structure optimization under constrained computational budgets.

---

## 论文详细总结（自动生成）

### 基于大语言模型的多智能体系统通信结构优化的主动学习总结

#### 1. 核心问题与研究动机
*   **核心问题**：在基于大语言模型的多智能体系统（LLM-MAS）中，优化智能体间的通信结构（即谁该和谁说话）能提升性能并减少 Token 消耗。然而，现有的优化方法通常随机选择训练任务，这在有限的计算预算下会导致优化结果极不稳定，且对特定任务集高度敏感。
*   **研究动机**：并非所有任务对优化通信结构的贡献都相同。作者旨在通过**主动学习（Active Learning）**识别最具“信息量”的任务，从而在受限预算下实现更稳健、高效的通信结构优化。

#### 2. 方法论
该论文提出了一种基于集成的、无导数的信息论任务选择框架，核心步骤如下：
*   **核心思想**：利用**信息增益（Information Gain）**来衡量任务的价值，即一个任务能多大程度上改变通信图参数的后验分布。
*   **关键技术细节**：
    *   **两阶段选择策略**：
        1.  **代表性选择**：利用文本嵌入（Embedding）和贪婪最远点采样，从原始大任务池中筛选出一个较小的代表性候选池，确保任务多样性。
        2.  **信息量选择**：从候选池中挑选对图参数更新贡献最大的任务。
    *   **集成卡尔曼反演（EKI）**：由于 LLM-MAS 是不可微的黑盒系统，传统的梯度方法难以奏效。EKI 作为一种无导数的近似贝叶斯更新方法，通过维护一个参数粒子集成（Ensemble），根据任务反馈调整粒子分布，从而高效估算 KL 散度（即信息增益）。
    *   **代理模型与采样**：引入高斯过程（GP）作为代理模型，结合批量汤普森采样（Thompson Sampling），在无需评估所有候选任务的情况下预测任务价值，提升了可扩展性。

#### 3. 实验设计
*   **数据集**：使用了 **MMLU**（多任务语言理解）和 **GSM8K**（数学推理）两个主流基准。
*   **实验场景**：
    *   **良性设置**：所有智能体正常协作。
    *   **对抗设置（智能体攻击）**：系统中存在 3/6 的恶意智能体，故意提供错误推理和答案以误导系统。
*   **对比方法（Baselines）**：
    *   **无训练（No Train）**：全连接图。
    *   **随机训练（Random）**：随机抽取任务进行优化。
    *   **其他主动学习指标**：预期梯度长度（EGL）、预期模型改变（EMC）、基于 Fisher 信息矩阵的 Coreset 方法。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **成本衡量**：实验主要以 **Token 消耗量**（百万级别）作为成本衡量指标，这符合 LLM 领域的研究惯例。智能体模型采用了 GPT-4.1-nano。

#### 5. 实验数量与充分性
*   **实验规模**：针对每个设置进行了多次独立运行（每组 10 到 23 次不等），并提供了标准差和四分位数（Q1）分析。
*   **充分性**：
    *   **消融实验**：分别验证了“代表性选择”和“信息量选择”的贡献。
    *   **敏感性分析**：研究了集成规模（Ensemble Size）和 EKI 迭代步数对结果的影响。
    *   **统计显著性**：使用了自助法（Bootstrap）计算置信区间。
*   **评价**：实验设计较为全面，涵盖了从性能提升到系统鲁棒性的多个维度，对比实验考虑了多种主动学习变体，具有较高的客观性和公平性。

#### 6. 主要结论与发现
*   **稳定性显著提升**：在有限预算下，主动学习显著降低了优化结果的方差。在 MMLU 的良性设置下，最差 25% 运行的准确率提升了 1.30%。
*   **对抗环境表现优异**：在智能体受攻击时，该方法能更有效地识别并切断恶意智能体的通信路径。在 MMLU 攻击设置下，平均准确率比随机训练高出 1.45%。
*   **成本效益高**：在相同的 Token 预算下，主动学习获得的通信结构在下游测试任务中表现出更高的准确率和更低的推理成本。

#### 7. 优点
*   **无导数优化**：巧妙地将 EKI 应用于黑盒 MAS 系统，避开了 LLM 交互不可微的难题。
*   **鲁棒性导向**：不仅关注平均性能，更关注优化过程的稳定性和“尾部”表现（Worst-case performance）。
*   **实际应用价值**：在对抗环境下（如存在幻觉或恶意输入）展示了强大的结构过滤能力。

#### 8. 不足与局限
*   **集成规模限制**：实验中使用的集成粒子数较小（J=6），对于参数维度极高的复杂系统，其近似效果可能受限。
*   **标签依赖**：该方法假设训练池中的任务带有标准答案（用于计算性能得分），在完全无监督或无标签的场景下无法直接应用。
*   **计算开销**：虽然减少了训练任务数，但评估任务信息量本身需要多次 MAS 运行（集成评估），这在某些实时性要求极高的场景下可能仍显昂贵。

（完）
