---
title: Improving the Efficiency of Language Agent Teams with Adaptive Task Graphs
title_zh: 通过自适应任务图提升语言智能体团队的效率
authors: "Elizabeth Mieczkowski, Alexander Ku, Tiwalayo Eisape, Dilip Arumugam, John Matters, Katherine M. Collins, Ilia Sucholutsky, Thomas L. Griffiths"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06320v1"
tags: ["query:malm"]
score: 10.0
evidence: 通过自适应任务图协调大语言模型团队的框架
tldr: 针对大语言模型智能体团队在协作中存在的结构化过度僵化或非结构化效率低下的问题，本文提出了 LATTE 框架。该框架受分布式系统启发，通过让智能体共同维护一个动态演化的协作图来管理子任务依赖和分配。实验表明，LATTE 在保持或提升准确率的同时，显著降低了资源消耗、通信成本和协作冲突，提高了团队整体效率。
selection_source: fresh_fetch
motivation: 现有的智能体协作模式在高度结构化的僵化与非结构化的低效之间难以平衡，导致资源浪费和协作冲突。
method: 提出 LATTE 框架，利用动态演化的协作图来编码子任务依赖、智能体分配及进度状态，实现自适应的任务分配。
result: 在多项协作任务中，LATTE 显著减少了 Token 使用量、运行时间和通信故障，且性能优于 MetaGPT 等主流架构。
conclusion: 通过动态协作图实现的自适应协调机制，能有效提升大语言模型智能体团队的运行效率和任务处理能力。
---

## 摘要
大语言模型（LLM）越来越多地以团队形式部署，然而现有的协作方法往往处于两个极端。高度结构化的方法依赖于预先分配的固定角色、流水线或任务分解。相比之下，完全非结构化的团队虽然具备适应性和探索能力，但存在效率低下的问题，如错误传播、智能体间冲突以及资源浪费（以时间、Token 或文件操作衡量）。我们提出了用于任务演化的语言智能体团队（LATTE），这是一个受分布式系统启发的 LLM 团队协作框架，其中处理器必须在部分可观测性和通信限制下运行。在 LATTE 中，智能体团队协作构建并维护一个共享的、不断演化的协作图，该图编码了子任务依赖关系、单个智能体分配以及子任务进度的当前状态。该协议在保持一致性的同时，赋予智能体动态分配工作、调整协作和发现新任务的能力。在多项协作任务和多种基础模型中，我们证明了 LATTE 如何在减少 Token 使用量、实际运行时间、通信和协作失败（如文件冲突和冗余输出）的同时，达到或超过标准设计的准确率，包括 MetaGPT、去中心化团队、自顶向下的领导者-执行者（Leader-Worker）层级结构以及静态分解。

## Abstract
Large language models (LLMs) are increasingly deployed in teams, yet existing coordination approaches often occupy two extremes. Highly structured methods rely on fixed roles, pipelines, or task decompositions assigned a priori. In contrast, fully unstructured teams enable adaptability and exploration but suffer from inefficiencies such as error propagation, inter-agent conflicts, and wasted resources (measured in time, tokens, or file operations). We introduce Language Agent Teams for Task Evolution (LATTE), a framework for coordinating LLM teams inspired by distributed systems, where processors must operate under partial observability and communication constraints. In LATTE, a team of agents collaboratively construct and maintain a shared, evolving coordination graph which encodes sub-task dependencies, individual agent assignment, and the current state of sub-task progress. This protocol maintains consistency while empowering agents to dynamically allocate work, adapt coordination, and discover new tasks. Across multiple collaborative tasks and a variety of base models, we demonstrate how LATTE reduces token usage, wall-clock time, communication, and coordination failures (e.g. file conflicts and redundant outputs) while matching or exceeding the accuracy of standard designs including MetaGPT, decentralized teams, top-down Leader-Worker hierarchies, and static decompositions.

---

## 论文详细总结（自动生成）

以下是对论文《Improving the Efficiency of Language Agent Teams with Adaptive Task Graphs》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的多智能体（LLM Agents）协作模式存在两个极端：
    *   **高度结构化**（如 MetaGPT）：依赖预设的角色和流水线，缺乏应对执行中突发情况的灵活性。
    *   **完全非结构化**（如去中心化团队）：虽然灵活，但容易产生错误传播、智能体冲突（如同时修改同一文件）和资源浪费（Token 和时间）。
*   **研究动机**：受分布式系统启发，研究者试图寻找一种既能保持全局一致性，又能实现局部动态适应的协作框架，以解决 LLM 团队在复杂任务中的效率与可靠性平衡问题。

### 2. 方法论：LATTE 框架
*   **核心思想**：引入**动态协作图（Dynamic Coordination Graph）**。将任务表示为有向无环图（DAG），节点代表子任务，边代表依赖关系。团队通过共同维护和演化这张图来管理进度。
*   **关键技术细节**：
    *   **混合协调模型**：
        *   **Leader（领导者）**：拥有全局视野，负责维护图结构、分配任务、验证结果及处理掉队者（Stragglers）。
        *   **Workers（执行者）**：拥有局部视野，负责执行具体子任务，并能根据执行情况提议发现新任务。
    *   **七大图变异算子**：
        1.  `DISCOVER`：发现并添加新子任务。
        2.  `ASSIGN`：由 Leader 分配任务。
        3.  `CLAIM`：Worker 主动申领任务（自调度，减少 Leader 瓶颈）。
        4.  `COMPLETE`：标记任务完成。
        5.  `RELEASE`：释放停滞的任务以重新分配。
        6.  `CLOSE`：强制关闭任务。
        7.  `VERIFY`：触发验证节点进行质量控制。
    *   **概率论动机**：将图的演化视为后验推理过程，Worker 提出局部更新建议（类似采样），Leader 进行全局评估（类似接受/拒绝步骤），从而在推理质量和计算成本间取得平衡。

### 3. 实验设计
*   **实验场景**：
    1.  **探索性数据分析（EDA）**：对不透明数据集进行预处理、分析和总结，需动态发现分析方向。
    2.  **代码调试（Debugging）**：修复信号处理库中的多个 Bug，强调并行性与依赖一致性。
    3.  **库扩展（Library Extension）**：根据存根文件构建新模块，测试顺序依赖与集成能力。
*   **对比方法（Baselines）**：
    *   **MetaGPT**：基于固定 SOP 的流水线系统。
    *   **Leader-Worker**：传统的层级结构（无图管理）。
    *   **Decentralized**：去中心化对等团队。
    *   **Static Graph**：静态任务图（消融实验，禁止执行中修改图）。
*   **基础模型**：使用了 Claude Sonnet 4-6 和 GPT-5.2（注：论文设定在 2026 年背景）。

### 4. 资源与算力
*   **算力说明**：文中未提及具体的 GPU 训练算力，因为该研究主要基于现有的 LLM API 进行推理协作。
*   **资源消耗统计**：
    *   Claude 实验消耗约 40.7M Input Tokens / 8.5M Output Tokens（约 250 美元）。
    *   GPT 实验消耗约 83.1M Input Tokens / 14M Output Tokens（约 260 美元）。

### 5. 实验数量与充分性
*   **实验规模**：共进行了 **300 组独立实验**（5 种团队结构 × 2 种模型 × 3 类任务 × 10 次重复）。
*   **充分性评价**：实验设计较为充分。除了最终的准确率，还引入了细粒度的协作指标（如文件覆盖率、并发冲突数、浪费的字符数、通信开销等），能够客观、公平地评估不同框架在资源效率上的差异。

### 6. 主要结论与发现
*   **效率大幅提升**：LATTE 的 Token 成本仅为次优方法（静态图）的一半左右，且显著低于 MetaGPT 和去中心化团队。
*   **准确率领先**：在保持高效率的同时，LATTE 的综合准确率（79.7%）高于所有基准方法。
*   **冲突减少**：相比 Leader-Worker 和去中心化模式，LATTE 将智能体间的文件覆盖冲突减少了 5-8 倍，显著降低了“无效劳动”。
*   **自适应能力**：实验观察到团队能自发调用 `RELEASE` 处理掉队智能体，并针对高不确定性任务调用 `VERIFY` 进行额外检查。

### 7. 优点与亮点
*   **可解释性**：通过外部化的任务图，人类可以直观地审计和诊断智能体团队的决策过程。
*   **自调度机制**：允许 Worker 主动 `CLAIM` 任务，有效缓解了 Leader 智能体的通信瓶颈。
*   **容错性**：心跳监测和任务释放机制使团队能够从智能体幻觉或崩溃中恢复。

### 8. 不足与局限
*   **规划开销**：对于非常简单或短小的任务，初始化任务图的开销可能超过其带来的收益。
*   **任务类型限制**：目前主要在代码和数据分析等具有明确子任务边界的领域验证，在极度模糊或非结构化的创意任务中表现尚待研究。
*   **规模化挑战**：虽然固定了 5 个智能体的团队规模，但随着智能体数量进一步增加，图维护的复杂度和 Leader 的上下文压力可能会成为新的瓶颈。

（完）
