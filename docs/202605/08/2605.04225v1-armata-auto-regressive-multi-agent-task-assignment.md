---
title: "ARMATA: Auto-Regressive Multi-Agent Task Assignment"
title_zh: ARMATA：自回归多智能体任务分配
authors: "Yazan Youssef, Aboelmagd Noureldin, Sidney Givigi"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.04225v1"
tags: ["query:malm"]
score: 9.0
evidence: 多智能体任务分配的集中式自回归框架
tldr: 针对多智能体任务分配中分配与路径规划解耦导致的效率低下问题，本文提出ARMATA框架。该框架采用集中式端到端自回归模型，通过多阶段解码机制将高层区域分配与底层路径规划统一在单次推理中。实验表明，该方法在解质量上优于Google OR-Tools等工业求解器，且将计算时间从数小时缩短至秒级。
selection_source: fresh_fetch
motivation: 传统方法通常将任务分配与路径规划阶段解耦，忽略了阶段间的依赖关系，且去中心化启发式算法缺乏全局视野。
method: 提出一种集中式端到端自回归框架，通过多阶段解码机制在单次推理中同步生成分配决策与路由序列。
result: "该方法在解质量上比Google OR-Tools和LKH-3等工业求解器提升高达20%，并将计算时间从数小时缩短至秒级。"
conclusion: ARMATA通过联合优化分配与路径规划，有效平衡了工作负载与路由效率，为复杂多智能体协调问题提供了高效的端到端解决方案。
---

## 摘要
在空间分布区域协调多智能体系统需要解决一个复杂的层级问题：首先在智能体之间分配区域（分配），随后确定最优访问顺序（路径规划）。现有方法通常将这些阶段解耦，忽略了阶段间的依赖关系，或者依赖于缺乏全局上下文的去中心化启发式方法。在这项工作中，我们提出了一个中心化的、完全端到端的自回归框架，用于联合生成分配决策和路径序列。我们方法的核心贡献是一个多阶段解码机制，它在保持中心化全局状态的同时，通过单次自回归过程统一了高层分配和底层路径规划。这使得模型能够隐式地平衡工作负载分布与路径规划效率，从而避免去中心化方法中常见的局部最优。广泛的实验表明，我们的方法显著优于各种基准方法，与 Google OR-Tools、IBM CPLEX 和 LKH-3 等工业求解器相比，解的质量提升了高达 20%，同时将计算时间从数小时缩短至数秒。

## Abstract
Coordinating multi-agent systems over spatially distributed areas requires solving a complex hierarchical problem: first distributing areas among agents (allocation) and subsequently determining the optimal visitation order (routing). Existing methods typically decouple these stages ignoring inter-stage dependencies or rely on decentralized heuristics that lack global context. In this work, we propose a centralized, fully end-to-end auto-regressive framework that jointly generates allocation decisions and routing sequences. The core contribution of our approach is a multi-stage decoding mechanism that unifies high-level allocation and low-level routing in a single autoregressive pass while maintaining a centralized global state. This enables the model to implicitly balance workload distribution with routing efficiency, avoiding local optima common in decentralized methods. Extensive experiments demonstrate that our method significantly outperforms diverse baselines, achieving up to a 20\% improvement in solution quality over industrial solvers such as Google OR-Tools, IBM CPLEX, and LKH-3, while reducing computation time from hours to seconds.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **ARMATA** 的新型框架，旨在解决多智能体系统中的任务分配与路径规划联合优化问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与背景（研究动机）
在多智能体系统（MAS）中，协调多个智能体覆盖分布区域涉及两个相互依赖的层级问题：**任务分配**（谁去哪）和**路径规划**（按什么顺序去）。
*   **传统痛点：** 现有方法通常将这两个阶段解耦（先分配再规划），忽略了两者间的耦合关系，导致解的质量不佳。
*   **现有工具局限：** 精确数学求解器（如 CPLEX）面临组合爆炸问题，处理大规模实例需数小时甚至数天；启发式算法（如 OR-Tools）虽快但易陷入局部最优；去中心化方法则缺乏全局视野，容易导致重复覆盖或负载不均。
*   **研究目标：** 开发一个中心化的、端到端的学习框架，在单次推理中同时完成分配与路由决策。

### 2. 方法论：ARMATA 框架
ARMATA 采用**集中式端到端自回归架构**，核心思想是通过多阶段解码机制模拟人类决策过程。
*   **输入表示：** 使用四个矩阵编码环境，包括智能体位置、区域邻接关系、区域特征（顶点坐标）和区域中心位置。
*   **双编码器结构：**
    *   **节点编码器（Nodes Encoder）：** 使用多层门控图卷积网络（Gated GCN）提取地理区域的结构特征。
    *   **智能体编码器（Agents Encoder）：** 同样使用 Gated GCN 提取智能体之间的相对位置和分布特征。
*   **多阶段自回归解码器：** 包含三个连续的 Transformer 网络：
    1.  **网络 I（区域分配）：** 基于全局上下文和当前状态，决定当前智能体应访问哪个区域。
    2.  **网络 II（起点选择）：** 在选定区域内选择最优的进入顶点（起点）。
    3.  **网络 III（扫描模式）：** 决定在该区域内的移动/扫描模式。
*   **训练算法：** 采用强化学习（RL）策略梯度算法（主要使用 **REINFORCE**，并对比了 **PPO**），目标函数是最小化所有智能体的总行驶距离。

### 3. 实验设计
*   **数据集：** 在单位正方形内随机生成地图，每轮（Epoch）包含 128,000 个样本。
*   **测试场景：**
    *   **范围内（In-range）：** 5智能体/40区域、8智能体/72区域（与训练分布一致）。
    *   **范围外（Out-of-range）：** 4智能体/24区域、12智能体/180区域（测试泛化能力）。
*   **基准方法（Benchmarks）：** 工业级求解器 **Google OR-Tools**、**IBM CPLEX** 以及最先进的元启发式算法 **LKH-3**。

### 4. 资源与算力
*   **硬件：** 使用了一台配备 **NVIDIA GeForce RTX-3090 GPU** 和 **Intel i9 11900KF CPU** (8核, 3.5GHz-5.3GHz) 的工作站。
*   **训练细节：** 模型包含约 360,000 个可训练参数，训练时长为 100 个 Epoch。
*   **软件环境：** 基于 PyTorch 2.1 和 OpenAI Gym 开发，便于未来集成到 ROS 机器人系统中。

### 5. 实验数量与充分性
*   **实验规模：** 论文进行了多维度的对比，包括不同规模的测试集（1280个样本/组）、消融实验（如禁用智能体编码器的影响）以及不同 RL 算法（REINFORCE vs PPO）的对比。
*   **客观性：** 实验涵盖了从小型到超大型（180个区域）的规模，并与公认的最强工业求解器进行对比。通过“范围外”测试验证了模型的泛化性，证明其并非简单的过拟合，实验设计较为充分且公平。

### 6. 主要结论与发现
*   **解的质量：** ARMATA 在解的质量上比 LKH-3 和 CPLEX 提升了 **14% - 26%**。
*   **计算效率：** 实现了 **4 个数量级** 的加速。在处理 180 个区域的复杂任务时，CPLEX 需 405 秒，LKH-3 需 152 秒，而 ARMATA 仅需 **0.0872 秒**。
*   **泛化能力：** 模型在未见过的更大规模实例上依然保持高效，优于传统的端到端学习模型。
*   **关键组件：** 消融实验证明，**智能体编码器**对提升性能至关重要，移除它会导致性能下降约 20%。

### 7. 优点（亮点）
*   **联合优化：** 突破了分配与路由解耦的传统框架，实现了真正的端到端联合决策。
*   **极速推理：** 秒级的处理速度使其非常适合实时动态重规划（Real-time Replanning）。
*   **自回归机制：** 顺序生成决策天然避免了任务分配冲突，无需复杂的后处理掩码。
*   **架构创新：** 多阶段解码器（区域->起点->模式）精细地捕捉了层级决策的逻辑。

### 8. 不足与局限
*   **智能体顺序固定：** 解码时智能体按固定顺序处理，虽然自回归能缓解，但可能仍存在顺序偏差。
*   **任务同质化：** 实验假设每个智能体分配的任务数量相等（n/m），在实际应用中，智能体可能需要处理负载极度不均的情况。
*   **环境静态性：** 实验主要在静态生成的地图上进行，对于动态障碍物或实时变化的区域属性，其鲁棒性尚待进一步验证。
*   **应用领域限制：** 目前主要针对区域扫描/搜索任务，对于更复杂的取送货（Pickup and Delivery）等带时间窗约束的问题，模型结构可能需要大幅调整。

（完）
