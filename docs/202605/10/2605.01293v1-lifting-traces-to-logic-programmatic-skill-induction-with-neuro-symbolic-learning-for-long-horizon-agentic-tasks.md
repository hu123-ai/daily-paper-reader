---
title: "Lifting Traces to Logic: Programmatic Skill Induction with Neuro-Symbolic Learning for Long-Horizon Agentic Tasks"
title_zh: 将轨迹提升为逻辑：面向长程智能体任务的神经符号学习程序化技能归纳
authors: "Jie-Jing Shao, Haiyan Yin, Yueming Lyu, Xingrui Yu, Lan-Zhe Guo, Ivor Tsang, James Kwok, Yu-Feng Li"
date: 2026-05-02
pdf: "https://arxiv.org/pdf/2605.01293v1"
tags: ["query:malm"]
score: 8.0
evidence: 智能体任务的逻辑落地程序
tldr: 针对基础模型驱动的智能体在长程规划中缺乏逻辑鲁棒性的问题，本文提出神经符号技能归纳（NSI）框架。该方法将交互轨迹提升为包含显式控制流和动态变量绑定的逻辑程序，使智能体能从少量示例中学习“何时”及“为何”执行动作。NSI 显著提升了智能体在动态环境中的泛化与自进化能力，在多项长程任务中超越了现有技术。
selection_source: fresh_fetch
motivation: 现有的技能归纳方法多为状态盲目的参数化脚本，难以捕捉动态环境中复杂的条件逻辑需求。
method: 提出 NSI 框架，通过神经符号学习将交互轨迹转化为具有显式控制流和变量绑定的模块化逻辑程序。
result: 实验证明 NSI 在多项智能体任务中显著优于现有基准，展现出极强的少样本学习能力和对未知目标的适应性。
conclusion: NSI 成功将技能归纳从简单的动作序列提升为逻辑驱动的程序，为构建可自进化的长程智能体提供了有效路径。
---

## 摘要
受限于纯提示词推理的瞬时性，基础模型驱动的智能体在长程规划方面往往表现不佳。虽然现有的技能归纳方法通过将经验蒸馏为与状态无关的参数化脚本来缓解这一问题，但它们无法捕捉在动态环境中稳健执行所需的条件逻辑。在本文中，我们提出了神经符号技能归纳（NSI），这是一个将交互轨迹提升为模块化、基于逻辑的程序的框架。通过合成显式控制流和动态变量绑定，NSI 使智能体能够发现“何时”以及“为何”采取行动。这种范式实现了高效的泛化，允许智能体从少样本示例中归纳技能，并灵活地适应未见过的目标。在一系列智能体任务上的实验表明，NSI 一贯优于最先进的基准方法，使智能体能够自我进化为基于逻辑的技能构建者。

## Abstract
Foundation model-driven agents often struggle with long-horizon planning due to the transient nature of purely prompting-based reasoning. While existing skill induction methods mitigate this by distilling experience into state-blind parameterized scripts, they fail to capture the conditional logic required for robust execution in dynamic environments. In this paper, we propose Neuro-Symbolic Skill Induction (NSI), a framework that lifts interaction traces into modular, \textit{logic-grounded} programs. By synthesizing explicit control flows and dynamic variable binding, NSI empowers agents to discover \textit{when} and \textit{why} to act. This paradigm enables the efficient generalization, allowing agents to induce skills from few-shot examples and flexibly adapt to unseen goals. Experiments on a series of agentic tasks demonstrate that NSI consistently outperforms state-of-the-art baselines, empowering agents to self-evolve into architects of logic-grounded skills.

---

## 论文详细总结（自动生成）

### 论文总结：将轨迹提升为逻辑（NSI 框架）

#### 1. 论文的核心问题与整体含义
基础模型（如 LLM）驱动的智能体在处理**长程规划（Long-horizon Planning）**任务时，常因推理的瞬时性和缺乏领域知识而失败。现有的“技能归纳”方法试图将成功经验转化为可重用的脚本，但这些脚本通常是**状态盲目（State-blind）**的线性序列，无法应对动态环境中的逻辑分支（例如：如果冰箱门关着则开门，如果开着则直接取物）。
本文的研究动机是打破这种“硬编码”式的脚本归纳，提出一种**神经符号技能归纳（Neuro-Symbolic Skill Induction, NSI）**框架，旨在将交互轨迹转化为具有显式控制流和动态变量绑定的逻辑程序，使智能体具备“理解何时以及为何行动”的能力。

#### 2. 方法论：核心思想与关键技术
NSI 的核心思想是将技能表示为**模块化的逻辑图（Workflow Graph）**，通过神经感知与符号执行的解耦来实现鲁棒性。

*   **神经符号表示**：
    *   **神经感知（$\phi_\omega$）**：利用 LLM 作为语义感知器，将非结构化观察转化为结构化的符号状态（基于一阶逻辑谓词）。
    *   **符号执行逻辑（$G_\omega$）**：由四种逻辑节点组成的有向图：
        *   `DataOp`：动态变量绑定（如：从环境中识别出“苹果”并绑定到变量）。
        *   `CheckOp/LoopOp`：控制流决策（分支与循环）。
        *   `PrimitiveOp`：执行原子动作。
        *   `TerminalOp`：反馈成功或带有诊断信息的失败。
*   **归纳流程（从轨迹到逻辑）**：
    *   **经验一致性目标**：通过最大化“经验程序一致性”（Empirical Programmatic Consistency）来合成程序，即诱导的技能必须能在历史状态下重现专家的动作序列。
    *   **渐进式整合**：
        1.  **轨迹内整合**：从单条轨迹中提取局部专家逻辑。
        2.  **轨迹间整合**：利用**分支（Branching）**、**交叉（Crossover）**、**提升（Lifting，将常量抽象为变量）**和**循环折叠（Loop Folding）**等算子，将多个局部专家合并为通用的全局技能。
*   **在线进化（反射式规划）**：当技能执行失败时，智能体通过“反射”生成修复轨迹，并将其作为新的逻辑分支“嫁接”到原有的技能图中，实现技能的自我完善。

#### 3. 实验设计
论文在三个具有代表性的智能体 Benchmark 上进行了验证：
*   **ALFWorld**：文本驱动的具身智能体环境，涉及复杂的家庭任务。
*   **WebShop**：模拟真实电商平台的购物任务，考察属性选择和导航能力。
*   **TextCraft**：基于 Minecraft 的合成任务，考察递归组合泛化能力。
*   **对比方法**：包括基础提示词方法（ReAct）、带有记忆的方法（Reflexion）、任务分解方法（ADaPT）、状态跟踪方法（StateAct）以及现有的程序化技能归纳方法（AWM, ASI）。

#### 4. 资源与算力
*   **模型**：所有实验均采用 **GPT-4o** 作为骨干模型。
*   **算力说明**：论文**未明确说明**具体的物理算力资源（如 GPU 型号、数量或训练时长）。由于该方法主要基于 LLM 的 In-context Learning 和程序合成，而非大规模参数微调，其算力消耗主要集中在 API 调用和推理上。

#### 5. 实验数量与充分性
*   **实验规模**：在三个领域共计数百个测试实例上进行了实验（ALFWorld 134个，TextCraft 200个等）。
*   **充分性**：
    *   每项实验均运行 3 次并报告了均值和标准差，具有统计学意义。
    *   包含了**消融实验**（如对比有无在线进化模块），验证了各组件的有效性。
    *   进行了**生存分析（Survival Analysis）**，量化了 NSI 在超长程任务（>50步）中的鲁棒性。
*   **客观性**：对比了多种不同范式的 SOTA 方法，实验设计较为公正。

#### 6. 主要结论与发现
*   **性能领先**：NSI 在所有基准测试中均显著优于现有方法，尤其在 WebShop 上的得分提升巨大（从 51.8% 提升至 76.5%）。
*   **时界压缩（Horizon Compression）**：NSI 能将平均 7.4 个原子动作封装进一个逻辑技能中，有效缩短了高层规划的步数，降低了 LLM 的推理负担。
*   **自进化能力**：通过反射式规划，智能体能将运行时的失败转化为永久的逻辑改进，使技能图从简单的线性链演变为复杂的决策网络。

#### 7. 优点（亮点）
*   **逻辑鲁棒性**：不同于以往的“状态盲目”脚本，NSI 引入了显式的条件分支，使其能适应环境的动态变化。
*   **高解释性**：技能以 Mermaid 流程图和一阶逻辑形式存在，人类可读且易于调试。
*   **高效泛化**：通过“变量提升”机制，智能体能从特定物体的操作经验中归纳出处理同类物体的通用能力。

#### 8. 不足与局限
*   **谓词依赖**：NSI 依赖于预定义的一阶逻辑谓词词汇表（如 `is_open`, `contains`），在完全未知的开放域环境中，如何自动发现这些谓词仍是挑战。
*   **感知偏差风险**：如果底层的神经感知模块（LLM 状态跟踪）出现错误，会导致符号执行逻辑进入错误分支。
*   **复杂性上限**：对于极度复杂的逻辑，生成的 Mermaid 图可能会变得过于庞大，增加 LLM 处理长上下文的压力。

（完）
