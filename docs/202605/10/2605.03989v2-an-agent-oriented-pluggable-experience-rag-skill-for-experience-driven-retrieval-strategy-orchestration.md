---
title: An Agent-Oriented Pluggable Experience-RAG Skill for Experience-Driven Retrieval Strategy Orchestration
title_zh: 一种面向智能体的、用于经验驱动检索策略编排的可插拔 Experience-RAG 技能
authors: "Dutao Zhang, Tian Liao"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03989v2"
tags: ["query:malm"]
score: 9.0
evidence: 面向智能体的可插拔检索策略编排
tldr: 本研究针对检索增强生成（RAG）系统中固定检索流程难以适配异构任务的问题，提出了一种面向智能体的可插拔“经验检索技能”（Experience-RAG Skill）。该技能作为智能体与检索池之间的编排层，能根据当前场景咨询经验存储器并动态选择最优检索策略。实验表明，该方法在BeIR多个任务上表现优异，证明了将检索策略选择封装为可复用技能的有效性，提升了系统在处理复杂任务时的灵活性。
selection_source: fresh_fetch
motivation: 传统的RAG系统通常采用固定的检索流程，难以满足事实问答、多跳推理和科学验证等不同任务对检索策略的差异化需求。
method: 提出一种可插拔的经验驱动检索编排层，通过分析任务场景并利用经验存储器动态调度检索策略，为智能体提供结构化证据。
result: "在BeIR/nq、hotpotqa和scifact数据集上实现了0.8924的nDCG@10，超越了固定单检索器基准，并与自适应路由方法保持竞争力。"
conclusion: 检索策略的选择可以被有效地封装为一种可复用的智能体技能，从而避免在顶层工作流中进行硬编码，提升系统的通用性。
---

## 摘要
检索增强生成系统通常假设单一的固定检索流水线足以应对各种异构任务，然而事实型问答、多跳推理和科学验证表现出不同的检索偏好。我们提出了 Experience-RAG 技能，这是一种位于智能体与检索器池之间的面向智能体的可插拔检索编排层。该技能分析当前场景，咨询经验记忆，选择合适的检索策略，并向智能体返回结构化证据。在固定候选池下，Experience-RAG 技能在 BeIR/nq、BeIR/hotpotqa 和 BeIR/scifact 上实现了 0.8924 的总 nDCG@10，优于固定的单检索器基准，并与 Adaptive-RAG 风格的路由保持竞争力。结果表明，检索策略选择可以有效地封装为可重用的智能体技能，而不是在上层工作流中进行硬编码。

## Abstract
Retrieval-augmented generation systems often assume that one fixed retrieval pipeline is sufficient across heterogeneous tasks, yet factoid question answering, multi-hop reasoning, and scientific verification exhibit different retrieval preferences. We present Experience-RAG Skill, an agent-oriented pluggable retrieval orchestration layer positioned between the agent and the retriever pool. The proposed skill analyzes the current scene, consults an experience memory, selects an appropriate retrieval strategy, and returns structured evidence to the agent. Under a fixed candidate pool, Experience-RAG Skill achieves an overall nDCG@10 of 0.8924 on BeIR/nq, BeIR/hotpotqa, and BeIR/scifact, outperforming fixed single-retriever baselines and remaining competitive with Adaptive-RAG-style routing. The results suggest that retrieval strategy selection can be productively encapsulated as a reusable agent skill rather than being hard-coded in the upper workflow.

---

## 论文详细总结（自动生成）

以下是对论文《An Agent-Oriented Pluggable Experience-RAG Skill for Experience-Driven Retrieval Strategy Orchestration》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的检索增强生成（RAG）系统通常采用固定的检索流水线，但在实际应用中，不同类型的任务（如事实问答、多跳推理、科学验证）对检索策略的偏好各不相同。
*   **研究动机**：现有的系统往往将检索逻辑硬编码在工作流中，缺乏灵活性。作者提出将“检索策略编排”封装为一种面向智能体（Agent-oriented）的、可插拔的“技能”（Skill），使智能体能根据当前场景动态选择最优检索策略。

### 2. 方法论
该论文提出了一种名为 **Experience-RAG Skill** 的中间层架构，位于智能体与检索器池之间。
*   **核心思想**：通过分析任务场景并咨询“经验存储器”，自动在多个检索器（如BM25、Dense、Hybrid等）中选择最匹配当前查询的策略。
*   **关键流程**：
    1.  **场景分析（Scene Analysis）**：提取查询（q）、对话历史（h）和元数据（m）的特征，包括任务类型、领域、复杂度、查询风格等。
    2.  **经验查找与路由（Experience Lookup & Routing）**：在经验存储器（M）中检索类似场景的记录。目前最有效的配置是**基于规则的路由**（例如：简单任务指向稠密检索，复杂任务指向混合重排序检索）。
    3.  **执行与封装（Retrieve & Package）**：调用选定的检索器，并将结果封装为标准化的证据对象返回给智能体。
*   **数学表达**：$s = Analyze(q, h, m)$；$r^* = Route(s, M)$；$o = RetrieveAndPackage(q, r^*)$。

### 3. 实验设计
*   **数据集/场景**：选取了 BeIR 基准测试中的三个代表性任务：
    *   **BeIR/nq**（事实问答）
    *   **BeIR/hotpotqa**（多跳推理）
    *   **BeIR/scifact**（科学证据验证）
*   **Benchmark**：每个数据集抽取 120 个查询，并使用采样后的候选语料库。
*   **对比方法**：
    *   **基础基准**：BM25、查询改写（Rewrite-BM25）、稠密检索（Dense）、混合重排序（Hybrid RRF）。
    *   **现代/先进基准**：HyDE（假设文档嵌入）、RAPTOR（递归摘要树检索）、LongRAG（长文本检索）、Adaptive-RAG（自适应路由）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的硬件配置（如 GPU 型号、数量）或训练时长。由于该研究侧重于策略编排层而非底层模型的重新训练，其核心开销主要集中在推理和特征提取阶段。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了主实验对比（表1），涵盖了 5 种基础方法。
    *   进行了现代基准对比（表2），涵盖了 4 种先进 RAG 变体。
    *   进行了消融实验（表3），验证了策略编排相对于固定策略的增益。
    *   探索了不同路由算法（规则 vs. 学习型）的效果对比。
*   **充分性评价**：实验设计较为全面，覆盖了异构任务场景。但由于每个数据集仅抽取 120 个查询且使用了采样语料，实验规模属于中小型，其在全量大规模数据上的泛化能力仍有待进一步验证。

### 6. 主要结论与发现
*   **性能领先**：Experience-RAG Skill 实现了 **0.8924 的 nDCG@10**，优于所有固定的单检索器基准。
*   **编排价值**：该技能的优势在于“异构编排”，即在混合任务负载下表现稳健，能够针对不同任务切换到该任务下的最强策略。
*   **路由机制**：在当前经验规模下，**基于规则的路由**表现优于基于机器学习的路由（分类或回归），说明简单的启发式逻辑在特定场景下依然非常高效。
*   **竞争性**：该方法与复杂的 Adaptive-RAG 性能相当，但提供了更清晰的系统边界和可插拔性。

### 7. 优点
*   **模块化设计**：将检索策略选择封装为“技能”，降低了智能体上层逻辑的复杂度，增强了系统的可维护性。
*   **经验驱动**：引入经验存储器记录策略边际收益（Margin），为后续的持续学习提供了数据基础。
*   **标准化接口**：为智能体提供了统一的证据获取接口，屏蔽了底层不同检索算法的差异。

### 8. 不足与局限
*   **语料规模**：实验基于采样语料而非全规模语料库，可能无法完全反映真实生产环境中的检索压力。
*   **学习瓶颈**：目前学习型路由（Learned Routing）的效果尚未超越人工规则，说明经验特征的提取或模型训练仍需优化。
*   **评估维度**：缺乏端到端的智能体任务完成率评估，目前的评估指标（Recall, MRR, nDCG）仅局限于检索质量本身。
*   **静态假设**：目前假设检索器池是固定的，尚未探讨如何动态接入和评估新的第三方检索工具。

（完）
