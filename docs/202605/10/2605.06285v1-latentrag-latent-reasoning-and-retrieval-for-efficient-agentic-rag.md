---
title: "LatentRAG: Latent Reasoning and Retrieval for Efficient Agentic RAG"
title_zh: LatentRAG：面向高效智能体 RAG 的潜空间推理与检索
authors: "Yijia Zheng, Marcel Worring"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06285v1"
tags: ["query:malm"]
score: 9.0
evidence: 高效智能体RAG的潜空间推理与检索
tldr: "针对 Agentic RAG 在处理复杂问题时因多步推理和子查询生成导致的显著延迟问题，本文提出 LatentRAG 框架。该框架将推理和检索过程从离散语言空间转移到连续潜空间，通过单次前向传播生成潜标记，并与密集检索模型对齐。实验表明，LatentRAG 在保持与显式 Agentic RAG 相当性能的同时，将推理延迟降低了约 90%，极大提升了效率。"
selection_source: fresh_fetch
motivation: 传统的 Agentic RAG 依赖自回归生成长篇推理和子查询，导致推理延迟过高，难以满足实时性需求。
method: 提出 LatentRAG 框架，在潜空间中直接生成推理和检索标记，并结合并行解码机制将潜标记转化为自然语言以增强透明度。
result: "在七个基准数据集上的实验显示，该方法在性能不减的前提下，将推理延迟降低了约 90%。"
conclusion: LatentRAG 证明了在潜空间进行推理和检索是实现高效、高性能 Agentic RAG 的有效途径。
---

## 摘要
单步检索增强生成 (RAG) 为简单问答任务提供了一种整合外部信息的有效方式，但在处理复杂问题时表现不佳。智能体 RAG (Agentic RAG) 扩展了这一范式，将单步检索替换为多步过程，其中大语言模型 (LLM) 充当搜索智能体，生成中间思维和子查询，以与检索系统进行迭代交互。由于冗长思维和子查询的自回归生成，这种迭代过程会产生巨大的延迟。为了解决这一局限性，我们提出了 LatentRAG，这是一个将推理和检索从离散语言空间转移到连续潜空间的新型框架。与现有的逐标记生成自然语言思维或子查询的显式方法不同，LatentRAG 在单次前向传播中直接从隐藏状态生成思维和子查询的潜标记 (latent tokens)。我们在潜空间中将 LLM 与稠密检索模型对齐，实现了基于潜子查询标记的检索，并支持端到端的联合优化。为了提高透明度并促进具有语义意义的潜表示，我们引入了一种并行潜解码机制，将潜标记翻译回自然语言。在七个基准数据集上的广泛实验表明，LatentRAG 达到了与显式智能体 RAG 方法相当的性能，同时将推理延迟降低了约 90%，大幅缩小了与传统单步 RAG 的延迟差距。

## Abstract
Single-step retrieval-augmented generation (RAG) provides an efficient way to incorporate external information for simple question answering tasks but struggles with complex questions. Agentic RAG extends this paradigm by replacing single-step retrieval with a multi-step process, in which the large language model (LLM) acts as a search agent that generates intermediate thoughts and subqueries to iteratively interact with the retrieval system. This iterative process incurs substantial latency due to the autoregressive generation of lengthy thoughts and subqueries. To address this limitation, we propose LatentRAG, a novel framework that shifts both reasoning and retrieval from discrete language space to continuous latent space. Unlike existing explicit methods that generate natural language thoughts or subqueries token-by-token, LatentRAG produces latent tokens for thoughts and subqueries directly from the hidden states in a single forward pass. We align LLMs with dense retrieval models in the latent space, enabling retrieval over latent subquery tokens and supporting end-to-end joint optimization. To improve transparency and encourage semantically meaningful latent representations, we incorporate a parallel latent decoding mechanism that translates latent tokens back into natural language. Extensive experiments on seven benchmark datasets show that LatentRAG achieves performance comparable to explicit agentic RAG methods while reducing inference latency by approximately 90%, substantially narrowing the latency gap with traditional single-step RAG.

---

## 论文详细总结（自动生成）

以下是对论文《LatentRAG: Latent Reasoning and Retrieval for Efficient Agentic RAG》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的**智能体 RAG (Agentic RAG)** 虽然在处理复杂多步问答时表现优异，但其推理延迟极高。
*   **研究动机**：Agentic RAG 需要大模型（LLM）迭代生成长篇的“思维链（CoT）”和“搜索子查询”，这种自回归的逐标记生成方式导致了巨大的计算开销。实验显示，像 Search-R1 这样的方法，其延迟是传统单步 RAG 的 16-22 倍，其中 90% 的时间耗费在思维和子查询的生成上。
*   **整体含义**：本文旨在通过将推理和检索过程从“离散语言空间”转移到“连续潜空间（Latent Space）”，在不损失性能的前提下，大幅提升 Agentic RAG 的运行效率。

### 2. 方法论
LatentRAG 框架由三个核心组件组成：
*   **潜标记生成 (Generation with Latent Tokens)**：
    *   不再生成自然语言文本，而是向 LLM 输入特殊的“思维标记”和“子查询标记”。
    *   通过单次前向传播（Single Forward Pass）获取这些标记在最后一层的隐藏状态（Hidden States），作为潜推理和潜子查询的表示。
*   **潜空间检索 (Latent Retrieval)**：
    *   使用轻量级投影层（Projector）将 LLM 的隐藏状态映射到检索模型的嵌入空间。
    *   **关键技术**：引入 **KL 散度对齐目标**。由于缺乏中间子查询的标注数据，模型通过最小化“潜子查询嵌入”与“参考自然语言子查询嵌入”在文档分布上的 KL 散度，实现端到端的联合优化。
*   **并行潜解码 (Latent Decoding)**：
    *   为了解决潜空间不可解释的问题，设计了一个可选的解码机制，将潜标记还原为自然语言。
    *   **效率优化**：由于各步骤的潜标记已生成，所有步骤的思维和子查询可以**并行解码**，而非传统的串行生成。
*   **训练目标**：综合了生成损失（预测动作和答案）、检索对齐损失（KL 散度）和解码重建损失。

### 3. 实验设计
*   **数据集**：涵盖 7 个基准问答数据集，包括通用问答（NQ, TriviaQA, PopQA）和多跳复杂问答（HotpotQA, 2wiki, Musique, Bamboogle）。
*   **检索语料库**：使用 2018 年维基百科全量转储（约 2100 万个文档分块）。
*   **对比方法 (Baselines)**：
    *   直接推理（无检索）。
    *   传统单步 RAG（Naive RAG）。
    *   基于提示的智能体 RAG（Iter-RetGen, Search-o1）。
    *   基于训练的智能体 RAG（R1-Searcher, ZeroSearch, DeepRAG, Search-R1, AutoRefine）。
*   **检索模型**：测试了多种规模和架构的检索器（如 Qwen3-0.6B/4B/8B, e5-base, jina-v5 等）。

### 4. 资源与算力
*   **训练算力**：使用 2 张 NVIDIA H100 GPU (94GB)。每个训练任务耗时约 24 至 48 小时。
*   **推理算力**：默认在单张 H100 GPU 上测量延迟。对于超大规模检索模型（如 8B 检索器），使用 3 张 H100 部署索引，1 张 H100 部署 LLM。
*   **模型参数**：主模型采用 Qwen2.5-7B，使用 LoRA（秩为 16）进行参数高效微调。

### 5. 实验数量与充分性
*   **实验规模**：在 7 个数据集上进行了全面测试，并针对 5 种不同的检索模型验证了通用性。
*   **消融实验**：对比了不同的检索损失函数（KL vs. Cosine vs. InfoNCE）、有无预训练检索器、有无解码损失等。
*   **扩展性分析**：研究了 LLM 规模（3B, 7B, 14B）和检索器规模（0.6B, 4B, 8B）对性能和延迟的影响。
*   **客观性**：实验采用了标准的 Exact Match (EM) 指标，并详细拆解了每一阶段的延迟（Prefill, Thought, Retrieval 等），对比过程考虑了硬件环境的一致性，实验设计较为客观、公平。

### 6. 主要结论
*   **极高的效率提升**：LatentRAG 在推理延迟上比显式 Agentic RAG 降低了约 **90%**，其速度接近传统的单步 Naive RAG。
*   **性能保持**：在所有数据集上，LatentRAG 的性能与最强的显式方法（如 Search-R1）相当，性能差异通常在 5% 以内。
*   **并行优势**：即使开启潜解码以增强透明度，由于支持跨步骤并行处理，其延迟仍比基线低 40%-60%。
*   **模型扩展性**：随着 LLM 和检索器规模的增大，LatentRAG 的性能稳步提升，且延迟增长远低于显式方法。

### 7. 优点
*   **创新性强**：首次将潜空间推理（Latent Reasoning）与智能体检索（Agentic Retrieval）结合，解决了 Agentic RAG 的落地痛点。
*   **端到端优化**：通过潜空间对齐，使得 LLM 和检索系统可以通过梯度进行联合优化，而不再受限于离散文本生成的不可导性。
*   **透明度兼顾**：通过并行解码机制，在追求效率的同时保留了模型决策过程的可解释性。

### 8. 不足与局限
*   **依赖教师模型**：LatentRAG 的训练依赖于现有显式方法生成的轨迹（SFT），其性能上限在一定程度上受限于教师模型的质量。
*   **检索器兼容性**：实验发现，对于具有严重“各向异性（Anisotropy）”的检索模型（如 e5-base-v2），潜空间对齐的效果会变差，这限制了检索器的选择范围。
*   **精确度微损**：在某些需要极高词汇精确度的场景下，潜表示可能导致答案生成出现轻微的拼写或一致性错误（如 Case Study 中提到的名称拼写偏差）。

（完）
