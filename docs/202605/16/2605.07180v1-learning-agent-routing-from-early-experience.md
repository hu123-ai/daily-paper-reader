---
title: Learning Agent Routing From Early Experience
title_zh: 从早期经验中学习智能体路由
authors: "Yimin Wang, Jiahao Qiu, Xuan Qi, Xinzhe Juan, Jingzhe Shi, Zelin Zhao, Hongru Wang, Shilong Liu, Mengdi Wang"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07180v1"
tags: ["query:malm"]
score: 8.0
evidence: 大语言模型推理与完整智能体执行之间的路由框架
tldr: LLM智能体在复杂任务中表现优异但延迟和成本较高，而许多简单查询无需完整智能体执行。本文提出BoundaryRouter，一种无需训练的路由框架，通过利用早期行为经验和准则引导推理，在轻量级LLM推理与完整智能体执行之间进行决策。通过构建经验记忆库并检索相似案例，该方法在保持高性能的同时显著降低了推理成本，并引入了RouteBench基准进行评估。
selection_source: fresh_fetch
motivation: 旨在解决LLM智能体推理延迟高、成本贵的问题，通过有效路由区分简单查询与复杂任务以优化资源分配。
method: 提出BoundaryRouter框架，利用种子集的早期执行经验构建记忆库，并结合准则引导的推理进行动态路由决策。
result: "实验显示该方法比完整智能体节省了60.6%的推理时间，且性能比直接LLM推理提升了28.6%，优于现有基准。"
conclusion: BoundaryRouter证明了利用早期经验进行路由能有效平衡LLM推理的效率与智能体的高性能，是冷启动场景下的高效方案。
---

## 摘要
LLM 智能体在复杂推理任务上表现出色，但会产生高延迟和计算成本。在实践中，许多查询处于前沿 LLM 的能力边界内，不需要完整的智能体执行，因此在 LLM 和智能体之间进行有效的路由成为一个关键挑战。我们研究了在现实的冷启动设置下，如何在轻量级 LLM 推理和完整智能体执行之间进行查询路由的问题。为此，我们提出了 BoundaryRouter，这是一个无需训练的路由框架，它利用早期行为经验和准则引导的推理来决定是直接通过 LLM 推理回答查询，还是升级到智能体。BoundaryRouter 通过在共享种子集上执行这两个系统来构建紧凑的经验记忆，并在推理时检索相似案例以指导路由决策。为了评估该方法，我们引入了 RouteBench，这是一个涵盖域内、改写和域外路由设置的基准测试。实验表明，与智能体相比，BoundaryRouter 减少了 60.6% 的推理时间，同时比直接 LLM 推理的性能提高了 28.6%，平均分别优于基于提示和仅检索的路由 37.9% 和 8.2%。

## Abstract
LLM agents achieve strong performance on complex reasoning tasks but incur high latency and compute cost. In practice, many queries fall within the capability boundary of cutting-edge LLMs and do not require full agent execution, making effective routing between LLMs and agents a key challenge. We study the problem of routing queries between lightweight LLM inference and full agent execution under realistic cold-start settings. To address this, we propose BoundaryRouter, a training-free routing framework that uses early behavioral experience and rubric-guided reasoning to decide whether to answer a query with direct LLM inference or escalate to an agent. BoundaryRouter builds a compact experience memory by executing both systems on a shared seed set and retrieves similar cases at inference time to guide routing decisions. To evaluate this method, we introduce RouteBench, a benchmark covering in-domain, paraphrased, and out-of-domain route settings. Experiments show that BoundaryRouter reduces inference time by 60.6% compared to the agent while improving performance by 28.6% over direct LLM inference, outperforming prompt-based and retrieval-only routing by an average of 37.9% and 8.2%, respectively.

---

## 论文详细总结（自动生成）

这篇论文由普林斯顿大学、密歇根大学、清华大学等机构的研究人员合作完成，发表于 ICLR 2026 的 Lifelong Agent Workshop。以下是对该论文的结构化深入总结：

### 1. 核心问题与研究背景
*   **研究动机**：虽然大语言模型（LLM）智能体在复杂推理任务中表现卓越，但其多步推理、工具调用和长上下文管理带来了极高的延迟和计算成本。
*   **核心矛盾**：在实际应用中，许多查询（Queries）其实处于前沿 LLM 的能力边界之内，直接通过单次 LLM 推理即可解决，无需动用昂贵的完整智能体流程。
*   **挑战**：如何在“冷启动”设置下（即缺乏标注数据、不知道查询的地面真值 Ground-truth）有效地在轻量级 LLM 和全能智能体之间进行路由决策。

### 2. 方法论：BoundaryRouter 框架
BoundaryRouter 是一种**无需训练（Training-free）**的路由框架，其核心思想是利用“早期经验”来探测 LLM 的智能边界。
*   **早期经验记忆构建（Early Experience Memory）**：
    *   在系统部署初期，选取一小部分种子查询集（Seed Set）。
    *   同时运行轻量级 LLM 和完整智能体，记录两者的输出结果和运行延迟（Latency）。
    *   **关键点**：不需要知道正确答案，仅记录行为表现（如输出的结构、长度、耗时等）。
*   **检索增强路由（Retrieval-Augmented Routing）**：
    *   推理时，针对新查询，从经验记忆库中检索最相似的案例。
    *   将这些案例作为上下文提供给路由模型，使其了解类似问题在两种系统下的表现差异。
*   **准则引导的思维链推理（Rubric-Guided CoT）**：
    *   路由模型不直接输出结果，而是遵循一套严格的“准则（Rubric）”进行推理。
    *   推理步骤包括：分析上下文相似性、比较历史性能、推断行为模式、做出路由决策。

### 3. 实验设计
*   **基准测试（RouteBench）**：论文构建了专门的路由基准，包含来自 GAIA 和 MMLU 的题目。
    *   **Base Set**：域内任务，作为经验来源。
    *   **Rephrase Set**：对 Base Set 进行改写，测试路由的鲁棒性。
    *   **Advanced Set**：域外任务，测试泛化能力。
*   **对比方法**：
    *   **LLM-only**：全部由轻量级 LLM 处理。
    *   **Agent-only**：全部由智能体处理。
    *   **Prompt Routing**：仅靠提示词描述能力进行路由。
    *   **RAG Routing**：仅使用检索增强，无准则引导推理。
*   **评估指标**：路由准确率（Acc）、各求解器 F1 分数、推理时间（Time）以及综合评分（RouteBenchScore）。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或训练时长。
*   **原因**：由于 BoundaryRouter 是**无需训练**的方法，其核心开销在于构建早期经验时的推理调用（API 调用）以及推理时的检索开销。实验中使用了 GPT-4o 作为代表性 LLM，Claude Sonnet 4 作为智能体逻辑核心。

### 5. 实验数量与充分性
*   **实验规模**：
    *   评估了 **14 种**当代主流模型（包括 GPT-5、Gemini-3-Pro、Claude-4.5 等，注：论文中出现的模型版本号反映了其前瞻性或特定实验环境）。
    *   涵盖了 3 个不同难度的评估集（Base, Rephrase, Advanced）。
    *   进行了详细的消融实验（对比 Prompt-only, RAG-only, BoundaryRouter）。
*   **充分性评价**：实验设计较为充分，通过跨模型、跨数据集以及对分布偏移（Distribution Shift）的测试，客观地验证了路由器的泛化能力。

### 6. 主要结论与发现
*   **性能平衡**：BoundaryRouter 相比于纯智能体执行，**减少了 60.6% 的推理时间**；相比于纯 LLM 推理，**性能提升了 28.6%**。
*   **模型排名**：在路由任务中，GPT-5、Gemini-3-Pro-Preview 和 Gemini-2.5-Pro 表现最强，显示出更好的决策边界感知能力。
*   **组件贡献**：消融实验证明，早期经验（RAG）和准则引导推理（Rubric-CoT）缺一不可，两者结合使路由质量平均提升了 37.9%（对比简单提示词路由）。
*   **泛化性**：即使在域外（Advanced Set）任务中，该方法依然能保持较好的准确率-成本平衡。

### 7. 优点
*   **实用性强**：无需标注数据和梯度更新，非常适合新任务的快速部署（冷启动）。
*   **透明度高**：通过准则引导的 CoT，路由决策过程具有可解释性。
*   **鲁棒性**：通过引入改写集和域外集，证明了系统在面对语言变化和主题切换时的稳定性。

### 8. 不足与局限
*   **二元限制**：目前主要研究在“一个 LLM”和“一个智能体”之间路由，尚未探讨在多个异构智能体或多种工具组合间的复杂路由。
*   **依赖种子集**：路由效果高度依赖于初始种子集的覆盖范围和代表性。
*   **检索开销**：虽然减少了智能体执行时间，但增加了检索和路由模型推理的额外开销（尽管相比智能体执行通常较小）。

（完）
