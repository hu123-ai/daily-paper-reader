---
title: "SkillLens: Adaptive Multi-Granularity Skill Reuse for Cost-Efficient LLM Agents"
title_zh: SkillLens：面向高成本效益 LLM 智能体的自适应多粒度技能复用
authors: "Yongliang Miao, Ziyang Yu, Liang Zhao, Bowen Zhu, Hasibul Haque"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.08386v1"
tags: ["query:malm"]
score: 9.0
evidence: 大语言模型代理的分层技能演化框架
tldr: 针对LLM智能体在技能重用中面临的上下文冗余与高昂重写成本问题，本文提出SkillLens框架。该框架将技能组织为包含策略、方案、过程和原语的四层分级图，支持混合粒度的自适应检索。通过随机游走扩展和验证器决策，智能体能直接复用兼容子技能并仅对不匹配部分进行局部重写。实验表明，SkillLens在Bug定位和ALFWorld任务中均显著优于现有基准，实现了高效且低成本的技能演化。
selection_source: fresh_fetch
motivation: 现有的LLM技能库通常采用扁平的单分辨率结构，导致在重用技能时难以平衡上下文相关性与推理成本。
method: 提出一种分层技能演化框架，通过四层技能图和验证器机制实现混合粒度的技能检索、分解与局部重写。
result: "在MuLocbench和ALFWorld基准测试中，SkillLens分别实现了6.31%的准确率提升和从45%到51.31%的成功率增长。"
conclusion: 混合粒度的自适应技能重用机制能有效降低智能体的适配成本，并通过持续演化不断优化决策性能。
---

## 摘要
技能库已成为 LLM 智能体跨任务复用过程性经验的一种实用方式。然而，现有系统通常将技能视为扁平的、单一分辨率的提示块。这导致了相关性与成本之间的矛盾：注入粗粒度技能可能会引入无关或误导性的上下文，而重写整个技能则成本高昂且通常没有必要。我们提出了 SkillLens，这是一个分层技能演化框架，它将技能组织成由策略 (policies)、战略 (strategies)、程序 (procedures) 和原语 (primitives) 组成的四层图，并以混合粒度进行检索。给定一个任务，SkillLens 首先检索语义相关的技能种子，通过在技能图上进行度校正随机游走 (degree-corrected random walk) 来扩展它们，然后使用验证器决定每个访问的单元应当被接受、分解、重写还是跳过。这使得智能体能够直接复用兼容的子技能，同时仅对局部不匹配的组件进行调整。为了随时间改进系统，SkillLens 进一步细化多粒度技能和验证器，以优化其路由决策。我们提供的理论分析表明，在稀疏不匹配假设下，混合粒度自适应产生的成本是次线性的，并且演化更新规则单调地改进验证目标，直到达到局部最优。在 MuLocbench 和 ALFWorld 实验中，SkillLens 始终优于强大的基于技能的基准模型，在缺陷定位方面实现了高达 6.31 个百分点的 Acc@1 提升，并将智能体成功率从 45.00% 提高到 51.31%。

## Abstract
Skill libraries have become a practical way for LLM agents to reuse procedural experience across tasks. However, existing systems typically treat skills as flat, single-resolution prompt blocks. This creates a tension between relevance and cost: injecting coarse skills can introduce irrelevant or misleading context, while rewriting entire skills is expensive and often unnecessary. We propose SkillLens, a hierarchical skill-evolution framework that organizes skills into a four-layer graph of policies, strategies, procedures, and primitives, and retrieves them at mixed granularity. Given a task, SkillLens first retrieves semantically relevant skill seeds, expands them through degree-corrected random walk over the skill graph, and then uses a verifier to decide whether each visited unit should be accepted, decomposed, rewritten, or skipped. This enables the agent to reuse compatible subskills directly while adapting only locally mismatched components. To improve the system over time, SkillLens further refines multi-granularity skills and verifier in order to improve its routing decisions. We provide theoretical analysis showing that mixed-granularity adaptation incurs sublinear cost under sparse mismatch assumptions and that the evolutionary update rule monotonically improves the validation objective until a local optimum. Across MuLocbench and ALFWorld, SkillLens consistently improves over strong skill-based baselines, achieving up to a 6.31 percentage-point Acc@1 gain for bug localization and raising agent success rate from 45.00% to 51.31%.

---

## 论文详细总结（自动生成）

以下是对论文《SkillLens: Adaptive Multi-Granularity Skill Reuse for Cost-Efficient LLM Agents》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的 LLM 智能体技能库（如 Voyager, AutoSkill）通常将技能视为“扁平”的、原子化的提示块（Prompt Blocks）。
*   **核心矛盾**：当检索到的技能与当前任务仅部分匹配时，智能体面临两难选择：要么注入整个粗粒度技能（引入无关噪声或误导信息），要么完全重写/丢弃技能（成本高昂且浪费了可用的子结构）。
*   **整体含义**：论文提出了 **SkillLens** 框架，旨在通过“混合粒度”的自适应复用机制，让智能体能够精确地识别并复用技能中有效的部分，仅对不匹配的局部组件进行调整，从而在提升性能的同时降低计算成本。

### 2. 方法论
SkillLens 的核心由**分层技能图**、**自适应推理**和**双注册表演化**三部分组成：
*   **四层技能图 (Hierarchical Skill Graph)**：将技能组织为四个抽象层级：
    1.  **策略 (Policy)**：任务路由。
    2.  **战略 (Strategy)**：长程规划。
    3.  **过程 (Procedure)**：标准作业程序（SOP）。
    4.  **原语 (Primitive)**：原子级工具调用或动作。
*   **混合粒度自适应推理 (Inference)**：
    1.  **种子检索与扩展**：通过嵌入检索获取种子，并利用**度校正随机游走 (RWR)** 在图中扩展，避免陷入密集的相似性簇。
    2.  **验证器引导的递归遍历**：验证器 $V_\phi$ 对访问的单元做出四种决策：`ACCEPT`（直接复用）、`DECOMPOSE`（向下钻取更细粒度）、`REWRITE`（局部重写）、`SKIP`（跳过）。
*   **双注册表演化 (Dual-Registry Evolution)**：
    *   同时维护**智能体注册表**（存储过程性知识）和**验证器注册表**（存储路由决策知识）。
    *   利用失败任务的“差异报告 (Gap Report)”驱动 `W_\phi`（编写器）提出编辑建议，通过迭代优化使系统达到局部最优。

### 3. 实验设计
*   **数据集/场景**：
    1.  **MuLocbench**：仓库级缺陷定位任务，涉及 36 个真实 Python 项目，评估文件、模块、函数三个层级的定位准确率。
    2.  **ALFWorld**：长程交互式文本环境任务（如：在厨房中寻找并冷却苹果）。
*   **对比方法 (Baselines)**：
    *   **Vanilla**：直接使用 Codex GPT-5.2 基础模型。
    *   **AutoSkill**：扁平化技能自演化方法。
    *   **EvoSkill**：基于失败驱动的技能精炼方法。
*   **评估指标**：Acc@k、Recall@k、Precision@k（定位任务）；成功率（ALFWorld）。

### 4. 资源与算力
*   **模型使用**：智能体与编写器使用 **Codex GPT-5.2**；验证器使用 **GPT-5-mini**；嵌入模型使用 `text-embedding-3-small`。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号和数量。但在消融实验中提到了时间成本：例如，一次完整的演化迭代在特定设置下耗时约 78.6 至 102.7 分钟。
*   **Token 消耗**：论文强调了成本效益，SkillLens 的选择性重写策略仅消耗 16.41M tokens，远低于全量重写策略的 115.51M tokens。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了代码定位（36 个仓库）和具身智能模拟（ALFWorld 多个子任务），实验设置较为全面。
*   **消融实验**：针对 RWR 机制、不同的重写策略（全量 vs 局部 vs 父节点）、以及初始技能比例对成本的影响进行了详细测试。
*   **充分性评价**：实验设计较为客观，通过跨领域的两个基准测试验证了通用性。特别是针对不同粒度（文件/模块/函数）的对比，充分证明了混合粒度处理的必要性。

### 6. 主要结论与发现
*   **性能提升**：SkillLens 在 MuLocbench 的函数级定位上 Acc@1 提升了 6.31%，在 ALFWorld 上的成功率从 45.00% 提升至 51.31%。
*   **任务复杂度相关性**：任务越复杂、要求的粒度越细，SkillLens 的优势越明显。
*   **成本效益**：通过“先分解再局部重写”的策略，SkillLens 在保持高精度的同时，显著降低了 API 调用成本和推理延迟。
*   **理论保证**：证明了在稀疏不匹配假设下，自适应成本是次线性的，且演化规则具有单调改进性。

### 7. 优点
*   **结构化思维**：将技能从扁平块转变为分层图，更符合人类解决复杂问题的逻辑。
*   **高效率**：验证器引导的递归遍历避免了冗余的上下文注入，极大地节省了 Token。
*   **双重演化**：不仅更新技能内容，还更新“如何使用技能”的路由知识，形成了闭环学习。

### 8. 不足与局限
*   **依赖基础模型能力**：验证器和编写器的决策高度依赖于底层 LLM 的推理能力，若基础模型在逻辑判断上出错，可能导致错误的分解或重写。
*   **冷启动问题**：虽然支持演化，但系统初期仍依赖一定质量的初始技能种子。
*   **应用限制**：目前主要在文本和代码领域验证，尚未在多模态或实时性要求极高的物理机器人环境中测试。

（完）
