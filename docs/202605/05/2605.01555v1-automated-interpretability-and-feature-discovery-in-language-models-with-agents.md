---
title: Automated Interpretability and Feature Discovery in Language Models with Agents
title_zh: 基于智能体的语言模型自动化可解释性与特征发现
authors: "Arnau Marin-Llobet, Javier Ferrando"
date: 2026-05-02
pdf: "https://arxiv.org/pdf/2605.01555v1"
tags: ["query:malm"]
score: 9.0
evidence: 用于机械可解释性的自主多智能体框架
tldr: 本研究提出了一种基于多智能体的自动化机械解释性框架，旨在自动解释和发现大语言模型中的内部特征。该系统包含解释细化和特征发现两个耦合循环，通过智能体自主生成假设、进行针对性测试及构建激活空间图谱，显著提升了特征解释的准确性。在Gemma-2等模型上的实验表明，该方法优于传统的单次自动解释，能发现语言特定及安全相关的特征，并提供可审计的解释追踪，为理解模型内部机制提供了新工具。
selection_source: fresh_fetch
motivation: 传统的单次自动解释方法难以准确捕捉大语言模型内部特征的复杂性，且缺乏有效的验证和新特征发现机制。
method: 该框架通过解释细化循环进行假设测试，并利用基于激活空间k-NN图的特征发现循环来检索候选特征。
result: 实验表明该方法在Gemma-2模型上优于单次自动解释，并成功发现了安全相关和特定语言的内部特征。
conclusion: 智能体驱动的经验循环能够产生比传统单次标注更精准、可证伪且具备可审计追踪的模型内部特征解释。
---

## 摘要
我们介绍了一种用于机械可解释性（mechanistic interpretability）的自主多智能体框架，该框架实现了大语言模型内部特征解释与发现的自动化。该系统运行两个耦合循环：(1) 解释细化（explanation refinement），由智能体提出竞争性假设，并通过有针对性的提示词控制和多指标评估进行迭代测试；(2) 特征发现（feature discovery），由智能体生成提示词集，在激活空间中构建 k-最近邻图，并利用统计可分性和语义连贯性标准检索候选特征。在 Gemma-2 系列模型和权重稀疏 Transformer 的 MLP 神经元上，我们的智能体表现优于单次自动解释（one-shot auto-interpretations），发现了特定语言和安全相关的特征，并生成了可审计的解释追踪，表明智能体驱动的经验循环比单次标签能产生更清晰、更具可证伪性的解释。

## Abstract
We introduce an autonomous multiagent framework for mechanistic interpretability that automates both explaining and finding internal features in large language models. The system runs two coupled loops: (1) explanation refinement, where an agent proposes competing hypotheses and iteratively tests them with targeted prompt controls and a multi-metric evaluation; and (2) feature discovery, where an agent generates prompt sets, constructs a k-nearest-neighbor graph in activation space, and retrieves candidate features using statistical separability and semantic coherence criteria. On Gemma-2 family models and MLP neurons in weight-sparse transformers, our agent improves over one-shot auto-interpretations, discovers language-specific and safety-relevant features, and produces auditable explanation traces, showing that agent-driven empirical loops yield sharper and more falsifiable explanations than one-shot labels.

---

## 论文详细总结（自动生成）

以下是对论文《Automated Interpretability and Feature Discovery in Language Models with Agents》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在大规模语言模型（LLM）中高效、准确地发现并解释内部特征（Features）。
*   **研究动机**：
    *   **现有方法局限**：目前的自动化可解释性方法（如 OpenAI 的 auto-interp）多为“单次（one-shot）”标注，即仅根据激活量最高的示例生成描述。这种方法缺乏验证，容易受到反例干扰，且难以处理多义性（polysemanticity）。
    *   **特征发现瓶颈**：随着稀疏自编码器（SAE）生成的特征数量激增，研究者往往不知道该关注哪些特征。现有的研究大多假设目标特征已知，缺乏系统性的特征自动发现机制。
    *   **动态过程需求**：可解释性不应是静态标注，而应是一个提出假设、测试、修正的迭代实验过程。

### 2. 方法论
论文提出了 **InterpAgent** 框架，由一个主管智能体协调两个专门的子智能体：
*   **FeatureFinder（特征发现循环）**：
    *   **核心思想**：利用统计学方法在激活空间中寻找能区分不同提示词组（如不同语言、主题）的特征。
    *   **技术细节**：对 SAE 激活向量进行对数变换和归一化，通过 PCA 降维后构建 **k-最近邻（k-NN）图**，利用 **Leiden 社区检测算法** 进行聚类。最后使用 **Wilcoxon 秩和检验**（配合 Benjamini-Hochberg FDR 校正）筛选出在特定类别中显著激活的“标记特征”。
*   **FeatureExplainer（解释细化循环）**：
    *   **核心思想**：通过“提出-测试-修正”的迭代循环生成可证伪的解释。
    *   **流程**：
        1.  **生成假设**：基于初始示例提出竞争性假设。
        2.  **针对性测试**：智能体生成 12 个正样本和 12 个匹配的负控制样本。
        3.  **多指标评估**：使用 7 项指标（包括检测 F1、扰动 F1、Surprisal AUROC、嵌入相似度、t 检验 p 值、Cohen's d 效应量、LLM 评分）对假设进行量化评分。
        4.  **迭代修正**：根据测试失败的情况修正假设，直到收敛或达到迭代上限。
        5.  **多义性检测**：若多个语义迥异的假设得分接近，则标记为多义性特征。

### 3. 实验设计
*   **实验场景**：
    *   **Gemma-2 系列模型**（2B 和 9B 版本）的 SAE 特征解释。
    *   **权重稀疏 Transformer** 的原始 MLP 神经元解释。
    *   **安全审计案例**：发现并验证与有害内容（如爆炸物）相关的拒绝行为特征。
*   **数据集**：Pile-uncopyrighted（通用语料）、自定义的多语言提示词集（20 种语言）、代码（Python）、数学及安全敏感提示词。
*   **Benchmark 与对比方法**：
    *   对比基准是 **One-shot Auto-interpretation**（单次自动标注）。
    *   评估智能体在不同模型（GPT-4o-mini, Gemini 1.5 Pro, Claude 3.5 Sonnet）作为骨干时的表现。

### 4. 资源与算力
*   **算力说明**：论文未详细列出具体的 GPU 型号和训练时长，因为该框架主要侧重于**推理阶段**的智能体交互。
*   **成本分析**：论文提供了详细的 API 成本分析。使用 `gpt-4o-mini` 时，迭代解释一个特征的成本约为 **0.006 美元**，比单次标注贵约 34 倍，但在绝对数值上依然极低（解释 1000 个特征仅需约 6 美元）。

### 5. 实验数量与充分性
*   **实验规模**：在 GPT-4o-mini 上测试了 1600 个特征，Gemini 上 300 个，Claude 上 60 个。
*   **消融与敏感性分析**：对 FeatureFinder 的超参数（PCA 维度、k 值、聚类分辨率）进行了系统的消融实验，验证了聚类质量的稳定性。
*   **充分性**：实验覆盖了语言、代码、数学、安全等多个维度，并从 SAE 特征泛化到了原始神经元，实验设计较为客观、全面。

### 6. 主要结论与发现
*   **性能提升**：迭代细化显著优于单次标注。在强模型（如 Gemini/Claude）辅助下，约 **80%** 的特征解释在迭代后获得了更高的准确度和可信度。
*   **特征发现能力**：FeatureFinder 能准确识别出特定语言家族、代码语法（如 Python 的 try/except）和安全相关的内部特征。
*   **因果验证**：通过干预发现的特征方向，可以成功引导模型行为（如抑制拒绝行为），证明了发现的特征具有功能性意义。
*   **通用性**：该框架同样适用于非 SAE 表示，能自动找回人类专家通过手动电路分析发现的神经元。

### 7. 优点
*   **科学严谨性**：将解释过程从“描述性”转变为“实验性”，引入了负控制组和多维度量化指标。
*   **可审计性**：系统记录了每一个假设、测试用例和修正逻辑，生成了完整的审计追踪。
*   **模块化设计**：特征发现与解释解耦，使得系统可以灵活处理不同层级和类型的内部表示。

### 8. 不足与局限
*   **模型依赖**：解释质量高度依赖于底层 LLM 的推理和测试用例生成能力，弱模型可能无法有效修正假设。
*   **弱监督限制**：FeatureFinder 目前仍需要带标签的提示词组（如“这是法语”），尚未实现完全无监督的开放式特征发现。
*   **收敛性风险**：迭代循环并不保证单调收敛，有时可能陷入局部错误的解释。
*   **计算开销**：虽然绝对成本低，但相比单次标注，其 API 调用次数和 Token 消耗显著增加。

（完）
