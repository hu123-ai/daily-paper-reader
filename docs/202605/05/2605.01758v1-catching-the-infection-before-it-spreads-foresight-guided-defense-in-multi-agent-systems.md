---
title: "Catching the Infection Before It Spreads: Foresight-Guided Defense in Multi-Agent Systems"
title_zh: 防患于未然：多智能体系统中的前瞻引导防御
authors: "Yue Ma, Ziyuan Yang, Yi Zhang"
date: 2026-05-03
pdf: "https://arxiv.org/pdf/2605.01758v1"
tags: ["query:malm"]
score: 9.0
evidence: 基于大型多模态模型的多智能体系统
tldr: "针对多智能体系统（MAS）中易受传染性越狱攻击的问题，本文提出了一种无需训练的预见性引导局部净化框架（FLP）。该框架通过模拟未来交互轨迹并利用多角色模拟策略检测感染，结合相册回滚和递归二分诊断（RBD）技术精准清除恶意样本。实验证明，FLP能将感染率从95%以上降至5.47%以下，同时有效保留了智能体交互的多样性。"
selection_source: fresh_fetch
motivation: 现有的全局防御方法难以应对局部交互产生的传染性越狱，且容易导致智能体响应同质化，缺乏真正的恢复能力。
method: 提出FLP框架，通过多角色模拟预测未来行为轨迹，利用响应多样性检测感染，并采用递归二分诊断定位并清除恶意图像。
result: "实验结果显示，FLP将最大累积感染率从95%以上降至5.47%以下，且检索和语义指标均接近良性基准。"
conclusion: FLP通过局部化的预见性推理和精准净化，为多智能体系统提供了一种高效且能保持行为多样性的防御方案。
---

## 摘要
基于大型多模态模型的多智能体系统（MAS）通过专门的智能体实现协作式复杂问题解决。然而，MAS 容易受到传染性越狱（infectious jailbreak）的影响，即攻击单个智能体可能会扩散到其他智能体，导致大规模沦陷。现有的防御措施通过训练一种更具传染性的“治愈因子”来应对，使智能体倾向于检索该因子而非病毒对抗样本（VirAEs）。然而，这会导致智能体响应的同质化，仅提供表层抑制而非真正的恢复。我们重新审视了这些防御措施，它们通过共享的治愈因子进行全局操作，而传染性越狱则源于局部交互行为。这种不匹配限制了它们的有效性。为解决这一问题，我们提出了一种无需训练的前瞻引导局部净化（FLP）框架，其中每个智能体通过对未来交互进行推理来追踪行为演变并消除感染。具体而言，每个智能体在随后的对话轮次中模拟未来的行为轨迹。为了反映 MAS 中的多样性，我们引入了一种多角色模拟策略，以便在不同的交互语境下进行鲁棒预测。然后，我们利用响应多样性作为诊断信号，通过分析基于角色的预测在检索结果和语义层面的不一致性来检测感染。对于受感染的智能体，我们应用局部净化：通过立即回滚相册来缓解近期感染，而长期感染则使用递归二分诊断（RBD）处理，该方法递归地划分图像相册并应用相同的诊断策略来定位和消除 VirAEs。实验表明，FLP 将最大累积感染率从 95% 以上降低到 5.47% 以下。此外，检索和语义指标与良性基准紧密匹配，表明其有效地保留了交互多样性。

## Abstract
Large multimodal model-based Multi-Agent Systems (MASs) enable collaborative complex problem solving through specialized agents. However, MASs are vulnerable to infectious jailbreak, where compromising a single agent can spread to others, leading to widespread compromise. Existing defenses counter this by training a more contagious cure factor, biasing agents to retrieve it over virus adversarial examples (VirAEs). However, this homogenizes agent responses, providing only superficial suppression rather than true recovery. We revisit these defenses, which operate globally via a shared cure factor, while infectious jailbreak arise from localized interaction behaviors. This mismatch limits their effectiveness. To address this, we propose a training-free Foresight-Guided Local Purification (FLP) framework, where each agent reasons over future interactions to track behavioral evolution and eliminate infections. Specifically, each agent simulates future behavioral trajectories over subsequent chat rounds. To reflect diversity in MASs, we introduce a multi-persona simulation strategy for robust prediction across interaction contexts. We then use response diversity as a diagnostic signal to detect infection by analyzing inconsistencies across persona-based predictions at both retrieval-result and semantic levels. For infected agents, we apply localized purification: recent infections are mitigated via immediate album rollback, while long-term infections are handled using Recursive Binary Diagnosis (RBD), which recursively partitions the image album and applies the same diagnosis strategy to localize and eliminate VirAEs. Experiments show that FLP reduces the maximum cumulative infection rate from over 95% to below 5.47%. Moreover, retrieval and semantic metrics closely match benign baselines, indicating effective preservation of interaction diversity.

---

## 论文详细总结（自动生成）

### 论文结构化深入总结：防患于未然（FLP 框架）

#### 1. 核心问题与研究动机
*   **核心问题**：基于大型多模态模型（LMM）的多智能体系统（MAS）面临“传染性越狱”（Infectious Jailbreak）的严重威胁。攻击者只需向一个智能体注入病毒对抗样本（VirAEs），该样本就会通过智能体间的通信和检索机制在整个系统中呈指数级扩散。
*   **研究动机**：
    *   **现有防御的局限**：目前的防御方法（如 Cowpox）通常引入全局共享的“治愈因子”，强制智能体检索治愈因子而非病毒。这导致了智能体响应的**同质化**（失去多样性），且仅是表层抑制而非根源清除。
    *   **失配问题**：全局干预手段与局部交互驱动的感染动态之间存在失配。
    *   **目标**：在不牺牲交互多样性的前提下，实现受感染智能体的真正恢复，并阻止病毒传播。

#### 2. 方法论：前瞻引导局部净化（FLP）
FLP 是一个**无需训练（Training-free）**的防御框架，核心思想是让智能体在实际交互前进行“自我预演”。
*   **核心思想**：通过内部多角色模拟预测未来行为演变，识别并清除导致行为异常的根源（VirAEs）。
*   **关键技术细节**：
    1.  **多角色模拟（Multi-Persona Simulation）**：智能体从角色池中实例化多个不同角色，在内部模拟多轮“计划-检索-提问-回答”流程。正常智能体在不同角色下响应应具有多样性，而受感染智能体则会表现出行为坍缩。
    2.  **感染诊断（Infection Diagnosis）**：
        *   **检索熵（Retrieval Entropy, $E_{ret}$）**：量化检索结果的分布。若智能体反复检索同一个 VirAE，熵值会显著降低。
        *   **语义多样性（Semantic Diversity, $S_{div}$）**：计算不同角色生成响应的语义嵌入余弦相似度。恶意行为通常会导致语义高度趋同。
    3.  **局部净化策略（Localized Purification）**：
        *   **语义漂移（Semantic Drift, $\Delta_t$）**：测量连续两次模拟间的语义变化。
        *   **近期状态回滚**：若检测到剧烈的语义漂移，判定为近期感染，直接删除相册和对话历史中的最新条目。
        *   **递归二分诊断（Recursive Binary Diagnosis, RBD）**：若语义漂移小但持续表现恶意，判定为长期感染。通过二分法递归划分相册，利用诊断指标定位并精准剔除隐藏在相册深处的 VirAEs。

#### 3. 实验设计
*   **场景与数据集**：模拟了一个包含 128 个智能体的 MAS 环境，进行 64 轮长周期交互。
*   **基准模型（Backbones）**：主要使用 **LLaVA-1.5-7B**，并扩展至 **InternVL2-8B**、**Qwen2VL-8B** 和 **InstructBLIP-7B** 以验证泛化性。
*   **对比方法**：
    *   **AgentSmith**：无防御状态下的传染性越狱攻击。
    *   **Cowpox**：基于全局治愈因子的现有先进防御方法。
*   **攻击设置**：包括边界攻击（Border Attack）和像素攻击（Pixel Attack），设置不同的扰动预算（如 $\ell_\infty$ 约束）。
*   **评估指标**：感染率（当前/累积）、病毒激活概率、检索熵保留率、语义漂移距离、语义分散指数等。

#### 4. 资源与算力
*   **算力说明**：论文中**未明确列出**具体的 GPU 型号、数量及总训练/推理时长。
*   **计算特性**：由于 FLP 是**无需训练**的（Training-free），它避免了昂贵的模型微调或对抗训练成本。主要的计算开销集中在推理阶段的“内部模拟”过程。论文提到，通过设置合理的模拟角色数量（如 $n=4$），可以在诊断准确率和推理延迟之间取得平衡。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了 64 轮的大规模 MAS 交互模拟。
    *   在诊断性能测试中使用了 800 个智能体的高密度感染场景（70% 感染率）。
    *   **消融实验**：针对多角色异质性、诊断指标（熵 vs 语义）、角色数量 $n$、相册大小 $|B|$、初始感染率 $r_0$ 等进行了详细测试。
*   **充分性评价**：实验设计非常全面，不仅验证了防御的有效性（感染率降低），还深入探讨了对系统多样性的保护，并跨越了多个主流开源多模态模型，证明了结果的客观性和鲁棒性。

#### 6. 主要结论与发现
*   **极高的防御效能**：FLP 将最大累积感染率从 95% 以上降低至 **5.47% 以下**，在多数情况下能实现 0% 的感染率。
*   **多样性保护**：与 Cowpox 导致熵值下降不同，FLP 的检索熵和语义指标与良性基准（Benign Baseline）高度一致，成功解决了防御导致的响应同质化问题。
*   **预传播干预**：FLP 能够在病毒实际传播给其他智能体之前，通过内部模拟提前发现并清除威胁。
*   **泛化性**：在不同架构的 LMM 上均表现出一致的防御效果。

#### 7. 优点与亮点
*   **理论支撑**：论文从信息论角度分析了全局防御导致的“信息瓶颈”问题，并证明了局部净化能绕过此瓶颈。
*   **无需训练**：完全在推理侧实现，易于部署到现有的 MAS 框架中。
*   **精准恢复**：RBD 算法类似于“杀毒软件”的扫描机制，能够精准定位恶意样本而非盲目清空存储。
*   **主动性**：通过“前瞻”（Foresight）机制，在攻击发生早期甚至传播前进行拦截。

#### 8. 不足与局限
*   **推理延迟**：内部多角色模拟会增加每个交互轮次的计算开销，虽然论文建议异步执行，但在实时性要求极高的场景下可能存在挑战。
*   **模态局限**：实验主要集中在多模态（图像-文本）越狱，对于纯文本或其他模态（如音频、视频）的传染性攻击尚未充分探讨。
*   **对抗演进风险**：如果攻击者已知晓 FLP 机制，可能会设计能够维持响应多样性的“隐蔽型”越狱样本，这可能需要更复杂的诊断指标。

（完）
