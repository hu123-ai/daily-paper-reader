---
title: Neuro-Symbolic Agents for Hallucination-Free Requirements Reuse
title_zh: 用于无幻觉需求复用的神经符号智能体
authors: Ahmed Ibrahim
date: 2026-05-02
pdf: "https://arxiv.org/pdf/2605.01562v1"
tags: ["query:malm"]
score: 9.0
evidence: 用于需求复用的神经符号多智能体系统
tldr: 本研究针对需求重用框架OOMRAM在灵活性与准确性之间的矛盾，提出了一种神经符号多智能体系统。该系统将需求重用重新定义为模型驱动的启发式过程，利用大语言模型作为非确定性启发式工具，在确定的形式化需求格模型中进行导航。通过在智能体循环中引入确定性符号验证器，强制执行结构约束，从而在保证灵活性的同时，从根本上消除了幻觉导致的需求组合错误，实现了高度合规的需求生成。
selection_source: fresh_fetch
motivation: 传统需求重用方法因匹配规则僵化而缺乏适应性，而引入大语言模型虽能提升灵活性却面临生成无效或不一致需求组合的幻觉风险。
method: 采用神经符号多智能体架构，将大语言模型作为启发式工具在形式化需求格中搜索，并利用符号验证器实时强制执行结构约束。
result: "实验显示该系统达到了100%的需求覆盖率，约束违反率低至0.2%，且所有生成的规范在结构上均完全有效。"
conclusion: 该方法成功结合了深度学习的灵活性与符号逻辑的严谨性，为实现无幻觉且可审计的自动化需求工程提供了可靠方案。
---

## 摘要
面向对象的需求编写与管理方法 (OOMRAM) 是一种需求复用框架，它依赖于精确的标识符匹配和僵化的模板，限制了其在不同语境下调整规范的能力。虽然大语言模型 (LLM) 提供了克服这一瓶颈的灵活性，但它们也带来了生成结构无效或不一致的需求组合的风险。为了解决这一矛盾，我们提出了一种神经符号多智能体系统，将需求复用重新概念化为一种模型驱动的启发过程。在这种范式中，LLM 作为一种非确定性启发式方法，用于遍历由形式化 OOMRAM 需求格表示的确定性领域模型。一个确定性的符号验证器在智能体循环中强制执行所有结构约束，通过构造有效地消除了幻觉产生的需求组合。在跨两个应用系列的自主基准测试中，我们的系统实现了 100% 的需求覆盖率，且约束违反率仅为 0.2%。尽管针对单一金标准的 F1 分数适中（0.47--0.51），但生成的每个规范在结构上都是有效的，并满足所有强制性的领域需求。这种与模型无关的实现通过子图导航扩展到更大的格，并为监管合规性提供透明的审计追踪。

## Abstract
The Object-Oriented Method for Requirements Authoring and Management (OOMRAM) is a requirements reuse framework that relies on exact identifier matching and rigid templates, limiting its ability to adapt specifications across diverse contexts. While Large Language Models (LLMs) offer the flexibility to overcome this bottleneck, they introduce the risk of generating structurally invalid or inconsistent requirement combinations. To address this tension, we present a neuro-symbolic multi-agent system that re-conceptualizes requirements reuse as a \textbf{Model-Driven Elicitation process}. In this paradigm, an LLM serves as a \textbf{non-deterministic heuristic} for traversing a \textbf{deterministic domain model} represented by a formal OOMRAM requirement lattice. A deterministic, symbolic validator enforces all structural constraints within the agent loop, effectively eliminating hallucinated requirement combinations by construction. Evaluated on an autonomous benchmark across two application families, our system achieves 100\% requirement coverage and a constraint-violation rate of only 0.2\%. Although the F1-score against a single gold standard is moderate (0.47--0.51), every generated specification is structurally valid and satisfies all mandatory domain requirements. The model-agnostic implementation scales to larger lattices via subgraph navigation and provides transparent audit trails for regulatory compliance.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为“用于无幻觉需求复用的神经符号智能体”的系统，旨在解决软件需求工程中自动化复用的可靠性问题。以下是对该论文的深度总结：

### 1. 核心问题与研究动机
*   **研究背景**：需求复用（Requirements Reuse）是降低软件开发成本的关键，但传统方法（如 OOMRAM 框架）依赖精确的标识符匹配，缺乏灵活性，难以处理自然语言描述。
*   **核心矛盾**：大语言模型（LLM）虽然能理解自然语言，但存在“幻觉”风险，容易生成逻辑不一致、违反强制约束或虚构需求标识符的规范。
*   **研究目标**：结合 LLM 的灵活性与形式化方法的严谨性，构建一个既能理解用户愿景，又能保证生成结果 100% 符合领域约束的自动化系统。

### 2. 方法论
该论文提出了一种**神经符号多智能体架构**，将需求复用视为“模型驱动的启发式过程”：
*   **核心思想**：将 LLM 视为在确定性领域模型（需求格/Lattice）中进行搜索的“非确定性启发式工具”，而由符号逻辑（Python 代码）构成的验证器负责强制执行约束。
*   **关键技术细节**：
    *   **需求格表示**：将需求定义为有向无环图（DAG），包含核心（Core）、单选（SA）、多选（MA）和可选（Op）四种类型。
    *   **多智能体协作（基于 LangGraph）**：
        *   **Navigator（导航员）**：负责在需求格中建议下一个待扩展的节点。
        *   **Interpreter（解释员）**：由 LLM 驱动，将用户愿景映射为具体的需求 ID。
        *   **Validator（验证器）**：**纯符号算法（无 LLM）**，根据布尔约束检查选择是否合法（如 XOR 约束、父子依赖）。
        *   **Scribe（记录员）**：编译最终的规范文档。
    *   **子图导航策略**：每次仅让 LLM 处理局部子图，确保上下文窗口恒定（O(1)），从而支持大规模需求库的线性扩展。

### 3. 实验设计
*   **数据集/场景**：
    *   构建了一个包含 **12 个领域系列**的语料库（如汽车信息娱乐、核心银行、电信、智能电网等），这些模型基于 ISO 26262、3GPP 等国际标准构建。
    *   重点测试了 `eRecordKeeping`（57 个节点）和 `SmartHome`（20 个节点）两个格模型。
*   **Benchmark**：设计了 10 个项目愿景（自然语言描述场景）。
*   **对比方法**：
    *   **消融实验**：对比了“完整智能体集群”与“纯 LLM 生成（无验证器）”的表现。
    *   **金标准对比**：与人工编写的需求规范进行 F1 分数对比。

### 4. 资源与算力
*   **硬件环境**：消费级设备（Apple M1 Pro 芯片，32 GB RAM）。
*   **模型使用**：通过 **Ollama** 本地部署的 `llama3.1:8b-instruct-q4_0` 量化模型。
*   **参数设置**：Temperature 设为 0 以保证确定性采样。
*   **训练时长**：论文未涉及模型训练，仅为推理实验。平均每个愿景的处理耗时约 210 秒，涉及约 78 次 LLM 调用。

### 5. 实验数量与充分性
*   **实验规模**：针对 10 个愿景进行了完整测试，并在 12 个不同复杂度的领域模型上验证了鲁棒性。
*   **充分性评价**：
    *   **客观性**：通过消融实验清晰展示了符号验证器的必要性。
    *   **公平性**：使用了本地开源模型，确保了实验的可重复性。
    *   **局限性**：虽然涵盖了多个领域，但目前主要基于合成的（Synthetic）格模型，而非真实工业界的杂乱遗留数据。

### 6. 主要结论与发现
*   **零幻觉保证**：通过符号验证器的实时拦截，最终生成的规范达到了 **100% 的结构有效性**，约束违反率仅为 0.2%（且在循环中被立即纠正）。
*   **F1 分数的误区**：实验发现 F1 分数仅为 0.47-0.81。作者指出，这是因为一个愿景可以对应多种合法的需求组合，F1 分数低并不代表失败，只要满足结构有效性（SVR）即可。
*   **自我纠错能力**：系统展现了“意图冲突”处理能力，即当用户愿景与安全法规冲突时，验证器能强制 LLM 选择符合合规性的替代方案。

### 7. 优点
*   **架构创新**：成功将神经推理（LLM）与符号逻辑（Validator）解耦，解决了 LLM 在严谨工程领域难以落地的痛点。
*   **可扩展性**：子图导航策略解决了长文本处理中的“迷失”问题，理论上可处理包含数千个需求的工业级模型。
*   **透明度**：提供了完整的审计追踪，每一步决策都有据可查，符合监管要求。

### 8. 不足与局限
*   **预处理依赖**：系统假设需求已经以 OOMRAM 格的形式存在，但从非结构化遗留文档到格模型的转换过程尚未自动化。
*   **延迟问题**：由于采用多步迭代和多次 LLM 调用，生成速度（约 3-4 分钟）可能不适合极高实时要求的交互场景。
*   **评估指标**：传统的 NLP 指标（如 F1）在产品线工程中表现不佳，需要建立更科学的“结构有效性”评估标准。

（完）
