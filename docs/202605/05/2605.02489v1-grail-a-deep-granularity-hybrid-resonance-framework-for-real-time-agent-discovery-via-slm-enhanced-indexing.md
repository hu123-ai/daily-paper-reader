---
title: "GRAIL: A Deep-Granularity Hybrid Resonance Framework for Real-Time Agent Discovery via SLM-Enhanced Indexing"
title_zh: GRAIL：一种通过 SLM 增强索引实现实时智能体发现的深粒度混合共振框架
authors: Jinliang Xu
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02489v1"
tags: ["query:malm"]
score: 10.0
evidence: 多智能体协作中的实时智能体发现框架
tldr: 针对大规模多智能体协作中智能体发现效率与精度难以兼得的问题，本文提出GRAIL框架。该框架通过小语言模型（SLM）增强索引、伪文档扩展及MaxSim共振匹配机制，实现了亚400毫秒的实时发现延迟。在AgentTaxo-9K数据集上的实验表明，GRAIL在显著提升检索精度的同时，将端到端延迟降低了79倍以上，为“智能体互联网”提供了工业级解决方案。
selection_source: fresh_fetch
motivation: 现有的智能体发现方法在重型LLM的高延迟与传统向量检索的低精度之间存在矛盾，难以满足大规模实时协作需求。
method: 采用微调的SLM进行快速能力标签预测，结合伪文档扩展增强语义密度，并利用MaxSim共振机制实现细粒度的查询与示例匹配。
result: "在包含9,240个智能体的新数据集上，GRAIL将发现延迟缩短至400ms以内，且Recall@10指标显著优于传统向量搜索。"
conclusion: GRAIL成功平衡了智能体发现的速度与精度，为构建大规模、实时的多智能体生态系统奠定了技术基础。
---

## 摘要
随着基于大语言模型（LLM）的智能体生态系统迅速扩张，高效且准确的智能体发现已成为大规模多智能体协作的关键瓶颈。现有方法通常面临两难境地：要么依赖重量级 LLM 进行意图解析，导致极高的延迟（通常超过 30 秒）；要么使用单一的向量检索，以牺牲语义精度来换取速度。为了弥补这一差距，我们提出了 GRAIL（基于粒度共振的智能体/AI 链接），这是一个在不牺牲准确性的情况下实现低于 400 毫秒发现延迟的新型框架。GRAIL 引入了三项关键创新：(1) SLM 增强预测，使用专门微调的小语言模型（SLM）取代通用 LLM 解析器，实现毫秒级的能力标签预测；(2) 伪文档扩展，通过合成查询增强智能体描述，以提高语义密度，从而实现鲁棒的稠密检索；(3) MaxSim 共振，一种细粒度匹配机制，计算用户查询与离散智能体使用示例之间的最大相似度，有效缓解了语义稀释问题。在包含 9,240 个智能体的新型大规模数据集 AgentTaxo-9K 上进行的验证表明，与基于 LLM 解析的基准方法相比，GRAIL 将端到端发现延迟降低了 79 倍以上，同时在 Recall@10 指标上显著优于传统的向量搜索。该框架为实时“智能体互联网”提供了一种可扩展的工业级解决方案。

## Abstract
As the ecosystem of Large Language Model (LLM)-based agents expands rapidly, efficient and accurate Agent Discovery becomes a critical bottleneck for large-scale multi-agent collaboration. Existing approaches typically face a dichotomy: either relying on heavy-weight LLMs for intent parsing, leading to prohibitive latency (often exceeding 30 seconds), or using monolithic vector retrieval that sacrifices semantic precision for speed. To bridge this gap, we propose \textbf{GRAIL} (Granular Resonance-based Agent/AI Link), a novel framework achieving sub-400ms discovery latency without compromising accuracy. GRAIL introduces three key innovations: (1) \textbf{SLM-Enhanced Prediction}, replacing the generalized LLM parser with a specialized, fine-tuned Small Language Model (SLM) for millisecond-level capability tag prediction; (2) \textbf{Pseudo-Document Expansion}, augmenting agent descriptions with synthetic queries to enhance semantic density for robust dense retrieval; and (3) \textbf{MaxSim Resonance}, a fine-grained matching mechanism computing maximum similarity between user queries and discrete agent usage examples, effectively mitigating semantic dilution. Validated on \textbf{AgentTaxo-9K}, our new large-scale dataset of 9,240 agents, GRAIL reduces end-to-end discovery latency by over \textbf{79$\times$} compared to LLM-parsing baselines, while significantly outperforming traditional vector search in Recall@10. This framework offers a scalable, industrial-grade solution for the real-time ``Internet of Agents."

---

## 论文详细总结（自动生成）

以下是对论文《GRAIL: A Deep-Granularity Hybrid Resonance Framework for Real-Time Agent Discovery via SLM-Enhanced Indexing》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在大规模多智能体系统（Internet of Agents）中，如何实现**实时且高精度**的智能体发现（Agent Discovery）。
*   **研究背景**：
    *   **LLM解析法（高延迟）**：依赖重型LLM解析意图，延迟常超过30秒，无法满足实时交互需求。
    *   **单一向量检索（低精度）**：将智能体多种能力压缩为单个向量，导致“语义稀释”（Semantic Dilution），难以匹配复杂的细粒度约束。
*   **目标**：打破“高精度”与“低延迟”之间的二律背反，实现亚400ms级别的工业级搜索。

### 2. 核心方法论
GRAIL 框架引入了“深粒度混合共振”机制，主要包含三个关键技术：
*   **SLM增强预测（SLM-Enhanced Prediction）**：使用微调后的 3B 参数小语言模型（SLM）取代通用 LLM。SLM 专门负责将用户查询映射为能力标签（Tags），在毫秒级完成意图解析。
*   **伪文档扩展（Pseudo-Document Expansion, PDE）**：在离线阶段，利用 LLM 为智能体描述生成合成查询（Synthetic Queries），将其与原始描述拼接。这增强了索引的语义密度，解决了用户查询与技术描述之间的词汇不匹配问题。
*   **MaxSim 共振算法（MaxSim Resonance）**：
    *   **核心思想**：不再对智能体的多个使用示例（Examples）进行平均池化，而是保留每个示例的独立向量。
    *   **计算逻辑**：计算用户查询向量与智能体示例矩阵中每个向量的余弦相似度，取其**最大值**作为共振得分。这确保了只要有一个示例匹配，智能体就能被精准检索，有效对抗语义稀释。
*   **混合索引结构**：构建了稀疏索引（标签）、上下文索引（伪文档向量）和意图索引（示例矩阵）三位一体的检索体系。

### 3. 实验设计
*   **数据集**：发布了 **AgentTaxo-9K**，包含 154 个行业、924 个子领域、9,240 个智能体元数据，以及 27,720 条高质量用户查询。
*   **Benchmark 逻辑**：基于 OASF 标准构建层级化分类体系，确保覆盖的广泛性和专业性。
*   **对比方法**：
    *   **MDR (Monolithic Dense Retrieval)**：传统的单一向量稠密检索。
    *   **SLM-Sort**：仅依赖 SLM 预测标签进行过滤。
    *   **LLM-Router**：基于重型 LLM 的“思考后查找”模式（作为精度上限参考）。

### 4. 资源与算力
*   **硬件环境**：单张 **NVIDIA T4 GPU** (15GB VRAM)，12.7GB 系统内存。
*   **模型配置**：
    *   **SLM**：Qwen2.5-3B-Instruct，使用 LoRA 技术进行微调。
    *   **Embedding**：BGE-Small-EN-v1.5 (33M 参数)。
*   **训练细节**：在 27,720 个（查询-标签）对上进行微调，推理采用 FP16 半精度以降低延迟。

### 5. 实验数量与充分性
*   **实验规模**：在近万个智能体的大规模池中进行全量检索测试，而非小规模子集。
*   **实验组别**：
    *   主实验：对比了 Recall@1, Recall@10, MRR@10 和端到端延迟。
    *   消融实验：分别验证了 MaxSim 机制和 SLM 过滤器的贡献。
*   **客观性**：实验严格区分了用于扩展的合成查询和用于评估的用户查询，避免了数据泄露；使用了工业级常用的 T4 显卡，具有较强的现实参考价值。

### 6. 主要结论与发现
*   **性能飞跃**：GRAIL 的 Recall@10 达到 **91.45%**，显著超过 MDR (76.77%)。
*   **延迟极低**：端到端延迟仅为 **394.48ms**，比 LLM 解析法快了 **79 倍**以上。
*   **理论验证**：通过数学证明（定理1）确认了平均池化在多技能智能体表示中的失效，而 MaxSim 能保持信号强度。
*   **发现**：单纯的标签过滤（SLM-Sort）在大规模场景下几乎失效，必须结合深粒度向量共振。

### 7. 优点与亮点
*   **架构创新**：巧妙地将 SLM 的速度与 MaxSim 的细粒度匹配结合，解决了长尾能力的检索难题。
*   **理论支撑**：不仅有实验，还提供了关于“语义稀释”问题的理论分析和证明。
*   **实用性强**：针对“智能体互联网”这一前沿趋势，提供了可落地的工业级索引方案。
*   **贡献数据集**：AgentTaxo-9K 为该领域提供了急需的大规模层级化基准。

### 8. 不足与局限
*   **语言局限**：实验主要基于英文模型（BGE-Small-EN），在多语言环境下的表现尚未验证。
*   **存储开销**：由于需要存储每个智能体的多个示例向量矩阵，其索引占用的空间会高于传统的单一向量索引。
*   **合成数据依赖**：伪文档扩展的效果高度依赖于离线阶段生成合成查询的 LLM 质量。
*   **硬件扩展性**：虽然在单卡 T4 上表现良好，但在超大规模（如百万级智能体）分布式环境下的同步开销未做深入讨论。

（完）
