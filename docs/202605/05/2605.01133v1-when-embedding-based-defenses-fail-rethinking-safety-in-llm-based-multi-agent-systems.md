---
title: "When Embedding-Based Defenses Fail: Rethinking Safety in LLM-Based Multi-Agent Systems"
title_zh: 当基于嵌入的防御失效时：重新思考基于大语言模型的多智能体系统的安全性
authors: "Lingxi Zhang, Guangtao Zheng, Hanjie Chen"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.01133v1"
tags: ["query:malm"]
score: 9.0
evidence: 基于大语言模型的多智能体系统安全与群体决策
tldr: 本研究探讨了基于大语言模型的多智能体系统（MAS）在面对恶意攻击时的安全性。研究发现，现有的基于嵌入的防御手段在攻击者刻意伪装消息时会失效。为此，作者提出了三种新型攻击方式，并揭示了嵌入防御忽略Token级置信度信号的局限性。研究提出利用Logits置信度分数来过滤或降权恶意消息，实验证明该方法能显著提升MAS在多种场景下的鲁棒性，并强调了早期干预在防御中的关键作用。
selection_source: fresh_fetch
motivation: 现有基于嵌入的防御方法在恶意代理通过伪装使消息嵌入接近良性数据时容易失效，且忽略了模型内部的置信度信号。
method: 提出利用Token级别的置信度分数（Logits）作为信号，在多智能体通信过程中对可疑消息进行剪枝或降权处理。
result: 实验结果显示，该方法在不同的模型、数据集和通信拓扑结构中均能有效提升系统的抗攻击能力和鲁棒性。
conclusion: 置信度信号是增强MAS安全性的关键，但其防御效力随通信轮数增加而减弱，因此在攻击早期进行干预至关重要。
---

## 摘要
由大语言模型（LLM）驱动的多智能体系统（MAS）使智能体能够进行通信和共享信息，从而在复杂任务上取得强大的性能。然而，这种通信也产生了一个攻击面，恶意智能体可以传播错误信息并操纵群体决策，从而破坏 MAS 的安全性。现有的基于嵌入的防御旨在检测并剔除可疑智能体，但其有效性取决于恶意消息和良性消息的文本嵌入之间是否存在清晰的分离。攻击者可以通过精心构造嵌入与良性消息接近的消息来规避此类防御。我们从理论上分析了这种失效模式，并通过三种攻击（慢速漂移、良性包装和混沌播种）进行了实证验证。我们的分析进一步揭示了基于嵌入的防御的一个根本局限性：由于它们仅依赖于文本嵌入，因此忽略了诸如 Logits 之类的 Token 级置信度信号，而当嵌入在攻击下无法区分时，这些信号仍能提供信息。我们建议在 MAS 通信过程中使用置信度分数来剔除或降低消息的权重。实验表明，该方法在不同模型、数据集和通信拓扑结构中均提高了鲁棒性。此外，我们发现置信度信号的有效性会随着通信轮次的增加而衰减，这突显了早期干预的重要性。这些见解可以为未来关于 MAS 攻击和防御的研究提供参考和启发。

## Abstract
Large language model (LLM)-powered multi-agent systems (MAS) enable agents to communicate and share information, achieving strong performance on complex tasks. However, this communication also creates an attack surface where malicious agents can propagate misinformation and manipulate group decisions, undermining MAS safety. Existing embedding-based defenses aim to detect and prune suspicious agents, but their effectiveness depends on a clear separation between the text embeddings of malicious and benign messages. Attackers can circumvent such defenses by crafting messages whose embeddings lie close to benign ones. We analyze this failure mode theoretically and validate it empirically with three attacks, Slow Drift, Benign Wrapper, and Chaos Seeding. Our analysis further reveals a fundamental limitation of embedding-based defenses: because they rely solely on the text embeddings, they ignore token-level confidence signals such as logits, which can remain informative when embeddings are not distinguishable under attack. We propose using confidence scores to prune or down-weight messages during MAS communication. Experiments show improved robustness across models, datasets, and communication topologies. Moreover, we find that the effectiveness of confidence signals decays over communication rounds, highlighting the importance of early intervention. This insights can inform and inspire future work on MAS attacks and defenses.

---

## 论文详细总结（自动生成）

这篇论文对基于大语言模型（LLM）的多智能体系统（MAS）的安全性进行了深入探讨，以下是详细的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：随着 MAS 在协作任务（如软件工程、聊天机器人）中的广泛应用，恶意智能体通过传播错误信息操纵群体决策的风险日益增加。
*   **核心问题**：现有的防御手段（如 G-Safeguard, GUARDIAN）主要依赖**文本嵌入（Embedding）**来识别异常消息。作者指出，这种防御假设恶意消息在嵌入空间中是明显的离群点。然而，如果攻击者刻意伪装，使恶意消息的嵌入与良性消息极度接近（即“近良性攻击”），这些防御就会失效。
*   **研究意义**：论文揭示了基于嵌入防御的理论缺陷，并提出利用模型内部的**Token 级置信度信号**作为补充防御手段。

### 2. 方法论
*   **理论分析**：利用 Lipschitz 连续性证明，基于嵌入的分类器在训练时若良性/恶意样本分离度过高，会学习到一个过宽的“良性接受区”，从而为近良性攻击留下漏洞。
*   **三种新型攻击模式**：
    1.  **慢速漂移 (Slow Drift)**：在多轮对话中逐渐改变嵌入方向，避免突发性异样。
    2.  **良性包装 (Benign Wrapper)**：将短小的恶意指令隐藏在长篇良性文本中，使整体嵌入受良性文本主导。
    3.  **混沌播种 (Chaos Seeding)**：诱导良性智能体产生分歧，扩大良性嵌入的分布范围，使防御者难以界定离群点。
*   **置信度防御策略**：
    *   **核心指标**：计算生成消息时 Token 的 Logits 熵（Entropy），取 Top-k 最大熵的平均值作为不确定性度量。
    *   **策略一：置信度剪枝 (Pruning)**：直接拦截低于置信度阈值的消息。
    *   **策略二：置信度降权 (Down-weighting)**：将置信度作为元数据随消息发送，提示接收方智能体在推理时降低对该消息的信任权重。

### 3. 实验设计
*   **数据集**：
    *   **MMLU**：涵盖多学科的知识性选择题。
    *   **GSM8K**：需要多步推理的小学数学题。
    *   **BBH**：具有挑战性的逻辑推理和指令遵循任务。
*   **对比基准 (Benchmark)**：
    *   **无防御状态**。
    *   **G-Safeguard**：基于图神经网络（GNN）的拓扑防御。
    *   **GUARDIAN**：基于时序图建模的无监督防御。
*   **实验模型**：LLaMA-3.1-8B、Qwen3-4B（开源模型，提取 Logits）以及 GPT-4o-mini（通过 API 获取 Token 概率）。
*   **通信拓扑**：星型 (Star)、链式 (Chain)、随机稀疏图 (Sparse Random)。

### 4. 资源与算力
*   **算力说明**：论文提到开源模型使用 **vLLM** 框架在本地运行以提取 Logits，GPT-4o-mini 通过 API 调用。
*   **具体细节**：文中**未明确列出**具体的 GPU 型号、数量或总训练/推理时长。由于主要涉及推理和简单的熵计算，其防御成本远低于需要重新训练或微调模型的方案。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个主流数据集、3 种不同架构的 LLM、3 种攻击方式以及多种通信拓扑。
*   **消融实验**：对置信度阈值 $\delta$、降权强度 $\alpha$ 以及通信轮数的影响做了详细的消融研究。
*   **充分性评价**：实验设计较为全面，不仅验证了攻击的有效性，还深入分析了信号随时间衰减的动态过程，具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **嵌入防御的脆弱性**：在近良性攻击下，现有基于嵌入的防御性能大幅下降，甚至可能误伤良性智能体。
*   **置信度信号的互补性**：Token 级的置信度在嵌入信号失效时依然具有辨别力，能有效识别恶意操纵。
*   **信号衰减效应**：随着通信轮数增加，恶意信息会被良性智能体“回声”式传播，导致置信度和嵌入信号都会发生坍缩（Self-mixing），辨别力下降。
*   **拓扑影响**：通信拓扑越稠密（如全连接），错误信息传播越快，防御信号失效越快；稀疏拓扑有助于保留防御信号。

### 7. 优点
*   **视角新颖**：首次系统性地指出了 MAS 防御中“嵌入空间分离假设”的局限性。
*   **理论与实证结合**：通过数学证明和三种精心设计的攻击方式验证了安全漏洞。
*   **低成本高效率**：提出的置信度防御无需额外训练，利用模型原生信号即可实现，易于部署。

### 8. 不足与局限
*   **置信度信号的局限**：仅依赖熵值可能无法应对极其隐蔽或经过校准（Calibration）的恶意模型。
*   **攻击者自适应风险**：如果攻击者知道防御方在使用置信度检测，可能会通过特定的采样策略（如降低温度）来伪造高置信度的恶意消息。
*   **实验覆盖**：虽然涵盖了多种任务，但对于更复杂的长文本协作或工具调用场景（Agentic workflows）的探讨较少。

（完）
