---
title: "Accurate Legal Reasoning at Scale: Neuro-Symbolic Offloading and Structural Auditability for Robust Legal Adjudication"
title_zh: 大规模准确法律推理：面向稳健法律裁决的神经符号卸载与结构化可审计性
authors: "Stanisław Sójka, Witold Kowalczyk"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02472v1"
tags: ["query:profile-1"]
score: 10.0
evidence: 利用大语言模型进行稳健法律裁决和推理的神经符号方法
tldr: "针对法律文本中复杂逻辑导致的推理错误和高昂成本，本文提出一种名为“摊销智能”的神经符号方法。该方法利用大模型将法律文本一次性转换为确定性自主合约语言（DACL）的图表示，通过确定性图执行实现法律裁决。该系统在保持高一致性的同时，将计算成本降低了90%以上，并满足了法律审计的严苛要求，为大规模法律推理提供了稳健的解决方案。"
selection_source: fresh_fetch
motivation: 现有大语言模型在处理复杂法律条款时存在推理错误、成本高昂且缺乏可审计性的问题。
method: 提出一种神经符号框架，利用大模型将法律文本转化为确定性自主合约语言（DACL）的图中间表示，并进行确定性执行。
result: "该方法在法律裁决中实现了近乎完美的逻辑一致性，消除了概率模型的推理缺陷，并降低了超过90%的计算成本。"
conclusion: 结合大模型的翻译能力与符号系统的确定性执行，是实现大规模、高精度且可审计法律推理的有效途径。
---

## 摘要
法律文本通常包含计算型法律条款——即理解这些条款需要复杂的逻辑。虽然前沿的大型推理模型（LRM）能够描述此类条款，但构建生产级系统仍受限于推理错误和高昂的推理成本。我们提出了“摊销智能”（Amortized Intelligence），这是一种神经符号方法，通过一次性使用大语言模型（LLM）将法律文本转换为确定性自主合约语言（DACL）：一种类型化的图中间表示。随后，裁决过程依赖于具有可视化可审计追踪的确定性图执行。与运行时 LRM 基准（包括 GPT-5.2 和 Gemini 3 Pro）相比，我们基于 DACL 的智能体实现了近乎完美的一致性，并缓解了在概率模型中观察到的“推理悬崖”现象。该系统在高吞吐量工作流中降低了 90% 以上的计算成本，同时满足了法律裁决中严格的可审计性要求。

## Abstract
Legal texts often contain computational legal clauses--provisions whose understanding requires complex logic. While frontier Large Reasoning Models (LRMs) can describe such clauses, building production-ready systems is limited by reasoning errors and the high cost of inference. We propose Amortized Intelligence, a neuro-symbolic approach where we use an LLM once to translate a legal text into Deterministic Autonomous Contract Language (DACL): a typed graph intermediate representation. Adjudication then relies on deterministic graph executions with a visually auditable trace. In comparison against runtime LRM baselines (including GPT-5.2 and Gemini 3 Pro), our DACL-based Agent achieves near-perfect consistency and mitigates the "reasoning cliff" observed in probabilistic models. The system reduces compute costs by over 90% in high-volume workflows while satisfying the strict auditability requirements of legal adjudication.

---

## 论文详细总结（自动生成）

这是一份关于论文《Accurate Legal Reasoning at Scale: Neuro-Symbolic Offloading and Structural Auditability for Robust Legal Adjudication》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：法律文本（如合同、法规）包含大量“计算型法律条款”，涉及复杂的逻辑嵌套、数学公式和程序性规则。
*   **现有挑战**：
    *   **可靠性风险**：前沿大型推理模型（LRM）虽具语义灵活性，但在处理多层嵌套逻辑和精确算术时存在“推理悬崖”（Reasoning Cliff），易产生幻觉且输出不确定。
    *   **经济成本**：对每一笔交易（如数千张发票审核）都调用高昂的思维链（CoT）推理，导致成本随业务量线性增长，商业上不可持续。
    *   **审计需求**：法律裁决要求决策过程透明、可追溯，而概率性的黑盒模型难以满足严苛的合规审计。
*   **核心目标**：提出一种既能利用 LLM 语义理解能力，又能保证符号系统确定性和低成本的大规模法律推理框架。

### 2. 方法论
论文提出了 **“摊销智能”（Amortized Intelligence）** 架构，核心思想是将 LLM 从“运行时解释器”转变为“一次性逻辑编译器”：
*   **DACL（确定性自主合约语言）**：一种强类型的图中间表示（IR）。它将合同逻辑建模为有向无环图（DAG），包含四种原语：
    1.  **程序（Procedure）**：顺序工作流。
    2.  **逻辑条款（Logical Clause）**：一阶命题逻辑。
    3.  **范围条款（Range Clause）**：处理数值区间映射（如阶梯计费）。
    4.  **定价公式（Pricing Formula）**：沙箱化的算术表达式。
*   **编译流水线**：通过五个阶段（语义分割、可计算性分类、动态少样本生成、自动化场景测试、专家监督）将自然语言合同转化为 DACL。
*   **神经符号智能体（Runtime Agent）**：在执行阶段，使用轻量级模型（如 GPT-5-mini）作为调度员，仅负责语义路由和结果合成，而核心逻辑计算则卸载（Offloading）给确定性的 DACL 符号引擎执行。

### 3. 实验设计
*   **数据集/场景**：选取了四个真实工业领域的商业协议：医疗（Health-PPO）、能源（Energy-Sup）、物流（Logistics-MSA）和市政（Muni-IFB）。逻辑复杂度从简单的线性算术到拥有 76 个决策状态的复杂树状逻辑不等。
*   **基准测试（Benchmark）**：
    *   **流量模拟**：基于生产环境数据生成 400 个随机测试事件。
    *   **地面真值**：由资深律师和领域专家独立编写的确定性代码作为“金标准”。
*   **对比方法**：包括 GPT-5.2（无推理模式/中等推理模式）、Claude 4.5 Sonnet、Gemini 3 Pro 等前沿 LRM 的运行时直接推理。

### 4. 资源与算力
*   **模型使用**：实验使用了 OpenAI GPT-5 系列、Anthropic Claude 4.5 和 Google Gemini 3 Pro。
*   **算力说明**：论文**未明确提及**具体的 GPU 硬件型号或训练时长。这主要是因为该研究侧重于推理架构的设计和智能体编排，而非从头训练基础模型。其核心贡献在于通过“卸载”推理负担来降低对高性能算力的依赖。

### 5. 实验数量与充分性
*   **实验规模**：针对 4 个不同复杂度的领域，共进行了 400 组测试。
*   **充分性评价**：
    *   **覆盖度**：实验覆盖了从简单算术到极高复杂度决策树的梯度，能够有效验证模型在不同压力下的表现。
    *   **客观性**：引入了“LLM-as-Judge”机制进行错误分类，并与专家编写的确定性代码进行对比，评估标准客观。
    *   **公平性**：所有基准模型均开启了各自的最强推理模式（如 Extended Thinking），确保了对比的公正性。

### 6 主要结论与发现
*   **性能卓越**：DACL 智能体实现了 **99.5%** 的综合准确率，远超表现最好的基准模型（82.8%）。
*   **复杂度不变性**：DACL 的表现不随逻辑复杂度增加而下降，而 LRM 在处理复杂物流合同（76 个状态）时准确率骤降至 46% 以下。
*   **错误根源分析**：LRM 的主要失败模式是**变量依赖（VD）错误**（占比 71%），即模型能做对数学题，但会找错数据（在错误的状态下应用公式），而纯算术幻觉（AH）极少。
*   **经济性**：相比于 GPT-5.2 推理模式，DACL 方案将 Token 消耗降低了 **9.9 倍**，且显著提升了处理速度（吞吐量）。

### 7. 优点
*   **架构创新**：通过“一次编译，多次运行”的模式，解决了 LLM 在法律领域应用中“昂贵且不可靠”的痛点。
*   **结构化可审计性**：DACL 引擎能生成结构化的审计追踪（Audit Trail），每一步决策都有据可查，符合法律合规要求。
*   **实战验证**：该系统已在生产环境中运行超过 12 个月，处理了大量真实账单业务，证明了其工程可行性。

### 8. 不足与局限
*   **表达力边界**：目前 DACL 仅支持确定性逻辑，尚无法处理“合理注意”（Reasonable Care）等开放性法律标准或辩驳性推理。
*   **编译依赖**：如果 LLM 在初始编译阶段误解了条款，该错误会变成确定性的重复错误，仍需人工专家进行最终审核。
*   **语言与领域限制**：实验主要集中在英文商业合同，尚未验证其在非英语司法管辖区或非结构化非正式协议中的表现。
*   **基准局限**：未对比“检索增强生成（RAG）”变体，虽然作者认为 RAG 无法解决变量依赖问题，但缺乏直接实验数据支持。

（完）
